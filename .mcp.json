{
  "mcpServers": {
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "@playwright/mcp@latest",
        "--browser", "chrome",
        "--user-data-dir", ".playwright-mcp/chrome-profile",
        "--output-dir", ".playwright-mcp"
      ]
    }
  }
}

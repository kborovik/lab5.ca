# SPEC.archive.md — closed §T rows from lab5.ca SPEC.md

archived 2026-05-29 per SPEC.md slim (phases 1-3 dedup). full task text preserved verbatim. open §T live in SPEC.md @ repo root.

id|status|task|cites
T2|x|? define post-proof auto-reply CTA → resolved ⊥ — auto-reply ≡ AI answer only; ⊥ funnel back to /proof (MailPilot ≡ evidence, ⊥ product pitch ∴ ⊥ self-promo ∈ agent reply)|
T3|x|set CF redirect `/services` → `/expertise` ∧ later drop 2026-05-21 (route ⊥ ∈ §I ∴ redirect retired w/ B16 fold-in)|I.routes
T4|x|? decide analytics → resolved CF Web Analytics (server-side, automatic ∈ CF dashboard, ⊥ in-repo script)|I.analytics
T5|x|? decide staging → resolved ⊥ — local dev only (`make dev`); push → main ≡ prod per V22 ∧ V32, ⊥ preview/staging layer|V44,V47
T6|x|prune palette token drift — scope: `{gh-green-muted, gh-blue-muted, gh-red, gh-red-hover, gh-yellow}` decl ∈ `src/styles/global.css` + `src/components/Logo.astro`; remediation ≡ drop unused tokens + Logo.astro ∨ amend §I to admit|V34,B1
T7|x|sweep blog external-link anchor wraps — scope: `\[\*\*[^]]+\*\*\]\(` ∨ `` \[`[^`]+`\]\( `` ∈ `src/content/blog/**/*.md`; remediation ≡ unwrap anchor text → plain prose|V22,B2
T8|x|rename /demo route → /proof: rename `src/pages/demo.astro` → `src/pages/proof.astro`, update internal link refs ∈ `Nav.astro` + home tile + any anchors; reframe page H1 + copy — product ≡ KB skills, MailPilot ≡ evidence (⊥ "try the demo" SaaS framing)|I.routes,V3
T9|x|set CF redirect `/demo` → `/proof` (301) ∈ `public/_redirects`|I.routes
T10|x|add MailPilot source-code link button ∈ `/proof` "under the hood" section — mirror home-page proof-of-capability pattern (bordered button, not pulse); href ≡ §I.cta.mailpilot-source|I.cta.mailpilot-source
T11|x|create `.claude/check-extras.md` w/ `§-cite drift recipe extension` section excluding `src/content/blog/**/*.md` from §-cite drift scope per V37; verify post-create `/sdd:check` ⊥ flag blog cross-repo cites|V91,B5
T12|x|sweep linkedin headline anti-example class — scope: line 1 grep `^(I |Across [0-9])` ∈ `linkedin/*.txt − linkedin/resume.txt`; remediation ≡ rewrite sentence 1 per V36 (≤ ~80 chars, ∋ bold/specific claim ∨ stance ∨ provocation)|V58,B6
T13|x|fix V35 link-form @ src/content/blog/where-pytest-stops-and-claude-code-starts.md:149 — replace `[lab5.ca/demo](https://lab5.ca/proof/)` w/ absolute-path form `[/proof](/proof)`|V22,B7
T14|x|author linkedin/where-pytest-stops-and-claude-code-starts.txt per V36 — headline ≤ ~80 chars (bold/specific claim ∨ stance ∨ provocation), body, trailing `Write-up: https://lab5.ca/blog/where-pytest-stops-and-claude-code-starts/` block; ⊥ markdown syntax, ⊥ `word.tld` pattern, ⊥ `--` literal (em-dash → `—`)|V58,B7
T15|x|update `.claude/check-extras.md` per V37 amend — add filter excluding §B `cause` ∧ §T `task` narrative columns ∈ SPEC.md from free-text `§[VTB]\.[0-9]+` grep; verify post-update /sdd:check ⊥ flag any documentary cites in §B/§T narrative as UNRESOLVED|V91,B8
T16|x|sweep linkedin/*.txt per V36 amend — scope: `rg --pcre2 '^(?!Blog:\|Write-up:)[A-Z][a-z-]+:\s*https?://' linkedin/*.txt` (non-Write-up trailing URL rows) ∧ `head -1 linkedin/*.txt` ⊥ match `^Blog: ` (missing Blog header) — both ∈ `linkedin/*.txt − linkedin/resume.txt`; remediation ≡ (1) prepend `Blog: <title>` line 1 mirroring `src/content/blog/<slug>.md` frontmatter `title` verbatim, (2) remove non-Write-up labeled URL rows (current violators: `Skill:`, `Benchmark:`, `Plugin:`)|V58,B9
T17|x|relabel `Write-up:` → `Read the full post:` ∈ linkedin/*.txt per V36 amend — scope: `rg --pcre2 '^Write-up:\s*https?://' linkedin/*.txt` ∈ `linkedin/*.txt − linkedin/resume.txt`; remediation ≡ replace `Write-up: ` prefix w/ `Read the full post: ` on matched lines|V58
T18|x|retitle measuring-glyph-compression post per V38 — scope: `rg 'Measuring math-glyph compression(?! token)' src/content/blog/*.md linkedin/*.txt`; remediation ≡ insert `token` qualifier → `Measuring math-glyph token compression` (frontmatter title + V36-mirrored Blog: line) ∧ amend frontmatter `description` to carry dimension marker (e.g. `compresses tokens` ∨ `token compression`); slug ⊥ rename per V23|V59,B10
T19|x|retitle + voice rewrite where-pytest-stops-and-claude-code-starts post — scope: `rg 'Where pytest stops and Claude Code starts' src/content/blog/*.md linkedin/*.txt`; remediation ≡ (1) swap title → literal technical noun-phrase per V38 (proposed: `Smoke-testing an LLM agent with a Claude Code skill` — sentence case per V6, names what + how) ∈ frontmatter title + V36-mirrored Blog: line ∧ amend frontmatter `description` to lead w/ smoke-test ∧ LLM-agent dimensions; (2) rewrite TLDR (lines 10–18) + section ledes in first-person singular per V28 — current impersonal "MailPilot is...", "Standard test layers verify...", "The runner is..." → "I built MailPilot...", "I run...", "I let Claude Code drive..."; slug ⊥ rename per V23|V59,V56,B11,B12
T20|x|copy `src/assets/konstantin-borovik-headshot.jpg` → `public/kb-headshot.jpg` (stable URL ∀ JSON-LD per V41)|V62
T21|x|refactor `Layout.astro` — accept `schemaType` prop ∈ {ProfilePage,WebPage,CollectionPage,BlogPosting} ∧ emit JSON-LD `<script type="application/ld+json">` ∈ `<head>` w/ `@graph` ∋ Person + WebSite + page-specific node + BreadcrumbList per V40|V61
T22|x|finalize `Person.knowsAbout` keyword set (§I capability set ∪ lower-level skills: Python, TypeScript, Pydantic AI, Astro, Cloudflare Workers, LLM agents, RAG, structured extraction) ∧ embed ∈ Layout Person node|V61
T23|x|amend `src/pages/about.astro` per V46 — add `<address>` semantic tag w/ `Toronto, Ontario, Canada`; add `// claims` dash-bullet section listing falsifiable facts; add `// proof` bordered link list → LinkedIn ∧ GitHub ∧ MailPilot source ∧ blog index; amend meta `description` to ∋ `Toronto, Canada` + `AI Automation Engineer` + `20+ years`|V4
T24|x|sweep primary pages — scope: `rg -L 'description=' $(rg -l '<Layout' src/pages/{*.astro,blog/*.astro})` (Layout invocations w/o `description=` prop); remediation ≡ add explicit `description` prop per V44|V65
T25|x|extend `src/pages/blog/[...slug].astro` — pass `schemaType="BlogPosting"` ∧ supply blog-specific JSON-LD fields (datePublished, dateModified?, headline, description, keywords) per V43|V64
T26|x|add `<link rel="alternate" hreflang="en-ca">` ∧ `<link rel="alternate" hreflang="x-default">` to `Layout.astro` `<head>` per V45|V66
T27|x|author `public/llms.txt` (pointer-form per V47) ∧ `public/llms-full.txt` (prose dump per V47)|V71
T28|x|verify post-amend: `make check` + `make build` pass; spot-check JSON-LD via Schema.org validator (https://validator.schema.org/) ∧ Google Rich Results Test on `/`, `/about`, `/blog/<slug>` — manual, out-of-repo|V45,V46,V61
T29|x|swap `text-gh-fg-subtle` → `text-gh-fg-muted` @ body-size text sites — scope: `rg "text-gh-fg-subtle" src/`; exempt large-text sites (≥18pt ∨ ≥14pt bold) per V49(a); V15 styling string already updated ∈ this fold-in|V23,B13
T30|x|add `aria-label="Home"` ∈ `<a href="/">` @ `Nav.astro:14` (brand-logo link wrapping pinwheel SVG)|V23,B13
T31|x|wrap `<slot />` ∈ `<main>` ∈ `Layout.astro:221` between `<Nav />` ∧ `<Footer />`|V23,B13
T32|x|promote section kickers `// xxx` → `<h2>` element retaining V14 class string — scope: `rg 'text-sm font-bold uppercase tracking-\[0\.2em\] text-gh-blue' src/pages/`; ! `<h2>` ∀ kicker preceding tile `<h3>`s per V49(b); 3 existing kickers promoted (`// why hire me`, `// how it works`, `// under the hood`); 1 missing kicker added (`// what I build` ∈ `index.astro` tile section) to satisfy V49(b) heading-order ∀ h1→h3-skipping section|V23,B13
T33|x|re-run Lighthouse @ `/`, `/expertise`, `/proof`, `/about`, `/blog`, `/blog/<slug>` (one sample) post-fix; verify a11y ≥ 0.95 ∀; record result → Lighthouse v13 (headless Chrome, npx lighthouse@latest, accessibility-only) 2026-05-20 ∀ 6 primary pages = 1.00 (`/blog/<slug>` sample ≡ spec-driven-development); pre-fix /=0.73 per B13 ⇒ T29–T32 fold-in closes ∀ 4 defect classes (color-contrast, heading-order, link-name, landmark-one-main)|V23
T34|x|author `src/pages/resume.astro` per V50 — sentence-case H1, IBM Plex Mono, GH Primer palette, single column, sections: contact → `// skills` → `// engagements` → `// education`; pass explicit `description` prop to Layout per V44; emit JSON-LD via Layout `schemaType="ProfilePage"` per V40|V81,V87,V65,V61
T35|x|author `public/kb-resume.txt` plain-text mirror per V51 — equivalent claims to `/resume` HTML content (skills, engagements, education, contact links); `## section` headings only, ⊥ markup beyond|V82
T36|x|amend `src/layouts/Layout.astro` `Person.sameAs` array → 4-entry set `["https://www.linkedin.com/in/kborovik", "https://github.com/kborovik", "https://x.com/kborovik", "https://bsky.app/profile/lab5.ca"]` per V42 amend|V63
T37|x|amend `public/llms.txt` + `public/llms-full.txt` — add `/resume` entry ∈ `## Pages`, `https://lab5.ca/kb-resume.txt` ∈ `## Contact`, `## Notes` section ∋ gated-LinkedIn pointer line per V47/V48 amend|V71,V72
T38|x|amend `src/pages/about.astro` `// proof` list — prepend `/resume` link per V46 amend|V4
T39|x|add `@media print` stylesheet for `/resume` route per V50(5) — drop `<Nav />` + `<Footer />` chrome, expand content to full page width|V86
T40|x|verify post-amend — `make check` + `make build` pass; browser-print `/resume` → PDF, confirm Nav/Footer absent + single-column full-width per V50(5); spot-check `kb-resume.txt` content matches `resume.astro` claims per V51 → 2026-05-20 ∀ `/resume/index.html` (9 pages built, 0 check errors): H1=1, H2=3 ∋ {// skills, // engagements, // education}, H3=9 ∋ {AI automation, Stack, 6 employers, NAIT}, `<main>`=1 ∀ V49(d), ProfilePage JSON-LD ∋ `sameAs` 4-entry set per V42, inline `@media print` ∋ Nav/Footer hidden + full-width container, `kb-resume.txt` parity ≡ resume.astro {contact, skills, engagements, education}; browser-print PDF visual ≡ manual out-of-repo follow-up|V45,V46,V81,V86,V87,V82
T41|x|sweep `src/pages/resume.astro` ∧ `public/kb-resume.txt` content vs §I `/resume primary audience` keyword set per V52 — scope: vocab-table compare across declared keyword set ∈ §I vs claim-token set extracted from skill bullets + engagement bullets; remediation ≡ amend bullet wording to surface primary-audience-recognizable framing per §I where KB's experience admits (⊥ fabricate)|V83
T42|x|sweep `src/pages/resume.astro` ∧ `public/kb-resume.txt` engagement bullets vs §I `tech-token era-floor` per V53 → 2026-05-20 ∀ 6 engagements: lab5.ca 2026 (Pub/Sub ≥2015, Cloud Run ≥2019, Pydantic AI ≥2024) ✓, Colliers 2023 (Cloud Run/Functions, Airflow, Terraform, Azure DevOps ≥2018) ✓, PayPal 2018 (GKE ≥2015, Airflow ≥2014, Composer ≥2018) ✓, CDW 2014/Tech Data 2010 ⊥ era-token, TD Bank 2003 (virtualization ESX ≥2003 = floor) ✓; no remediation — scope: vocab-table compare engagement-bullet token mentions against era-floor; flag any token-year-floor > engagement-start-year; remediation ≡ rewrite violating bullet to era-plausible framing|V84
T43|x|author engagement ledes ∈ `src/pages/resume.astro` ∧ `public/kb-resume.txt` ∀ recent 3 engagements (lab5.ca, Colliers, PayPal) per V54; verify post-amend `make check` ∧ `make build` pass → 2026-05-20 (9 pages built, 0 check errors) ∀ 3 ledes ∈ {lab5.ca, Colliers, PayPal} (1-2 sentences ≤ 50 words, first-person, kb-resume.txt mirror per V51)|V87,V82,V85
T44|x|drop X, Bluesky, book-call from `/resume` ∧ `public/kb-resume.txt` visible contact-block links per V50(6) amend — scope: remove `<li>` entries ∈ `src/pages/resume.astro` contact block + matching lines ∈ `kb-resume.txt`; retained surfaces ≡ V42 `Person.sameAs` JSON-LD (X ∧ Bluesky ∀ entity binding) + V48 `llms-full.txt` link set (book-call CTA) + site `<Nav />` "Book a Call" button (unchanged); verify `make check` ∧ `make build` pass + Playwright snapshot `/resume` contact list ≡ {LinkedIn, GitHub, email}|V87,V82
T45|x|restructure `/resume` ∧ `public/kb-resume.txt` skills section per V50(6) amend — rename `// skills` → `// tech stack`; drop `AI automation` H3 subgroup (capability set per V2 surfaces ∈ engagement ledes + `Person.knowsAbout` per V40, ⊥ duplicated `/resume` subgroup); replace flat `Stack` H3 w/ 6 grouped H3 subgroups ordered by scan-priority: (1) AI & agents [Pydantic AI, LLM agents and agentic workflows, RAG, structured-extraction pipelines, MCP], (2) Programming languages [Python, TypeScript], (3) Google Cloud [Cloud Run, Cloud Functions, BigQuery, Vertex AI, Cloud SQL, IAM, Composer, Pub/Sub], (4) SQL databases [PostgreSQL, psycopg], (5) Web development [Astro, Cloudflare Workers], (6) DevOps & CI/CD [Terraform, GitHub Actions, Azure DevOps, Docker, Kubernetes (GKE)]; verify `make check` ∧ `make build` pass + Playwright snapshot `/resume` ∋ 6 H3 ∈ `// tech stack` section|V87,V82,V83
T46|x|amend /resume contact-block emphasis ∧ print font hierarchy per V50(1,5,6) amend — scope: `src/pages/resume.astro` contact block (replace H1 `Resume` w/ H1 role title `AI Automation Engineer`; demote name to secondary `<p>`; `@media print` font sizes satisfy H1 > H2 ≥ H3, all ≥ body w/ section-marker H2 ! ≥ body) + `public/kb-resume.txt` mirror header per V51; verify `make check` ∧ `make build` pass + browser-print PDF spot-check shows role-title H1 dominant ∧ section markers ≥ body|V81,V86,V87,V82
T47|x|extend /resume font-size hierarchy from print-only to screen per V50(5) amend — scope: `src/pages/resume.astro` `<style is:global>` block — add `.resume h2 { font-size: 1rem }` rule (lifts V14 `text-sm` H2 kicker to body-size on /resume; V14 other props bold/uppercase/tracking/blue preserved); verify `make check` ∧ `make build` pass + Playwright snapshot `/resume` ∋ `// engagements` H2 ≥ engagement body prose|V86
T48|x|drop visible email mailto + add canonical site URL link per V50(6) amend — scope: `src/pages/resume.astro` contact block (replace `<li>` ∋ `mailto:kb@lab5.ca` w/ `<li>` ∋ `https://lab5.ca` link) + `public/kb-resume.txt` header link rows (replace `Email: kb@lab5.ca` w/ `Web: https://lab5.ca`); verify `grep -r 'kb@lab5.ca' src public` ≡ ∅ + `make check` ∧ `make build` pass + Playwright snapshot /resume contact list ≡ {LinkedIn, GitHub, lab5.ca}|V87,V82
T49|x|lift engagement-row job-title visibility per V50(6) amend — scope: `src/pages/resume.astro` — change `<p>` class ∀ engagement job-title line from `mt-1 text-sm text-gh-fg-muted` to `mt-1 text-sm font-medium text-gh-fg` (6 engagement rows: lab5.ca, Colliers, PayPal, CDW, Tech Data, TD Bank); verify `make check` ∧ `make build` pass + Playwright snapshot `/resume` job-title color ≡ `text-gh-fg` ∧ weight 500|V87
T50|x|raise engagement-row job-title font-size to ≥ body per V50(6) amend — scope: `src/pages/resume.astro` — change `<p>` class ∀ 6 engagement job-title lines from `mt-1 text-sm font-medium text-gh-fg` to `mt-1 text-base font-medium text-gh-fg`; verify `make check` ∧ `make build` pass + Playwright job-title computed font-size ≥ surrounding body lede font-size|V87
T51|x|reduce body weight site-wide per V11 amend — scope: `src/styles/global.css:8` — change `body { font-weight: 500 }` to `body { font-weight: 400 }`; verify `make check` ∧ `make build` pass + Playwright sweep ∀ primary page ({`/`, `/expertise`, `/proof`, `/about`, `/resume`, `/blog`}) body `<p>` computed font-weight ≡ 400, ⊥ readability regression; ⊥ ripple to existing `font-medium`/`font-semibold`/`font-bold` Tailwind classes — they retain their explicit weight per V10 hierarchy lever|V33,V32
T52|x|switch /resume print heading sizes pt → rem per V50(5) amend — scope: grep `font-size: \d+pt` ∈ `src/pages/resume.astro` `<style is:global>` block; remediation ≡ replace `.resume h1 { font-size: 20pt }`, `.resume h2 { font-size: 11pt }`, `.resume h3 { font-size: 10.5pt }` w/ rem-based values (proposed: H1 ≡ 1.5rem [27px≡20pt], H2 ≡ 1.05rem [18.9px≡14pt], H3 ≡ 1rem [18px≡13.5pt]) ∴ hierarchy holds against prose `sm:text-base` 1rem floor; body 10pt baseline kept ∀ unstyled text but prose carries Tailwind utility size; verify Playwright print-emulation H1 > H2 ≥ H3 ≥ 18px (prose floor) + browser-print → PDF spot-check section markers visually dominant over prose|V86,B14
T53|x|patch /resume print html base 18px→14px per V50(5) amend — scope: `src/pages/resume.astro` `<style is:global>` `@media print` block; remediation ≡ (1) add `html { font-size: 14px }` ∈ `@media print`, (2) drop `body { font-size: 10pt }` line (vestigial post-amend per V50(5)); verify `make check` ∧ `make build` pass + Chrome headless `--print-to-pdf` `/resume` page count ≤ 2pp + Playwright print-emulation prose `<p>` computed font-size ≡ 14px ∧ H1 > H2 ≥ H3 ≥ body hold|V86
T54|x|fold engagement-row print sizing per V50(5) amend — scope: `src/pages/resume.astro` `<style is:global>` `@media print` block (changes already applied ∈ working tree, awaiting commit); remediation ≡ confirm (1) `html { font-size: 10px }`, (2) `.resume { padding: 0.75in !important }`, (3) `.resume h1: 1.6rem`, `.resume h2: 1.15rem`, `.resume h3: 1.3rem`, (4) new selector `.resume article > div + p { font-size: 1.1rem }` (engagement job-title); verify `make check` ∧ `make build` pass + Chrome headless `--print-to-pdf` `/resume` shows hierarchy H1 > H3 > H2 ≈ job-title > body visible|V86
T55|x|drop X ∧ Bluesky entries from `Person.sameAs` ∈ `src/layouts/Layout.astro` per V42 amend — scope: `rg -nE "(x\.com/kborovik\|bsky\.app)" src/layouts/Layout.astro` (matches 2 array entries @ L57-58); remediation ≡ remove the 2 matched lines leaving 2-entry array `["https://www.linkedin.com/in/kborovik", "https://github.com/kborovik"]`; verify `make check` ∧ `make build` pass + view-source `/about` confirms `sameAs` ≡ 2-entry LinkedIn + GitHub array|V63
T56|x|build-gen llms-full.txt — add Astro static endpoint `src/pages/llms-full.txt.ts` emitting `dist/llms-full.txt` body w/ (1) Resume section ≡ Vite `?raw` import of `public/kb-resume.txt` w/ `^## ` → `### ` demote ∀ embedded headings, (2) Blog section ≡ `getCollection('blog')` filtered non-draft ∧ sorted pubDate desc per V48(a), (3) inline TS strings ∀ hero/claims/expertise-blurbs/proof/contact-links/notes blocks (literal carry-over from existing `public/llms-full.txt` narrative content); endpoint sets `Content-Type: text/plain; charset=utf-8`; delete `public/llms-full.txt`; verify post-amend → 2026-05-21: `make check` (0 errors) ∧ `make build` (9 pages built, `/llms-full.txt` ∈ static routes) pass; `dist/llms-full.txt` ∋ Resume section reflecting kb-resume.txt (6 tech-stack `### ` H3 subgroups {AI & agents, Programming languages, Google Cloud, SQL databases, Web development, DevOps & CI/CD} ∧ 6 engagement rows {lab5.ca, Colliers, PayPal, CDW, Tech Data, TD Bank}); Blog section ∋ 3 posts pubDate desc {2026-05-19, 2026-05-18, 2026-05-13}; mechanical-gen path ∴ kb-resume.txt edit → rebuild reflects by construction; `https://lab5.ca/llms-full.txt` URL preserved post-deploy ≡ manual out-of-repo follow-up|V72
T57|x|build-gen llms.txt — add Astro static endpoint `src/pages/llms.txt.ts` emitting `dist/llms.txt` pointer-form body per V47 ∋ (1) `## Pages` link list w/ hand-tuned page descriptions inline (literal carry-over from existing `public/llms.txt`), (2) `## Posts` ≡ `getCollection('blog')` filtered non-draft ∧ sorted pubDate desc per V48(a), (3) `## Contact` link list inline mirroring §I cta refs ∪ `https://github.com/kborovik` ∪ `https://lab5.ca/kb-resume.txt` per V48(c), (4) `## Notes` gated-LinkedIn pointer line per V47 inline; endpoint sets `Content-Type: text/plain; charset=utf-8`; delete `public/llms.txt`; verify `make check` ∧ `make build` pass ∧ `dist/llms.txt` ∋ 3 blog posts pubDate desc + Contact link set + Notes line|V71,V72
T58|x|drop `@astrojs/sitemap` integration per B15 resolve — scope: `rg -nE '@astrojs/sitemap\|sitemap-index\.xml\|sitemap\(\)' astro.config.mjs package.json public/robots.txt` (matches integration import + dep + `integrations: [sitemap()]` + robots.txt `Sitemap:` line); remediation ≡ remove all matches + `pnpm install` regen lock; verify `make check` ∧ `make build` pass + `dist/sitemap-*.xml` absent post-build + `/sdd:check --incremental --all` V55 ⊥ flag sitemap surface; §I amend admits `_redirects` ∈ asset brace-list (resolves B16) ∈ same commit|V92,B15,B16
T59|x|patch html base 18px→16px per V11 amend — scope: `src/styles/global.css:4`; remediation ≡ change `font-size: 18px` → `font-size: 16px`; verify `make check` ∧ `make build` pass + local `make preview` visual confirmation 2026-05-21 (rem-based scales auto-track new base ∴ ⊥ layout regression)|V33
T60|x|sweep section kicker class string per V14 amend — scope: pattern `text-sm font-bold uppercase tracking-\[0\.2em\] text-gh-blue` ∈ `src/pages/**/*.astro` (7 files: about, expertise, index, proof, resume screen kickers, blog index, blog [...slug]); remediation ≡ swap `text-sm` → `text-base`, retain `font-bold uppercase tracking-[0.2em] text-gh-blue`; ∧ drop vestigial `.resume h2 { font-size: 1rem }` screen-side CSS rule ∈ `src/pages/resume.astro` `<style is:global>` (vacuous post-V14 amend per V50(5) sync); resume print `@media print { .resume h2: 1.15rem }` untouched per V50(5) print special-case; verify `make check` ∧ `make build` pass + local `make preview` visual confirmation 2026-05-21|V15,V81
T61|x|sweep tile H3 + body typography per V11 amend follow-through — scope: pattern `text-sm font-semibold text-gh-fg` (tile H3, w/ optional `group-hover:text-gh-green transition-colors` suffix) → `text-base font-semibold text-gh-fg` ∧ pattern `text-xs leading-relaxed text-gh-fg-muted` (tile body `<p>` ∧ `<span>`) → `text-sm leading-relaxed text-gh-fg-muted` ∈ `src/pages/**/*.astro` (index.astro // what I build + // why hire me tiles, proof.astro // how it works + // sample questions + // under the hood); /resume engagement rows ⊥ tile pattern ∴ ⊥ in scope; verify `make check` ∧ `make build` pass + local `make preview` visual confirmation 2026-05-21|V33,V15
T62|x|merge `/about` + `/expertise` + hero content into `src/pages/index.astro` — emit 6 sections per V46 amend (hero w/ 3-line title block + `// what I do` 3-tile + `// tech stack` 4-group summary + `// claims` + `// proof` link list + `// résumé` button); verify `make check` + `make build` pass|V1,V2,V65,V4
T63|x|author `src/pages/projects.astro` per §I route addition — 4 hand-authored skill cards mapping skill label → GitHub repo: (1) Google Cloud → gcp-devops, (2) Python → mailpilot, (3) AI agent tooling → pilot-skills, (4) Web development → lab5.ca; hand-authored inline, ⊥ runtime/build-time GitHub API fetch (determinism > freshness); append-only ∀ future cards (Salesforce reserved)|I.routes
T64|x|delete `src/pages/expertise.astro` + `src/pages/about.astro`; sweep dead-route refs — scope: `rg -F '/expertise' src/ ; rg -F '/about' src/` (excluding HTML `<address>` element refs); remediation ≡ patch `Nav.astro` + footer + cross-page link refs to drop `/expertise` ∧ `/about` href values; ⊥ CF redirect (site too new, ⊥ inbound campaigns)|I.routes
T65|x|swap `/resume` H1 + `Person.jobTitle` JSON-LD + `kb-resume.txt` header — `AI Automation Engineer` → `Software Developer \| DevOps Engineer` (pipe verbatim ∀ surface incl. JSON-LD; Google Knowledge Panel renders pipe raw, accepted per §V.3 dev-tooling voice)|V81,V82
T66|x|restructure `/resume` `// tech stack` 6→9 subgroups per V50(6) amend → [1] Programming languages [Python, TypeScript, Bash, SQL] → [2] Google Cloud [Cloud Run, Cloud Functions, BigQuery, Vertex AI, Cloud SQL, Composer, Pub/Sub] → [3] Azure [App Service, Functions, Service Bus, Key Vault] (NEW) → [4] DevOps & CI/CD [Terraform, Ansible, GitHub Actions, Azure DevOps, Docker, Kubernetes (GKE)] → [5] AI & agents [Pydantic AI, LLM agents and agentic workflows, RAG, structured-extraction pipelines, MCP] → [6] APIs & integration [REST, event-driven, Pub/Sub patterns, webhooks, OpenAPI] (NEW) → [7] Security & compliance [IAM, NIST-800-53, PCI-DSS, secrets management] (NEW) → [8] SQL databases [PostgreSQL, SQLite] → [9] Web development [Astro, Cloudflare Workers]; mirror change ∈ `public/kb-resume.txt` `## tech stack`|V87,V82,V83
T67|x|add `/resume` `// projects` section between `// tech stack` ∧ `// engagements` per V50(6) amend — 4 entries matching `/projects` card grid order ∧ content: (1) gcp-devops [Terraform / Ansible / IaC, Cloud Run, Pub/Sub], (2) MailPilot [Pydantic AI agent, LLM workflows, GCP backend], (3) Pilot Skills [Claude Code skills SDK, agentic workflows], (4) lab5.ca [Astro 6, Tailwind v4, TypeScript, Cloudflare Workers]; ∀ entry ! H3 + 1-line tech context + bordered GitHub repo link; mirror ∈ `public/kb-resume.txt` as `## projects`; verify 3-way mirror (resume.astro ↔ kb-resume.txt ↔ projects.astro)|V87,V82
T68|x|rewrite `Person.knowsAbout` JSON-LD set ∈ `src/layouts/Layout.astro` against merged generalist keyword set per §I amend ∧ V40 ∧ V52|V61,V83
T69|x|regen `public/og.png` (1200×630 via `og.html`) + `public/banner-linkedin.png` (1584×396 via `linkedin-banner.html`) per CLAUDE.md regen recipes — new visible strings `Software Developer \| DevOps Engineer` (pipe verbatim) + stack-token sub-line `Python · Google Cloud · LLM agents · CI/CD`|I.assets
T70|x|update `src/pages/llms.txt.ts` + `src/pages/llms-full.txt.ts` per V47 + V48 amend — drop `/expertise` + `/about` pointers; add `/projects` pointer ∈ `## Pages` link list; update hero / claims / capability blurbs (inline TS strings) to new pitch per §G + V1 + V2 amend|V71,V72
T71|x|add `index.astro` `// résumé` section per V46(f) — single bordered button `Download résumé (PDF)` → `/resume` (new tab); confirm V4 (≤ 1 prominent CTA per page) holds — Book-a-Call stays primary `cta-pulse` ∈ `<Nav />`, résumé button ≡ secondary bordered|V21,V4
T72|x|sweep residual `AI Automation Engineer` strings — scope: `rg -F 'AI Automation Engineer' src/ public/` (! ≡ ∅ post-fold); remediation ≡ patch any stragglers to `Software Developer \| DevOps Engineer`|V1,V81
T73|x|verify post-amend — `make check` + `make build` pass; Lighthouse a11y ≥ 0.95 ∀ primary page per V49 ({`/`, `/projects`, `/proof`, `/blog`, `/blog/<slug>`}); Playwright snapshot `/` + `/projects` + `/resume` confirms structure (hero 3-line block, 3-tile services grid, 9-subgroup tech stack, 4-entry projects grid, hidden /resume in nav)|V45,V46,V23
T74|x|sweep V1 pitch-language carriers per V1(∆) — scope: `rg --pcre2 'AI automation for business operations\|ERP integration\|document workflow\|AI email\|production engineering'` ∈ {`src/layouts/Layout.astro`, `src/pages/blog/index.astro`, `src/pages/resume.astro`, `public/kb-resume.txt`, `src/pages/llms.txt.ts`, `src/pages/llms-full.txt.ts`}; remediation ≡ rewrite carrier content to V1(∆) generalist pitch (cloud infrastructure / AI agents for business workflows / web development); verify post-amend `make check` ∧ `make build` pass ∧ `/sdd:check --all` ⊥ flag V1|V1,B17
T75|x|amend `src/pages/llms-full.txt.ts` Contact block per V48(c) — scope: lines 67-73; remediation ≡ insert `- Resume: https://lab5.ca/resume` line between GitHub ∧ Plain-text resume entries (mirrors `llms.txt.ts` ordering); verify post-amend `/sdd:check --all` ⊥ flag V48|V72,B18
T76|x|sweep V55(∆) internal-ref completeness per V55(∆) — scope: (1) `public/_headers` lines 28-31 → drop `/sitemap-index.xml` ∧ `/sitemap-0.xml` cache blocks; (2) `src/layouts/Layout.astro:151-156` `segmentLabel` map → drop dead keys {`expertise`, `about`} ∧ add live route labels {`projects`, `resume`}; verify post-amend `make check` ∧ `make build` pass ∧ `/sdd:check --all` ⊥ flag V55|V92,B19
T77|x|sweep tile/step title casing per V7(∆) — scope: vocab-table sweep across {`src/pages/index.astro` tile H3, `src/pages/proof.astro` step + stack H3, `src/pages/llms-full.txt.ts` capability block prose}; vocab table: {`cloud infrastructure` → `Cloud Infrastructure`; `AI agents for business workflows` → `AI Agents for Business Workflows`; `web development` → `Web Development`; `send a product question` → `Send a Product Question`; `agent searches the knowledge base` → `Agent Searches the Knowledge Base`; `you get a sourced reply` → `You Get a Sourced Reply`; `frontend` → `Frontend`; `backend` → `Backend`; `ai layer` → `AI Layer`}; remediation ≡ rewrite per vocab table; verify post-amend `make check` ∧ `make build` pass|V14,V2,V72
T78|x|sweep `// what I do` tile body per V46(b)(∆) — scope: 3 `<p>` elements following tile H3 ∈ `src/pages/index.astro` `// what I do` section (current lines 53-86); remediation ≡ rewrite tile body prose: drop specific tech tokens (Cloud Run, Cloud Functions, Pub/Sub, BigQuery, Terraform, GitHub Actions), bold platform keywords w/ `<strong>` tags (Google Cloud, Azure, GitHub, Pydantic AI, Claude API, Astro per tile theme); verify `make check` ∧ `make build` pass|V4
T79|x|restructure `// tech stack` 4-group set per V46(c)(∆) — scope: 4 H3 + 4 `<p>` subgroups ∈ `src/pages/index.astro` `// tech stack` section (current lines 92-128); remediation ≡ rename H3 set from {Google Cloud, Python, JavaScript / TypeScript, Web development} → {Python & TypeScript, Google Cloud & Azure, AI Automation, CI/CD & Terraform}; update body `<p>` token list to match new group definitions; verify `make check` ∧ `make build` pass|V4
T80|x|sweep hero stack-token sub-line per V46(a)(∆) — scope: grep pattern `Python &middot; Google Cloud &middot; LLM agents &middot; CI/CD` ∈ `src/pages/index.astro`; remediation ≡ remove matched `<p>` element + animation-delay style (current lines 21-23); ⊥ sweep `assets-src/og.html` ∨ `assets-src/linkedin-banner.html` (social-card surfaces ⊥ V46 scope — no scroll-down stack ∴ retain stack-token signal); verify `make check` ∧ `make build` pass|V4
T81|x|sweep `## What I do` capability ordering ∈ `src/pages/llms-full.txt.ts` per V48(b)(∆) ∧ V2(∆) — scope: pattern `^0[1-3]\s+(Cloud Infrastructure\|AI Agents for Business Workflows\|Web Development)\.` ∈ `src/pages/llms-full.txt.ts` (3 paragraph leaders @ current lines 40, 42, 44); remediation ≡ rewrite 3 capability paragraphs to V2 new ordering `01 Back-End Development → 02 Front-End Development → 03 Cloud Infrastructure` w/ hand-tuned narrative prose per V48(d); verify `make check` ∧ `make build` pass ∧ `/sdd:check --incremental --all` ⊥ flag V48|V72,V2,B20
T82|x|fix V35 internal-link form @ `src/pages/resume.astro:32` per V50(6)(∆) — scope: pattern `<a href="https://lab5\.ca"` ∈ `src/pages/resume.astro`; remediation ≡ replace href value `https://lab5.ca` w/ `/` (absolute path per V35 internal-link rule); visible text `lab5.ca` ∧ class attrs preserved; verify `make check` ∧ `make build` pass ∧ `/sdd:check --incremental --all` ⊥ flag V35|V22,V87,B21
T83|x|rename `/proof` → `/mailpilot` route + sweep internal refs per §I(∆) ∧ V44(∆) ∧ V46(e)(∆) ∧ V47(∆) ∧ V49(∆) — scope: (1) `git mv src/pages/proof.astro src/pages/mailpilot.astro`; (2) pattern `/proof` ∈ {`src/pages/index.astro`, `src/components/Nav.astro`, `src/components/Footer.astro`, `src/pages/llms.txt.ts`, `src/pages/llms-full.txt.ts`, `public/_redirects`, `src/content/blog/where-pytest-stops-and-claude-code-starts.md`} — replace href ∧ visible URL labels w/ `/mailpilot`; (3) `src/components/Nav.astro` `links[]` array drop `{ href: '/proof', label: 'Proof' }` entry ∴ menu set ≡ {Home, Projects, Blog} per §I(∆) hidden-surface declaration; (4) `src/components/Footer.astro` drop `<a href="/proof">Proof</a>` `<nav>` entry; (5) `public/_redirects` change `/demo /proof 301` → `/demo /mailpilot 301` ∧ add `/proof /mailpilot 301` (backward-compat ∀ inbound `/proof` links); verify `make check` ∧ `make build` pass + Playwright `/` nav menu ≡ {Home, Projects, Blog} + `/mailpilot` route renders + `/proof` returns 301 → `/mailpilot`|V65,V4,V71,V23,I.routes
T84|x|elevate MailPilot to project #1 + link H3 → `/mailpilot` per §I(∆) — scope: (1) `src/pages/projects.astro` `projects` array — reorder entries to `[mailpilot, gcp-devops, pilot-skills, lab5]` (mailpilot moved from position 2 → 1); (2) wrap H3 `{p.repo}` w/ internal absolute-path link `<a href="/mailpilot">` ∀ entry where `p.id === 'mailpilot'` only (other 3 projects ⊥ have internal detail page ∴ H3 unlinked); (3) mirror reorder ∈ `src/pages/resume.astro` `// projects` section ∧ `public/kb-resume.txt` `## projects` per V50(6) 3-way mirror; (4) mirror reorder ∈ `src/pages/llms-full.txt.ts` `## Projects` block per V48; verify `make check` ∧ `make build` pass + Playwright `/projects` shows mailpilot as card #1 w/ H3 link → `/mailpilot`|V87,V82,V72,I.routes
T85|x|sweep V55(∆) `Layout.astro` `segmentLabel` post `/proof` → `/mailpilot` rename per B22 — scope: pattern `proof: 'Proof'` ∈ `src/layouts/Layout.astro` `segmentLabel` map (current line 153); remediation ≡ replace key `proof: 'Proof'` w/ `mailpilot: 'MailPilot'` (∵ `/mailpilot` ≡ live route per §I(∆); `/proof` ≡ 301-redirect surface ∈ `public/_redirects` only, ⊥ live route ∴ ⊥ §I-enumerated); verify `make check` ∧ `make build` pass ∧ `/sdd:check --incremental --all` ⊥ flag V55|V92,B22
T86|x|sweep V57 link-affordance carriers per V57 ∧ V46(b)(∆) ∧ V46(e)(∆) ∧ V2(∆) — scope: (1) `src/pages/index.astro:46,59,72` pattern `<a href="/projects" class="animate-on-scroll group block"` ∀ 3 //what-I-do tiles → replace anchor wrapper w/ `<div>`, drop closing `</a>` ∧ drop `group-hover:text-gh-green transition-colors` from H3 class (V46(b)(∆) tile-link drop); (2) `src/pages/index.astro:124` pattern `<span class="text-sm font-bold text-gh-fg sm:text-base">Let AI` → replace `text-gh-fg` token w/ `text-gh-blue` ∈ class string per V46(e)(∆) + V57(c) accent-title affordance; (3) `src/pages/projects.astro:69` pattern `hover:text-gh-green transition-colors` ∈ mailpilot H3 `<a>` → replace w/ `text-gh-blue hover:text-gh-blue-hover transition-colors` (resting-state accent per V57(b)); verify `make check` ∧ `make build` pass|V24,V4,V2
T87|x|sweep V57(∆) accent-color narrowing gh-blue → gh-green across link carriers (gh-blue reserved for V14 kicker scope) + fix /blog post-title H2 hover-only affordance drift — scope: (1) `src/pages/index.astro:124` proof-of-skills title `text-gh-blue` → `text-gh-green` per V46(e)(∆); (2) `src/pages/projects.astro:69` mailpilot H3 link `text-gh-blue hover:text-gh-blue-hover` → `text-gh-green hover:text-gh-green-hover` per V57(b); (3) `src/styles/global.css:191,198` `.prose a` color `gh-blue` → `gh-green`, `.prose a:hover` color `gh-blue-hover` → `gh-green-hover` per V57(a); (4) `src/pages/blog/index.astro:51` post-title H2 link add `text-gh-green hover:text-gh-green-hover` (currently hover-only via `link-animated` + `hover:text-gh-green` ∴ violates V57 resting-affordance rule); (5) `src/pages/blog/index.astro:66` "read →" link `text-gh-blue hover:text-gh-blue-hover` → `text-gh-green hover:text-gh-green-hover` per V57(b); (6) `src/pages/blog/[...slug].astro:76` `← all posts` link `text-gh-blue hover:text-gh-blue-hover` → `text-gh-green hover:text-gh-green-hover` per V57(b); (7) `src/pages/resume.astro:23,27,32` contact links `hover:text-gh-blue` (hover-only + gh-blue ∴ double-violation) → `text-gh-green hover:text-gh-green-hover` resting per V57(a); verify `make check` ∧ `make build` pass|V24,V4,V15
T88|x|sweep V57(b)(∆) ∧ V57(c)(∆) resting-underline carriers per B23 — scope: pattern `text-gh-green hover:text-gh-green-hover` ∈ {`src/pages/projects.astro`, `src/pages/index.astro`, `src/pages/blog/index.astro`, `src/pages/blog/[...slug].astro`, `src/pages/resume.astro`} (5 files, 6 link sites: projects mailpilot H3, index proof-of-skills card title span, blog/index post-title H2, blog/index `read →`, blog/[...slug] `← all posts`, resume contact 3-link sweep); remediation ≡ append `underline underline-offset-[3px]` Tailwind utility ∀ matched carrier (matches `.prose a` 3px offset ∈ `src/styles/global.css:192` ∀ visual parity), drop `link-animated` class @ `src/pages/blog/index.astro:51` (hover-only underline animation superseded by resting underline); verify `make check` ∧ `make build` pass → 2026-05-22 (8 pages built, 0 check errors)|V24,B23
T89|x|sweep V50(6)(∆) canonical-name carriers per B24 — scope: pattern `<h3[^>]*>(MailPilot\|Pilot Skills)</h3>` ∈ `src/pages/resume.astro` (2 sites: L97 ≡ `MailPilot`, L131 ≡ `Pilot Skills`) ∧ pattern `^(MailPilot\|Pilot Skills)$` ∈ `public/kb-resume.txt` `## projects` section (2 sites: L42 ≡ `MailPilot`, L50 ≡ `Pilot Skills`); remediation ≡ replace `MailPilot` → `mailpilot`, `Pilot Skills` → `pilot-skills` ∀ matched site; verify `make check` ∧ `make build` pass + /resume `// projects` H3 set ≡ /projects card H3 set ≡ {`mailpilot`, `gcp-devops`, `pilot-skills`, `lab5.ca`}|V87,V82,B24
T90|x|sweep V3(∆) written-out-number carriers per B25 — scope: grep pattern `\b(zero|one|two|three|four|five|six|seven|eight|nine|ten|eleven|twelve|thirteen|fourteen|fifteen|twenty|thirty|forty|fifty|sixty|seventy|eighty|ninety|hundred|thousand)(\s|-)(second|minute|hour|day|year|week|month|percent|page|line)s?\b` (case-insensitive) ∈ {`src/pages/{index,projects,mailpilot,resume}.astro`, `src/pages/blog/index.astro`, `src/pages/llms{,-full}.txt.ts`, `src/layouts/Layout.astro`} (8 files); current violations: `src/pages/mailpilot.astro:18` `Within sixty seconds`, `src/pages/mailpilot.astro:69` `under sixty seconds`; remediation ≡ `sixty seconds` → `60 seconds` ∀ matched site (matches `/index.astro:128` site-wide form `under 60 seconds`); verify `make check` ∧ `make build` pass + grep ∀ primary-page set ≡ ∅|V11,B25
T91|x|sweep V58(b) footer-nav link set — scope: `src/components/Footer.astro:9-11` `<nav>` block (currently ∋ `<a href="/blog" class="text-xs text-gh-fg-muted transition-colors hover:text-gh-fg">Blog</a>` only); remediation ≡ prepend `<a href="/">Home</a>` ∧ `<a href="/projects">Projects</a>` ∈ `<nav>` block matching `<Nav />` `links[]` order (Home → Projects → Blog), retain class string `text-xs text-gh-fg-muted transition-colors hover:text-gh-fg` ∀ each ∈ V57 nav-surface exemption; verify `make check` ∧ `make build` pass + Playwright snapshot `/` footer nav text set ≡ {Home, Projects, Blog}|V5
T92|x|sweep V50(5)(∆) sub-base-prose carriers per B26 — scope: grep pattern `text-(xs|sm)\b` ∈ `src/pages/resume.astro` (case-sensitive, word-boundary; 30 carrier sites @ amend-time per B26 inventory — contact secondary prose (3), tech-stack container `text-sm sm:text-base` (1), project descriptions (4), GitHub button labels (4), engagement-row date ranges (6), engagement ledes ∧ bullet lists (9), education degree line (1), education date range (1), footer-nav `text-xs` exempt per V57 nav-surface but ⊥ ∈ resume.astro scope ∴ ⊥ relevant); remediation ≡ replace `text-sm` ∧ `text-xs` ∧ `text-sm sm:text-base` ∧ `text-sm leading-relaxed text-... sm:text-base` patterns w/ `text-base` (∨ `text-base leading-relaxed text-...`) ∀ visible prose carrier — drop `sm:text-base` mobile-breakpoint pair entirely ∵ base-at-all-viewports per V50(5)(∆); verify `make check` ∧ `make build` pass + grep `text-(xs|sm)\b` ∈ `src/pages/resume.astro` ≡ ∅|V86,B26
T93|x|sweep V50(6)(∆) project-link form per /resume print-canonical-path — scope: grep pattern `rounded-lg border border-gh-border bg-white px-3 py-1\.5` ∈ `src/pages/resume.astro` (4 carrier sites: project entries @ mailpilot/gcp-devops/pilot-skills/lab5.ca, L102-109/L119-126/L136-143/L153-160; pattern matches the discriminative button-class substring); remediation ≡ replace each bordered button anchor wrapper (incl. inline GitHub SVG icon + `github.com/kborovik/<repo>` label text) w/ inline V57(a) text link form `<a href=... target="_blank" rel="noopener noreferrer" class="text-gh-green hover:text-gh-green-hover underline underline-offset-[3px]">github.com/kborovik/<repo></a>` matching V50(6) contact-block link form @ resume.astro:23/27/32 (LinkedIn ∧ GitHub ∧ lab5.ca); drop wrapping `<p class="mt-2">` if it carries the button anchor only (re-inline the link); kb-resume.txt mirror per V51 no-op ∵ plain-text already carries `https://github.com/kborovik/<repo>` URL on its own line; verify `make check` ∧ `make build` pass + grep `rounded-lg border border-gh-border bg-white px-3 py-1\.5` ∈ `src/pages/resume.astro` ≡ ∅ + Playwright `/resume` `// projects` section ∀ 4 entries ∋ green-URL link form|V87,V24
T94|x|implement V50(5)(∆) 2026-05-23 print-page-budget per /resume 3-page form — scope: `src/pages/resume.astro` `<style is:global>` `@media print` block + tech-stack container @ resume.astro:44 (`<div class="mt-8 space-y-6 text-base leading-relaxed text-gh-fg-muted">`); remediation ≡ (1) revert unstaged `html { font-size: 6px }` → `10px` per V50(5) print-base, (2) add `@media print { .resume .tech-stack-grid { display: grid; grid-template-columns: repeat(3, 1fr); column-gap: 1rem; row-gap: 0.4rem; } }` rule + add `tech-stack-grid` class to tech-stack container, (3) tighten section ∧ article ∧ subgroup gaps ∈ `@media print` per V50(5)(∆) (b), (4) add `.resume section { break-inside: avoid }` ∈ `@media print`, (5) verify via `make dev` → Chrome `--print-to-pdf` → `pdftoppm -r 100 /tmp/resume.pdf /tmp/p -png` → 3-page output ∧ no orphan section-header @ page bottom; verify `make check` ∧ `make build` pass|V86
T95|x|implement V50(5)(∆∆) print-pacing follow-up — scope: `src/pages/resume.astro` `<style is:global>` `@media print` block; remediation ≡ (1) `.resume .tech-stack-grid grid-template-columns` `repeat(3, 1fr)` → `repeat(2, 1fr)`, (2) section gap `.resume .mt-12 margin-top` ~0.8rem → ~1.5rem, section pt `.resume .pt-10 padding-top` ~0.5rem → ~1rem, h2 ↔ content gap `.resume section > h2 + div / + article margin-top` ~0.35rem → ~0.75rem, (3) tech-stack subgroup gap `.resume .space-y-6 > :not([hidden]) ~ :not([hidden]) margin-top` 0 → ~0.5rem; verify via `make dev` → Chrome `--print-to-pdf` → `pdftoppm -r 100 /tmp/resume.pdf /tmp/p -png` → ≤ 3 pages ∧ visible wider section breaks|V86
T96|x|sweep V3(∆) numeric-form clause carrier set per B27 audit-gap recurrence — scope: case-insensitive grep pattern `\b(zero|one|two|three|four|five|six|seven|eight|nine|ten|eleven|twelve|thirteen|fourteen|fifteen|twenty|thirty|forty|fifty|sixty|seventy|eighty|ninety|hundred|thousand)[ -](second|minute|hour|day|year|week|month|percent|page|line)s?\b` ∈ V3 primary-page carrier set ≡ {`src/pages/{index,projects,mailpilot,resume}.astro`, `src/pages/blog/index.astro`, `src/pages/llms{,-full}.txt.ts`, `src/layouts/Layout.astro`} (8 files); current violations: `src/pages/blog/index.astro:30` `five years` ∈ /blog meta lede (`...whether a system runs for a quarter or five years.`); remediation ≡ `five years` → `5 years` ∀ matched site; verify `make check` ∧ `make build` pass + re-run pattern ∀ V3 carrier set ≡ ∅ post-fix (close T90 execution-gap per B27)|V11,B27
T97|x|swap `public/kb-resume.txt` → `public/kb-resume.md` ∀ surface per V51(∆) — scope: grep pattern `kb-resume\.txt` ∈ {`src/`, `public/`} (current violations: `public/kb-resume.txt` itself, `src/pages/llms-full.txt.ts:3` Vite `?raw` import, `src/pages/llms-full.txt.ts:59` Resume narrative ref, `src/pages/llms-full.txt.ts:72` Contact line, `src/pages/llms.txt.ts:33` Contact list line); remediation ≡ `git rm public/kb-resume.txt` + rewrite carriers to `kb-resume.md` per T100 ∧ T101 (this task ≡ removal step only, carrier-rewrites delegated to T100/T101); verify post-fix `rg -F 'kb-resume.txt' src/ public/` ≡ ∅|V71,V72,V82
T98|x|rewrite `src/pages/resume.astro` `// tech stack` section per V50(6)(∆) — scope: tech-stack-grid block @ resume.astro:37-89 (current 9 H3 + 1-line body each); remediation ≡ replace w/ 3 H3 groups {Software Development, DevOps, AI & LLM} ∀ matching kb-resume.md Skills section L14-47; ∀ group ∋ multiple labeled body lines (`<p>` per `Label: tokens` line per V50(6)(∆) form, e.g. `Languages: Python, TypeScript, Node.js, Bash, SQL`); `tech-stack-grid` CSS class retained; print-form reflow per V50(5)(∆) ≤ 3-page budget — adjust `@media print { .tech-stack-grid }` grid-template-columns ∨ row-gap as needed to fit budget (impl-determined); verify `make check` ∧ `make build` pass + Playwright `/resume` ∋ exactly 3 H3 ∈ `// tech stack` section + Chrome `--print-to-pdf` `/resume` ≤ 3 pages|V87,V82,V83
T99|x|rewrite `src/pages/index.astro` `// what I do` 3-tile per V46(b)(∆) — scope: tile H3 + body `<p>` set @ index.astro:46-83 (current 3 tiles: Back-End Development, Front-End Development, Cloud Infrastructure); remediation ≡ replace tile H3 set → {Software Development, DevOps, AI & LLM} + rewrite each body `<p>` to middot-separated keyword stream per V46(b)(∆) draft (Software Development: `Python · TypeScript · Bash · SQL · FastAPI · Pydantic · Astro · Cloudflare Workers · REST APIs · OpenAPI · Event-Driven Architecture · Microservices · PostgreSQL · ETL · Structured-Extraction Pipelines · Pytest · TDD`; DevOps: `Google Cloud · Azure · Terraform · Ansible · Helm · GitHub Actions · Azure DevOps · Docker · Kubernetes · GKE · AKS · Cloud Run · Apache Airflow · IaC · CI/CD · OpenTelemetry · IAM · NIST 800-53 · PCI DSS`; AI & LLM: `Anthropic Claude · Google Gemini · Pydantic AI · LLM Agents · Agentic AI Development · RAG · Structured Extraction · MCP · Prompt Engineering · Claude Code · Claude Skills · AI-Augmented Development`); verify `make check` ∧ `make build` pass + Playwright `/` ∋ 3 tile H3 matching new set|V4
T100|x|patch `src/pages/llms-full.txt.ts` per V48(e)(∆) — scope: import line @ :3 (`import resumeRaw from '../../public/kb-resume.txt?raw'`), demote logic @ :6 (`resumeRaw.replace(/^## /gm, '### ')`), narrative ref @ :59 (`plain-text mirror at https://lab5.ca/kb-resume.txt`), Contact line @ :72 (`Plain-text resume: https://lab5.ca/kb-resume.txt`); remediation ≡ (1) swap import path → `'../../public/kb-resume.md?raw'`, (2) extend demote → strip kb-resume.md H1 (`^# .+\n+`) + dual demote (`^### ` → `^#### ` ∧ `^## ` → `### ` — order ! reverse-deepest-first to ⊥ collide), (3) narrative ref `.txt` → `.md` (label may read `markdown mirror` ∨ similar), (4) Contact line `.txt` → `.md` (label may read `Markdown resume`); verify `make check` ∧ `make build` pass + `dist/llms-full.txt` Resume section ∋ kb-resume.md content w/ `###` H3 ∀ section headings ∧ ⊥ orphan `# Konstantin Borovik` H1|V72
T101|x|patch `src/pages/llms.txt.ts` per V47(∆) ∧ V48(c)(∆) — scope: Contact list entry @ :33 (`- [Plain-text resume](https://lab5.ca/kb-resume.txt)`); remediation ≡ swap `.txt` → `.md` ∧ optional re-label `Plain-text resume` → `Markdown resume`; verify `make check` ∧ `make build` pass + `dist/llms.txt` Contact set ∋ updated URL|V71,V72
T102|x|rewrite `src/layouts/Layout.astro` `knowsAbout` JSON-LD array per V40 ∧ V52 against §I(∆) primary-audience keyword set — scope: array @ Layout.astro:58-103 (current 41 tokens shorthand form); remediation ≡ replace w/ canonical LinkedIn-skill token set per §I(∆) vocab line (3-group ordered: Software Development → DevOps → AI & LLM), token form ≡ LinkedIn-skill verbatim (`Google Cloud Platform (GCP)` ⊥ `Google Cloud`, `Identity and Access Management (IAM)` ⊥ `IAM`, etc.); verify `make check` ∧ `make build` pass + view-source `/` ∋ updated `knowsAbout` array w/ canonical-name tokens|V61,V83
T103|x|swap role-title canonical `Software Developer | DevOps Engineer` → `Software Engineer | DevOps Engineer` ∀ live carrier per V46(a)(∆) ∧ V50(1)(∆) — scope: grep pattern `Software Developer` ∈ {`src/`, `public/`, `assets-src/`} (live invariant carriers, ⊥ historical §T row text @ T65/T69/T72 ∈ SPEC.md which preserves prior canonical as history); current carriers (pre-T98/T99/T100 fold): `src/pages/index.astro:19` hero subtitle, `src/pages/resume.astro:8` description prop ∧ :13 H1, `src/layouts/Layout.astro:8` default `description` ∧ :47 `Person.jobTitle`, `src/pages/llms-full.txt.ts:20` H1 string, `src/pages/llms.txt.ts:15` blockquote intro, `assets-src/og.html` + `assets-src/linkedin-banner.html` visible string; remediation ≡ `Software Developer` → `Software Engineer` ∀ matched site + regen `public/og.png` + `public/banner-linkedin.png` per CLAUDE.md headless-Chrome screenshot recipe (1200×630 ∧ 1584×396); verify post-fix `rg -F 'Software Developer' src/ public/ assets-src/` ≡ ∅ (excluding historical SPEC.md §T row text) + `make check` ∧ `make build` pass|V1,V4,V81
T104|x|verify post-amend — scope: `make check` (0 errors) ∧ `make build` (page count unchanged); Playwright `/resume` ∋ 3 H3 ∈ `// tech stack` + matching kb-resume.md Skills group set; Playwright `/` ∋ 3 tile H3 set {Software Development, DevOps, AI & LLM}; Chrome `--print-to-pdf` `/resume` page count ≤ 3 per V50(5)(∆); `https://lab5.ca/kb-resume.md` serves 200 OK (manual post-deploy); `/sdd:check --incremental --all` ⊥ flag V46/V47/V48/V50/V51/V52|V45,V46,V23,V81,V86,V87
T105|x|sweep — port `public/kb-resume.md` engagement content (lede + bullets) to `src/pages/resume.astro` `// engagements` per V51 ∧ V54(∆) ∧ V50(6); scope vocab table: {kb-resume.md L80-92 → resume.astro lab5.ca article L158-185, L94-102 → Colliers L187-216, L104-112 → PayPal L218-247, L114-120 → CDW L239-262, L122-126 → TD Bank L264-290}; remediation ≡ replace lede + bullet prose w/ kb-resume.md verbatim equivalent (HTML markup preserved, content sourced from kb-resume.md — lab5.ca rich lede + `What I deliver:` 3-bullet sub-block per V54(∆) richer-content admit, Colliers/PayPal compact lede + dash-bullets per V54(∆) compact-form preference, CDW/TD Bank bullets-only per V50(6) older-engagement pattern); engagement-row composition baseline per V50(6) preserved (H3 + job-title + date range + lede + bullets); CDW date range 2014-2018 → 2010-2018 + Tech Data row removal (mirror of prior-turn unstaged code change) folded into this §T ∴ resume.astro engagement set ≡ kb-resume.md 5-row set {lab5.ca, Colliers, PayPal, CDW, TD Bank} per V51; verify `make check` ∧ `make build` pass + Playwright snapshot `/resume` engagements section ≡ kb-resume.md content + Chrome `--print-to-pdf` `/resume` ≤ 3 pages per V50(5)(∆)|V87,V82,V85,B28
T106|x|sweep — `src/pages/llms-full.txt.ts` `## What I do` capability block ordering → V2(∆) 2026-05-23 (B28 recurrence of B20); scope grep pattern `^0[1-3]\s+(Back-End Development\|Front-End Development\|Cloud Infrastructure)\.` ∈ `src/pages/llms-full.txt.ts` (3 paragraph leaders @ current lines 40, 42, 44); remediation ≡ rewrite 3 capability paragraphs to V2 ordering {01 Software Development, 02 DevOps, 03 AI & LLM} w/ hand-tuned narrative prose per V48(d) (wording hand-tuned, ⊥ machine-derived; carry kb-resume.md keyword stream signal — `Python · TypeScript · FastAPI · PostgreSQL · REST APIs · Pydantic AI · LLM agents` ∀ Software Development paragraph, `Google Cloud · Azure · Terraform · GitHub Actions · Docker · Kubernetes · GKE · IaC · CI/CD · OpenTelemetry` ∀ DevOps paragraph, `Anthropic Claude · Google Gemini · Pydantic AI · LLM agents · RAG · MCP · Claude Code · Claude Skills` ∀ AI & LLM paragraph); verify `make check` ∧ `make build` pass + grep pattern ≡ ∅ post-fix + `/sdd:check --incremental --all` ⊥ flag V48|V72,V2,B28
T107|x|sweep — `public/kb-resume.md` `## Projects` section reorder to V50(6) canonical {mailpilot, gcp-devops, pilot-skills, lab5.ca}; scope vocab table: {kb-resume.md L52-74 `## Projects` block — entries: mailpilot (L52-56), pilot-skills (L58-62), gcp-devops (L64-68), lab5.ca (L70-74)}; current order {mailpilot, pilot-skills, gcp-devops, lab5.ca}; remediation ≡ swap pilot-skills ∧ gcp-devops H3+body blocks (3-way mirror w/ resume.astro + projects.astro per V50(6) ∧ V51); verify `make check` ∧ `make build` pass + diff `## Projects` section ∈ kb-resume.md ≡ project order ∈ projects.astro + `/sdd:check --incremental --all` ⊥ flag V50|V87,V82,B28
T108|x|sweep `// tech stack` → `// tech skills` label per V50(6)(∆∆∆) — scope: grep pattern `[Tt]ech [Ss]tack` ∈ {`src/pages/resume.astro`, `src/pages/llms.txt.ts`, `src/pages/llms-full.txt.ts`} (3 files, 5 carrier sites: resume.astro:8 description prop, :37 HTML comment, :39 visible H2 kicker, llms.txt.ts:21 narrative ref, llms-full.txt.ts:63 narrative ref); remediation ≡ swap `tech stack` → `tech skills` preserving casing (`Tech stack` → `Tech skills` ∀ description-prop site, `tech stack` → `tech skills` ∀ kicker ∧ narrative sites); CSS class identifier `tech-stack-grid` retained (technical id ⊥ visible string); `public/kb-resume.md ## Skills` heading ⊥ ∈ scope per V50(6)(∆∆∆) (Markdown heading convention independent of kicker form); verify post-fix `rg -i 'tech stack' src/` ≡ ∅ + `make check` ∧ `make build` pass + Playwright `/resume` H2 set ∋ `// tech skills`|V87,V82
T109|x|implement /resume print-section page-break per V50(5)(∆∆∆) — scope: `src/pages/resume.astro` `<style is:global>` `@media print` block; remediation ≡ add rule `.resume section + section { break-before: page }` (adjacent-sibling selector matches projects + engagements + education sections — tech-skills excluded ∵ preceded by `<header>` not `<section>`); existing `.resume section { break-inside: avoid }` retained; verify via `make dev` → Chrome `--print-to-pdf` per CLAUDE.md /resume PDF render recipe → `pdftoppm -r 100 /tmp/resume.pdf /tmp/p -png` → 4-page output {p1: contact + tech-skills, p2: projects, p3: engagements, p4: education} per V50(5)(∆∆∆) budget + `make check` ∧ `make build` pass|V86
T110|x|init `src/pages/blog/[...slug].md.ts` Astro endpoint per V59 — `getStaticPaths()` ∀ non-draft blog post; GET emits frontmatter front-matter block (YAML-fenced: title, description, pubDate ISO, optional updatedDate, optional tags) + `entry.body` raw text; Content-Type `text/markdown; charset=utf-8`; verify `make build` emits `dist/blog/<slug>.md` ∀ 3 current posts ∧ `curl -sI` returns `Content-Type: text/markdown` post-deploy|V73,I.routes
T111|x|patch `src/pages/blog/[...slug].astro` per V45(∆) — emit `<link rel="alternate" type="text/markdown" href="/blog/<slug>.md">` ∈ `<head>` via new Layout `markdownAlternate` prop (∨ inline `<link>` in slot if Layout extension defers to T116); discovery hint per V59|V66,V73
T112|x|init `src/pages/rss.xml.ts` Astro endpoint via `@astrojs/rss` per V60 — `pnpm add @astrojs/rss`; channel{title:'lab5.ca blog', description:'Notes on cloud infrastructure, AI agents for business workflows, and web development.', site:'https://lab5.ca'}; items ≡ `getCollection('blog', !draft)` sorted pubDate desc, ∀ item {title, link:`${site}/blog/${id}/`, pubDate, description, categories: tags}; verify `make build` emits `dist/rss.xml` ∧ XML validates as RSS 2.0 (e.g. `xmllint --noout dist/rss.xml`)|V74,I.routes
T113|x|patch `src/layouts/Layout.astro` `<head>` per V45(∆) — emit `<link rel="alternate" type="application/rss+xml" title="lab5.ca blog" href="/rss.xml">` site-wide (∀ page); verify view-source `/` ∧ `/resume` ∧ `/blog` ∧ `/blog/<slug>` ∋ RSS alternate link tag|V66,V74
T114|x|patch `src/pages/llms.txt.ts:10` Posts entry shape per V47(∆) + V48(a)(∆) — render `- [${p.data.title}](https://lab5.ca/blog/${p.id}/): ${p.data.description}` (drop pubDate, add description per per-file shape); verify `dist/llms.txt` Posts entries ∋ description per V47(∆) shape; closes §B.29|V71,V72,B29
T115|x|patch `src/pages/llms.txt.ts:43` ∧ `src/pages/llms-full.txt.ts:86` Content-Type header per V47(∆) ∧ V48(e)(∆) — `text/plain; charset=utf-8` → `text/markdown; charset=utf-8`; verify `curl -sI https://lab5.ca/llms.txt` ∧ `curl -sI https://lab5.ca/llms-full.txt` return `Content-Type: text/markdown; charset=utf-8` post-deploy|V71,V72
T116|x|patch `src/layouts/Layout.astro` per V45(∆) — accept optional `markdownAlternate?: string` prop; emit `<link rel="alternate" type="text/markdown" href={markdownAlternate}>` ∈ `<head>` when set; patch `src/pages/resume.astro` to pass `markdownAlternate="/kb-resume.md"`; verify view-source `/resume` ∋ markdown alternate link tag pointing @ `/kb-resume.md` (mirror per V51)|V66,V82
T117|x|patch `src/layouts/Layout.astro` Person JSON-LD `personNode` block per V40(∆) — add `potentialAction: {@type:'EmailAction', name:'Email MailPilot live agent', description:'Send a question to demo@lab5.ca; live AI agent reply returns under 60 seconds.', target:{@type:'EntryPoint', urlTemplate:'mailto:demo@lab5.ca', actionPlatform:['http://schema.org/DesktopWebPlatform','http://schema.org/MobileWebPlatform']}}`; verify view-source `/` JSON-LD `@graph[0].potentialAction.target.urlTemplate` ≡ `mailto:demo@lab5.ca` + schema.org validator passes|V61
T118|x|sweep section kicker per V14(∆) — scope: grep pattern `(>// (what I do|claims|proof of skills|résumé|projects|proof of capability|tech skills|engagements|education|blog|how it works|sample questions|under the hood)<|>\\s*// (what I do|claims|proof of skills|résumé|projects|proof of capability|tech skills|engagements|education|blog|how it works|sample questions|under the hood)\\s*<)` ∈ `src/pages/{index,projects,mailpilot,resume}.astro`, `src/pages/blog/{index,[...slug]}.astro` (covers H2/p kickers w/ literal `// ` prefix); remediation ≡ (1) `src/styles/global.css` add `@layer components { .section-kicker::before { content: '// '; } }` (∨ raw CSS class outside @layer for Tailwind v4 @theme stack); (2) ∀ kicker element: append ` section-kicker` to existing Tailwind utility `class` string ∧ strip literal `// ` prefix from inner text content; verify `make check` ∧ `make build` pass + Playwright snapshot ∀ primary page renders `// xxx` visually identical to pre-amend + DOM text content of `.section-kicker` element ⊥ ∋ `// ` literal (i.e. `textContent` ≡ section name only)|V15
T119|x|restore `@astrojs/sitemap` integration per V61 ∧ §B.15(∆) — (1) `pnpm add @astrojs/sitemap`, (2) `astro.config.mjs` add `import sitemap from '@astrojs/sitemap'` + `integrations: [sitemap()]`, (3) `public/robots.txt` add `Sitemap: https://lab5.ca/sitemap-index.xml` line, (4) `public/_headers` restore cache-rule block ∀ `/sitemap-index.xml` ∧ `/sitemap-0.xml` (pre-T76 form: `Cache-Control: public, max-age=3600`); verify `make check` ∧ `make build` emits `dist/sitemap-{index,0}.xml` + `curl -sI https://lab5.ca/sitemap-index.xml` returns 200 post-deploy + `/sdd:check --all` V55 ⊥ flag sitemap surface (now §I-declared)|V92,V75,B15
T120|x|verify post-amend per V30/V31 — in-repo verification 2026-05-24: `make check` (0 errors, 0 warnings, 0 hints) ∧ `make build` (8 HTML pages + 3 blog .md + rss.xml + llms.txt + llms-full.txt + sitemap-{index,0}.xml emitted). HTML head spot-check ∀ {/, /resume/, /blog/spec-driven-development/}: hreflang en-ca + x-default present, `<link rel="alternate" type="application/rss+xml" href="/rss.xml">` site-wide, `<link rel="alternate" type="text/markdown" href="/kb-resume.md">` @ /resume, `<link rel="alternate" type="text/markdown" href="/blog/<slug>.md">` @ blog detail; `.section-kicker` class applied @ 14 carriers, DOM `// xxx` literal text grep ≡ ∅ post-sweep; Person JSON-LD ∋ `potentialAction.target.urlTemplate ≡ mailto:demo@lab5.ca`. Out-of-repo follow-ups (manual): `curl -sI https://lab5.ca/{rss.xml,sitemap-index.xml,blog/<slug>.md,llms.txt,llms-full.txt}` post-deploy + schema.org validator on `/` per V40; defer to next /sdd:check pass — `make check` (0 errors) ∧ `make build` (page count = pre-amend + N new endpoints: `/rss.xml`, `/sitemap-{index,0}.xml`, `/blog/<slug>.md` ∀ slug); endpoint sniff via `curl -sI https://lab5.ca/{rss.xml,sitemap-index.xml,llms.txt,llms-full.txt,blog/<sample-slug>.md}` confirms Content-Type per V47(∆)/V48(e)(∆)/V59/V60/V61; view-source `/` confirms `<link rel="alternate" type="application/rss+xml">` + Person JSON-LD `potentialAction.target.urlTemplate` ≡ `mailto:demo@lab5.ca`; view-source `/resume` confirms `<link rel="alternate" type="text/markdown" href="/kb-resume.md">`; view-source `/blog/<slug>` confirms `<link rel="alternate" type="text/markdown" href="/blog/<slug>.md">`; view-source ∀ kicker carrier site (per T118 grep scope) confirms `.section-kicker` element `textContent` ⊥ ∋ literal `// ` prefix + visual rendering matches pre-amend; out-of-repo schema.org validator (https://validator.schema.org/) on `/` passes ∀ Person + WebSite + WebPage nodes|V45,V46
T121|x|sweep — `demo@lab5.ca` → `hello@lab5.ca` per V40(∆∆∆) ∧ §I `cta.demo-email` amend (Gmail account migration 2026-05-25); scope: grep pattern `demo@lab5\.ca` ∈ {`src/`, `src/content/blog/`} (live carriers, ⊥ historical §T row text @ T117/T120 ∈ SPEC.md which preserves prior literal as history per T103 precedent); current carriers (pre-fix): `src/layouts/Layout.astro` Person JSON-LD `potentialAction` description + urlTemplate (L166, L169), `src/pages/index.astro` proof-of-skills body (L128), `src/pages/mailpilot.astro` body + widget label + clipboard `writeText` literal (L8, L69, L234), `src/pages/projects.astro` mailpilot card context (L9), `src/pages/llms-full.txt.ts` 3 narrative carriers (L39, L52, L59), `src/content/blog/where-pytest-stops-and-claude-code-starts.md` body prose (L149); remediation ≡ literal swap ∀ matched site; verify post-fix `rg -F 'demo@lab5.ca' src/` ≡ ∅ + `make check` ∧ `make build` pass + view-source `/` JSON-LD `@graph[0].potentialAction.target.urlTemplate` ≡ `mailto:hello@lab5.ca` + Playwright `/mailpilot` clipboard-copy widget copies `hello@lab5.ca` + out-of-repo: schema.org validator on `/` passes post-deploy|V61,I.cta
T122|x|sweep — `AI & LLM` → `AI & LLM Engineering` H3 + section label per §I vocab Group (3) rename ∧ V2(∆∆) ∧ V46(b)(∆∆) ∧ V50(6)(∆∆∆∆∆) 2026-05-26; scope: case-sensitive grep pattern `AI &(amp;)? LLM\b(?! Engineering)` ∈ {`src/pages/index.astro`, `src/pages/resume.astro`, `src/pages/llms-full.txt.ts`, `public/kb-resume.md`} (4 files); carrier sites @ amend-time: (a) H3 + section-label sites — `src/pages/index.astro` home tile H3 (L78), `src/pages/resume.astro` /resume `// tech skills` H3 (L73), `src/pages/llms-full.txt.ts` `03 AI & LLM.` paragraph leader (L48), `public/kb-resume.md` Skills H3 (L41) + project meta-pointer lines `*Skills: AI & LLM, ...*` (L54, L66); (b) lowercase prose-suffix sites — `src/pages/resume.astro` engagement lede + bullet (L177, L187), `public/kb-resume.md` engagement lede + bullet (L84, L88); remediation ≡ (a) sites: `AI & LLM` → `AI & LLM Engineering` (preserve `&amp;` HTML encoding ∀ .astro carriers, plain `&` ∀ .md ∧ .ts), (b) sites: lowercase `engineering` → Title Case `Engineering` ∀ matched site so prose ≡ Title Case canonical Group (3) name; verify post-fix `rg 'AI &(amp;)? LLM\b(?! Engineering)' src/ public/kb-resume.md` ≡ ∅ + `make check` ∧ `make build` pass + Playwright snapshot `/` home tile H3 set ≡ {Software Development, DevOps, AI & LLM Engineering} + view-source `/resume` `// tech skills` H3 set ∋ `AI & LLM Engineering` + `/sdd:check --incremental --all` ⊥ flag I.vocab/V2/V46/V50|I.vocab,V2,V46,V63
T123|x|sweep — /resume section order reorder per V50(6)(∆∆∆∆∆∆) — {contact → // tech skills → // projects → // engagements → // education} → {contact → // engagements → // projects → // education → // tech skills}; scope vocab table: {(a) `src/pages/resume.astro` H2 visual sequence — current `<section>` blocks @ L40 (// tech skills), L84 (// projects), L167 (// engagements), L331 (// education) reorder to engagements → projects → education → tech skills (preserve each section's internal content tree, move whole `<section>` blocks), (b) `src/pages/resume.astro:8` description prop string `Tech skills, projects, engagements, and education.` → `Engagements, projects, education, and tech skills.`, (c) `public/kb-resume.md` H2 sequence — current `## Skills` (L12), `## Projects` (L50), `## Engagements` (L78), `## Education` (L130) reorder to `## Engagements` → `## Projects` → `## Education` → `## Skills` (move whole H2-bounded blocks), (d) `src/pages/llms.txt.ts:24` Resume entry narrative `CV — tech skills, projects, engagements, education` → `CV — engagements, projects, education, tech skills`, (e) `src/pages/llms-full.txt.ts:63` Section-order narrative `Section order: contact → tech skills → projects → engagements → education.` → `Section order: contact → engagements → projects → education → tech skills.`}; remediation ≡ structural block-move ∀ (a) ∧ (c) (whole-section reorder preserving in-section content + class strings), literal string swap ∀ (b) ∧ (d) ∧ (e); CSS class identifier `tech-stack-grid` retained per V50(6)(∆∆∆) precedent (technical id ⊥ visible string); verify post-fix `make check` ∧ `make build` pass + Playwright snapshot `/resume` H2 order ≡ [// engagements, // projects, // education, // tech skills] + diff `public/kb-resume.md` H2 order ≡ [Engagements, Projects, Education, Skills] + `make dev` → Chrome `--print-to-pdf` `/resume` → `pdftoppm -r 100 /tmp/resume.pdf /tmp/p -png` → page count ≤ 4 per V50(5)(∆∆∆∆) budget (exceedance triggers follow-up V50(5) amend per (∆∆∆∆) clause)|V87,V82,I.routes

# SPEC.md — lab5.ca

## §G — goal

personal portfolio + career site @ `lab5.ca` ∀ Konstantin Borovik (Software Engineer | AI & LLM Engineering). pitch ≡ AI & LLM engineering + software development + DevOps & Cloud Platforms; 3 surfaced offerings, each portfolio-anchored on `/projects`. proof-of-capability surfaces ≡ MailPilot demo (`/mailpilot`) + `/projects` GitHub-repo showcase + `/blog` writing. resume content split out 2026-06-04 → `github.com/kborovik/resume` (separate repo w/ own SPEC.md); `/resume` route ∧ `public/kb-resume.md` retired ∴ lab5.ca SPEC ⊥ governs resume content forward — keyword set ∀ tile bodies ∧ Person.knowsAbout JSON-LD ∧ llms.txt narrative sourced from §I `vocab: SEO keyword set` (self-canonical, ⊥ external mirror); SEO posture re-tunes toward AI & LLM Engineering primary keyword density per T135. §G(∆) 2026-06-07 per T140 — index-page positioning amend: `/` recast as CFO/CIO cold-email landing surface ∴ hero H1 + section sequence + voice register on `/` reorient toward buyer-pain (LLM cost-per-outcome) ∧ engagement-structure (Build / Optimize-rescue) framing; internal routes (`/projects`, `/blog`, `/mailpilot`) retain portfolio/career-site role; MailPilot remains proof-of-capability anchor.

## §C — constraints

- stack: Astro 6 static (⊥ SSR adapter), Tailwind v4 via `@theme`, TS 6, pnpm 10, Node ≥22.
- host: Cloudflare Workers static assets; deploy via GH Actions on push → `main`; domain via CF DNS + auto SSL.
- typeface: Space Grotesk (body/headings/nav) + IBM Plex Mono (`code`/`pre`) across site + social assets (social assets carry ⊥ code ∴ render Space Grotesk only).

## §I — interfaces

- route: `/` → home (hero w/ 2-line title block: H1 `Konstantin Borovik` + subtitle `Software Engineer | AI & LLM Engineering` (pipe verbatim); sections: hero, `// what I do` 3-tile, `// claims`, `// proof`, `// résumé`)
- route: `/projects` → skills→GitHub-repo proof grid (4 hand-authored cards: gcp-devops, mailpilot, pilot-skills, lab5.ca; Salesforce card reserved ∀ future)
- route: `/mailpilot` → MailPilot evidence (code-chip CTA, how-it-works, sample Q's, architecture); hidden surface ⊥ ∈ `<Nav />` (reachable via `/projects` card #1 H3 link only — analogous to `/resume` hidden via index `// résumé` button); `/demo` ∧ `/proof` ≡ legacy → CF redirect → `/mailpilot`
- route: `/blog` → post list (sort pubDate desc)
- route: `/blog/<slug>` → post detail
- route: `/blog/<slug>.md` → markdown mirror of `/blog/<slug>` HTML; build-output via `src/pages/blog/[...slug].md.ts` Astro endpoint; content ≡ frontmatter front-matter block (title, description, pubDate, optional updatedDate, optional tags) + raw `entry.body` text; `Content-Type: text/markdown; charset=utf-8`; agent-discovery surface per V59
- route: `/rss.xml` → RSS 2.0 feed via `@astrojs/rss`; ∀ non-draft blog post enumerated, order ≡ pubDate desc; item ∋ {title, link, pubDate, description, categories=tags}; build-output via `src/pages/rss.xml.ts` per V60
- route: `/sitemap-{index,0}.xml` → `@astrojs/sitemap` integration build-output; auto-tracks ∀ Astro route; restored 2026-05-24 per §B.15(∆) LLM-agent usability audit override; declared §I surface ∴ V55 surface-completeness gate satisfied (pre-T58 drop ≡ undeclared-surface class); per V61
- domain: `lab5.ca` ≡ primary; `mailpilot.ca` ≡ alias → serves same site (`wrangler.jsonc` routes)
- analytics: Cloudflare Web Analytics — server-side, automatic via CF dashboard; ⊥ in-repo script tag, ⊥ cookies
- cta: book-call `https://calendar.app.google/cYM3H3TsHsequR587`
- cta: linkedin `https://www.linkedin.com/in/kborovik`
- cta: mailpilot-source `https://github.com/kborovik/mailpilot` — link to MailPilot source; surfaced ∈ `/mailpilot` (under-the-hood) ∧ home (proof-of-capability)
- cta: demo-email ≡ code-chip clipboard-copy widget @ `/mailpilot` — copies `hello@lab5.ca`; ⊥ `<a href="mailto:...">`
- schema: blog frontmatter ≡ {title:str, description:str, pubDate:date, updatedDate?:date, draft?:bool=false, tags?:str[]=[]} per `src/content.config.ts`
- cmd: `make {help,install,dev,preview,build,check,clean,clean-all,deploy,status,wrangler,playwright}`
- env: GH Actions ! set `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`
- asset: `public/{og.png,banner-linkedin.png,favicon.{ico,svg},_headers,_redirects,robots.txt,logo-linkedin.{svg,png},kb-headshot.jpg}`; `public/blog/<slug>/*.webp` ≡ per-post blog assets per V57; `llms.txt` ∧ `llms-full.txt` ≡ build-output via `src/pages/llms.txt.ts` ∧ `src/pages/llms-full.txt.ts` per V48(e), ⊥ ∈ `public/`; `rss.xml` ≡ build-output via `src/pages/rss.xml.ts` (`@astrojs/rss` dep) per V60; `sitemap-{index,0}.xml` ≡ build-output via `@astrojs/sitemap` integration per V61 (restored per §B.15(∆)); `blog/<slug>.md` ≡ build-output via `src/pages/blog/[...slug].md.ts` per V59; `kb-resume.md` retired 2026-06-04 per resume migration (content → `github.com/kborovik/resume`)
- token: palette ≡ GH Primer Light — `gh-canvas`, `gh-canvas-subtle`, `gh-border`, `gh-fg`, `gh-fg-muted`, `gh-fg-subtle`, `gh-green`, `gh-green-hover`, `gh-blue`, `gh-blue-hover`
- token: typeface ≡ Space Grotesk (400/500/600/700) prose default + IBM Plex Mono (400/500/600/700) code surfaces, both via Google Fonts `display=swap`
- vocab: SEO keyword set ≡ self-canonical 3-group H3 set (resume-migration retire of prior `vocab: /resume primary audience` per T134 2026-06-04 — kb-resume.md ## Skills canonical-source mirror dropped, vocab block now self-canonical ∵ kb-resume.md retired) — H3 group set {(1) AI & LLM Engineering, (2) Software Development, (3) DevOps & Cloud Platforms} (group order ≡ tile-set order per V2 — primary AI & LLM Engineering positioning per V4(∆∆∆) 2026-06-04 pivot; prior `DevOps` ∧ `DevOps & Operations` shortened-forms retired forward 2026-06-04 per T132 + T133, historical refs preserve prior literals per documentary convention) + per-group labeled body lines (`Label: token, token, ...` form): (1) AI & LLM Engineering — `AI agents: Pydantic AI, OpenAI Agents SDK, Claude Code, Model Context Protocol (MCP); Retrieval & extraction: Retrieval-Augmented Generation (RAG), structured extraction, Large Language Models (LLM); Foundations: OpenAI, Anthropic, Google Vertex AI`; (2) Software Development — `Languages: Python, TypeScript, SQL; Web & API: Astro, React, FastAPI, REST; Data: PostgreSQL, BigQuery, Redis`; (3) DevOps & Cloud Platforms — `Cloud: Google Cloud Platform (GCP), Microsoft Azure, Cloudflare; Platform Engineering: Google Kubernetes Engine (GKE), Cloud Run, Docker; IaC & CI/CD: Terraform, GitHub Actions, Infrastructure as Code (IaC); Observability: Pydantic Logfire, OpenTelemetry, structured logging`; token form ≡ LinkedIn-skill verbatim w/ qualifier-and-acronym shape — e.g. `Google Cloud Platform (GCP)` ⊥ `Google Cloud`, `Retrieval-Augmented Generation (RAG)` ⊥ `RAG`, `Large Language Models (LLM)` ⊥ `LLM`, `Model Context Protocol (MCP)` ⊥ `MCP`, `Google Kubernetes Engine (GKE)` ⊥ `GKE`, `Infrastructure as Code (IaC)` ⊥ `IaC`; mirror surfaces ≡ (a) `src/pages/index.astro` `// what I do` tile bodies in digest form per V4(b), (b) `src/layouts/Layout.astro` Person.knowsAbout JSON-LD per V61; set framed ∈ KB's actual experience per V83 (⊥ fabricate platform-fluency tokens); AWS tokens dropped 2026-05-21 (⊥ shipped AWS work), OAuth/RBAC dropped (IAM-config-only experience); history ≡ git log + §B

## §V — invariants

archived: §V.retired → SPEC.archive.md (1 retired row); live invariants below.

V1: pitch ≡ `LLM systems that do real work — measured cost per outcome` w/ 2-offer engagement structure (Build / Optimize-rescue) per V2 + V4; pitch-language carrier set ≡ {(1) `src/layouts/Layout.astro` default `description` fallback, (2) per-page meta `description` prop ∀ primary page per V44, (3) hero ∧ claims ∧ capability blurbs ∈ `src/pages/index.astro`, (4) narrative blocks (hero / claims / capability / proof) ∈ `src/pages/llms.txt.ts` ∧ `src/pages/llms-full.txt.ts`}; `/` pitch-carrier set ∀ index ≡ {hero H1+subhead+microline, problem-section narrative, what-I-do 2-offer blurbs, proof-section narrative w/ cost-table, money-discipline list, why-me wholesale-vs-retail + track-record, closing CTA} — all ∈ `src/pages/index.astro`; llms-full.txt `/` narrative mirrors `/` (2-offer engagement structure, ⊥ 3-discipline tile set) per V72(d) parity; Person.knowsAbout JSON-LD + §I vocab SEO keyword set retain 3-discipline taxonomy (orthogonal carriers); other primary-page pitch carriers (Layout.astro default description, /projects + /mailpilot meta description, llms-full.txt non-`/` narrative blocks) unaffected
V2: `// what I do` @ `/` ≡ 2-offer engagement structure side-by-side: {tile-1 H3 `Build Business Processes with AI` (body opens `A new business process, powered by an LLM agent…` — new business process w/ measured cost per outcome, high-volume lookup-heavy repetitive work), tile-2 H3 `Rescue a Runaway AI Project` (body describes stalled/runaway AI initiative — instrument it, find the money, cap runaway paths, add grounding + guardrails)} + closing senior-owner clause (one senior owner end-to-end, ⊥ account-manager layer); both H3 Title Case per V14; build-new vs rescue-existing contrast preserved; 3-discipline tile-set keyword surface (AI & LLM Engineering, Software Development, DevOps & Cloud Platforms) retained ∈ §I vocab + Person.knowsAbout JSON-LD ONLY; llms-full.txt `## What I do` 2-offer block mirrors `/` labels per V72(d) parity; carrier ≡ `src/pages/index.astro`
V4: `/` composition ∀ CFO/CIO landing pivot ≡ (a) hero — H1 `LLM systems that do real work — without becoming a money pit.` (sentence case per V13) + 2-line subhead value-prop block naming cost-per-outcome traced before scale (⊥ pilots that quietly bleed) + primary CTA button `Book a 30-minute call` linking §I cta.book-call + secondary bordered white button `See the cost-per-outcome math` (form ≡ `inline-flex items-center gap-2 rounded-lg border border-gh-border bg-white px-4 py-2 text-base font-semibold text-gh-fg transition-colors hover:bg-gh-canvas-subtle`, ⊥ icon, content-label-only per V11) linking `/blog/what-one-ai-email-costs` (CTA buttons ⊥ `→` arrow tail) + credibility microline (`20+ years shipping production software · AI agents running in production today · Toronto-based`) + identity-binding row (LinkedIn + github bordered buttons, form ≡ V24(d) bordered pattern, LinkedIn href ≡ §I cta.linkedin / github href `https://github.com/kborovik` per V63 sameAs, external link attrs `target="_blank"` + `rel="noopener noreferrer"` per V22) + headshot ∈ shared hero grid (KB name surfaces ∈ `<Footer />` brand wordmark + Person JSON-LD per V61); (b) `// the problem` 3-paragraph buyer-pain narrative: ¶1 repetitive lookup-heavy work consuming skilled time (answering same product questions, pulling number from doc, triaging email, filling forms), ¶2 hidden-cost class (⊥ line item, spread across people + customers), ¶3 LLM systems as genuine fit + the catch (⊥ trust + ⊥ afford gate); (c) `// what I do` 2-offer block per V2 — side-by-side {Build Business Processes with AI, Rescue a Runaway AI Project} w/ single-paragraph blurb each + closing senior-owner clause; (d) `// proof` MailPilot evidence block — opening honest-skepticism lead ¶ (concedes evidence ≡ self-reported + single-project + early-numbers-off-live-inbox, pivots to verifiability: open code ∈ GitHub + every model call traced + reader emails agent + checks reply, thesis `a claim you can verify beats a testimonial you can't`) + MailPilot-naming ¶ in buyer vocab (reads incoming business email, searches KB, replies w/ sourced answer in under a minute, declines out-of-scope, you can email it yourself) + cost-per-outcome 4-row table {Cost per sourced reply, Response time, Out-of-scope questions, At 1,000 emails / month} comparing MailPilot (measured) vs A person (assumption), header row `<empty> | MailPilot (measured) | A person (assumption)` + commentary ¶ (100× cost delta w/ `$5` person-cost labeled assumption reader swaps own loaded cost per reply [ratio shifts, order-of-magnitude holds], hours-to-seconds, ⊥ replace people, frees skilled work) + risk-framing ¶ (fabrication risk > cost risk, MailPilot grounds + cites + decline behavior measured in logs) + secondary inline text link `Read the full cost-per-outcome breakdown →` linking `/blog/what-one-ai-email-costs`; inline `hello@lab5.ca` ∈ MailPilot prose (bold ∨ mono) + `→ See MailPilot in action` ∨ inline anchor linking `/mailpilot`; (f) `// how the money stays under control` dash-bullet list per V17 (5 entries: measure cost per outcome, cap usage, instrument every call, ground + decline, scale to zero when idle) + 1-line closing ¶ framing 5%-vs-95% as build discipline ⊥ model choice; (g) `// why me` block — (g.i) wholesale-vs-retail economics 3-paragraph narrative (agency overhead → direct-pay structure → senior+lower-rate inversion) → (g.ii) bus-factor/continuity sub-block (every employee ≡ bus-factor, usually worse ∵ undocumented knowledge departs w/ them; client holds whole owned documented system [code ∈ their repository + their cloud account + retrieval/guardrails/observability documented plain-language], any competent engineer reads + takes over, lower bus-factor than most hires; handover ≡ `git clone` ⊥ renegotiation; honest scope-boundary disclaimer: work needing full standing team ≡ work for a team, system runs on own infrastructure whether ∨ ⊥ KB present; block phrased w/o round-the-clock/coverage tokens to stay clear of V18 SRE-adjacency, standing-team/scale framing only) → (g.iii) track-record 4-bullet credentials list per V17 {20+ years shipping production software (data centers, payments, real estate); solo end-to-end delivery (back-end, infrastructure, CI/CD, deployment, observability); AI agents in production today (LLMs, retrieval [RAG], structured extraction, guardrails + tracing); Toronto-based long-term contract (embedded, ⊥ drive-by engagement)} + compact tech-stack line (`Built on: Anthropic Claude · Google Gemini · Python · FastAPI · Pydantic AI · RAG · MCP · Google Cloud / Azure · Terraform · Kubernetes · OpenTelemetry`); (h) `// closing` block — 1-paragraph close (evaluating AI for real work w/o money pit ≡ question I build + measure; want cost-per-outcome math on workflow in your operation?) + primary CTA button `Book a 30-minute call` (2nd repetition, ≤ 2 CTA repetition per V21); voice ≡ buyer-pain register per V11; both `/` + llms-full.txt `/` narrative (`## Proof` + `## Why me` + `## What I do`) mirror per V72(d) parity; carrier ≡ `src/pages/index.astro`
V5: footer (`src/components/Footer.astro`) composition ≡ (a) brand wordmark `Konstantin Borovik` → `/` left (`text-sm font-bold text-gh-fg`); (b) nav link set ≡ {Home → `/`, Projects → `/projects`, Blog → `/blog`} right — mirrors `<Nav />` `links[]` minus Book-a-Call per V4 ("⊥ page-footer CTA repeat"); (c) copyright line `© <year> Konstantin Borovik. All rights reserved.` below (year ≡ `new Date().getFullYear()` runtime, ∴ year auto-rolls ∀ build); (d) bg ≡ `bg-gh-canvas-subtle` per V13 contained-surface clause. Footer-nav link affordance per V57 nav-surface exemption (hover-only color shift admitted ∀ link row). Hidden surfaces (`/resume`, `/mailpilot`) ⊥ admitted ∈ footer nav ∵ outside `<Nav />` `links[]` set per §I — hidden-by-Nav ≡ hidden-by-Footer. New route ∈ `<Nav />` `links[]` set ! pair w/ footer-nav update ∈ same commit per (b) mirror clause.
V11: voice ≡ factual + tech vocab + numeric form ∀ measurable quantity (duration, count, percentage, year span); ⊥ aspirational verbs, ⊥ persona breakouts, ⊥ marketing flourish, ⊥ written-out numbers (e.g. `sixty seconds`, `twenty percent`, `hundred-page report`) ∈ primary-page copy (`src/pages/{index,projects,mailpilot,resume}.astro`, `src/pages/blog/index.astro`, narrative blocks ∈ `src/pages/llms{,-full}.txt.ts`, Layout default `description` fallback); blog body (`src/content/blog/**/*.md`) exempt per V28 first-person narrative voice — written-out form admitted ∀ narrative time references (`three weeks later`, `six months`, `five years`); `/` index-page voice register exemption ∀ CFO/CIO landing pivot: forward `src/pages/index.astro` admits buyer-pain-naming vocabulary in service of cold-email-landing reader — admitted tokens (e.g. `money pit`, `cost-per-outcome`, `bleeds`, `becomes the budget line you regret`, `the catch is`, `the promise is simple`) ≡ buyer-pain register, ⊥ aspirational verb class; aspirational-verb ban (⊥ `move faster`, ⊥ `transform`, ⊥ `unlock`, ⊥ `empower`, ⊥ `ship more w/ same headcount`) retained ∀ `/` ∴ exemption ≡ register-shift, ⊥ rule-suspension; numeric form ∀ measurable quantity retained (e.g. `~$0.03`, `~20 seconds`, `1,000 emails / month`, `100×`, `20+ years`); ⊥ written-out numbers retained per V11 main body; `/` buyer-pain register propagates to llms-full.txt `/` narrative blocks (hero + the-problem + what-I-do + proof + how-money-stays-under-control + why-me + closing) per V72(d) parity invariant; other primary-page carriers (`/projects`, `/mailpilot`, `/blog`, `/blog/<slug>`, `Layout.astro` default description, llms-full.txt non-`/` narrative blocks) unaffected ∴ buyer-pain register confined to `/` ∧ its llms-full.txt mirror, V11 main register holds elsewhere.
V12: → V13 (folded; heading-no-trailing-dot)
V13: page H1 ∧ blog title casing ≡ sentence case; ⊥ trailing `.`
V14: tile title ∧ step title casing ≡ Title Case; section kicker `// xxx` retained lowercase per V14 (different element class — visual marker, ⊥ content header); H1 sentence case per V6 unchanged
V15: section kicker ≡ `// section name`; class ≡ `text-base font-bold uppercase tracking-[0.2em] text-gh-blue`; DOM text content of kicker element ! ≡ section name only (e.g. `what I do`); literal `// ` prefix ⊥ ∈ DOM text — injected via CSS `::before { content: '// '; }` on element bearing `.section-kicker` class (decl ∈ `src/styles/global.css`); rationale ≡ agent parsers (JSON-LD ingesters, llms.txt consumers, RSS readers, generic HTML→markdown crawlers) read heading text without `// ` comment-marker noise — `//` is C-family inline-comment syntax ∴ may be misparsed as comment opener by naive tooling. Carrier class augmentation ≡ append `section-kicker` to existing Tailwind utility string ∀ kicker carrier site (current set: index.astro:41/91/116/166, projects.astro:56, mailpilot.astro:58 + internal kickers, resume.astro:39/83/166/330, blog/index.astro:23, blog/[...slug].astro:43); orthogonal to V49(b) heading-element rule (V49(b) ≡ `<h2>` element semantic, V15 ≡ DOM text semantic — different axes).
V16: numbered hairline ≡ `<NN> ─────` above tiles, mono `text-xs text-gh-fg-muted`; numbering continues across grids when narrative ≡ one
V17: bullet list ≡ dash-prefix; ⊥ check/x SVG icons
V18: claims ∧ capability tiles ∧ llms-full.txt narrative blocks ! ⊥ ∋ on-call ∨ pager ∨ incident-response ∨ SRE ∨ oncall-engineer vocabulary — KB self-positions as `Software Engineer | AI & LLM Engineering` per V4(a) hero subtitle, ⊥ SRE/oncall-engineer adjacent role despite IAM-config ∧ on-call-rotation experience present; tile-3 H3 label `DevOps & Cloud Platforms` per V2 — "Cloud Platforms" admitted as platform-engineering noun ∈ tile label (⊥ SRE-adjacent ∴ ⊥ trigger role-shift vocabulary class); carrier set ≡ {`src/pages/index.astro` //claims dash bullets ∧ //what-I-do tile bodies, `src/pages/llms-full.txt.ts` claims ∧ capability blocks per V72}; dev-adjacent ops tokens ∈-bounds: `deploy`, `observability`, `IaC`, `CI/CD`, `infrastructure-as-code`, `Terraform`, `Docker`, `Kubernetes`; role-shifting tokens ⊥ admitted: `on-call`, `pager`, `paging`, `incident-response`, `incident response`, `SRE`, `site reliability`, `oncall-engineer`, `on-call engineer`; orthogonal to V83 (V83 ≡ ⊥ fabricate token coverage on absent capability; V18 ≡ ⊥ self-position into adjacent role despite present capability); on `/` the on-call/pager/incident-response/SRE/oncall-engineer vocab exclusion applies to (g.ii) `// why me` track-record 4-bullet credentials list per V4 — same exclusion set; `// what I do` 2-offer block engagement-action labels (Build, Optimize / Rescue) admit engagement vocab only ∴ on-call exclusion N/A ∈ tile labels; on-call/pager/incident-response/SRE/oncall-engineer vocab exclusion applies to llms-full.txt `/` narrative ∈ post-parity carrier shape per V72(d) (hero, the-problem, what-I-do, proof, how-money-stays-under-control, why-me — esp. (g.ii) track-record bullets carrying tech-stack tokens, closing); rule scope ≡ vocab class, ⊥ carrier shape ∴ shape evolution ⊥ weaken exclusion.
V21: Book-a-Call ∈ nav (`cta-pulse`); ∀ non-`/` page (`/projects`, `/mailpilot`, `/blog`, `/blog/<slug>`): ≤ 1 prominent CTA per page + nav-only Book-a-Call discipline (⊥ page-footer repeat); `/` index-page CTA-repetition admit ∀ CFO/CIO cold-email landing pivot: `Book a 30-minute call` primary CTA button admitted ≤ 2 repetitions on `/` (no arrow `→` tail per V4(a)) ∈ {hero block, closing block} per V4 (a)+(h); hero secondary form ≡ bordered white button (`See the cost-per-outcome math`, ⊥ counted as prominent CTA ∵ distinct visual weight from green primary — bordered white + content label, ⊥ accent-color CTA shape ∴ functions as paired-secondary not parallel primary); post-proof `Read the full cost-per-outcome breakdown →` retains inline text-link form ∴ ⊥ counted as prominent CTA ∵ V24(a) inline-text-link affordance form (underline + accent), ⊥ V24(d) button shape; secondary links unlimited on `/`; uniform primary-CTA visual form ∀ admitted repetitions on `/` (matching button class string, same href ≡ §I cta.book-call) — prevents reader confusion about whether each repeats one action ∨ proposes different actions; ∴ `/` admits Book-a-Call ∈ nav + 2 page-body repetitions.
V22: link href ≡ — internal: absolute path `/...`, ⊥ trailing `/`, ⊥ relative; cross-page anchor: `/path#frag`; intra-page anchor: `#frag`; external: full `https://...`; mailto: `mailto:<addr>`. ∈ `.astro`: external `<a>` ! `target="_blank"` ∧ `rel="noopener noreferrer"`; mailto ⊥ `target="_blank"`. ∈ blog `.md`: plain markdown `[text](url)` only — `rehype-external-links` (`astro.config.mjs`) auto-inserts attrs @ build ∴ ⊥ author raw `<a>`; external link anchor text ≡ plain inline prose, ⊥ `**bold**` wrap ∨ `` `code` `` wrap ∈ `[...]`. partial-wrap subclass: code-fragment ∨ bold ∨ italic wrap on sub-token ∈ otherwise-plain anchor-text (e.g. `[`token` description](url)`) ≡ violation per same plain-prose clause — sub-token markup wrap inside `[...]` falls under same prohibition as full-wrap; remediation ≡ promote sub-token to plain prose ∨ relocate markup outside anchor (`the [semantic conventions for `gen_ai.*`](url)` ⊥ `[`gen_ai.*` semantic conventions](url)`) per §B.32.
V23: ∀ primary page (`/`, `/projects`, `/blog`, `/blog/<slug>`) ! satisfy WCAG 2.1 Level AA baseline — (a) text contrast ≥ 4.5:1 normal-size, ≥ 3:1 large (≥18pt ∨ ≥14pt bold); `gh-fg-subtle` (#818b98) ≡ large-text-only — body-size sites ! use `gh-fg-muted` (#424a53); (b) heading order sequentially-descending — `<h3>` ⊥ appear w/o prior `<h2>` in scope; V14 section kicker `// xxx` ≡ `<h2>` element when section ⊃ tile `<h3>`s, V14 visual class string (`text-sm font-bold uppercase tracking-[0.2em] text-gh-blue`) retained; (c) ∀ interactive element ! ∋ accessible name — links ∋ visible text ∨ `aria-label`; brand-logo link `<a href="/">` ∈ `Nav.astro` ! `aria-label="Home"`; (d) exactly one `<main>` landmark per page — `Layout.astro` ! wrap `<slot />` ∈ `<main>` between `<Nav />` ∧ `<Footer />`; verification ≡ Lighthouse a11y category ≥ 0.95 ∀ primary page — manual periodic audit (⊥ CI-gated; matches T28 verification pattern, promote to CI only if drift recurs); verification clause ≡ HOLD-equivalent ∀ /sdd:check (manual-periodic-by-design; ⊥ UNVERIFIABLE — UNVERIFIABLE ≡ audit-infra-gap class only, ⊥ CI-opt-out rows); Promote-to-CI trigger unchanged ∴ a11y regression ∈ §B → V49 reclassifies VIOLATE.
V24: ∀ clickable surface (`<a href>` element, including card-as-link wrappers ∧ button-shaped links) @ lab5.ca user-facing pages ! signal clickability pre-hover via ≥ 1 admitted affordance form — (a) inline text link: underline + `text-gh-green` accent; (b) heading/title link: `text-gh-green` accent + resting underline; (c) card-as-link: bordered card-shape + (i) `text-gh-green` accent-colored title + resting underline ∨ (ii) visible corner chevron `→`; (d) button-shaped link: bordered + icon + label text (current /projects GitHub-link button pattern). Underline impl ∀ Tailwind carriers ≡ `underline underline-offset-[3px]` — 3px offset matches `.prose a` ∈ `src/styles/global.css:192` ∀ visual parity body-prose ↔ heading/title link sites. `text-gh-blue` reserved for V14 section-kicker scope ∴ ⊥ admitted ∀ link affordance ∵ visual collision w/ uppercase-tracked kicker class. Nav-surface exemption: top-nav links (`src/components/Nav.astro`) ∧ footer-nav links (`src/components/Footer.astro`) operate under nav-position convention (horizontal nav bar + grouped labels ≡ universally-recognized nav affordance) ∴ per-link resting accent ⊥ required, hover-only color shift admitted ∀ nav-surface link rows only. Hover-only signals (cursor change, color shift on hover, bg shift on hover) ⊥ sufficient ∵ touch devices ⊥ hover ∧ scan-first reader ⊥ test-by-hover. Uniformity within repeating group ! enforced — ∀ card H3 link ∈ single grid (e.g. /projects card grid) carry same affordance form ∨ none do; mixed treatment within one grid ≡ violation. Carrier set ≡ {`src/pages/index.astro` //what-I-do tiles (post-V46(b) ≡ content-only, ⊥ links), //proof-of-skills tile, //résumé bordered buttons; `src/pages/projects.astro` card H3 links (mailpilot only currently) ∧ GitHub-repo buttons; `src/pages/blog/index.astro` post entry links; `src/components/Nav.astro` ∧ `src/components/Footer.astro` nav links; `.prose` inline anchors @ blog body via `src/styles/global.css:191`}. Orthogonal to V49(c) (V49(c) ≡ accessible-name presence ∀ screen reader; V57 ≡ visible-affordance presence ∀ sighted scan — different reader, different signal).
V31: typeface ≡ Space Grotesk prose/headings/nav default + IBM Plex Mono ∀ `code`/`pre`/code-chip surfaces only; `@theme` declares `--font-mono` only (value ≡ Space Grotesk ∵ Tailwind `font-mono` class ≡ body default site-wide ∴ token rename ⊥ admitted absent class-sweep), ⊥ `font-sans`, ⊥ `font-serif`; code-surface family swap admitted via explicit `font-family: 'IBM Plex Mono', ui-monospace, SFMono-Regular, monospace` rule on carrier set `.prose code` ∧ `.prose pre` ∧ `.prose pre code` ∧ home-page code-chip CTA elements ∈ `src/styles/global.css`; hierarchy ∈ {weight, size, color}, ⊥ family swap on prose surfaces; html base 18px; body weight 400
V32: → V31 (folded; typography base rules)
V33: → V31 (folded; typography base rules)
V34: palette ≡ GH Primer Light `gh-*` tokens only (per §I); ∀ `--color-gh-*` decl ∈ `src/styles/global.css` @theme ≡ §I palette set ∧ ! ≥ 1 ref ∈ `src/`
V35: ⊥ alternating section bg; `bg-gh-canvas-subtle` ≡ contained surfaces only (cards, footer, code chips)
V36: logo ≡ 4-color pinwheel — `#0969da` ∧ `#1f883d` ∧ `#f9c513` ∧ `#cf222e`
V37: Tailwind v4 config ≡ `@theme` ∈ `src/styles/global.css`; ⊥ `tailwind.config.js`
V41: build ≡ static output; ⊥ SSR adapter, ⊥ API routes; Node `>=22` per `package.json` engines ∧ `.nvmrc`; package manager ≡ pnpm; host ≡ Cloudflare Workers; push → `main` ⇒ GH Actions deploy
V42: → V41 (folded; build/host constraints)
V43: → V41 (folded; build/host constraints)
V44: → V41 (folded; build/host constraints)
V45: pre-commit (local discipline) ! `make check` ∧ `make build` pass; enforcement ∈ V31; CI on PR → `main` ≡ `astro check` + `astro build`; deploy on push → `main` ≡ check + build + `wrangler deploy`; `.github/workflows/deploy.yml` ! ref `${{ secrets.CLOUDFLARE_API_TOKEN }}` ∧ `${{ secrets.CLOUDFLARE_ACCOUNT_ID }}` ∀ wrangler step; secret population ∈ GH repo settings ≡ out-of-scope ∀ static audit (deploy-fail-on-missing ≡ natural backstop)
V46: → V45 (folded; CI/CD pipeline)
V47: → V45 (folded; CI/CD pipeline)
V48: → V45 (folded; CI/CD pipeline)
V49: Playwright screenshot ⊥ SVG @ `file://` URL (timeout) — LATENT pre-trigger (`playwright.config.*` ∨ `*.spec.ts` ⊥ ∈ repo; Playwright ⊥ project dep, on-demand via MCP only); discipline-only rule ∈ CLAUDE.md
V51: blog post path ≡ `src/content/blog/<slug>.md` flat; filename ⊥ `.md` ≡ URL slug → rename ≡ URL break
V52: blog frontmatter ! match `src/content.config.ts` schema; undeclared fields (`author`, `image`, `canonical`) → `astro check` fails
V53: `src/content/blog/` ⊥ non-post `.md`; glob `**/*.md` loads ∀ → schema-fail breaks build
V54: blog body ⊥ `# Title`, ⊥ byline, ⊥ footer, ⊥ back-link → `[...slug].astro` renders these
V55: blog headings ≡ `##`/`###` sentence case, ⊥ trailing `.`
V56: blog voice ≡ first-person singular, factual + descriptive; audience ≡ technical reader (developer/engineer familiar w/ stacks, APIs, LLM tooling) ∴ ⊥ over-explain primitives, ⊥ define common acronyms (LLM, API, JSON, etc.); register ≡ simple ∧ clear — jargon ≡ precision shorthand, ⊥ decoration; sentence shape per `core:steno` — short, verb-first, one idea per sentence; ⊥ idiom, ⊥ marketing flourish (inherits §V.11); 2nd-person tutorial register (`you have an ID`, `you run`, `you open the file`) admitted ∀ step-by-step workflow posts (e.g. SQL-query sequence, debugging procedure, build pipeline) where instructional 2nd-person ≡ genre convention; first-person singular preferred ∀ narrative posts (post-mortem, lessons-learned, observation logs); register choice per-post ≡ author judgment based on genre dominance — mixed register admitted ∀ tutorial-w/-narrative-asides (2nd-person procedure body + 1st-person observation interjections), pure-narrative posts ⊥ admit 2nd-person register-shift mid-body per §B.33
V57: blog markdown ⊥ raw HTML (beyond entities ∧ `<br>`), ⊥ footnotes — `.prose` ⊥ support; images admitted via `![alt](src)` markdown form, src ∈ `public/blog/<slug>/` per-post asset dir (URL form `/blog/<slug>/<file>.webp`), alt ! mandatory per V23(c) accessible-name rule; tables admitted via standard pipe-syntax; carriers ∈ `src/styles/global.css` — `.prose img` (max-width 100%, height auto, bordered + rounded matching `.prose pre` ∧ `.prose blockquote` aesthetic, `cursor: zoom-in` click-affordance), `.prose table` (full-width, border-separate + spacing 0 ∵ border-radius compat, gh-border 1px outer + per-row bottom inner, gh-canvas-subtle thead bg, 0.5em × 0.85em cell padding); blog image format ! ≡ WebP — src ∈ `public/blog/<slug>/<file>.webp`; `.png` ∧ `.jpg` ∧ `.jpeg` ⊥ admitted (smaller bytes ∀ screen-density screenshots, modern-browser support universal post-2020); blog image click-to-enlarge viewer ≡ native HTML5 `<dialog>` overlay invoked on `.prose img` click — carriers ≡ (1) `src/pages/blog/[...slug].astro` (`<dialog>` element + client-side `<script>` attaching click handler on `.prose img`; on click → set dialog inner `<img>` src to clicked img src, `dialog.showModal()`; close on Esc key ∨ backdrop click ∨ explicit close-control), (2) `src/styles/global.css` `.prose img { cursor: zoom-in }` rule signalling click affordance ∀ sighted readers; ⊥ JS dep, ⊥ external lightbox library — native HTML5 dialog element only ∵ minimal-dep voice per §V.11 + framework-docs aesthetic per CLAUDE.md voice. Orthogonal to V57 main raw-HTML prohibition — markdown body stays `![alt](src)` form ∴ ⊥ raw HTML ∈ blog `.md`; viewer chrome (`<dialog>` element + `<script>`) lives ∈ Astro template (compiled HTML), ⊥ ∈ markdown content ∴ V57 main clause unaffected.
V58: LinkedIn feed post ≡ `linkedin/<slug>.txt` ∀ matching `src/content/blog/<slug>.md`; purpose ≡ promotional surface ∀ canonical blog write-up @ `lab5.ca/blog/<slug>/` — post ⊥ standalone substitute, ⊥ replace blog body, ⊥ self-contained read ∴ earns reader → write-up; objective ≡ short ∧ simple ∧ clear — reader ! grasp what blog post is about; format ≡ plain text + line breaks (`\n` → `<br>`, `\n\n` → paragraph; LinkedIn ⊥ render markdown); body ⊥ `word.tld` pattern — LinkedIn auto-linkifies (`SPEC.md` → `<a href="http://SPEC.md">` broken-scheme; `lab5.ca`, `github.com` → `lnkd.in/<hash>`), quotes/backticks ⊥ escape; rephrase ∨ drop TLD; body ⊥ markdown syntax (`**bold**`, `` `code` ``, `[text](url)`, `# heading`) → LinkedIn renders literal ∨ strips; dash-prefix bullets `- ` ≡ permitted per §V.17 (LinkedIn renders literal ≡ benign); em-dash ≡ real `—` (U+2014), ⊥ `--` literal (LinkedIn ⊥ auto-convert); voice ≡ §V.11; structure ≡ line 1 `Blog: <post-headline>` (mirrors `src/content/blog/<slug>.md` frontmatter `title` verbatim ∴ reader sees title-then-summary structure ∧ grasps subject pre-body) → body (short, simple, clear summary) → trailing `Read the full post: https://lab5.ca/blog/<slug>/` line (authored canonical; LinkedIn rewrites displayed URL → `lnkd.in/<hash>` post-publish ≡ expected, ⊥ authoring concern); URL count ≡ exactly 1 (the `Read the full post:` line) — LinkedIn auto-cards the LAST URL ∴ ≥ 2 URLs → non-canonical URL grabs card preview, suppresses canonical post link; ⊥ trailing `<Label>: <url>` rows beyond `Read the full post:` (⊥ `Skill:`, ⊥ `Plugin:`, ⊥ `Benchmark:`, etc.); `#hashtag` ∧ `@mention` ≡ LinkedIn syntax (sparingly). `linkedin/resume.txt` ≡ profile content, ⊥ scope.
V59: blog `title` (frontmatter @ `src/content/blog/<slug>.md`) ∧ mirrored linkedin `Blog:` line per V36 ∧ `description` frontmatter ! self-disambiguate ∧ literally name technical subject — (a) bare domain noun w/ ≥ 2 plausible dimension readings (e.g. `compression`, `drift`, `decoder`) ! qualifier specifying dimension; (b) rhetorical ∨ positional framing (e.g. `Where X stops and Y starts`, `X is dead`, `The future of Y`) ⊥ admitted ≡ standalone title — ! literal technical noun-phrase naming subject (what + how) ∴ keyword surface ⊥ rely on metaphor decode; reader landing cold via search ∨ social-card share ! grasp technical subject pre-body; slug ⊥ rename to fix (V23 URL-break rule).
V60: blog body ≡ skim-first + progressive disclosure — `##` heading sequence ! advance topical spine ∴ reader scanning headings alone grasps post arc; ∀ section ! lede sentence stating claim before code ∨ deep prose; ¶ ≤ ~4 lines (visual breath, ⊥ wall-of-text); detail ordered shallow → deep (1-line claim → bullet expansion → code block ∨ edge-case prose) ∴ reader stopping @ any depth retains coherent take-away
V61: ∀ page ! emit `<script type="application/ld+json">` ∈ `Layout.astro` `<head>` ∋ `@graph` w/ (1) `Person` @ `@id "https://lab5.ca/#person"` ∋ `name`, `alternateName "K. Borovik"`, `url`, `image`, `jobTitle`, `address` (`addressLocality "Toronto"` ∧ `addressRegion "ON"` ∧ `addressCountry "CA"`), `sameAs`, `knowsAbout`, `worksFor`, `potentialAction` (Person ! additionally ∋ `potentialAction: {@type:'AskAction', name:'Ask MailPilot live agent', description:'Send a question to hello@lab5.ca; live AI agent reply returns under 60 seconds.', target:{@type:'EntryPoint', urlTemplate:'mailto:hello@lab5.ca', actionPlatform:['http://schema.org/DesktopWebPlatform','http://schema.org/MobileWebPlatform']}}` — `AskAction` ≡ canonical CommunicateAction subclass for "act of posing a question to an entity" ∴ semantic match ∀ MailPilot framing "ask the agent, get a reply"; `hello@` ≡ canonical customer-comms alias ∀ `description` ∧ `target.urlTemplate`) — declares machine-callable email endpoint per §G `MailPilot ≡ proof-of-capability` framing ∴ Person advertises endpoint to LLM agents via Schema.org Action vocabulary; orthogonal to §I `cta.demo-email` visible code-chip widget (JSON-LD ≡ machine surface, code-chip ≡ visual surface) ∧ orthogonal to V35 `mailto:` href-form rule (V35 governs visible `<a href="mailto:">`, V40 governs JSON-LD machine-action declaration)); (2) `WebSite` @ `@id "https://lab5.ca/#website"` ∋ `url`, `name`, `publisher → Person.@id`, `inLanguage "en-CA"`; (3) page-specific node ∈ {`WebPage`,`CollectionPage`,`BlogPosting`} (selected via Layout `schemaType` prop) ref Person via `@id`; (4) `BreadcrumbList` ∀ non-root page; (5) `Person.knowsAbout` keyword set source ≡ `§I vocab: SEO keyword set`
V62: `Person.image` ! ref stable URL `https://lab5.ca/kb-headshot.jpg` (file ∈ `public/`); ⊥ astro:assets-hashed URL; ⊥ `og.png`
V63: `Person.sameAs` ≡ exactly `["https://www.linkedin.com/in/kborovik", "https://github.com/kborovik"]` — ⊥ X, ⊥ Bluesky, ⊥ Mastodon, ⊥ calendar URL, ⊥ MailPilot repo (LinkedIn ∧ GitHub ≡ primary maintained channels; X ∧ Bluesky ⊥ entity-binding signal — avoid sameAs entity-dilution); LinkedIn retained ∀ entity binding despite gated content (canonical surface @ `lab5.ca` per V47 pointer line)
V64: ∀ blog post → `BlogPosting` JSON-LD ! ∋ `author → Person.@id`, `datePublished ≡ frontmatter pubDate`, `dateModified ≡ frontmatter updatedDate` (if present), `headline ≡ frontmatter title`, `description ≡ frontmatter description`, `keywords ≡ frontmatter tags`
V65: ∀ primary page (`/`, `/projects`, `/blog`, `/blog/<slug>`) ! pass explicit `description` prop → `Layout.astro`; Layout default-fallback (`Layout.astro:13`) ≡ last-resort safety net only; primary list ≡ {Home, Projects, Blog}; /resume ∧ /mailpilot ⊥ ∈ primary list ∵ hidden surface per §I; `/mailpilot` reachable via `/projects` card #1 H3 link, ⊥ via primary nav (Nav menu set ≡ {Home, Projects, Blog})
V66: `Layout.astro` `<head>` ! emit `<link rel="alternate" hreflang="en-ca" href={canonical}>` ∧ `<link rel="alternate" hreflang="x-default" href={canonical}>`; existing `og:locale "en_CA"` stays; `<head>` additionally ! emit (a) `<link rel="alternate" type="text/markdown" href="<page>.md">` ∀ HTML page w/ markdown mirror — current mirror set ≡ {`/blog/<slug>` → `/blog/<slug>.md` per V59}; per-page emission via Layout `markdownAlternate` prop (string href); ⊥ emitted ∀ pages w/o mirror; (b) `<link rel="alternate" type="application/rss+xml" title="lab5.ca blog" href="/rss.xml">` site-wide (∀ Layout invocation) per V60 RSS feed surface — discovery affordance ∀ feed readers + agents monitoring for new writing; rationale ≡ markdown mirror + RSS feed otherwise undiscoverable from HTML alone (agent ! guess URL pattern absent `<link rel="alternate">` hint).
V71: `public/llms.txt` ! ∋ pointer-form summary per llms.txt convention (H1 `Konstantin Borovik`, blockquote intro, `## Pages` link list ∋ `/projects` entry, `## Contact` link list ∋ `https://github.com/kborovik/resume` external resume repo, `## Notes` section ∋ gated-LinkedIn pointer line `LinkedIn (linkedin.com/in/kborovik) requires login; canonical identity ∧ writing @ lab5.ca` — routes agents off doomed gated-fetch); `public/llms-full.txt` ! ∋ prose dump ≡ `/` hero + claims + `// what I do` capabilities + `/projects` card grid + `/mailpilot` description + blog post titles + descriptions (single-file machine-readable corpus ∀ agents that ⊥ multi-page crawl); both files ! carry gated-LinkedIn pointer line; `## Posts` link-list entry shape ! ∋ {title, frontmatter description, canonical URL `https://lab5.ca/blog/<slug>/`} rendered as `- [title](url): description` per llms.txt link-list+description form; endpoint Content-Type ! ≡ `text/markdown; charset=utf-8` per llmstxt.org convention; llms-full.txt endpoint Content-Type ≡ same per V48(e).
V72: `public/llms-full.txt` ∧ `public/llms.txt` ! sync w/ structured site sources — (a) blog-post enumeration ≡ ∀ non-draft `src/content/blog/<slug>.md` set, order ≡ pubDate desc (mirror `/blog` route per §I); per-file entry shape: llms-full.txt entry ∋ {title, pubDate, description, canonical `https://lab5.ca/blog/<slug>/`}; llms.txt entry ∋ {title, description, canonical URL}; (b) capability block ordering ! ≡ V2 (3-tile set; Title Case per V14); (c) contact link set ! ≡ §I `cta.linkedin` ∪ `https://github.com/kborovik` ∪ `cta.mailpilot-source` ∪ `cta.book-call` ∪ `https://github.com/kborovik/resume`; (d) narrative prose (hero, claims, capability stack descriptions, proof, voice register, closing) ! mirror corresponding human-facing primary-page content at all times — ∀ human-facing primary-page narrative change MUST propagate to llms-full.txt corresponding narrative block ∈ same commit ∨ paired commit; per-page mirror granularity ≡ `src/pages/index.astro` ↔ llms-full.txt `/` narrative blocks (hero + the-problem + what-I-do + proof + how-money-stays-under-control + why-me + closing per V4); `src/pages/projects.astro` ↔ llms-full.txt `/projects` description block; `src/pages/mailpilot.astro` ↔ llms-full.txt `/mailpilot` description block; blog corpus parity satisfied by V73 per-post markdown mirror (llms-full.txt blog enumeration ≡ index-level pointer per V72(a)); voice register (V11 main + V11 buyer-pain admit on `/`) ! propagate page-for-page (∴ llms-full.txt `/` narrative admits buyer-pain register, llms-full.txt non-`/` narrative retains V11 main register); narrative wording ≡ §V drift target — audit recipe → `.claude/check-extras.md § parity audit`; both files ! carry gated-LinkedIn pointer line per V71; (e) mechanical gen: `llms.txt` ∧ `llms-full.txt` ≡ build-output via Astro static endpoints `src/pages/llms.txt.ts` ∧ `src/pages/llms-full.txt.ts`, ⊥ check-in @ `public/`; both endpoints ! set `Content-Type: text/markdown; charset=utf-8` per V47; `llms-full.txt` Blog section auto-derived from `getCollection('blog')` filtered non-draft ∧ sorted pubDate desc per (a), hand-tuned narrative blocks (hero, claims, capability blurbs, proof blurb, contact links, notes) ≡ inline TS strings ∈ endpoint code (wording mirrors human pages per (d), emission ≡ build-time); `llms.txt` ∋ pointer-form per V71 — `## Pages` link list w/ hand-tuned page descriptions inline, `## Posts` auto-derived from `getCollection('blog')` per (a), `## Contact` link list inline mirroring §I cta refs ∪ `https://github.com/kborovik` ∪ `https://github.com/kborovik/resume` per (c), `## Notes` gated-LinkedIn pointer line per V71 inline; ∴ blog-post enumeration drift impossible by construction (`getCollection('blog')` single source ∀ Posts ∧ Blog sections).
V73: ∀ non-draft blog post @ `/blog/<slug>` ! ∃ matching markdown mirror @ `/blog/<slug>.md` — build-output via `src/pages/blog/[...slug].md.ts` Astro static endpoint; content ≡ frontmatter front-matter block (YAML-fenced: title, description, pubDate ISO-8601, optional updatedDate, optional tags) + raw `entry.body` text (CommonMark, unrendered); endpoint sets `Content-Type: text/markdown; charset=utf-8`; HTML page emits `<link rel="alternate" type="text/markdown" href="/blog/<slug>.md">` per V45; mirror equivalence by construction (same `src/content/blog/<slug>.md` source file). Rationale ≡ agent-discovery surface — LLM agents prefer raw markdown over HTML-stripping ∀ blog ingestion.
V74: `/rss.xml` ! emit per `@astrojs/rss` package — Astro endpoint `src/pages/rss.xml.ts`; RSS 2.0 channel{title:'lab5.ca blog', description:'Notes on cloud infrastructure, AI agents for business workflows, and web development.', site:'https://lab5.ca', items: ∀ non-draft blog post enumerated via `getCollection('blog')`, order ≡ pubDate desc}; ∀ item ∋ {title:frontmatter title, link:`https://lab5.ca/blog/<slug>/`, pubDate, description:frontmatter description, categories:frontmatter tags}; Layout.astro `<head>` emits `<link rel="alternate" type="application/rss+xml" title="lab5.ca blog" href="/rss.xml">` site-wide per V45; new dep `@astrojs/rss` ∈ `package.json` (Node ≥22 per V20 retained). Rationale ≡ agent-discovery surface — feed readers ∧ portfolio-monitoring agents ⊥ subscription path absent RSS; `/llms.txt` covers static enumeration, ⊥ delta-discovery.
V75: `@astrojs/sitemap` integration ∈ `astro.config.mjs integrations[]` ! ∋ `sitemap()`; auto-emits `/sitemap-index.xml` + `/sitemap-0.xml` @ build covering ∀ Astro route; `public/robots.txt` ! ∋ `Sitemap: https://lab5.ca/sitemap-index.xml` reference; `public/_headers` ! carry cache-rule block ∀ `/sitemap-index.xml` ∧ `/sitemap-0.xml`; dep `@astrojs/sitemap` ∈ `package.json`. DECLARED §I surface (per §I `route:` + `asset:`) ∴ V55 surface-completeness gate satisfied. Rationale ≡ general-purpose crawlers (search engines, ChatGPT search index, Perplexity) key off `/sitemap-*.xml` discovery; `/llms.txt` covers LLM-agent-first audience, ⊥ displaces sitemap ∀ general indexing reach. machine-discovery triple {V73, V74, V75}: sitemap ≡ search index, RSS ≡ feed readers, .md mirror ≡ LLM agents.
V81: retired (T134 resume migration batch) — `/resume` route, `src/pages/resume.astro`, `public/kb-resume.md` deleted; all resume-scoped rules vacuous post-migration; V82, V84, V85, V86, V87 ≡ same batch retire.
V82: → V81 (folded; retired T134 resume migration batch)
V83: tile bodies ∧ /projects card descriptions ∧ Person.knowsAbout JSON-LD ∧ llms*.txt narrative blocks ! frame KB's actual experience in vocab ∈ §I `vocab: SEO keyword set` (3-group canonical-name set per §I); ⊥ fabricate platform-fluency tokens absent from KB's actual project history — positioning ≡ frame what KB has shipped, ⊥ claim what he hasn't; SEO keyword set ∈ §I ≡ market-driven parameter — re-target ≡ §I amend.
V84: → V81 (folded; retired T134 resume migration batch)
V85: → V81 (folded; retired T134 resume migration batch)
V86: → V81 (folded; retired T134 resume migration batch)
V87: → V81 (folded; retired T134 resume migration batch)
V91: `.claude/check-extras.md` ! ∋ `§-cite drift recipe extension`; excludes (1) `src/content/blog/**/*.md` ∧ (2) §B/§T narrative columns from free-text `§[VTB]\.[0-9]+` scope — documentary cites, ⊥ live; live cites ∈ typed `§T.cites`/`§B.fix` columns only; full recipe → `.claude/check-extras.md § §-cite drift recipe`; REPO-LOCAL extension loaded per sdd:check path probe.
V92: ∀ URL-accessible surface {`public/` tracked files, `astro.config.mjs integrations[]` build-output, `src/pages/*.{ts,js}` static endpoints} ! ∈ §I `asset:`/`route:` list; new surface ! §I admit ∈ same §T ∨ §B defer; internal config refs (`public/_headers` cache-rules, `src/layouts/Layout.astro` segmentLabel map) ! reference only §I-enumerated live surfaces; audit + diff recipe → `.claude/check-extras.md § surface-completeness audit`.
V93: ∀ bold-rendered phrase ∈ user-facing prose ! ≡ complete self-sufficient claim — semantically intact w/o surrounding sentence context; carrier set ≡ {`src/pages/**/*.astro` (`<strong>` + `font-semibold` ∨ `font-bold` class), `src/content/blog/**/*.md` body (markdown `**bold**` + `<strong>`), narrative blocks ∈ `src/pages/llms.txt.ts` + `src/pages/llms-full.txt.ts`}; banned form: (a) leading-pronoun bold, (b) single-token conjunction ∨ preposition emphasis, (c) label-only bold w/o labeled value; admitted form: (i) declarative claim w/ explicit subject + verb, (ii) brand-noun ∨ proper-noun, (iii) imperative action-rule short phrase, (iv) self-descriptive credentials (implicit-`I` admitted ∵ first-person context site-wide); audit recipe → `.claude/check-extras.md § bold-self-sufficiency audit`; orthogonal to V11 ∧ V60; site-wide audit per T141

## §T — tasks

closed: T2–T123 archived → `SPEC.archive.md` (slim 2026-05-29 phases 1-3 dedup); open below.

id|status|task|cites
T124|x|SUPERSEDED — §V.81 ∧ §V.86 ∧ §V.87 amend (all retired per T134 resume migration)|V81,V86,V87
T125|x|wrote LinkedIn post file for the Logfire troubleshooting blog per V36 structure|V58
T126|x|dropped backtick wrap around gen_ai.* in anchor text of the Logfire troubleshooting post|V22
T127|x|confirmed Logfire troubleshooting post needs no rewrite under V28 2nd-person register admit|V56
T128|x|SUPERSEDED — §V.82 ∧ §V.87 amend (folded per T134 resume migration; resume.astro deleted)|V82,V87
T129|x|converted all blog images and their public/blog assets to WebP per V29(a)|V57
T130|x|added click-to-enlarge dialog viewer for blog images plus zoom-in cursor per V29(b)|V57
T131|x|swept post-reorg stale §V cross-refs, fixed V86 print 2-col, enumerated blog WebP assets in §I|V56,V57,V58,V72,V81,V85,V86,V87,V92
T132|x|positioning pivot to AI & LLM Engineering subtitle, retiled index, renamed DevOps skill group across all carriers|V1,V2,V4,V18,V72,V81,V82,V87
T133|x|renamed skill Group 3 from DevOps & Operations to DevOps & Cloud Platforms across all carriers|V2,V4,V18,V87
T134|x|migrated resume content out to separate repo, deleted resume.astro/kb-resume.md and all resume refs|V1,V4,V18,V61,V66,V71,V72,V83
T135|x|re-tuned SEO meta/JSON-LD/llms narrative to lead with AI and LLM keywords|V1,V11,V18,V61,V71,V72,V83
T136|x|replaced hero address with a LinkedIn bordered icon-label button on index per V4(a)|V4
T137|x|added hero github button in shared flex row and retired the proof-of-skills button strip per V4|V4
T138|x|published what-one-ai-email-costs blog post: moved/normalized frontmatter, WebP images, LinkedIn file|V13,V51,V52,V54,V57,V58,V59
T139|x|dual-family typeface swap to Space Grotesk body plus IBM Plex Mono code, regenerated social assets per V31|V31
T140|x|rewrote index as CFO/CIO cold-email landing with cost-per-outcome proof table and dual Book-a-Call CTAs|V1,V2,V4,V11,V18,V21
T141|x|audited and fixed non-self-sufficient bold spans across pages, blog, and llms endpoints per V93|V93
T142|x|rewrote llms-full.txt narrative to mirror the new cost-per-outcome index and re-tuned llms.txt intro per V72(d)|V1,V2,V4,V11,V18,V72
T143|x|added proof skepticism lead, $5-assumption clause, and why-me bus-factor block; removed unsourced hedge per V4|V4,V11,V18,V72
T144|x|expanded the two what-I-do tile labels to Build Business Processes with AI and Rescue a Runaway AI Project|V2,V4,V14,V72

## §B — bugs

id|date|cause|fix
B1|2026-05-18|`global.css` declares 5 `--color-gh-*` tokens (`gh-green-muted`, `gh-blue-muted`, `gh-red`, `gh-red-hover`, `gh-yellow`) ⊥ ∈ §I palette; `gh-red*`/`gh-yellow` ref only ∈ unused `Logo.astro`, `*-muted` pair zero refs; pinwheel uses raw hex ⊥ tokens → fix: drop dead tokens / align palette to §I per set-diff|V34
B2|2026-05-18|blog `.md` external-link anchor text wraps `**bold**` (`spec-driven-development.md:18`) ∧ `` `code` `` (`measuring-glyph-compression.md:214`) → fix: V35 anchor-text clause forbids markdown ∈ anchor text|V22
B3|2026-05-19|linkedin post body referenced `SPEC.md`; LinkedIn auto-linkified `SPEC.md` → broken `<a href="http://SPEC.md">`; quote/backtick variants ⊥ defeat matcher → fix: drop `.md` → `SPEC` ∈ source; 2 pre-fix live posts retain broken link / remediated manually ∈ LinkedIn UI|V58
B4|2026-05-19|V36 markdown-syntax prohibition list ∋ `- bullet` but §V.16 establishes dash-prefix ≡ canonical site bullet ∧ LinkedIn renders `- ` literal (benign) ∴ V36 over-reach → fix: narrow V36 list ∧ admit dash-prefix per §V.16|V58
B5|2026-05-19|documentary cross-repo cites (T.37, B.6/B.7/B.8) ∈ `measuring-glyph-compression.md` flagged UNRESOLVED — targets absent ∈ lab5.ca SPEC.md; mechanical grep ⊥ distinguish documentary from live cite ∴ false-positive class → fix: exclude `src/content/blog/**/*.md` from §-cite drift scope per V37 + drop § prefix on doc cites; broader §B/§T narrative filter ∈ B8|V91
B6|2026-05-19|V36 amend added "catchy ∨ controversial" headline rule but companion `linkedin/*.txt` sweep ⊥ run ∴ `spec-driven-development.txt:1` autobiographical setup matches anti-example class → fix: re-sweep linkedin headlines post-amend|V58
B7|2026-05-19|untracked `where-pytest-stops-and-claude-code-starts.md` ∋ 2 latent violations — (1) V35: line 149 internal link as full `https://lab5.ca/proof/` not absolute path; (2) V36: companion `.txt` absent; root cause ≡ author drafted blog w/o pre-publish audit, both rules pre-exist → fix: fix line 149 → `/proof` + author `linkedin/<slug>.txt` per V36|V22,V58
B8|2026-05-19|3 free-text §-cites @ SPEC.md:96 (B5 body) flagged UNRESOLVED — documentary cites quoting prior tool output (cross-repo refs); B5 captured blog variant via V37 but ⊥ extended to SPEC.md's own §B/§T narrative columns ∴ recurrence class → fix: V37 scope extension covering §B/§T narrative columns + check-extras.md filter ∈ T15; live cites stay ∈ typed columns|V91
B9|2026-05-19|`where-pytest-stops-and-claude-code-starts.txt` published w/ 2 URL rows; LinkedIn auto-card promotes LAST URL (`Skill:` GitHub) suppressing canonical `Write-up:` link ∴ ≥ 2 URLs ⇒ non-Write-up wins card; prior V36 admitted extra `<Label>: <url>` rows ∴ rule-gap → fix: V36 forbids non-Write-up trailing URL rows ∧ mandates `Blog: <title>` line 1 + §T16 collapse sweep|V58
B10|2026-05-20|blog title `Measuring math-glyph compression` — bare `compression` ⊥ disambiguate dimension; body measures token-count reduction ∴ title ! `token` qualifier; reader landing cold ⊥ infer subject → fix: amend title + Blog: + description per T18; slug ⊥ rename (V23)|V59
B11|2026-05-20|blog title `Where pytest stops and Claude Code starts` — rhetorical/positional framing ⊥ name technical subject; reader landing cold ⊥ infer subject; V38 covered bare-noun class only, ⊥ rhetorical-framing ∴ rule-gap → fix: amend V38 to admit rhetorical/positional class + retitle per T19; slug ⊥ rename (V23)|V59
B12|2026-05-20|`where-pytest-stops-and-claude-code-starts.md` TLDR opens impersonal ("[MailPilot] is...") setting impersonal register; body scatters first-person but lede dominates ∴ violates V28 first-person-singular + clear-register; single-post drift, ⊥ recurrence class → fix: remediation bundled into T19 single edit pass w/ V38 retitle|V56
B13|2026-05-20|Lighthouse v13.0.2 audit @ `/` returned accessibility=0.73 (others ≥0.99); 4 defect classes — color-contrast (5 nodes), heading-order, link-name, landmark-one-main; SPEC.md lacked a11y invariant; 3/4 ∈ `Layout.astro`/`Nav.astro` ∴ propagated ∀ page → fix: V49 a11y baseline + T29–T33 sweep|V23
B14|2026-05-20|/resume print PDF rendered H2 (`11pt`) ∧ H3 (`10.5pt`) smaller than prose `sm:text-base` (1rem=13.5pt @ print) ∴ section markers visually subordinated; root cause ≡ absolute pt units ⊥ tracking Tailwind rem sizes → fix: V50(5) amend → rem-based heading sizing + body-floor redefined as visible prose|V86
B15|2026-05-21|`@astrojs/sitemap` emits `sitemap-{index,0}.xml` referenced by `robots.txt` ∴ live URL-accessible surface but never crossed §I gate (∈ initial commit); `/sdd:check` §V-default ⊥ catch; B15 2026-05-24 drop-policy reversed — LLM-agent audit flagged sitemap absence as crawler discovery gap → fix: sitemap restored as DECLARED §I surface per V61 + §I route:/asset: extensions ∴ V55 satisfied; T58 drop retired, restoration ≡ T119|V92,V75
B16|2026-05-21|`public/_redirects` ∋ `/demo /proof 301` per T9; §I `asset:` brace-list ∋ `_headers` ∧ ⊥ `_redirects` ∴ CF-Pages routing-config-pair enumerated asymmetrically; T9 closure ⊥ paired w/ §I sweep; same §I surface-completeness class as B15 on `public/*` axis → fix: add `_redirects` to §I asset enumeration|V92
B17|2026-05-22|V1 re-design fold-in swept page bodies but ⊥ enumerate non-page-body carriers ∴ 3 surfaces retain pre-fold pitch vocab — `Layout.astro:25` default description, `blog/index.astro:18` meta description, `resume.astro:177-180` + `kb-resume.txt:64-66` lab5.ca lede; recurrence class ≡ pitch-amend carrier under-enumeration → fix: V1 amend enumerating carrier set + T74 sweep|V1
B18|2026-05-22|V48(c) mandates 6-element contact link set ∀ both `llms.txt` ∧ `llms-full.txt`; T70 swept `llms.txt.ts` to all 6 but `llms-full.txt.ts:67-73` Contact block remained 5-of-6 — `https://lab5.ca/resume` absent (∃ ∈ narrative @ L59 but V48(c) governs Contact-list set); ⊥ rule-gap → fix: single-surface code fix via T75|V72
B19|2026-05-22|two config refs point @ removed surfaces — `_headers:28-31` retains cache rules for dropped sitemap (B15) ∧ `Layout.astro:151-156` `segmentLabel` map holds dead keys {expertise, about} ∧ ⊥ live {projects, resume} ∴ `/projects` breadcrumb falls through to lowercase; same surface-completeness class as B15/B16 extended to internal config refs → fix: V55 amend extending scope + T76 sweep|V92
B20|2026-05-22|V2 tile-ordering amend (`d3f743f`) ∧ V46 page-body swept but ⊥ V48 carrier `llms-full.txt.ts:38-44` `## What I do` (prior ordering); V48(b) parenthetical also pinned stale ordering inline ∴ rule text stale; recurrence class ≡ amend-carrier under-enumeration + pinned-example-in-rule-body → fix: V48(b) drops pinned example, V2 ref by name + T81 sweep|V72
B21|2026-05-22|`resume.astro:32` "lab5.ca" link written `<a href="https://lab5.ca">` — full external form for internal home link, V35 mandates absolute path `/`; T48 task text ∧ V50(6) parenthetical pinned full-URL form inline ∴ collided w/ V35 general rule (pinned-example collision class) → fix: V50(6) drops href-form pin ∧ delegates form to V35 + T82 sweep|V22
B22|2026-05-22|`Layout.astro:151-156` `segmentLabel` retains dead key `proof: 'Proof'` post-`/proof`→`/mailpilot` rename ∧ ⊥ live `mailpilot: 'MailPilot'` ∴ breadcrumb falls through; V55 enumerated route-label maps as carrier but T83 task text ⊥ inherit V55 carrier list ∴ Layout.astro missed; carrier under-enumeration class (task-text axis) → fix: T85 sweep|V92
B23|2026-05-22|user feedback: /projects `mailpilot` H3 link rendered `text-gh-green` only (⊥ underline) vs /blog inline links `text-gh-green` + resting underline; V57(b) admitted "w/ ∨ w/o underline" optionality ∴ inconsistent green-link affordance signal → fix: V57(b) drop optionality + V57(c) require resting underline on accent-title form + T88 sweep|V24
B24|2026-05-22|user feedback: /resume H3 set {`MailPilot`, `gcp-devops`, `Pilot Skills`, `lab5.ca`} vs /projects card H3 {`mailpilot`, `gcp-devops`, `pilot-skills`, `lab5.ca`} (repo-slug verbatim); `kb-resume.txt` mirrors /resume variant ∴ 3-way casing drift; V50(6) required content ≡ but ⊥ pin canonical name form → fix: V50(6) extend 3-way mirror to require GitHub-repo-slug-verbatim H3 form + T89 sweep|V87
B25|2026-05-23|user feedback: `/mailpilot` carries 60-second reply fact in 3 places, 2 forms — numeric chip `<60s` vs written `sixty seconds` (L18, L69); site-wide pattern already numeric (`index.astro:128`) ∴ /mailpilot lone written-form straggler; V3 ⊥ governed number form → fix: V3 numeric-form clause + T90 sweep|V11
B26|2026-05-23|user feedback: `resume.astro:14` contact prose `text-sm` (14px) below 16px base; pattern repeats across ~30 `text-sm`/`text-xs` carrier sites ∈ resume.astro; V50(5) "all ≥ body" floor defined body as 1rem but ⊥ explicit enforcement clause ∀ visible prose ∴ sub-base carriers undetected; floor-defined-but-not-enforced class → fix: V50(5) explicit visible-prose-≥-base clause + T92 sweep|V86
B27|2026-05-23|T90 grep pattern matches `five years` ∈ `blog/index.astro:30` (/blog meta lede) ∴ site ∈ pattern ∩ scope; T90 verification step scanned cited-violations subset not full scope post-fix ∴ slipped through; sweep-§T scope-declared-but-under-executed class (V3 axis) → fix: T96 sweep|V11
B28|2026-05-23|3 carrier-drift violations from kb-resume.md fold-in (T97-T104) — (1) `llms-full.txt.ts:40-44` narrative on prior V2 ordering (carrier ⊥ re-swept); (2) `kb-resume.md:52-74` Projects order ≠ V50(6) canonical (canonical-tag w/o pre-tag audit); (3) kb-resume.md lab5.ca lede exceeds V54(b) ≤50-word cap ∧ subject-noun opening; all 3 ≡ §T author-discipline re-fire, ⊥ rule-gap → fix: V54 relax cap to ~120 words ∧ admit subject-noun opening (kb-resume.md ≡ canonical source drives shape) + T105 + T106 + T107|V72,V87,V85
B29|2026-05-24|LLM-agent audit: `llms.txt.ts:10` emits Posts `- [${title}](${url})` only; V48(a) required title+pubDate+description+URL ∀ both files but llms.txt diverged for pointer-form brevity w/o rule codifying it; latent since T57, V48 free-text scope ⊥ pin grep recipe ∴ ⊥ mechanically caught; rule-scope-ambiguous-between-N-surfaces class → fix: V47 per-file entry shape (llms.txt drops pubDate) + V48(a) admit per-file divergence + T114 sweep|V71,V72
B30|2026-05-24|V40 introduced `Person.potentialAction` w/ `@type:'EmailAction'`; schema.org validator flagged `EmailAction` ⊥ ∈ recognized vocabulary (recognized: CommunicateAction/AskAction/ReplyAction/...); authoring referenced training-data recall w/o out-of-repo verification; rule-cites-identifier-not-in-external-vocab class → fix: V40 `EmailAction` → `AskAction` + Layout.astro sweep; mitigation: cross-check schema.org `@type` against https://schema.org/<Type> pre-show-user|V61
B31|2026-06-01|new `troubleshooting-application-failures-with-logfire.md` shipped without paired `.txt` per V36 pair-rule; same companion-sweep-after-blog-add class as B6/B9, V36 already governs ∴ author-discipline gap → fix: T125 sweep|V58
B32|2026-06-01|`troubleshooting-application-failures-with-logfire.md:70` anchor wraps sub-token `gen_ai.*` in backticks inside `[...]`; V35 anti-example (B2) pinned full-anchor wrap class, partial-wrap subclass (code-fragment ∈ plain anchor) ⊥ enumerated ∴ rule-text ambiguity → fix: V35 extending anchor-text wrap prohibition to partial-wrap subclass + T126 sweep|V22
B33|2026-06-01|`troubleshooting-application-failures-with-logfire.md` body uses 2nd-person tutorial register ("You have an ID...") through majority; V28 mandates first-person singular; distinct subclass from B12 (3rd-person lede) — tutorial-genre industry convention admits 2nd-person instructional register → fix: V28 admitting 2nd-person tutorial register ∀ step-by-step workflow posts (rule extension, ⊥ rewrite) + T127 no-op carrier|V56
B34|2026-06-01|`kb-resume.md` projects carry `*Skills: <groups>*` italic annotation (L68/74/80/86); `resume.astro` projects lack mirror ∴ V51 content-equivalence drift; V63 governs description shape only, Skills line ⊥ enumerated; cause ≡ kb-resume.md fold-in authored annotation, resume.astro ⊥ swept (B28 class) → fix: V63 explicit Skills-annotation-line mirror clause ∋ resume.astro carrier + T128 sweep|V82,V87
B35|2026-06-01|`resume.astro:362–366` print `.resume { padding: 0 }` + `@page { margin: 0.5in 0.55in }`; V62 pins `.resume { padding: 0.75in }`; both value (0.75in vs 0.5in/0.55in) ∧ mechanism (padding vs @page margin) diverge; cause ≡ V62 split from V50(5) carried literal pinned value, impl evolved to @page-margin; pinned-example-in-rule-body class → fix: V62 drops pinned padding form + delegates mechanism abstractly (`@page margin ∨ container padding`)|V86
B36|2026-06-04|T134 pattern-scope assertion `rg -l '.../resume\b/...'` ≡ ∅ post-sweep collided w/ new `github.com/kborovik/resume` URL T134(e) adds; `/resume\b` matches `kborovik/resume` ∴ false-positive 2 files when intent ∅; sweep intent met (`make check`/`make build` pass), only over-broad grep flags; §T pattern-scope ⊥ anticipate own newly-added literal class → fix: T134 pattern amend (`/resume\b` → `lab5\.ca/resume\b`) scoping to stale refs|-

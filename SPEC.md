# SPEC.md — lab5.ca

## §G — goal

personal portfolio + career site @ `lab5.ca` ∀ Konstantin Borovik (Software Engineer | DevOps Engineer). pitch ≡ generalist software delivery across cloud infrastructure + AI agents for business workflows + web development; 3 surfaced offerings, each portfolio-anchored on `/projects`. proof-of-capability surfaces ≡ MailPilot demo (`/mailpilot`) + `/projects` GitHub-repo showcase + `/blog` writing. `public/kb-resume.md` ≡ canonical resume + skills-keyword source 2026-05-23 onward (prior `kb-resume.txt` retired) — drives `/resume` page content + Person.knowsAbout JSON-LD + index `// what I do` 3-tile keyword streams + llms-full.txt Resume embed via V48(e) Vite `?raw` import.

## §C — constraints

- stack: Astro 6 static (⊥ SSR adapter), Tailwind v4 via `@theme`, TS 6, pnpm 10, Node ≥22.
- host: Cloudflare Workers static assets; deploy via GH Actions on push → `main`; domain via CF DNS + auto SSL.
- typeface: IBM Plex Mono only across site + social assets.

## §I — interfaces

- route: `/` → home (hero w/ 2-line title block: H1 `Konstantin Borovik` + subtitle `Software Engineer | DevOps Engineer` (pipe verbatim); sections: hero, `// what I do` 3-tile, `// claims`, `// proof`, `// résumé`)
- route: `/projects` → skills→GitHub-repo proof grid (4 hand-authored cards: gcp-devops, mailpilot, pilot-skills, lab5.ca; Salesforce card reserved ∀ future)
- route: `/mailpilot` → MailPilot evidence (code-chip CTA, how-it-works, sample Q's, architecture); hidden surface ⊥ ∈ `<Nav />` (reachable via `/projects` card #1 H3 link only — analogous to `/resume` hidden via index `// résumé` button); `/demo` ∧ `/proof` ≡ legacy → CF redirect → `/mailpilot`
- route: `/blog` → post list (sort pubDate desc)
- route: `/blog/<slug>` → post detail
- route: `/blog/<slug>.md` → markdown mirror of `/blog/<slug>` HTML; build-output via `src/pages/blog/[...slug].md.ts` Astro endpoint; content ≡ frontmatter front-matter block (title, description, pubDate, optional updatedDate, optional tags) + raw `entry.body` text; `Content-Type: text/markdown; charset=utf-8`; agent-discovery surface per V59
- route: `/rss.xml` → RSS 2.0 feed via `@astrojs/rss`; ∀ non-draft blog post enumerated, order ≡ pubDate desc; item ∋ {title, link, pubDate, description, categories=tags}; build-output via `src/pages/rss.xml.ts` per V60
- route: `/sitemap-{index,0}.xml` → `@astrojs/sitemap` integration build-output; auto-tracks ∀ Astro route; restored 2026-05-24 per §B.15(∆) LLM-agent usability audit override; declared §I surface ∴ V55 surface-completeness gate satisfied (pre-T58 drop ≡ undeclared-surface class); per V61
- route: `/resume` → hidden surface (⊥ ∈ `<Nav />`, reachable via `// résumé` button @ `/` only); CV / engagements / skills / projects; H1 ≡ `Software Engineer | DevOps Engineer` (pipe verbatim); printable via `@media print` stylesheet (browser → PDF)
- domain: `lab5.ca` ≡ primary; `mailpilot.ca` ≡ alias → serves same site (`wrangler.jsonc` routes)
- analytics: Cloudflare Web Analytics — server-side, automatic via CF dashboard; ⊥ in-repo script tag, ⊥ cookies
- cta: book-call `https://calendar.app.google/cYM3H3TsHsequR587`
- cta: linkedin `https://www.linkedin.com/in/kborovik`
- cta: mailpilot-source `https://github.com/kborovik/mailpilot` — link to MailPilot source; surfaced ∈ `/mailpilot` (under-the-hood) ∧ home (proof-of-capability)
- cta: demo-email ≡ code-chip clipboard-copy widget @ `/mailpilot` — copies `demo@lab5.ca`; ⊥ `<a href="mailto:...">`
- schema: blog frontmatter ≡ {title:str, description:str, pubDate:date, updatedDate?:date, draft?:bool=false, tags?:str[]=[]} per `src/content.config.ts`
- cmd: `make {help,install,dev,preview,build,check,clean,clean-all,deploy,status,wrangler,playwright}`
- env: GH Actions ! set `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`
- asset: `public/{og.png,banner-linkedin.png,favicon.{ico,svg},_headers,_redirects,robots.txt,logo-linkedin.{svg,png},kb-headshot.jpg,kb-resume.md}`; `llms.txt` ∧ `llms-full.txt` ≡ build-output via `src/pages/llms.txt.ts` ∧ `src/pages/llms-full.txt.ts` per V48(e), ⊥ ∈ `public/`; `rss.xml` ≡ build-output via `src/pages/rss.xml.ts` (`@astrojs/rss` dep) per V60; `sitemap-{index,0}.xml` ≡ build-output via `@astrojs/sitemap` integration per V61 (restored per §B.15(∆)); `blog/<slug>.md` ≡ build-output via `src/pages/blog/[...slug].md.ts` per V59
- token: palette ≡ GH Primer Light — `gh-canvas`, `gh-canvas-subtle`, `gh-border`, `gh-fg`, `gh-fg-muted`, `gh-fg-subtle`, `gh-green`, `gh-green-hover`, `gh-blue`, `gh-blue-hover`
- token: typeface ≡ IBM Plex Mono (400/500/600/700) via Google Fonts `display=swap`
- vocab: /resume primary audience ≡ Software Engineer + DevOps Engineer hiring manager (generalist; prior primary/secondary split retired 2026-05-21 via website re-design fold-in — single-tier set replaces both); 3-group canonical-name keyword set anchored ∈ LinkedIn skill taxonomy 2026-05-23 (token form ≡ LinkedIn-skill verbatim — `Google Cloud Platform (GCP)` ⊥ `Google Cloud`, `Microsoft Azure` ⊥ `Azure`, `Identity and Access Management (IAM)` ⊥ `IAM`, `Retrieval-Augmented Generation (RAG)` ⊥ `RAG`, `Large Language Models (LLM)` ⊥ `LLM`, `Model Context Protocol (MCP)` ⊥ `MCP`, `Test-Driven Development (TDD)` ⊥ `TDD`, `Spec-Driven Development (SDD)` ⊥ `SDD`, `Google Kubernetes Engine (GKE)` ⊥ `GKE`, `Azure Kubernetes Service (AKS)` ⊥ `AKS`, `Infrastructure as Code (IaC)` ⊥ `IaC`); mirror surface ≡ `public/kb-resume.md` Skills section (3 H3 groups w/ labeled body lines); set framed ∈ KB's actual experience per V52 (⊥ fabricate platform-fluency tokens) — (1) **Software Development** {Python, TypeScript, Node.js, Bash, SQL, FastAPI, Pydantic, Astro, Cloudflare Workers, REST APIs, API Design, OpenAPI, Webhooks, Event-Driven Architecture, Microservices, Distributed Systems, Asynchronous Programming, PostgreSQL, SQLite, Cloud SQL, ETL, Data Pipelines, Structured-Extraction Pipelines, Salesforce Developer, Salesforce Integration, Salesforce APIs, Pytest, Test Automation, Unit Testing, Test-Driven Development (TDD), Spec-Driven Development (SDD), Git, Version Control, Object-Oriented Programming, Backend Development, Full-Stack Development, Software Architecture}; (2) **DevOps** {Google Cloud Platform (GCP), Cloud Run, Cloud Functions, BigQuery, Vertex AI, Cloud SQL, Cloud Composer, Pub/Sub, Microsoft Azure, Azure App Service, Azure Functions, Azure Service Bus, Azure Key Vault, DevOps, Platform Engineering, DevSecOps, Infrastructure as Code (IaC), Terraform, Ansible, Helm, Docker, Kubernetes, Google Kubernetes Engine (GKE), Azure Kubernetes Service (AKS), Container Orchestration, Continuous Integration, Continuous Deployment, GitHub Actions, Azure DevOps, Apache Airflow, Workflow Orchestration, Linux, Shell Scripting, YAML, Observability, OpenTelemetry, Pydantic Logfire, GCP Cloud Logging, GCP Log Analysis, Cloud Security, Secrets Management, Identity and Access Management (IAM), NIST 800-53, PCI DSS}; (3) **AI & LLM** {Agentic AI Development, AI Agents, AI Engineering, Large Language Models (LLM), LLM Integration, Anthropic Claude, Anthropic API, Google Gemini, Pydantic AI, Model Context Protocol (MCP), Retrieval-Augmented Generation (RAG), Prompt Engineering, Claude Code, Claude Skills, AI-Augmented Development}; prior single-tier set retired 2026-05-23 (kb-resume.md canonical fold-in) — flat token list w/ shorthand variants (Google Cloud, Azure, IAM, RAG, MCP, GKE, IaC) superseded by canonical LinkedIn-skill forms; AWS tokens dropped 2026-05-21 (⊥ shipped AWS work; V52 no-fabrication), OAuth/RBAC dropped (IAM-config-only experience), `multi-tenant SaaS architecture` dropped 2026-05-23 (⊥ ∈ kb-resume.md canonical Skills set ∴ retired per canonical-source discipline)
- vocab: tech-token era-floor (earliest-plausible-year per token) ∋ {Windows Server ≥2000, Dell PowerEdge ≥2000, IBM servers ≥2000, virtualization (enterprise ESX) ≥2003, NetApp ≥2003, FortiGate ≥2004, Puppet ≥2005, Apex ≥2007, Chef ≥2009, DevOps-as-discipline ≥2009, BigQuery ≥2010, Ansible ≥2012, Docker ≥2013, Terraform ≥2014, Cloud SQL ≥2014, Airflow ≥2014, Kubernetes ≥2015, Pub/Sub ≥2015, Cloud Functions ≥2016, Helm ≥2016, Salesforce DX ≥2017, Azure DevOps (renamed VSTS) ≥2018, Composer ≥2018, MuleSoft (as SF) ≥2018, Cloud Run ≥2019, GitHub Actions ≥2019, Lightning Web Components ≥2019, RAG (named pattern) ≥2020, Vertex AI ≥2021, GenAI / LLM-app ≥2022, LLM agents (production-ish) ≥2023, Pydantic AI ≥2024, MCP ≥2024, Agentforce ≥2024}; engagement bullet w/ token Y ∈ engagement starting year < Y floor ≡ violation; table append-only — new token added on first use ∈ /resume bullets

## §V — invariants

V1: pitch ≡ generalist software delivery — cloud infrastructure ∧ AI agents for business workflows ∧ web development; 3 surfaced offerings, each portfolio-anchored on `/projects` GitHub-repo card; cloud platforms ≡ first-class offering (⊥ impl detail per prior V1); AI-narrow framing retired 2026-05-21 (website re-design fold-in); pitch-language carrier set ≡ {(1) `src/layouts/Layout.astro` default `description` fallback, (2) per-page meta `description` prop ∀ primary page per V44, (3) hero ∧ claims ∧ capability blurbs ∈ `src/pages/index.astro`, (4) engagement ledes ∈ `src/pages/resume.astro` ∧ `public/kb-resume.md` per V54, (5) narrative blocks (hero / claims / capability / proof / resume) ∈ `src/pages/llms.txt.ts` ∧ `src/pages/llms-full.txt.ts`} — V1 amend ! sweep ∀ carrier; carrier-set under-enumeration class recorded §B.17
V2: `// what I do` tile order @ `/` ≡ 01 Software Development → 02 DevOps → 03 AI & LLM (3 tiles; Title Case per V7 — group names mirror `public/kb-resume.md` Skills section H3 set verbatim per V46(b)(∆) ∧ V50(6)(∆) kb-resume.md canonical fold-in 2026-05-23); tile→/projects link retired 2026-05-22 per V46(b)(∆) ∧ V57 affordance audit — tiles content-only blocks post-amend; prior "tiles link to `/projects` ∵ products belong on /projects directly" retired same date ∵ link form ⊥ resting-state affordance ∴ violated V57, top-nav + V46(e) bordered link strip cover /projects nav; prior product-anchor rule (∀ tile ! portfolio-anchored on `/projects` GitHub-repo card: cloud → gcp-devops, AI → mailpilot, web → lab5.ca) retired 2026-05-22 along w/ product-led tile set; prior 3-tile layer-based set {Back-End Development, Front-End Development, Cloud Infrastructure} retired 2026-05-23 via kb-resume.md canonical fold-in ∵ tile-H3 set ! ≡ kb-resume.md Skills group taxonomy (3-way mirror w/ /resume `// tech stack` per V50(6)(∆) ∧ kb-resume.md Skills section); prior 3-tile set {Cloud Infrastructure, AI Agents for Business Workflows, Web Development} retired 2026-05-22 in favor of layer-based identity framing matching "Software Developer | DevOps Engineer" generalist pitch — AI work folds into back-end as capability rather than headlining as separate offering; prior 5-tile ERP-led order superseded 2026-05-21 — `data & reporting` dropped (⊥ portfolio anchor; BigQuery/SQL coverage retained ∈ /resume tech-stack only), `document workflow automation` ∧ `AI automation` merged (MailPilot + future LeadPilot ≡ single product pattern)
V3: voice ≡ factual + tech vocab + numeric form ∀ measurable quantity (duration, count, percentage, year span); ⊥ aspirational verbs, ⊥ persona breakouts, ⊥ marketing flourish, ⊥ written-out numbers (e.g. `sixty seconds`, `twenty percent`, `hundred-page report`) ∈ primary-page copy (`src/pages/{index,projects,mailpilot,resume}.astro`, `src/pages/blog/index.astro`, narrative blocks ∈ `src/pages/llms{,-full}.txt.ts`, Layout default `description` fallback); blog body (`src/content/blog/**/*.md`) exempt per V28 first-person narrative voice — written-out form admitted ∀ narrative time references (`three weeks later`, `six months`, `five years`)
V4: ≤ 1 prominent CTA per page; Book-a-Call ∈ nav only (`cta-pulse`), ⊥ page-footer repeat
V5: heading ⊥ trailing `.`
V6: page H1 ∧ blog title casing ≡ sentence case
V7: tile title ∧ step title casing ≡ Title Case (rebrand 2026-05-22 — `cloud infrastructure` → `Cloud Infrastructure`); section kicker `// xxx` retained lowercase per V14 (different element class — visual marker, ⊥ content header); H1 sentence case per V6 unchanged
V8: retired 2026-05-21 — `/expertise` route deleted via website re-design fold-in (capability content folded into `/` `// what I do` 3-tile section per V2 + V46); rule vacuous post-fold (⊥ /expertise H2 to enforce)
V9: typeface ≡ IBM Plex Mono only; `@theme` declares `--font-mono` only, ⊥ `font-sans`, ⊥ `font-serif`
V10: hierarchy ∈ {weight, size, color}; ⊥ family swap
V11: html base size ≡ 16px; body weight ≡ 400
V12: palette ≡ GH Primer Light `gh-*` tokens only (per §I); ∀ `--color-gh-*` decl ∈ `src/styles/global.css` @theme ≡ §I palette set ∧ ! ≥ 1 ref ∈ `src/`
V13: ⊥ alternating section bg; `bg-gh-canvas-subtle` ≡ contained surfaces only (cards, footer, code chips)
V14: section kicker ≡ `// section name`; class ≡ `text-base font-bold uppercase tracking-[0.2em] text-gh-blue`; V14(∆) 2026-05-24 LLM-agent usability amend: DOM text content of kicker element ! ≡ section name only (e.g. `what I do`); literal `// ` prefix ⊥ ∈ DOM text — injected via CSS `::before { content: '// '; }` on element bearing `.section-kicker` class (decl ∈ `src/styles/global.css`); rationale ≡ agent parsers (JSON-LD ingesters, llms.txt consumers, RSS readers, generic HTML→markdown crawlers) read heading text without `// ` comment-marker noise — `//` is C-family inline-comment syntax ∴ may be misparsed as comment opener by naive tooling. Visual rendering ≡ pre-amend (CSS pseudo-element renders `// xxx` identically @ same class string). Carrier class augmentation ≡ append `section-kicker` to existing Tailwind utility string ∀ kicker carrier site (current set: index.astro:41/91/116/166, projects.astro:56, mailpilot.astro:58 + internal kickers, resume.astro:39/83/166/330, blog/index.astro:23, blog/[...slug].astro:43); orthogonal to V49(b) heading-element rule (V49(b) ≡ `<h2>` element semantic, V14(∆) ≡ DOM text semantic — different axes).
V15: numbered hairline ≡ `<NN> ─────` above tiles, mono `text-xs text-gh-fg-muted`; numbering continues across grids when narrative ≡ one
V16: bullet list ≡ dash-prefix; ⊥ check/x SVG icons
V17: logo ≡ 4-color pinwheel — `#0969da` ∧ `#1f883d` ∧ `#f9c513` ∧ `#cf222e`
V18: build ≡ static output; ⊥ SSR adapter, ⊥ API routes
V19: Tailwind v4 config ≡ `@theme` ∈ `src/styles/global.css`; ⊥ `tailwind.config.js`
V20: Node version `>=22` per `package.json` engines ∧ `.nvmrc`
V21: package manager ≡ pnpm
V22: host ≡ Cloudflare Workers; push → `main` ⇒ GH Actions deploy
V23: blog post path ≡ `src/content/blog/<slug>.md` flat; filename ⊥ `.md` ≡ URL slug → rename ≡ URL break
V24: blog frontmatter ! match `src/content.config.ts` schema; undeclared fields (`author`, `image`, `canonical`) → `astro check` fails
V25: `src/content/blog/` ⊥ non-post `.md`; glob `**/*.md` loads ∀ → schema-fail breaks build
V26: blog body ⊥ `# Title`, ⊥ byline, ⊥ footer, ⊥ back-link → `[...slug].astro` renders these
V27: blog headings ≡ `##`/`###` sentence case, ⊥ trailing `.`
V28: blog voice ≡ first-person singular, factual + descriptive; audience ≡ technical reader (developer/engineer familiar w/ stacks, APIs, LLM tooling) ∴ ⊥ over-explain primitives, ⊥ define common acronyms (LLM, API, JSON, etc.); register ≡ simple ∧ clear — jargon ≡ precision shorthand, ⊥ decoration; sentence shape per `core:steno` — short, verb-first, one idea per sentence; ⊥ idiom, ⊥ marketing flourish (inherits §V.3)
V29: blog markdown ⊥ raw HTML (beyond entities ∧ `<br>`), ⊥ images, ⊥ tables, ⊥ footnotes — `.prose` ⊥ support
V30: pre-commit (local discipline) ! `make check` ∧ `make build` pass; enforcement ∈ V31
V31: CI on PR → `main` ≡ `astro check` + `astro build`
V32: deploy on push → `main` ≡ check + build + `wrangler deploy`
V33: `.github/workflows/deploy.yml` ! reference `${{ secrets.CLOUDFLARE_API_TOKEN }}` ∧ `${{ secrets.CLOUDFLARE_ACCOUNT_ID }}` ∀ wrangler step; actual secret population ∈ GH repo settings ≡ out-of-scope ∀ static audit (deploy-fail-on-missing ≡ natural backstop)
V34: Playwright screenshot ⊥ SVG @ `file://` URL (timeout) — LATENT pre-trigger (`playwright.config.*` ∨ `*.spec.ts` ⊥ ∈ repo; Playwright ⊥ project dep, on-demand via MCP only); discipline-only rule ∈ CLAUDE.md
V35: link href ≡ — internal: absolute path `/...`, ⊥ trailing `/`, ⊥ relative; cross-page anchor: `/path#frag`; intra-page anchor: `#frag`; external: full `https://...`; mailto: `mailto:<addr>`. ∈ `.astro`: external `<a>` ! `target="_blank"` ∧ `rel="noopener noreferrer"`; mailto ⊥ `target="_blank"`. ∈ blog `.md`: plain markdown `[text](url)` only — `rehype-external-links` (`astro.config.mjs`) auto-inserts attrs @ build ∴ ⊥ author raw `<a>`; external link anchor text ≡ plain inline prose, ⊥ `**bold**` wrap ∨ `` `code` `` wrap ∈ `[...]`.
V36: LinkedIn feed post ≡ `linkedin/<slug>.txt` ∀ matching `src/content/blog/<slug>.md`; purpose ≡ promotional surface ∀ canonical blog write-up @ `lab5.ca/blog/<slug>/` — post ⊥ standalone substitute, ⊥ replace blog body, ⊥ self-contained read ∴ earns reader → write-up; objective ≡ short ∧ simple ∧ clear — reader ! grasp what blog post is about; format ≡ plain text + line breaks (`\n` → `<br>`, `\n\n` → paragraph; LinkedIn ⊥ render markdown); body ⊥ `word.tld` pattern — LinkedIn auto-linkifies (`SPEC.md` → `<a href="http://SPEC.md">` broken-scheme; `lab5.ca`, `github.com` → `lnkd.in/<hash>`), quotes/backticks ⊥ escape; rephrase ∨ drop TLD; body ⊥ markdown syntax (`**bold**`, `` `code` ``, `[text](url)`, `# heading`) → LinkedIn renders literal ∨ strips; dash-prefix bullets `- ` ≡ permitted per §V.16 (LinkedIn renders literal ≡ benign); em-dash ≡ real `—` (U+2014), ⊥ `--` literal (LinkedIn ⊥ auto-convert); voice ≡ §V.3; structure ≡ line 1 `Blog: <post-headline>` (mirrors `src/content/blog/<slug>.md` frontmatter `title` verbatim ∴ reader sees title-then-summary structure ∧ grasps subject pre-body) → body (short, simple, clear summary) → trailing `Read the full post: https://lab5.ca/blog/<slug>/` line (authored canonical; LinkedIn rewrites displayed URL → `lnkd.in/<hash>` post-publish ≡ expected, ⊥ authoring concern); URL count ≡ exactly 1 (the `Read the full post:` line) — LinkedIn auto-cards the LAST URL ∴ ≥ 2 URLs → non-canonical URL grabs card preview, suppresses canonical post link; ⊥ trailing `<Label>: <url>` rows beyond `Read the full post:` (⊥ `Skill:`, ⊥ `Plugin:`, ⊥ `Benchmark:`, etc.); `#hashtag` ∧ `@mention` ≡ LinkedIn syntax (sparingly). `linkedin/resume.txt` ≡ profile content, ⊥ scope.
V37: `.claude/check-extras.md` ! ∋ `§-cite drift recipe extension` section excluding (1) `src/content/blog/**/*.md` from §-cite drift recipe scope — blog body ≡ documentary prose discussing external SPECs (pilot-skills SPEC.md, MailPilot, etc.) ∴ cites ∈ blog content ≡ cross-repo references, ⊥ live cites into lab5.ca SPEC.md; ∧ (2) §B `cause` ∧ §T `task` narrative columns ∈ SPEC.md from free-text `§[VTB]\.[0-9]+` grep — narrative prose quoting historical cite forms (e.g. `/sdd:check` tool output records, prior-state snapshots) ≡ documentary, ⊥ live; live cites stay ∈ typed `§T.cites` ∧ `§B.fix` columns per cite-form convention; extension recipe loads via path probe per sdd:check skill body (REPO-LOCAL extension recipes).
V38: blog `title` (frontmatter @ `src/content/blog/<slug>.md`) ∧ mirrored linkedin `Blog:` line per V36 ∧ `description` frontmatter ! self-disambiguate ∧ literally name technical subject — (a) bare domain noun w/ ≥ 2 plausible dimension readings (e.g. `compression`, `drift`, `decoder`) ! qualifier specifying dimension; (b) rhetorical ∨ positional framing (e.g. `Where X stops and Y starts`, `X is dead`, `The future of Y`) ⊥ admitted ≡ standalone title — ! literal technical noun-phrase naming subject (what + how) ∴ keyword surface ⊥ rely on metaphor decode; reader landing cold via search ∨ social-card share ! grasp technical subject pre-body; slug ⊥ rename to fix (V23 URL-break rule).
V39: blog body ≡ skim-first + progressive disclosure — `##` heading sequence ! advance topical spine ∴ reader scanning headings alone grasps post arc; ∀ section ! lede sentence stating claim before code ∨ deep prose; ¶ ≤ ~4 lines (visual breath, ⊥ wall-of-text); detail ordered shallow → deep (1-line claim → bullet expansion → code block ∨ edge-case prose) ∴ reader stopping @ any depth retains coherent take-away
V40: ∀ page ! emit `<script type="application/ld+json">` ∈ `Layout.astro` `<head>` ∋ `@graph` w/ (1) `Person` @ `@id "https://lab5.ca/#person"` ∋ `name`, `alternateName "K. Borovik"`, `url`, `image`, `jobTitle`, `address` (`addressLocality "Toronto"` ∧ `addressRegion "ON"` ∧ `addressCountry "CA"`), `sameAs`, `knowsAbout`, `worksFor`, `potentialAction` (V40(∆) 2026-05-24 LLM-agent usability amend: Person ! additionally ∋ `potentialAction: {@type:'EmailAction', name:'Email MailPilot live agent', description:'Send a question to demo@lab5.ca; live AI agent reply returns under 60 seconds.', target:{@type:'EntryPoint', urlTemplate:'mailto:demo@lab5.ca', actionPlatform:['http://schema.org/DesktopWebPlatform','http://schema.org/MobileWebPlatform']}}` — declares machine-callable email endpoint per §G `MailPilot ≡ proof-of-capability` framing ∴ Person advertises endpoint to LLM agents via Schema.org Action vocabulary; orthogonal to §I `cta.demo-email` visible code-chip widget (JSON-LD ≡ machine surface, code-chip ≡ visual surface) ∧ orthogonal to V35 `mailto:` href-form rule (V35 governs visible `<a href="mailto:">`, V40 governs JSON-LD machine-action declaration)); (2) `WebSite` @ `@id "https://lab5.ca/#website"` ∋ `url`, `name`, `publisher → Person.@id`, `inLanguage "en-CA"`; (3) page-specific node ∈ {`ProfilePage`,`WebPage`,`CollectionPage`,`BlogPosting`} (selected via Layout `schemaType` prop) ref Person via `@id`; (4) `BreadcrumbList` ∀ non-root page
V41: `Person.image` ! ref stable URL `https://lab5.ca/kb-headshot.jpg` (file ∈ `public/`); ⊥ astro:assets-hashed URL; ⊥ `og.png`
V42: `Person.sameAs` ≡ exactly `["https://www.linkedin.com/in/kborovik", "https://github.com/kborovik"]` — ⊥ X, ⊥ Bluesky, ⊥ Mastodon, ⊥ calendar URL, ⊥ MailPilot repo (LinkedIn ∧ GitHub ≡ primary maintained channels; X ∧ Bluesky dropped 2026-05-21 ∵ unmaintained ∴ ⊥ entity-binding signal — avoid sameAs entity-dilution); LinkedIn retained ∀ entity binding despite gated content (canonical surface @ `lab5.ca` per V47 pointer line)
V43: ∀ blog post → `BlogPosting` JSON-LD ! ∋ `author → Person.@id`, `datePublished ≡ frontmatter pubDate`, `dateModified ≡ frontmatter updatedDate` (if present), `headline ≡ frontmatter title`, `description ≡ frontmatter description`, `keywords ≡ frontmatter tags`
V44: ∀ primary page (`/`, `/projects`, `/blog`, `/blog/<slug>`) ! pass explicit `description` prop → `Layout.astro`; Layout default-fallback (`Layout.astro:13`) ≡ last-resort safety net only; /expertise + /about removed 2026-05-21 (route deletion via website re-design fold-in), /projects added; /resume ∧ /mailpilot ⊥ ∈ primary list ∵ hidden surface per §I (`/proof` renamed `/mailpilot` 2026-05-22 ∧ dropped from primary list + Nav menu — Nav `links[]` retired `/proof` entry ∴ menu set ≡ {Home, Projects, Blog}; `/mailpilot` reachable via `/projects` card #1 H3 link, ⊥ via primary nav)
V45: `Layout.astro` `<head>` ! emit `<link rel="alternate" hreflang="en-ca" href={canonical}>` ∧ `<link rel="alternate" hreflang="x-default" href={canonical}>`; existing `og:locale "en_CA"` stays; V45(∆) 2026-05-24 LLM-agent usability amend: `<head>` additionally ! emit (a) `<link rel="alternate" type="text/markdown" href="<page>.md">` ∀ HTML page w/ markdown mirror — current mirror set ≡ {`/resume` → `/kb-resume.md` per V51, `/blog/<slug>` → `/blog/<slug>.md` per V59}; per-page emission via Layout `markdownAlternate` prop (string href); ⊥ emitted ∀ pages w/o mirror; (b) `<link rel="alternate" type="application/rss+xml" title="lab5.ca blog" href="/rss.xml">` site-wide (∀ Layout invocation) per V60 RSS feed surface — discovery affordance ∀ feed readers + agents monitoring for new writing; rationale ≡ markdown mirror + RSS feed otherwise undiscoverable from HTML alone (agent ! guess URL pattern absent `<link rel="alternate">` hint).
V46: `/` ! ∋ (a) hero block w/ H1 `Konstantin Borovik` + subtitle `Software Engineer | DevOps Engineer` (pipe verbatim; prior `Software Developer | DevOps Engineer` retired 2026-05-23 via kb-resume.md canonical fold-in — subtitle ≡ kb-resume.md L3 verbatim) + visible text `Toronto, Ontario, Canada` ∈ `<address>` semantic tag + headshot; stack-token sub-line `Python · Google Cloud · LLM agents · CI/CD` dropped 2026-05-22 ∵ prior V46(c) `// tech stack` 4-group covered same tokens w/ grouped structure ∴ hero sub-line ≡ redundant scan signal (V46(c) itself subsequently DROPPED 2026-05-22 per below); social-card surfaces (`assets-src/og.html`, `assets-src/linkedin-banner.html`) ⊥ V46 scope — separate surface, ⊥ scroll-down stack ∴ retain stack-token line, (b) `// what I do` 3-tile services grid per V2; tile H3 set 2026-05-23 onward ≡ {01 Software Development, 02 DevOps, 03 AI & LLM} mirroring `public/kb-resume.md` Skills 3 H3 groups (canonical 3-group skill taxonomy); tile body ≡ `&middot;`-separated keyword stream digest of corresponding kb-resume.md group, optimized for hiring-manager scan — Software Development tile: `Python · TypeScript · Bash · SQL · FastAPI · Pydantic · Astro · Cloudflare Workers · REST APIs · OpenAPI · Event-Driven Architecture · Microservices · PostgreSQL · ETL · Structured-Extraction Pipelines · Pytest · TDD`; DevOps tile: `Google Cloud · Azure · Terraform · Ansible · Helm · GitHub Actions · Azure DevOps · Docker · Kubernetes · GKE · AKS · Cloud Run · Apache Airflow · IaC · CI/CD · OpenTelemetry · IAM · NIST 800-53 · PCI DSS`; AI & LLM tile: `Anthropic Claude · Google Gemini · Pydantic AI · LLM Agents · Agentic AI Development · RAG · Structured Extraction · MCP · Prompt Engineering · Claude Code · Claude Skills · AI-Augmented Development`; tile-body token form ≡ scan-digest (shorthand admitted ∀ tile-body density e.g. `Google Cloud`, `Azure`, `IaC`, `RAG`, `MCP`, `GKE` — canonical LinkedIn-skill expansion lives in /resume tech-stack subgroup body lines per V50(6) ∧ §I vocab); prior 3-tile set {Back-End Development, Front-End Development, Cloud Infrastructure} retired 2026-05-23 ∵ kb-resume.md canonical Skills group ordering (Software Development → DevOps → AI & LLM) replaces layer-based identity framing; prior rule "bolded high-level platform keywords ∈ tile `<p>`, ⊥ specific tech tokens" retired 2026-05-22 ∵ keyword density > brand-name bolding ∀ hiring-manager grep; tile-link wrapper `<a href="/projects">` retired 2026-05-22 per V57 visible-affordance audit — tiles content-only post-amend ∵ prior link form carried no resting-state affordance ∴ violated V57, nav to /projects redundant w/ top-nav menu ∧ V46(e) bordered link strip, (c) DROPPED 2026-05-22 — prior `// tech stack` 4-group section {Python & TypeScript, Google Cloud & Azure, AI Automation, CI/CD & Terraform} retired ∵ V46(b) keyword-stream tile bodies carry same token surface ∧ /resume per V50(6) carries 9-subgroup depth ∴ home-page mid-tier redundant, (d) `// claims` dash-bullet section listing 4 falsifiable facts ∈ uniform `<phrase> &mdash; <comma-list>` shape ∀ one-line viewport fit: {20+ years shipping production software (data centers, payments, real estate); AI agents in production (LLMs, RAG, structured extraction); solo delivery, end-to-end (back-end, IaC, CI/CD, deploy, observability); Toronto-based, long-term contract (embedded, full product cycle ownership)}; content refined 2026-05-22 — uniform shape ∀ scanability, drops business-automation horizontal (3 industry verticals only), drops Python/TS/SQL daily-stack bullet ∵ V46(b) tech-grid covers token surface ∴ slot reassigned to AI-agents-in-production claim, drops on-call from ownership scope per V56 dev-not-SRE positioning, "owned end-to-end" → "full product cycle ownership"; prior content shifted to generalist identity 2026-05-22 — drops AI-narrow framing, drops MailPilot product-name claim (V46(e) carries live demo as evidence w/ stronger framing), (e) `// proof of skills` ≡ featured AI-agent call-out (bordered subtle-bg box → `/mailpilot`, title `Let AI handle your repeat email questions →`, body describes function in hiring-manager vocab: production LLM agent, RAG, knowledge base, sub-60s reply, send a question to `demo@lab5.ca`) + compact bordered icon-link strip → `/projects` ∧ `/blog` ∧ GitHub; LinkedIn moved to V46(f) per identity/CV semantic grouping; heading renamed `// proof` → `// proof of skills` 2026-05-22 ∀ non-technical-reader legibility; tile title renamed `Try the live AI email agent →` → `Let AI handle your repeat email questions →` 2026-05-22 — names buyer pain (repeat-email work) ∧ relief (AI delegation), retains "AI" keyword token ∀ scan; title rendered ∈ `text-gh-green` accent color per V57(c) visible-affordance form ∵ bordered card-shape + corner arrow alone ⊥ trigger card-as-link reader expectation w/o accent-colored title (user feedback 2026-05-22; `text-gh-blue` reserved for V14 kicker per V57); prior 5-destination flat list (`/projects` ∧ `/blog` ∧ `/proof` ∧ LinkedIn ∧ GitHub) retired 2026-05-22 — featured call-out elevates strongest single evidence (live demo) above flat link equality, (f) `// résumé` bordered button cluster → `/resume` (Download PDF, new tab; browser-print → PDF per V50(5)) ∧ LinkedIn profile (CV/identity surface, grouped w/ PDF per "where to verify employment history" semantic); prior single-bordered-button rule retired 2026-05-22 ∵ LinkedIn folded in from former V46(e) link strip; `/about` route deleted 2026-05-21 — (a)+(d)+(e) content owner re-targeted from prior /about to `/` via website re-design fold-in
V47: `public/llms.txt` ! ∋ pointer-form summary per llms.txt convention (H1 `Konstantin Borovik`, blockquote intro, `## Pages` link list ∋ `/projects` ∧ `/resume` entries, `## Contact` link list ∋ `https://lab5.ca/kb-resume.md` markdown resume, `## Notes` section ∋ gated-LinkedIn pointer line `LinkedIn (linkedin.com/in/kborovik) requires login; canonical identity ∧ writing @ lab5.ca` — routes agents off doomed gated-fetch); `public/llms-full.txt` ! ∋ prose dump ≡ `/` hero + claims + `// what I do` capabilities + `/projects` card grid + `/mailpilot` description + `/resume` skills ∧ engagements ∧ projects + blog post titles + descriptions (single-file machine-readable corpus ∀ agents that ⊥ multi-page crawl); both files ! carry gated-LinkedIn pointer line; `/about` + `/expertise` content refs dropped 2026-05-21 via website re-design fold-in (routes deleted); plain-text resume ref retired 2026-05-23 via kb-resume.md canonical fold-in — `kb-resume.txt` (plain-text) → `kb-resume.md` (CommonMark Markdown) ∴ Contact-list URL updated, label may read `Markdown resume`; V47(∆) 2026-05-24 LLM-agent usability amend: (a) `## Posts` link-list entry shape ! ∋ {title, frontmatter description, canonical URL `https://lab5.ca/blog/<slug>/`} rendered as `- [title](url): description` per llms.txt link-list+description form — pre-amend entry shape `- [title](url)` (title + URL only) retired ∵ agents ranking page relevance ⊥ infer subject from title alone; pubDate dropped from llms.txt entries ∵ pointer-form prioritizes scannability ∧ pubDate ≡ derivable via llms-full.txt for full-fidelity; (b) endpoint Content-Type ! ≡ `text/markdown; charset=utf-8` per llmstxt.org convention — pre-amend `text/plain; charset=utf-8` retired; (c) llms-full.txt endpoint Content-Type ≡ same per V48(e)(∆).
V48: `public/llms-full.txt` ∧ `public/llms.txt` ! sync w/ structured site sources — (a) blog-post enumeration ≡ ∀ non-draft `src/content/blog/<slug>.md` set, order ≡ pubDate desc (mirror `/blog` route per §I); per-file entry shape: llms-full.txt entry ∋ {title, pubDate, description, canonical `https://lab5.ca/blog/<slug>/`}; llms.txt entry ∋ {title, description, canonical URL} per V47(∆) — pubDate dropped from llms.txt entries 2026-05-24 LLM-agent usability amend ∵ pointer-form scannability ∧ pubDate ≡ derivable via llms-full.txt for full-fidelity; (a)(∆) 2026-05-24 amend retires prior unified entry shape pin (`entry ∋ frontmatter title ∧ pubDate ∧ description ∧ canonical`) — recurrence-class ≡ rule-scope-ambiguous-between-two-surfaces per §B.29; (b) capability block ordering ! ≡ V2 (3-tile set; Title Case per V7 amend 2026-05-22); prior parenthetical pinning ordering example text retired 2026-05-22 per B20 ∵ inline-pinned ordering went stale on V2(∆) re-amend even w/ carriers correctly enumerated ∴ rule references V2 by name only ⊥ pin example; (c) contact link set ! ≡ §I `cta.linkedin` ∪ `https://github.com/kborovik` ∪ `cta.mailpilot-source` ∪ `cta.book-call` ∪ `https://lab5.ca/resume` ∪ `https://lab5.ca/kb-resume.md`; (d) both files ! carry gated-LinkedIn pointer line per V47; narrative prose (hero, claims, capability stack descriptions) ≡ hand-tuned voice per §V.3 ∴ ⊥ machine-derived, ⊥ §V drift target — sync covers enumeration ∧ ordering ∧ link set only, ⊥ wording; (e) mechanical gen: `llms.txt` ∧ `llms-full.txt` ≡ build-output via Astro static endpoints `src/pages/llms.txt.ts` ∧ `src/pages/llms-full.txt.ts`, ⊥ check-in @ `public/`; both endpoints ! set `Content-Type: text/markdown; charset=utf-8` per V47(∆) (pre-amend `text/plain; charset=utf-8` retired 2026-05-24 LLM-agent usability amend); `llms-full.txt` ∋ Resume section auto-derived from `public/kb-resume.md` (Vite `?raw` import + embed-boundary transform — strip kb-resume.md H1 `# Konstantin Borovik` line, demote `^## ` → `### ` ∧ `^### ` → `#### ` ∀ remaining headings ∵ kb-resume.md ≡ canonical Markdown source w/ H1+H2+H3 levels), Blog section auto-derived from `getCollection('blog')` filtered non-draft ∧ sorted pubDate desc per (a), hand-tuned narrative blocks (hero, claims, capability blurbs, proof blurb, contact links, notes) ≡ inline TS strings ∈ endpoint code (wording hand-tuned per (d), emission ≡ build-time); `llms.txt` ∋ pointer-form per V47 — `## Pages` link list w/ hand-tuned page descriptions inline, `## Posts` auto-derived from `getCollection('blog')` per (a), `## Contact` link list inline mirroring §I cta refs ∪ `https://github.com/kborovik` ∪ `https://lab5.ca/kb-resume.md` per (c), `## Notes` gated-LinkedIn pointer line per V47 inline; ∴ /resume ↔ llms-full.txt Resume section drift impossible by construction (kb-resume.md single source via V51) ∧ blog-post enumeration drift impossible by construction (`getCollection('blog')` single source ∀ Posts ∧ Blog sections); embed-boundary mechanic 2026-05-23 amend (kb-resume.md canonical fold-in) — prior single `^## ` → `### ` demote retired ∵ kb-resume.md carries 3 heading levels (H1 name, H2 sections, H3 subgroups), demote ! preserve relative hierarchy ∴ both `^## ` → `### ` ∧ `^### ` → `#### ` per embed step; H1 line stripped ∵ Resume section already wrapped under llms-full.txt `## Resume` H2.
V49: ∀ primary page (`/`, `/projects`, `/blog`, `/blog/<slug>`) ! satisfy WCAG 2.1 Level AA baseline — (a) text contrast ≥ 4.5:1 normal-size, ≥ 3:1 large (≥18pt ∨ ≥14pt bold); `gh-fg-subtle` (#818b98) ≡ large-text-only — body-size sites ! use `gh-fg-muted` (#424a53); (b) heading order sequentially-descending — `<h3>` ⊥ appear w/o prior `<h2>` in scope; V14 section kicker `// xxx` ≡ `<h2>` element when section ⊃ tile `<h3>`s, V14 visual class string (`text-sm font-bold uppercase tracking-[0.2em] text-gh-blue`) retained; (c) ∀ interactive element ! ∋ accessible name — links ∋ visible text ∨ `aria-label`; brand-logo link `<a href="/">` ∈ `Nav.astro` ! `aria-label="Home"`; (d) exactly one `<main>` landmark per page — `Layout.astro` ! wrap `<slot />` ∈ `<main>` between `<Nav />` ∧ `<Footer />`; verification ≡ Lighthouse a11y category ≥ 0.95 ∀ primary page — manual periodic audit (⊥ CI-gated; matches T28 verification pattern, promote to CI only if drift recurs)
V50: `/resume` ! ∋ (1) page H1 ≡ role title in job-title casing (Title Case — narrowly overrides V6 sentence-case rule ∀ /resume H1 only ∵ role-title proper-noun-equivalent ∧ V52 primary-audience keyword scan favors title-cased canonical form; current value ≡ `Software Engineer | DevOps Engineer` (pipe separator verbatim; prior `Software Developer | DevOps Engineer` retired 2026-05-23 via kb-resume.md canonical fold-in — H1 ≡ kb-resume.md L3 subtitle verbatim; `AI Automation Engineer` dropped 2026-05-21 via website re-design fold-in ∵ identity pivot to generalist Software Dev + DevOps); decorative `Resume` H1 dropped — document context (URL `/resume`, ProfilePage JSON-LD per V40) carries document-type signal w/o redundant label), (2) IBM Plex Mono per V9, (3) GH Primer palette per V12, (4) single-column layout, (5) `@media print` stylesheet drops `<Nav />` + `<Footer />` chrome ∧ expands content to full page width (`.resume { padding: 0.75in !important }` ∀ page-margin breathing room) — browser-print → PDF ≡ canonical PDF path, ⊥ in-repo PDF artifact; font-size hierarchy (screen ∧ print) ≡ H1 > H3 > H2 ≈ engagement-row job-title > body, all ≥ body — H3 (company name @ engagement row, subgroup label @ tech-stack) ! > H2 (`// section` kicker) ∵ H3 ≡ primary scan signal per V50(6) engagement-row priority ∧ V52 tech-stack subgroup primacy, H2 ≡ contextual marker (uppercase + letter-tracking carries scan salience @ smaller pt); section-marker H2 (`// tech stack`, `// engagements`, `// education`) ! ≥ body, H1 role title ! > H3; print heading sizing ≡ rem-based ∀ `.resume h1/h2/h3` ∧ engagement-row job-title `<p>` selector (scales w/ `html { font-size }` base — 16px @ screen per §V.11; `@media print` overrides → `html { font-size: 10px }` ∴ 1rem = 10px ≈ 7.5pt @ print; print html base ≡ single lever, ∀ rem-based size scales proportionally ∴ hierarchy preserved); concrete print rem values ≡ `.resume h1: 1.6rem` (≈12pt), `.resume h3: 1.3rem` (≈9.75pt), `.resume h2: 1.15rem` (≈8.6pt), `.resume article > div + p: 1.1rem` (≈8.25pt — engagement job-title selector, immediate sibling of H3-containing div); print stylesheet ⊥ set `body { font-size }` (vestigial — body-element baseline ⊥ govern visible prose; ∀ prose carries Tailwind utility size); ⊥ absolute pt ∀ headings (breaks rank when prose `sm:text-base` outranks pt-pinned heading @ print width 816px > sm:640px breakpoint); body floor ∈ V50(5) "≥ body" ≡ visible prose font-size (1rem = 7.5pt @ print, 1rem = 16px @ screen), ⊥ `body` element baseline; V14 kicker class string (`text-base font-bold uppercase tracking-[0.2em] text-gh-blue` per V14 amend) carries 1rem globally ∴ /resume screen-side override vacuous (prior `.resume h2 { font-size: 1rem }` rule dropped per T60 fold); print stylesheet's `.resume h2: 1.15rem` retained per print-specific scaling, ⊥ ripple to other-page kickers per V14 global rule; V50(5)(∆) 2026-05-23 amend: ∀ visible prose ∈ /resume render @ ≥ 1rem screen (≡ Tailwind `text-base` ∀ all viewports) — ⊥ `text-sm` (0.875rem) ∨ `text-xs` (0.75rem) admitted ∀ visible prose ∈ /resume; covers contact secondary prose, contact links, tech-stack subgroup body container, project descriptions, engagement ledes ∧ bullet lists, engagement-row date ranges, education degree line, education date range, GitHub button labels; `text-sm sm:text-base` mobile-breakpoint pair ⊥ admitted ∵ mobile <640px reads sm-prefix → below-base body; body floor `≥ body` thereby extends from heading-floor enforcement only to visible-prose enforcement ∀; V50(5)(∆) 2026-05-23 print-page-budget amend: /resume print render ! fit ≤ 3 pages @ US-Letter via Chrome `--print-to-pdf` per CLAUDE.md /resume PDF render recipe; achieves via — (a) `@media print` multi-column compaction: `// tech stack` 9 subgroups render @ 3-column grid ∈ print (∀ subgroup ≡ H3 + 1 token-line ∴ single-col flow wastes vertical), screen retains single-column flow ∀ narrow-viewport legibility; (b) `@media print` spacing compaction: section gap `mt-12` (3rem screen) → ~1rem print, engagement gap `space-y-10` (2.5rem screen) → ~0.75rem print, tech-stack subgroup gap `space-y-6` (1.5rem screen) → ~0.4rem print, section `pt-10 border-t` (2.5rem) → ~0.6rem print; (c) page-break declarations: `break-inside: avoid` ∈ {`.resume section`, `.resume article`} (article rule pre-amend extant; section rule new), ⊥ forced `break-before: page` directives — natural flow tuned to budget; print html base ≡ 10px per V50(5) print-base (⊥ regress to sub-10px values); (d) per-page budget target (verify via `make dev` → Chrome `--print-to-pdf` → `pdftoppm -r 100 /tmp/resume.pdf /tmp/p -png` per CLAUDE.md): page 1 ≡ contact block + `// tech stack` (9 subgroups, 3-col grid) + `// projects` (4 entries); page 2 ≡ `// engagements` recent 3 (lab5.ca, Colliers, PayPal — ledes + bullets per V54); page 3 ≡ `// engagements` older 3 (CDW, Tech Data, TD Bank — bullets-only) + `// education`; regression rule: /resume content addition pushing printed page count > 3 ≡ violation requiring content cut ∨ further density tuning; recurrence class ≡ print-form bloat under content-add — re-fires on engagement ∨ tech-stack subgroup ∨ project-entry add absent print-density audit; V50(5)(∆∆) 2026-05-23 print-pacing follow-up amend (user feedback post-T94 PDF render: 'looks better but page space underused ∧ wider header spacing wanted'): (a) tech-stack-grid @ print: 3-col → 2-col (long subgroup labels `DevOps & CI/CD`, `Security & compliance`, `APIs & integration` breathe; 9 subgroups → 5 rows [4 full + 1 half] vs prior 3-row 3-col layout ∴ fills vertical space prior T94 underused); (b) section-boundary spacing widens @ print — section gap (`mt-12` carrier) ~0.8rem → ~1.5rem, section `pt-10` carrier ~0.5rem → ~1rem, section-marker H2 ↔ content gap (`section > h2 + div / + article`) ~0.35rem → ~0.75rem; restores scan rhythm lost in T94 tight-compaction; (c) tech-stack subgroup gap (`space-y-6` carrier) 0 → ~0.5rem ∴ vertical breath between subgroup rows in 2-col layout; (d) ≤ 3 page budget per V50(5)(∆) preserved; prior fixed per-page mapping {p1 ≡ contact + tech-stack 3-col + projects, p2 ≡ recent engagements, p3 ≡ older engagements + education} retired ∵ 2-col + wider gaps shifts content distribution — exact mapping impl-determined, regression rule (> 3 pages ≡ violation) unchanged; V50(5)(∆∆∆) 2026-05-23 print-section-page-break amend (user feedback 2026-05-23 image-compare /resume PDF page 1 of 2: `// projects` ∧ `// engagements` ∧ `// education` flowed inline w/ `// tech stack` on p1–p2 — preferred each major section on its own dedicated page): print render ! place each major H2 section (`// projects`, `// engagements`, `// education`) on its own page-break boundary via `@media print { .resume section + section { break-before: page } }` rule — adjacent-sibling selector matches each `<section>` preceded by another `<section>` ∴ first section (`// tech skills` post-V50(6)(∆) label rename) flows from contact `<header>` w/o break, projects + engagements + education each start new page; budget rebaselined ≤ 3 pages → ≤ 4 pages {p1: contact + tech-skills | p2: projects | p3: engagements | p4: education}; regression rule: /resume content addition pushing printed page count > 4 ≡ violation (was > 3 ≡ violation per V50(5)(∆)); V50(5)(∆∆) tech-stack-grid 2-col + spacing rules retained as in-section density baseline ∵ in-section compaction orthogonal to section-boundary page-break enforcement, (6) section order ≡ contact block (role title @ H1 primary; name + location + link set secondary ≡ supporting prose, links: LinkedIn ∧ GitHub ∧ home (canonical site `lab5.ca` ≡ home-page link; visible text `lab5.ca`; href form per V35 internal-link rule ∴ `/`, ⊥ full URL — prior parenthetical pinned `https://lab5.ca` as href form ∧ collided w/ V35, retired 2026-05-22 per B21) — visible email mailto dropped ∵ public-served HTML + PDF harvest-scrape exposure denied; private contact path retained via LinkedIn DM ∧ book-call CTA ∈ site nav; book-call CTA retained in site nav ∧ `llms-full.txt` link set per V48 but ⊥ rendered as visible /resume link) → `// tech stack` (3-group H3 ordering 2026-05-23 onward, mirrors `public/kb-resume.md` Skills section H3 set: [1] Software Development, [2] DevOps, [3] AI & LLM; ∀ H3 group ! ∋ labeled body lines per kb-resume.md group content — `Label: token, token, ...` form mirrors kb-resume.md (e.g. Software Development H3 body lines: `Languages: Python, TypeScript, Node.js, Bash, SQL` / `Disciplines: Backend Development, Full-Stack Development, Software Architecture` / `Frameworks & Web: FastAPI, Pydantic, Astro, Cloudflare Workers` / `APIs: REST APIs, API Design, OpenAPI, Webhooks` / `Architecture: Event-Driven Architecture, Microservices, Distributed Systems, Asynchronous Programming` / `Data: PostgreSQL, SQLite, Cloud SQL` / `Pipelines: ETL, Data Pipelines, Structured-Extraction Pipelines` / `Enterprise Integration: Salesforce Developer, Salesforce Integration, Salesforce APIs` / `Testing: Pytest, Test Automation, Unit Testing` / `Methodologies: Test-Driven Development (TDD), Spec-Driven Development (SDD)` / `Foundations: Git, Version Control, Object-Oriented Programming`; DevOps H3 body lines: `Google Cloud Platform (GCP): Cloud Run, Cloud Functions, BigQuery, Vertex AI, Cloud SQL, Cloud Composer, Pub/Sub` / `Microsoft Azure: Azure App Service, Azure Functions, Azure Service Bus, Azure Key Vault` / `Practice: DevOps, Platform Engineering, DevSecOps` / `IaC: Infrastructure as Code (IaC), Terraform, Ansible, Helm` / `Containers: Docker, Kubernetes, Google Kubernetes Engine (GKE), Azure Kubernetes Service (AKS), Container Orchestration` / `CI/CD: Continuous Integration, Continuous Deployment, GitHub Actions, Azure DevOps` / `Orchestration: Apache Airflow, Workflow Orchestration` / `Systems: Linux, Shell Scripting, YAML` / `Observability: Observability, OpenTelemetry, Pydantic Logfire, GCP Cloud Logging, GCP Log Analysis` / `Security & Compliance: Cloud Security, Secrets Management, Identity and Access Management (IAM), NIST 800-53, PCI DSS`; AI & LLM H3 body lines: `Agents: Agentic AI Development, AI Agents, AI Engineering` / `Models & APIs: Large Language Models (LLM), LLM Integration, Anthropic Claude, Anthropic API, Google Gemini` / `Tooling: Pydantic AI, Model Context Protocol (MCP), Retrieval-Augmented Generation (RAG), Prompt Engineering` / `AI-Augmented Development: Claude Code, Claude Skills, AI-Augmented Development`); H3 group name `DevOps` shortened from kb-resume.md verbatim `DevOps & Cloud Platforms & Platform Engineering` ∵ tile-header symmetry w/ `Software Development` ∧ `AI & LLM` (canonical-name expansion preserved in subgroup body lines — Google Cloud Platform (GCP), Microsoft Azure, Platform Engineering all named explicitly); prior 9-subgroup set {Programming languages, Google Cloud, Azure, DevOps & CI/CD, AI & agents, APIs & integration, Security & compliance, SQL databases, Web development} retired 2026-05-23 via kb-resume.md canonical fold-in; stack tokens ! cover §I primary-audience keyword set per V52 (canonical-name discipline — LinkedIn-skill verbatim per §I vocab), era-floor per V53; print-form impact: 3-H3 group structure + per-group multi-line body shifts content distribution vs prior 9-subgroup 2-col layout — exact print mapping impl-determined, ≤ 3-page budget per V50(5)(∆) preserved as regression rule ∴ tech-stack-grid CSS reflow ∈ T98 scope) → `// projects` (NEW per 2026-05-21 fold-in — plain-text mirror of `/projects` page card grid; 4 entries matching `/projects` order ∀ 3 surfaces: mailpilot, gcp-devops, pilot-skills, lab5.ca per T84 mailpilot-first elevation 2026-05-22; ∀ entry ! H3 project name + 1-line tech context + green-URL repo link — V50(6)(∆) 2026-05-23 amend per /resume print-canonical-path: ≡ V57(a) inline text link form (`text-gh-green hover:text-gh-green-hover underline underline-offset-[3px]`) matching V50(6) contact-block link form @ resume.astro:23/27/32; prior bordered V57(d) button-shaped form w/ GitHub SVG icon retired ∵ /resume `@media print` → PDF renders bordered+SVG-icon button as visual noise, plain URL prints cleanly + visually consistent w/ contact-block links above; project name H3 ≡ GitHub repo slug verbatim per `kborovik/<repo>` identifier — canonical form ∀ 3 surfaces, brand-cased variants (`MailPilot`, `Pilot Skills`) retired 2026-05-22 per §B.24 ∵ single canonical name form eliminates casing-drift surface; narrative product mentions outside 3-way mirror scope (engagement bullets ∈ resume.astro ∧ kb-resume.md, hero/claims/capability blurbs ∈ `llms*.txt.ts`, `/mailpilot` page H1 ∈ mailpilot.astro, segmentLabel ∈ Layout.astro) keep brand form (`MailPilot`); resume.astro `// projects` content ≡ projects.astro card grid ∧ ≡ kb-resume.md `## Projects` section — 3-way mirror, V51-style equivalence, drift between 3 surfaces ≡ violation; V50(6)(∆∆) 2026-05-23 H3-form-per-surface amend (user feedback post-T95 /resume PDF render: H3 short repo slug `mailpilot` ⊥ surface GitHub link target on printed PDF ∴ hiring manager reading PDF ⊥ follow): pins H3 form per surface — resume.astro + kb-resume.md: H3 ≡ full `github.com/kborovik/<repo>` URL form (URL ≡ heading text rendered as V57(b) green-underlined link on screen, black plain text on print per `@media print { .resume a { color: #000; text-decoration: none } }`; kb-resume.md uses markdown autolink `<https://github.com/kborovik/<repo>>` ∨ inline link `[github.com/kborovik/<repo>](https://github.com/kborovik/<repo>)`); print-canonical PDF rationale — H3 carries discoverable link signal directly, redundant URL-row-below dropped; projects.astro card grid: H3 ≡ short repo slug (`<repo>` form, e.g. `mailpilot`); web browse-surface context — card-as-link wrapper + GitHub button card carry URL affordance ∴ short H3 reads better in grid layout; prior 'canonical form ∀ 3 surfaces' clause retired ∵ surface-specific affordances differ; 3-way mirror clause refines: project ORDER + DESCRIPTION CONTENT mirror ∀ 3 surfaces (V51-style equivalence preserved on text content), H3 FORM diverges per surface (resume.astro + kb-resume.md ≡ URL form, projects.astro ≡ slug form); §B.24-class casing-drift recurrence still mitigated: ⊥ brand-cased variant (`MailPilot`, `Pilot Skills`) admitted ∈ any of the 3 surfaces — each surface uses a single canonical form for its own scope; V50(6)(∆∆∆∆) 2026-05-23 project-description length relaxation amend (per V51 canonical-source flow): "1-line tech context" length clause retired → "tech context per kb-resume.md `## Projects` entry length" — kb-resume.md description authoring drives length, resume.astro mirrors verbatim per V51 paragraph form (post-2026-05-23 canonical fold-in kb-resume.md project entries ≡ ~2 sentence / ~50-80 word paragraphs covering capability framing + stack signal); mirror clause refines further per shape-divergence-per-surface pattern (already established for H3 form via (∆∆)): description CONTENT semantically mirrors across 3 surfaces (V51-style equivalence preserved on canonical-claim content), description SHAPE diverges per surface — resume.astro + kb-resume.md ≡ paragraph form (single `<p>` ∨ Markdown paragraph), projects.astro ≡ card-grid 2-field split (`context` lead sentence + `stack` token list) ∵ projects.astro card grid layout incompatible w/ inline paragraph; recurrence class ≡ length-clause-pre-dating-canonical-source — clauses pinned ∀ resume.astro shape pre-2026-05-23 kb-resume.md bestowal go stale when canonical-content carries different length, same B28 sub-class as V54(∆) engagement-lede ≤50w → ≤120w relaxation (kb-resume.md canonical fold-in carries richer content); resume.astro `// projects` port already landed commit `7b5585e` 2026-05-23, projects.astro shape-adapted port deferred to follow-up §T row (drift between resume.astro/kb-resume.md ↔ projects.astro on description CONTENT remains pre-existing surface, ⊥ created by this amend)) → `// engagements` (prior employers named w/ neutral one-line framing per §V.3 voice; independent-contractor positioning under `lab5.ca` umbrella; engagement-row composition ≡ H3 company name (anchor) + visible job-title line (`text-gh-fg` full color ∧ `font-medium` weight ∧ font-size ≥ body — primary scan signal per V52, ⊥ muted ∧ ⊥ subordinated below surrounding body prose) + date range (top-right, muted small) → lede + dash-bullet evidence; recent 3 engagements (lab5.ca, Colliers, PayPal) ! ∋ first-person 1-2 sentence factual lede preceding dash-bullet evidence list per V54; older engagements bullets-only) → `// education` (degrees + institutions); V50(6)(∆∆∆) 2026-05-23 section-label rename amend (user feedback 2026-05-23): /resume H2 section kicker `// tech stack` → `// tech skills` — rationale: "skills" broader-accurate than "stack" ∵ section content ⊃ {languages, frameworks, methodologies, practices, foundations} beyond pure stack tokens, ∧ matches `public/kb-resume.md ## Skills` heading semantic intent (canonical-source label form); carrier set (5 sites) ≡ {`src/pages/resume.astro:8` description prop (`Tech stack, ...` → `Tech skills, ...`), `:37` HTML comment, `:39` visible H2 kicker, `src/pages/llms.txt.ts:21` narrative ref, `src/pages/llms-full.txt.ts:63` narrative ref}; CSS class identifier `tech-stack-grid` retained ∵ technical id ⊥ visible string ∴ rename ≡ churn w/o reader benefit; `public/kb-resume.md ## Skills` heading unchanged ∵ kb-resume.md heading convention ≡ Markdown noun-form (`## Skills`, `## Projects`, `## Engagements`, `## Education`) independent of resume.astro kicker convention (`// <lowercase noun>` form) — V51 mirror governs CONTENT equivalence ⊥ LABEL form (heading-form already divergent pre-amend); §V body forward-state refs to section label use `// tech skills`, historical/past-state refs preserve `// tech stack` per §V/§T/§B documentary convention; prior `// tech stack` label retired forward
V51: `src/pages/resume.astro` HTML content ≡ `public/kb-resume.md` content equivalence — both carry equivalent claims (skills set, projects, engagements, education, contact links); drift between them ≡ violation; `kb-resume.md` ≡ CommonMark Markdown mirror (2026-05-23 fold-in — prior `kb-resume.txt` plain-text mirror retired, format upgraded to admit headings `# ## ###`, bold `**...**`, italic `*...*`, inline links `[text](url)` ∨ autolinks `<url>`, lists, fenced code) ∵ kb-resume.md ≡ canonical resume + skills-keyword source (§G), LLM agents ∧ humans parse CommonMark cleanly ∴ format upgrade strictly extends mirror capacity ⊥ regress legibility; kb-resume.md serves @ `https://lab5.ca/kb-resume.md` (CF Workers static asset, default markdown MIME)
V52: `/resume` ∧ `public/kb-resume.md` skill ∧ engagement content ! frame KB's actual experience in vocab ∈ §I `/resume primary audience` keyword set (3-group canonical-name set post-2026-05-23 kb-resume.md canonical fold-in; prior single-tier flat set retired) — surface current-target recognition tokens where KB's experience admits; primary-audience keyword set ∈ §I ≡ market-driven parameter — re-target ≡ §I amend, ⊥ V52 amend; ⊥ fabricate platform-fluency tokens absent from KB's actual project history — positioning ≡ frame what KB has shipped, ⊥ claim what he hasn't; AWS tokens dropped 2026-05-21 (⊥ shipped AWS work), OAuth/RBAC dropped (IAM-config-only experience)
V53: engagement bullet ∈ `/resume` ∧ `kb-resume.md` ! satisfy tech-token era-floor per §I `tech-token era-floor` — claim w/ token Y ∈ engagement starting year < Y floor ≡ violation; e.g. ⊥ `DevOps` role-naming ∈ Systems Engineer position pre-2009, ⊥ Kubernetes claim pre-2015, ⊥ Docker claim pre-2013, ⊥ LLM-agent ∨ Pydantic AI claim pre-2023; audit recipe ≡ ∀ engagement row, parse tech-token mentions ∈ bullet text, lookup floor ∈ §I table, flag token-year-floor > engagement-start-year as violation; era-token vocab ! be appended to §I table on first use ∈ /resume bullets
V54: engagement lede @ `src/pages/resume.astro` ∧ `public/kb-resume.md` ! satisfy — (a) voice ≡ first-person-dominant per §V.3 ∧ §V.28-style clarity (short, verb-first, one idea per sentence) ∴ ⊥ idiom, ⊥ aspirational verbs (`empower`, `transform`, `unlock`), ⊥ marketing flourish (`real ROI`, `lasting business value`, `extreme ownership`); subject-noun opening clause admitted when 2nd clause shifts to first-person verb-first (e.g. `lab5.ca is my practice... I take systems to production` ≡ admissible — overall lede dominantly first-person); strict-opening rule retired 2026-05-23 (kb-resume.md canonical fold-in admit-richer-content amend per V54(∆)); (b) length ≤ ~120 words / ~4-6 sentences admitted ∀ engagements w/ richer delivery content (lab5.ca-class — "What I deliver:" sub-block pattern), compact lede (~30 words, ≤ 2 sentences) preferred ∀ engagements w/o sub-block; prior 50-word/2-sentence cap retired 2026-05-23 ∵ kb-resume.md canonical fold-in carries richer engagement detail per §G canonical-source role ∴ kb-resume.md authoring drives lede shape, resume.astro mirrors verbatim per V51; (c) content ≡ engagement context — what KB built ∨ shipped ∨ owned + domain ∨ stack signal; ⊥ values prose, ⊥ working-style prose, ⊥ pitch language; (d) ⊥ duplicate dash-bullet evidence content — lede frames, bullets enumerate; "What I deliver:" sub-block label admitted as bullet-list header pattern ∀ engagement w/ categorized delivery bullets (lab5.ca-class), ⊥ duplicates lede; (e) `kb-resume.md` lede content ≡ `resume.astro` lede content per V51 Markdown mirror — drift between them ≡ violation; flow direction ≡ kb-resume.md → resume.astro per §G canonical-source role (kb-resume.md authored first, resume.astro mirrors)
V55: ∀ URL-accessible surface ∈ {(1) tracked file ∈ `public/`, (2) build-output from `astro.config.mjs` `integrations[]` emitting URL-addressable asset (e.g. `@astrojs/sitemap` → `/sitemap-{index,0}.xml`), (3) static endpoint ∈ `src/pages/*.{ts,js}` (e.g. `llms.txt.ts` → `/llms.txt`)} ! enumerated ∈ §I `asset:` list ∨ `route:` list; audit ≡ `/sdd:check §I` list-shape sub-recipe — extract §I surface set vs code set (rg `public/*` tracked files ∪ rg `integrations:` ∈ `astro.config.mjs` ∪ ls `src/pages/*.{ts,js}`); diff: §I − code → MISSING, code − §I → EXTRA; new surface introduced via closed §T touching `public/` ∨ `astro.config.mjs integrations[]` ∨ `src/pages/*.{ts,js}` ! pair w/ §I admission ∈ same commit ∨ §B record if intentional deferral; ∧ internal config refs to URL-accessible surfaces (cache-rule blocks ∈ `public/_headers`, route-label maps ∈ `src/layouts/Layout.astro`) ! reference only §I-enumerated live surfaces — dead-route ∨ dead-build-output internal refs ≡ same surface-completeness class as B15/B16, new instance recorded §B.19
V56: claims ∧ capability tiles ∧ /resume ∧ kb-resume.md ∧ engagement bullets ∧ llms-full.txt narrative blocks ! ⊥ ∋ on-call ∨ pager ∨ incident-response ∨ SRE ∨ oncall-engineer vocabulary — KB self-positions as Software Engineer | DevOps Engineer per V50(1) H1 ∧ V46(a) hero subtitle, ⊥ SRE/oncall-engineer adjacent role despite IAM-config ∧ on-call-rotation experience present; carrier set ≡ {`src/pages/index.astro` //claims dash bullets ∧ //what-I-do tile bodies, `src/pages/resume.astro` engagement-row bullets ∧ //tech-stack subgroups, `public/kb-resume.md` engagement-row + skills mirror per V51, `src/pages/llms-full.txt.ts` claims ∧ capability ∧ resume blocks per V48}; dev-adjacent ops tokens ∈-bounds: `deploy`, `observability`, `IaC`, `CI/CD`, `infrastructure-as-code`, `Terraform`, `Docker`, `Kubernetes`; role-shifting tokens ⊥ admitted: `on-call`, `pager`, `paging`, `incident-response`, `incident response`, `SRE`, `site reliability`, `oncall-engineer`, `on-call engineer`; orthogonal to V52 (V52 ≡ ⊥ fabricate token coverage on absent capability; V56 ≡ ⊥ self-position into adjacent role despite present capability)
V57: ∀ clickable surface (`<a href>` element, including card-as-link wrappers ∧ button-shaped links) @ lab5.ca user-facing pages ! signal clickability pre-hover via ≥ 1 admitted affordance form — (a) inline text link: underline + `text-gh-green` accent; (b) heading/title link: `text-gh-green` accent + resting underline (prior `w/ ∨ w/o underline` optionality dropped 2026-05-22 ∵ no-underline branch ⊥ trigger "this is a link" reader-expectation when visually adjacent to body emphasis bold/`<strong>`, per user feedback 2026-05-22 image-compare /projects mailpilot H3 vs /blog body inline link → §B.23); (c) card-as-link: bordered card-shape + (i) `text-gh-green` accent-colored title + resting underline ∨ (ii) visible corner chevron `→` (accent-without-underline branch dropped 2026-05-22 per (b)(∆) consistency → §B.23; border alone ⊥ sufficient per prior 2026-05-22 user feedback); (d) button-shaped link: bordered + icon + label text (current /projects GitHub-link button pattern). Underline impl ∀ Tailwind carriers ≡ `underline underline-offset-[3px]` — 3px offset matches `.prose a` ∈ `src/styles/global.css:192` ∀ visual parity body-prose ↔ heading/title link sites. `text-gh-blue` reserved for V14 section-kicker scope ∴ ⊥ admitted ∀ link affordance ∵ visual collision w/ uppercase-tracked kicker class (user feedback 2026-05-22). Nav-surface exemption: top-nav links (`src/components/Nav.astro`) ∧ footer-nav links (`src/components/Footer.astro`) operate under nav-position convention (horizontal nav bar + grouped labels ≡ universally-recognized nav affordance) ∴ per-link resting accent ⊥ required, hover-only color shift admitted ∀ nav-surface link rows only. Hover-only signals (cursor change, color shift on hover, bg shift on hover) ⊥ sufficient ∵ touch devices ⊥ hover ∧ scan-first reader ⊥ test-by-hover. Uniformity within repeating group ! enforced — ∀ card H3 link ∈ single grid (e.g. /projects card grid) carry same affordance form ∨ none do; mixed treatment within one grid ≡ violation. Carrier set ≡ {`src/pages/index.astro` //what-I-do tiles (post-V46(b)(∆) ≡ content-only, ⊥ links), //proof-of-skills tile, //résumé bordered buttons; `src/pages/projects.astro` card H3 links (mailpilot only currently) ∧ GitHub-repo buttons; `src/pages/blog/index.astro` post entry links; `src/components/Nav.astro` ∧ `src/components/Footer.astro` nav links; `.prose` inline anchors @ blog body via `src/styles/global.css:191`}. Orthogonal to V49(c) (V49(c) ≡ accessible-name presence ∀ screen reader; V57 ≡ visible-affordance presence ∀ sighted scan — different reader, different signal).
V58: footer (`src/components/Footer.astro`) composition ≡ (a) brand wordmark `Konstantin Borovik` → `/` left (`text-sm font-bold text-gh-fg`); (b) nav link set ≡ {Home → `/`, Projects → `/projects`, Blog → `/blog`} right — mirrors `<Nav />` `links[]` minus Book-a-Call per V4 ("⊥ page-footer CTA repeat"); (c) copyright line `© <year> Konstantin Borovik. All rights reserved.` below (year ≡ `new Date().getFullYear()` runtime, ∴ year auto-rolls ∀ build); (d) bg ≡ `bg-gh-canvas-subtle` per V13 contained-surface clause. Footer-nav link affordance per V57 nav-surface exemption (hover-only color shift admitted ∀ link row). Hidden surfaces (`/resume`, `/mailpilot`) ⊥ admitted ∈ footer nav ∵ outside `<Nav />` `links[]` set per §I — hidden-by-Nav ≡ hidden-by-Footer. New route ∈ `<Nav />` `links[]` set ! pair w/ footer-nav update ∈ same commit per (b) mirror clause; recurrence-class ≡ symmetric route-churn carrier per V55 surface-completeness pattern (Nav ∪ Footer both governed).
V59: ∀ non-draft blog post @ `/blog/<slug>` ! ∃ matching markdown mirror @ `/blog/<slug>.md` — build-output via `src/pages/blog/[...slug].md.ts` Astro static endpoint; content ≡ frontmatter front-matter block (YAML-fenced: title, description, pubDate ISO-8601, optional updatedDate, optional tags) + raw `entry.body` text (CommonMark, unrendered); endpoint sets `Content-Type: text/markdown; charset=utf-8`; HTML page emits `<link rel="alternate" type="text/markdown" href="/blog/<slug>.md">` per V45(∆); mirror equivalence by construction (same `src/content/blog/<slug>.md` source file). Orthogonal to V51 — V51 ≡ hand-maintained content equivalence b/w two human-authored surfaces (`kb-resume.md` ↔ `resume.astro`) requiring sweep discipline; V59 ≡ build-generated alternate-format export surface (no equivalence sweep ∵ derivation deterministic from single source). Rationale ≡ agent-discovery surface — LLM agents prefer raw markdown over HTML-stripping for blog content ingestion; mirrors the `/kb-resume.md` pattern at scale across blog corpus.
V60: `/rss.xml` ! emit per `@astrojs/rss` package — Astro endpoint `src/pages/rss.xml.ts`; RSS 2.0 channel{title:'lab5.ca blog', description:'Notes on cloud infrastructure, AI agents for business workflows, and web development.', site:'https://lab5.ca', items: ∀ non-draft blog post enumerated via `getCollection('blog')`, order ≡ pubDate desc}; ∀ item ∋ {title:frontmatter title, link:`https://lab5.ca/blog/<slug>/`, pubDate, description:frontmatter description, categories:frontmatter tags}; Layout.astro `<head>` emits `<link rel="alternate" type="application/rss+xml" title="lab5.ca blog" href="/rss.xml">` site-wide per V45(∆); new dep `@astrojs/rss` ∈ `package.json` (Node ≥22 per V20 retained). Rationale ≡ agent-discovery surface — feed readers ∧ agents monitoring portfolio for new writing have no subscription path absent RSS; `/llms.txt` covers static enumeration but ⊥ delta-discovery.
V61: `@astrojs/sitemap` integration restored 2026-05-24 per §B.15(∆) LLM-agent usability audit override — `astro.config.mjs integrations[]` ! ∋ `sitemap()`; auto-emits `/sitemap-index.xml` + `/sitemap-0.xml` @ build covering ∀ Astro route; `public/robots.txt` ! ∋ `Sitemap: https://lab5.ca/sitemap-index.xml` reference; `public/_headers` ! restore cache-rule block ∀ `/sitemap-index.xml` ∧ `/sitemap-0.xml` (parity w/ pre-T76 form, swept out per §B.19 fix); new dep `@astrojs/sitemap` re-added to `package.json` (pre-T58 ∈ `^3.7.2`, dropped per §B.15, restored via this fold-in). Originally dropped (T58 per §B.15) ∵ undeclared §I surface ∴ V55 violation — now restored as DECLARED §I surface (per §I `route:` + `asset:` extensions 2026-05-24) ∴ V55 surface-completeness gate satisfied. Rationale ≡ general-purpose crawlers (search engines, ChatGPT search index, Perplexity) still key off `/sitemap-*.xml` discovery; `/llms.txt` covers LLM-agent-first audience but doesn't displace sitemap for general indexing reach. Cousin to V59 ∧ V60 — all 3 ≡ machine-discovery enumeration surfaces but distinct protocols/consumers (sitemap ≡ search index, RSS ≡ feed readers, .md mirror ≡ LLM agents).

## §T — tasks

id|status|task|cites
T2|x|? define post-proof auto-reply CTA → resolved ⊥ — auto-reply ≡ AI answer only; ⊥ funnel back to /proof (MailPilot ≡ evidence, ⊥ product pitch ∴ ⊥ self-promo ∈ agent reply)|
T3|x|set CF redirect `/services` → `/expertise` ∧ later drop 2026-05-21 (route ⊥ ∈ §I ∴ redirect retired w/ B16 fold-in)|I.routes
T4|x|? decide analytics → resolved CF Web Analytics (server-side, automatic ∈ CF dashboard, ⊥ in-repo script)|I.analytics
T5|x|? decide staging → resolved ⊥ — local dev only (`make dev`); push → main ≡ prod per V22 ∧ V32, ⊥ preview/staging layer|V22,V32
T6|x|prune palette token drift — scope: `{gh-green-muted, gh-blue-muted, gh-red, gh-red-hover, gh-yellow}` decl ∈ `src/styles/global.css` + `src/components/Logo.astro`; remediation ≡ drop unused tokens + Logo.astro ∨ amend §I to admit|V12,B1
T7|x|sweep blog external-link anchor wraps — scope: `\[\*\*[^]]+\*\*\]\(` ∨ `` \[`[^`]+`\]\( `` ∈ `src/content/blog/**/*.md`; remediation ≡ unwrap anchor text → plain prose|V35,B2
T8|x|rename /demo route → /proof: rename `src/pages/demo.astro` → `src/pages/proof.astro`, update internal link refs ∈ `Nav.astro` + home tile + any anchors; reframe page H1 + copy — product ≡ KB skills, MailPilot ≡ evidence (⊥ "try the demo" SaaS framing)|I.routes,V3
T9|x|set CF redirect `/demo` → `/proof` (301) ∈ `public/_redirects`|I.routes
T10|x|add MailPilot source-code link button ∈ `/proof` "under the hood" section — mirror home-page proof-of-capability pattern (bordered button, not pulse); href ≡ §I.cta.mailpilot-source|I.cta.mailpilot-source
T11|x|create `.claude/check-extras.md` w/ `§-cite drift recipe extension` section excluding `src/content/blog/**/*.md` from §-cite drift scope per V37; verify post-create `/sdd:check` ⊥ flag blog cross-repo cites|V37,B5
T12|x|sweep linkedin headline anti-example class — scope: line 1 grep `^(I |Across [0-9])` ∈ `linkedin/*.txt − linkedin/resume.txt`; remediation ≡ rewrite sentence 1 per V36 (≤ ~80 chars, ∋ bold/specific claim ∨ stance ∨ provocation)|V36,B6
T13|x|fix V35 link-form @ src/content/blog/where-pytest-stops-and-claude-code-starts.md:149 — replace `[lab5.ca/demo](https://lab5.ca/proof/)` w/ absolute-path form `[/proof](/proof)`|V35,B7
T14|x|author linkedin/where-pytest-stops-and-claude-code-starts.txt per V36 — headline ≤ ~80 chars (bold/specific claim ∨ stance ∨ provocation), body, trailing `Write-up: https://lab5.ca/blog/where-pytest-stops-and-claude-code-starts/` block; ⊥ markdown syntax, ⊥ `word.tld` pattern, ⊥ `--` literal (em-dash → `—`)|V36,B7
T15|x|update `.claude/check-extras.md` per V37 amend — add filter excluding §B `cause` ∧ §T `task` narrative columns ∈ SPEC.md from free-text `§[VTB]\.[0-9]+` grep; verify post-update /sdd:check ⊥ flag any documentary cites in §B/§T narrative as UNRESOLVED|V37,B8
T16|x|sweep linkedin/*.txt per V36 amend — scope: `rg --pcre2 '^(?!Blog:\|Write-up:)[A-Z][a-z-]+:\s*https?://' linkedin/*.txt` (non-Write-up trailing URL rows) ∧ `head -1 linkedin/*.txt` ⊥ match `^Blog: ` (missing Blog header) — both ∈ `linkedin/*.txt − linkedin/resume.txt`; remediation ≡ (1) prepend `Blog: <title>` line 1 mirroring `src/content/blog/<slug>.md` frontmatter `title` verbatim, (2) remove non-Write-up labeled URL rows (current violators: `Skill:`, `Benchmark:`, `Plugin:`)|V36,B9
T17|x|relabel `Write-up:` → `Read the full post:` ∈ linkedin/*.txt per V36 amend — scope: `rg --pcre2 '^Write-up:\s*https?://' linkedin/*.txt` ∈ `linkedin/*.txt − linkedin/resume.txt`; remediation ≡ replace `Write-up: ` prefix w/ `Read the full post: ` on matched lines|V36
T18|x|retitle measuring-glyph-compression post per V38 — scope: `rg 'Measuring math-glyph compression(?! token)' src/content/blog/*.md linkedin/*.txt`; remediation ≡ insert `token` qualifier → `Measuring math-glyph token compression` (frontmatter title + V36-mirrored Blog: line) ∧ amend frontmatter `description` to carry dimension marker (e.g. `compresses tokens` ∨ `token compression`); slug ⊥ rename per V23|V38,B10
T19|x|retitle + voice rewrite where-pytest-stops-and-claude-code-starts post — scope: `rg 'Where pytest stops and Claude Code starts' src/content/blog/*.md linkedin/*.txt`; remediation ≡ (1) swap title → literal technical noun-phrase per V38 (proposed: `Smoke-testing an LLM agent with a Claude Code skill` — sentence case per V6, names what + how) ∈ frontmatter title + V36-mirrored Blog: line ∧ amend frontmatter `description` to lead w/ smoke-test ∧ LLM-agent dimensions; (2) rewrite TLDR (lines 10–18) + section ledes in first-person singular per V28 — current impersonal "MailPilot is...", "Standard test layers verify...", "The runner is..." → "I built MailPilot...", "I run...", "I let Claude Code drive..."; slug ⊥ rename per V23|V38,V28,B11,B12
T20|x|copy `src/assets/konstantin-borovik-headshot.jpg` → `public/kb-headshot.jpg` (stable URL ∀ JSON-LD per V41)|V41
T21|x|refactor `Layout.astro` — accept `schemaType` prop ∈ {ProfilePage,WebPage,CollectionPage,BlogPosting} ∧ emit JSON-LD `<script type="application/ld+json">` ∈ `<head>` w/ `@graph` ∋ Person + WebSite + page-specific node + BreadcrumbList per V40|V40
T22|x|finalize `Person.knowsAbout` keyword set (§I capability set ∪ lower-level skills: Python, TypeScript, Pydantic AI, Astro, Cloudflare Workers, LLM agents, RAG, structured extraction) ∧ embed ∈ Layout Person node|V40
T23|x|amend `src/pages/about.astro` per V46 — add `<address>` semantic tag w/ `Toronto, Ontario, Canada`; add `// claims` dash-bullet section listing falsifiable facts; add `// proof` bordered link list → LinkedIn ∧ GitHub ∧ MailPilot source ∧ blog index; amend meta `description` to ∋ `Toronto, Canada` + `AI Automation Engineer` + `20+ years`|V46
T24|x|sweep primary pages — scope: `rg -L 'description=' $(rg -l '<Layout' src/pages/{*.astro,blog/*.astro})` (Layout invocations w/o `description=` prop); remediation ≡ add explicit `description` prop per V44|V44
T25|x|extend `src/pages/blog/[...slug].astro` — pass `schemaType="BlogPosting"` ∧ supply blog-specific JSON-LD fields (datePublished, dateModified?, headline, description, keywords) per V43|V43
T26|x|add `<link rel="alternate" hreflang="en-ca">` ∧ `<link rel="alternate" hreflang="x-default">` to `Layout.astro` `<head>` per V45|V45
T27|x|author `public/llms.txt` (pointer-form per V47) ∧ `public/llms-full.txt` (prose dump per V47)|V47
T28|x|verify post-amend: `make check` + `make build` pass; spot-check JSON-LD via Schema.org validator (https://validator.schema.org/) ∧ Google Rich Results Test on `/`, `/about`, `/blog/<slug>` — manual, out-of-repo|V30,V31,V40
T29|x|swap `text-gh-fg-subtle` → `text-gh-fg-muted` @ body-size text sites — scope: `rg "text-gh-fg-subtle" src/`; exempt large-text sites (≥18pt ∨ ≥14pt bold) per V49(a); V15 styling string already updated ∈ this fold-in|V49,B13
T30|x|add `aria-label="Home"` ∈ `<a href="/">` @ `Nav.astro:14` (brand-logo link wrapping pinwheel SVG)|V49,B13
T31|x|wrap `<slot />` ∈ `<main>` ∈ `Layout.astro:221` between `<Nav />` ∧ `<Footer />`|V49,B13
T32|x|promote section kickers `// xxx` → `<h2>` element retaining V14 class string — scope: `rg 'text-sm font-bold uppercase tracking-\[0\.2em\] text-gh-blue' src/pages/`; ! `<h2>` ∀ kicker preceding tile `<h3>`s per V49(b); 3 existing kickers promoted (`// why hire me`, `// how it works`, `// under the hood`); 1 missing kicker added (`// what I build` ∈ `index.astro` tile section) to satisfy V49(b) heading-order ∀ h1→h3-skipping section|V49,B13
T33|x|re-run Lighthouse @ `/`, `/expertise`, `/proof`, `/about`, `/blog`, `/blog/<slug>` (one sample) post-fix; verify a11y ≥ 0.95 ∀; record result → Lighthouse v13 (headless Chrome, npx lighthouse@latest, accessibility-only) 2026-05-20 ∀ 6 primary pages = 1.00 (`/blog/<slug>` sample ≡ spec-driven-development); pre-fix /=0.73 per B13 ⇒ T29–T32 fold-in closes ∀ 4 defect classes (color-contrast, heading-order, link-name, landmark-one-main)|V49
T34|x|author `src/pages/resume.astro` per V50 — sentence-case H1, IBM Plex Mono, GH Primer palette, single column, sections: contact → `// skills` → `// engagements` → `// education`; pass explicit `description` prop to Layout per V44; emit JSON-LD via Layout `schemaType="ProfilePage"` per V40|V50,V44,V40
T35|x|author `public/kb-resume.txt` plain-text mirror per V51 — equivalent claims to `/resume` HTML content (skills, engagements, education, contact links); `## section` headings only, ⊥ markup beyond|V51
T36|x|amend `src/layouts/Layout.astro` `Person.sameAs` array → 4-entry set `["https://www.linkedin.com/in/kborovik", "https://github.com/kborovik", "https://x.com/kborovik", "https://bsky.app/profile/lab5.ca"]` per V42 amend|V42
T37|x|amend `public/llms.txt` + `public/llms-full.txt` — add `/resume` entry ∈ `## Pages`, `https://lab5.ca/kb-resume.txt` ∈ `## Contact`, `## Notes` section ∋ gated-LinkedIn pointer line per V47/V48 amend|V47,V48
T38|x|amend `src/pages/about.astro` `// proof` list — prepend `/resume` link per V46 amend|V46
T39|x|add `@media print` stylesheet for `/resume` route per V50(5) — drop `<Nav />` + `<Footer />` chrome, expand content to full page width|V50
T40|x|verify post-amend — `make check` + `make build` pass; browser-print `/resume` → PDF, confirm Nav/Footer absent + single-column full-width per V50(5); spot-check `kb-resume.txt` content matches `resume.astro` claims per V51 → 2026-05-20 ∀ `/resume/index.html` (9 pages built, 0 check errors): H1=1, H2=3 ∋ {// skills, // engagements, // education}, H3=9 ∋ {AI automation, Stack, 6 employers, NAIT}, `<main>`=1 ∀ V49(d), ProfilePage JSON-LD ∋ `sameAs` 4-entry set per V42, inline `@media print` ∋ Nav/Footer hidden + full-width container, `kb-resume.txt` parity ≡ resume.astro {contact, skills, engagements, education}; browser-print PDF visual ≡ manual out-of-repo follow-up|V30,V31,V50,V51
T41|x|sweep `src/pages/resume.astro` ∧ `public/kb-resume.txt` content vs §I `/resume primary audience` keyword set per V52 — scope: vocab-table compare across declared keyword set ∈ §I vs claim-token set extracted from skill bullets + engagement bullets; remediation ≡ amend bullet wording to surface primary-audience-recognizable framing per §I where KB's experience admits (⊥ fabricate)|V52
T42|x|sweep `src/pages/resume.astro` ∧ `public/kb-resume.txt` engagement bullets vs §I `tech-token era-floor` per V53 → 2026-05-20 ∀ 6 engagements: lab5.ca 2026 (Pub/Sub ≥2015, Cloud Run ≥2019, Pydantic AI ≥2024) ✓, Colliers 2023 (Cloud Run/Functions, Airflow, Terraform, Azure DevOps ≥2018) ✓, PayPal 2018 (GKE ≥2015, Airflow ≥2014, Composer ≥2018) ✓, CDW 2014/Tech Data 2010 ⊥ era-token, TD Bank 2003 (virtualization ESX ≥2003 = floor) ✓; no remediation — scope: vocab-table compare engagement-bullet token mentions against era-floor; flag any token-year-floor > engagement-start-year; remediation ≡ rewrite violating bullet to era-plausible framing|V53
T43|x|author engagement ledes ∈ `src/pages/resume.astro` ∧ `public/kb-resume.txt` ∀ recent 3 engagements (lab5.ca, Colliers, PayPal) per V54; verify post-amend `make check` ∧ `make build` pass → 2026-05-20 (9 pages built, 0 check errors) ∀ 3 ledes ∈ {lab5.ca, Colliers, PayPal} (1-2 sentences ≤ 50 words, first-person, kb-resume.txt mirror per V51)|V50,V51,V54
T44|x|drop X, Bluesky, book-call from `/resume` ∧ `public/kb-resume.txt` visible contact-block links per V50(6) amend — scope: remove `<li>` entries ∈ `src/pages/resume.astro` contact block + matching lines ∈ `kb-resume.txt`; retained surfaces ≡ V42 `Person.sameAs` JSON-LD (X ∧ Bluesky ∀ entity binding) + V48 `llms-full.txt` link set (book-call CTA) + site `<Nav />` "Book a Call" button (unchanged); verify `make check` ∧ `make build` pass + Playwright snapshot `/resume` contact list ≡ {LinkedIn, GitHub, email}|V50,V51
T45|x|restructure `/resume` ∧ `public/kb-resume.txt` skills section per V50(6) amend — rename `// skills` → `// tech stack`; drop `AI automation` H3 subgroup (capability set per V2 surfaces ∈ engagement ledes + `Person.knowsAbout` per V40, ⊥ duplicated `/resume` subgroup); replace flat `Stack` H3 w/ 6 grouped H3 subgroups ordered by scan-priority: (1) AI & agents [Pydantic AI, LLM agents and agentic workflows, RAG, structured-extraction pipelines, MCP], (2) Programming languages [Python, TypeScript], (3) Google Cloud [Cloud Run, Cloud Functions, BigQuery, Vertex AI, Cloud SQL, IAM, Composer, Pub/Sub], (4) SQL databases [PostgreSQL, psycopg], (5) Web development [Astro, Cloudflare Workers], (6) DevOps & CI/CD [Terraform, GitHub Actions, Azure DevOps, Docker, Kubernetes (GKE)]; verify `make check` ∧ `make build` pass + Playwright snapshot `/resume` ∋ 6 H3 ∈ `// tech stack` section|V50,V51,V52
T46|x|amend /resume contact-block emphasis ∧ print font hierarchy per V50(1,5,6) amend — scope: `src/pages/resume.astro` contact block (replace H1 `Resume` w/ H1 role title `AI Automation Engineer`; demote name to secondary `<p>`; `@media print` font sizes satisfy H1 > H2 ≥ H3, all ≥ body w/ section-marker H2 ! ≥ body) + `public/kb-resume.txt` mirror header per V51; verify `make check` ∧ `make build` pass + browser-print PDF spot-check shows role-title H1 dominant ∧ section markers ≥ body|V50,V51
T47|x|extend /resume font-size hierarchy from print-only to screen per V50(5) amend — scope: `src/pages/resume.astro` `<style is:global>` block — add `.resume h2 { font-size: 1rem }` rule (lifts V14 `text-sm` H2 kicker to body-size on /resume; V14 other props bold/uppercase/tracking/blue preserved); verify `make check` ∧ `make build` pass + Playwright snapshot `/resume` ∋ `// engagements` H2 ≥ engagement body prose|V50
T48|x|drop visible email mailto + add canonical site URL link per V50(6) amend — scope: `src/pages/resume.astro` contact block (replace `<li>` ∋ `mailto:kb@lab5.ca` w/ `<li>` ∋ `https://lab5.ca` link) + `public/kb-resume.txt` header link rows (replace `Email: kb@lab5.ca` w/ `Web: https://lab5.ca`); verify `grep -r 'kb@lab5.ca' src public` ≡ ∅ + `make check` ∧ `make build` pass + Playwright snapshot /resume contact list ≡ {LinkedIn, GitHub, lab5.ca}|V50,V51
T49|x|lift engagement-row job-title visibility per V50(6) amend — scope: `src/pages/resume.astro` — change `<p>` class ∀ engagement job-title line from `mt-1 text-sm text-gh-fg-muted` to `mt-1 text-sm font-medium text-gh-fg` (6 engagement rows: lab5.ca, Colliers, PayPal, CDW, Tech Data, TD Bank); verify `make check` ∧ `make build` pass + Playwright snapshot `/resume` job-title color ≡ `text-gh-fg` ∧ weight 500|V50
T50|x|raise engagement-row job-title font-size to ≥ body per V50(6) amend — scope: `src/pages/resume.astro` — change `<p>` class ∀ 6 engagement job-title lines from `mt-1 text-sm font-medium text-gh-fg` to `mt-1 text-base font-medium text-gh-fg`; verify `make check` ∧ `make build` pass + Playwright job-title computed font-size ≥ surrounding body lede font-size|V50
T51|x|reduce body weight site-wide per V11 amend — scope: `src/styles/global.css:8` — change `body { font-weight: 500 }` to `body { font-weight: 400 }`; verify `make check` ∧ `make build` pass + Playwright sweep ∀ primary page ({`/`, `/expertise`, `/proof`, `/about`, `/resume`, `/blog`}) body `<p>` computed font-weight ≡ 400, ⊥ readability regression; ⊥ ripple to existing `font-medium`/`font-semibold`/`font-bold` Tailwind classes — they retain their explicit weight per V10 hierarchy lever|V11,V10
T52|x|switch /resume print heading sizes pt → rem per V50(5) amend — scope: grep `font-size: \d+pt` ∈ `src/pages/resume.astro` `<style is:global>` block; remediation ≡ replace `.resume h1 { font-size: 20pt }`, `.resume h2 { font-size: 11pt }`, `.resume h3 { font-size: 10.5pt }` w/ rem-based values (proposed: H1 ≡ 1.5rem [27px≡20pt], H2 ≡ 1.05rem [18.9px≡14pt], H3 ≡ 1rem [18px≡13.5pt]) ∴ hierarchy holds against prose `sm:text-base` 1rem floor; body 10pt baseline kept ∀ unstyled text but prose carries Tailwind utility size; verify Playwright print-emulation H1 > H2 ≥ H3 ≥ 18px (prose floor) + browser-print → PDF spot-check section markers visually dominant over prose|V50,B14
T53|x|patch /resume print html base 18px→14px per V50(5) amend — scope: `src/pages/resume.astro` `<style is:global>` `@media print` block; remediation ≡ (1) add `html { font-size: 14px }` ∈ `@media print`, (2) drop `body { font-size: 10pt }` line (vestigial post-amend per V50(5)); verify `make check` ∧ `make build` pass + Chrome headless `--print-to-pdf` `/resume` page count ≤ 2pp + Playwright print-emulation prose `<p>` computed font-size ≡ 14px ∧ H1 > H2 ≥ H3 ≥ body hold|V50
T54|x|fold engagement-row print sizing per V50(5) amend — scope: `src/pages/resume.astro` `<style is:global>` `@media print` block (changes already applied ∈ working tree, awaiting commit); remediation ≡ confirm (1) `html { font-size: 10px }`, (2) `.resume { padding: 0.75in !important }`, (3) `.resume h1: 1.6rem`, `.resume h2: 1.15rem`, `.resume h3: 1.3rem`, (4) new selector `.resume article > div + p { font-size: 1.1rem }` (engagement job-title); verify `make check` ∧ `make build` pass + Chrome headless `--print-to-pdf` `/resume` shows hierarchy H1 > H3 > H2 ≈ job-title > body visible|V50
T55|x|drop X ∧ Bluesky entries from `Person.sameAs` ∈ `src/layouts/Layout.astro` per V42 amend — scope: `rg -nE "(x\.com/kborovik\|bsky\.app)" src/layouts/Layout.astro` (matches 2 array entries @ L57-58); remediation ≡ remove the 2 matched lines leaving 2-entry array `["https://www.linkedin.com/in/kborovik", "https://github.com/kborovik"]`; verify `make check` ∧ `make build` pass + view-source `/about` confirms `sameAs` ≡ 2-entry LinkedIn + GitHub array|V42
T56|x|build-gen llms-full.txt — add Astro static endpoint `src/pages/llms-full.txt.ts` emitting `dist/llms-full.txt` body w/ (1) Resume section ≡ Vite `?raw` import of `public/kb-resume.txt` w/ `^## ` → `### ` demote ∀ embedded headings, (2) Blog section ≡ `getCollection('blog')` filtered non-draft ∧ sorted pubDate desc per V48(a), (3) inline TS strings ∀ hero/claims/expertise-blurbs/proof/contact-links/notes blocks (literal carry-over from existing `public/llms-full.txt` narrative content); endpoint sets `Content-Type: text/plain; charset=utf-8`; delete `public/llms-full.txt`; verify post-amend → 2026-05-21: `make check` (0 errors) ∧ `make build` (9 pages built, `/llms-full.txt` ∈ static routes) pass; `dist/llms-full.txt` ∋ Resume section reflecting kb-resume.txt (6 tech-stack `### ` H3 subgroups {AI & agents, Programming languages, Google Cloud, SQL databases, Web development, DevOps & CI/CD} ∧ 6 engagement rows {lab5.ca, Colliers, PayPal, CDW, Tech Data, TD Bank}); Blog section ∋ 3 posts pubDate desc {2026-05-19, 2026-05-18, 2026-05-13}; mechanical-gen path ∴ kb-resume.txt edit → rebuild reflects by construction; `https://lab5.ca/llms-full.txt` URL preserved post-deploy ≡ manual out-of-repo follow-up|V48
T57|x|build-gen llms.txt — add Astro static endpoint `src/pages/llms.txt.ts` emitting `dist/llms.txt` pointer-form body per V47 ∋ (1) `## Pages` link list w/ hand-tuned page descriptions inline (literal carry-over from existing `public/llms.txt`), (2) `## Posts` ≡ `getCollection('blog')` filtered non-draft ∧ sorted pubDate desc per V48(a), (3) `## Contact` link list inline mirroring §I cta refs ∪ `https://github.com/kborovik` ∪ `https://lab5.ca/kb-resume.txt` per V48(c), (4) `## Notes` gated-LinkedIn pointer line per V47 inline; endpoint sets `Content-Type: text/plain; charset=utf-8`; delete `public/llms.txt`; verify `make check` ∧ `make build` pass ∧ `dist/llms.txt` ∋ 3 blog posts pubDate desc + Contact link set + Notes line|V47,V48
T58|x|drop `@astrojs/sitemap` integration per B15 resolve — scope: `rg -nE '@astrojs/sitemap\|sitemap-index\.xml\|sitemap\(\)' astro.config.mjs package.json public/robots.txt` (matches integration import + dep + `integrations: [sitemap()]` + robots.txt `Sitemap:` line); remediation ≡ remove all matches + `pnpm install` regen lock; verify `make check` ∧ `make build` pass + `dist/sitemap-*.xml` absent post-build + `/sdd:check --incremental --all` V55 ⊥ flag sitemap surface; §I amend admits `_redirects` ∈ asset brace-list (resolves B16) ∈ same commit|V55,B15,B16
T59|x|patch html base 18px→16px per V11 amend — scope: `src/styles/global.css:4`; remediation ≡ change `font-size: 18px` → `font-size: 16px`; verify `make check` ∧ `make build` pass + local `make preview` visual confirmation 2026-05-21 (rem-based scales auto-track new base ∴ ⊥ layout regression)|V11
T60|x|sweep section kicker class string per V14 amend — scope: pattern `text-sm font-bold uppercase tracking-\[0\.2em\] text-gh-blue` ∈ `src/pages/**/*.astro` (7 files: about, expertise, index, proof, resume screen kickers, blog index, blog [...slug]); remediation ≡ swap `text-sm` → `text-base`, retain `font-bold uppercase tracking-[0.2em] text-gh-blue`; ∧ drop vestigial `.resume h2 { font-size: 1rem }` screen-side CSS rule ∈ `src/pages/resume.astro` `<style is:global>` (vacuous post-V14 amend per V50(5) sync); resume print `@media print { .resume h2: 1.15rem }` untouched per V50(5) print special-case; verify `make check` ∧ `make build` pass + local `make preview` visual confirmation 2026-05-21|V14,V50
T61|x|sweep tile H3 + body typography per V11 amend follow-through — scope: pattern `text-sm font-semibold text-gh-fg` (tile H3, w/ optional `group-hover:text-gh-green transition-colors` suffix) → `text-base font-semibold text-gh-fg` ∧ pattern `text-xs leading-relaxed text-gh-fg-muted` (tile body `<p>` ∧ `<span>`) → `text-sm leading-relaxed text-gh-fg-muted` ∈ `src/pages/**/*.astro` (index.astro // what I build + // why hire me tiles, proof.astro // how it works + // sample questions + // under the hood); /resume engagement rows ⊥ tile pattern ∴ ⊥ in scope; verify `make check` ∧ `make build` pass + local `make preview` visual confirmation 2026-05-21|V11,V14
T62|x|merge `/about` + `/expertise` + hero content into `src/pages/index.astro` — emit 6 sections per V46 amend (hero w/ 3-line title block + `// what I do` 3-tile + `// tech stack` 4-group summary + `// claims` + `// proof` link list + `// résumé` button); verify `make check` + `make build` pass|V1,V2,V44,V46
T63|x|author `src/pages/projects.astro` per §I route addition — 4 hand-authored skill cards mapping skill label → GitHub repo: (1) Google Cloud → gcp-devops, (2) Python → mailpilot, (3) AI agent tooling → pilot-skills, (4) Web development → lab5.ca; hand-authored inline, ⊥ runtime/build-time GitHub API fetch (determinism > freshness); append-only ∀ future cards (Salesforce reserved)|I.routes
T64|x|delete `src/pages/expertise.astro` + `src/pages/about.astro`; sweep dead-route refs — scope: `rg -F '/expertise' src/ ; rg -F '/about' src/` (excluding HTML `<address>` element refs); remediation ≡ patch `Nav.astro` + footer + cross-page link refs to drop `/expertise` ∧ `/about` href values; ⊥ CF redirect (site too new, ⊥ inbound campaigns)|I.routes
T65|x|swap `/resume` H1 + `Person.jobTitle` JSON-LD + `kb-resume.txt` header — `AI Automation Engineer` → `Software Developer \| DevOps Engineer` (pipe verbatim ∀ surface incl. JSON-LD; Google Knowledge Panel renders pipe raw, accepted per §V.3 dev-tooling voice)|V50,V51
T66|x|restructure `/resume` `// tech stack` 6→9 subgroups per V50(6) amend → [1] Programming languages [Python, TypeScript, Bash, SQL] → [2] Google Cloud [Cloud Run, Cloud Functions, BigQuery, Vertex AI, Cloud SQL, Composer, Pub/Sub] → [3] Azure [App Service, Functions, Service Bus, Key Vault] (NEW) → [4] DevOps & CI/CD [Terraform, Ansible, GitHub Actions, Azure DevOps, Docker, Kubernetes (GKE)] → [5] AI & agents [Pydantic AI, LLM agents and agentic workflows, RAG, structured-extraction pipelines, MCP] → [6] APIs & integration [REST, event-driven, Pub/Sub patterns, webhooks, OpenAPI] (NEW) → [7] Security & compliance [IAM, NIST-800-53, PCI-DSS, secrets management] (NEW) → [8] SQL databases [PostgreSQL, SQLite] → [9] Web development [Astro, Cloudflare Workers]; mirror change ∈ `public/kb-resume.txt` `## tech stack`|V50,V51,V52
T67|x|add `/resume` `// projects` section between `// tech stack` ∧ `// engagements` per V50(6) amend — 4 entries matching `/projects` card grid order ∧ content: (1) gcp-devops [Terraform / Ansible / IaC, Cloud Run, Pub/Sub], (2) MailPilot [Pydantic AI agent, LLM workflows, GCP backend], (3) Pilot Skills [Claude Code skills SDK, agentic workflows], (4) lab5.ca [Astro 6, Tailwind v4, TypeScript, Cloudflare Workers]; ∀ entry ! H3 + 1-line tech context + bordered GitHub repo link; mirror ∈ `public/kb-resume.txt` as `## projects`; verify 3-way mirror (resume.astro ↔ kb-resume.txt ↔ projects.astro)|V50,V51
T68|x|rewrite `Person.knowsAbout` JSON-LD set ∈ `src/layouts/Layout.astro` against merged generalist keyword set per §I amend ∧ V40 ∧ V52|V40,V52
T69|x|regen `public/og.png` (1200×630 via `og.html`) + `public/banner-linkedin.png` (1584×396 via `linkedin-banner.html`) per CLAUDE.md regen recipes — new visible strings `Software Developer \| DevOps Engineer` (pipe verbatim) + stack-token sub-line `Python · Google Cloud · LLM agents · CI/CD`|I.assets
T70|x|update `src/pages/llms.txt.ts` + `src/pages/llms-full.txt.ts` per V47 + V48 amend — drop `/expertise` + `/about` pointers; add `/projects` pointer ∈ `## Pages` link list; update hero / claims / capability blurbs (inline TS strings) to new pitch per §G + V1 + V2 amend|V47,V48
T71|x|add `index.astro` `// résumé` section per V46(f) — single bordered button `Download résumé (PDF)` → `/resume` (new tab); confirm V4 (≤ 1 prominent CTA per page) holds — Book-a-Call stays primary `cta-pulse` ∈ `<Nav />`, résumé button ≡ secondary bordered|V4,V46
T72|x|sweep residual `AI Automation Engineer` strings — scope: `rg -F 'AI Automation Engineer' src/ public/` (! ≡ ∅ post-fold); remediation ≡ patch any stragglers to `Software Developer \| DevOps Engineer`|V1,V50
T73|x|verify post-amend — `make check` + `make build` pass; Lighthouse a11y ≥ 0.95 ∀ primary page per V49 ({`/`, `/projects`, `/proof`, `/blog`, `/blog/<slug>`}); Playwright snapshot `/` + `/projects` + `/resume` confirms structure (hero 3-line block, 3-tile services grid, 9-subgroup tech stack, 4-entry projects grid, hidden /resume in nav)|V30,V31,V49
T74|x|sweep V1 pitch-language carriers per V1(∆) — scope: `rg --pcre2 'AI automation for business operations\|ERP integration\|document workflow\|AI email\|production engineering'` ∈ {`src/layouts/Layout.astro`, `src/pages/blog/index.astro`, `src/pages/resume.astro`, `public/kb-resume.txt`, `src/pages/llms.txt.ts`, `src/pages/llms-full.txt.ts`}; remediation ≡ rewrite carrier content to V1(∆) generalist pitch (cloud infrastructure / AI agents for business workflows / web development); verify post-amend `make check` ∧ `make build` pass ∧ `/sdd:check --all` ⊥ flag V1|V1,B17
T75|x|amend `src/pages/llms-full.txt.ts` Contact block per V48(c) — scope: lines 67-73; remediation ≡ insert `- Resume: https://lab5.ca/resume` line between GitHub ∧ Plain-text resume entries (mirrors `llms.txt.ts` ordering); verify post-amend `/sdd:check --all` ⊥ flag V48|V48,B18
T76|x|sweep V55(∆) internal-ref completeness per V55(∆) — scope: (1) `public/_headers` lines 28-31 → drop `/sitemap-index.xml` ∧ `/sitemap-0.xml` cache blocks; (2) `src/layouts/Layout.astro:151-156` `segmentLabel` map → drop dead keys {`expertise`, `about`} ∧ add live route labels {`projects`, `resume`}; verify post-amend `make check` ∧ `make build` pass ∧ `/sdd:check --all` ⊥ flag V55|V55,B19
T77|x|sweep tile/step title casing per V7(∆) — scope: vocab-table sweep across {`src/pages/index.astro` tile H3, `src/pages/proof.astro` step + stack H3, `src/pages/llms-full.txt.ts` capability block prose}; vocab table: {`cloud infrastructure` → `Cloud Infrastructure`; `AI agents for business workflows` → `AI Agents for Business Workflows`; `web development` → `Web Development`; `send a product question` → `Send a Product Question`; `agent searches the knowledge base` → `Agent Searches the Knowledge Base`; `you get a sourced reply` → `You Get a Sourced Reply`; `frontend` → `Frontend`; `backend` → `Backend`; `ai layer` → `AI Layer`}; remediation ≡ rewrite per vocab table; verify post-amend `make check` ∧ `make build` pass|V7,V2,V48
T78|x|sweep `// what I do` tile body per V46(b)(∆) — scope: 3 `<p>` elements following tile H3 ∈ `src/pages/index.astro` `// what I do` section (current lines 53-86); remediation ≡ rewrite tile body prose: drop specific tech tokens (Cloud Run, Cloud Functions, Pub/Sub, BigQuery, Terraform, GitHub Actions), bold platform keywords w/ `<strong>` tags (Google Cloud, Azure, GitHub, Pydantic AI, Claude API, Astro per tile theme); verify `make check` ∧ `make build` pass|V46
T79|x|restructure `// tech stack` 4-group set per V46(c)(∆) — scope: 4 H3 + 4 `<p>` subgroups ∈ `src/pages/index.astro` `// tech stack` section (current lines 92-128); remediation ≡ rename H3 set from {Google Cloud, Python, JavaScript / TypeScript, Web development} → {Python & TypeScript, Google Cloud & Azure, AI Automation, CI/CD & Terraform}; update body `<p>` token list to match new group definitions; verify `make check` ∧ `make build` pass|V46
T80|x|sweep hero stack-token sub-line per V46(a)(∆) — scope: grep pattern `Python &middot; Google Cloud &middot; LLM agents &middot; CI/CD` ∈ `src/pages/index.astro`; remediation ≡ remove matched `<p>` element + animation-delay style (current lines 21-23); ⊥ sweep `assets-src/og.html` ∨ `assets-src/linkedin-banner.html` (social-card surfaces ⊥ V46 scope — no scroll-down stack ∴ retain stack-token signal); verify `make check` ∧ `make build` pass|V46
T81|x|sweep `## What I do` capability ordering ∈ `src/pages/llms-full.txt.ts` per V48(b)(∆) ∧ V2(∆) — scope: pattern `^0[1-3]\s+(Cloud Infrastructure\|AI Agents for Business Workflows\|Web Development)\.` ∈ `src/pages/llms-full.txt.ts` (3 paragraph leaders @ current lines 40, 42, 44); remediation ≡ rewrite 3 capability paragraphs to V2 new ordering `01 Back-End Development → 02 Front-End Development → 03 Cloud Infrastructure` w/ hand-tuned narrative prose per V48(d); verify `make check` ∧ `make build` pass ∧ `/sdd:check --incremental --all` ⊥ flag V48|V48,V2,B20
T82|x|fix V35 internal-link form @ `src/pages/resume.astro:32` per V50(6)(∆) — scope: pattern `<a href="https://lab5\.ca"` ∈ `src/pages/resume.astro`; remediation ≡ replace href value `https://lab5.ca` w/ `/` (absolute path per V35 internal-link rule); visible text `lab5.ca` ∧ class attrs preserved; verify `make check` ∧ `make build` pass ∧ `/sdd:check --incremental --all` ⊥ flag V35|V35,V50,B21
T83|x|rename `/proof` → `/mailpilot` route + sweep internal refs per §I(∆) ∧ V44(∆) ∧ V46(e)(∆) ∧ V47(∆) ∧ V49(∆) — scope: (1) `git mv src/pages/proof.astro src/pages/mailpilot.astro`; (2) pattern `/proof` ∈ {`src/pages/index.astro`, `src/components/Nav.astro`, `src/components/Footer.astro`, `src/pages/llms.txt.ts`, `src/pages/llms-full.txt.ts`, `public/_redirects`, `src/content/blog/where-pytest-stops-and-claude-code-starts.md`} — replace href ∧ visible URL labels w/ `/mailpilot`; (3) `src/components/Nav.astro` `links[]` array drop `{ href: '/proof', label: 'Proof' }` entry ∴ menu set ≡ {Home, Projects, Blog} per §I(∆) hidden-surface declaration; (4) `src/components/Footer.astro` drop `<a href="/proof">Proof</a>` `<nav>` entry; (5) `public/_redirects` change `/demo /proof 301` → `/demo /mailpilot 301` ∧ add `/proof /mailpilot 301` (backward-compat ∀ inbound `/proof` links); verify `make check` ∧ `make build` pass + Playwright `/` nav menu ≡ {Home, Projects, Blog} + `/mailpilot` route renders + `/proof` returns 301 → `/mailpilot`|V44,V46,V47,V49,I.routes
T84|x|elevate MailPilot to project #1 + link H3 → `/mailpilot` per §I(∆) — scope: (1) `src/pages/projects.astro` `projects` array — reorder entries to `[mailpilot, gcp-devops, pilot-skills, lab5]` (mailpilot moved from position 2 → 1); (2) wrap H3 `{p.repo}` w/ internal absolute-path link `<a href="/mailpilot">` ∀ entry where `p.id === 'mailpilot'` only (other 3 projects ⊥ have internal detail page ∴ H3 unlinked); (3) mirror reorder ∈ `src/pages/resume.astro` `// projects` section ∧ `public/kb-resume.txt` `## projects` per V50(6) 3-way mirror; (4) mirror reorder ∈ `src/pages/llms-full.txt.ts` `## Projects` block per V48; verify `make check` ∧ `make build` pass + Playwright `/projects` shows mailpilot as card #1 w/ H3 link → `/mailpilot`|V50,V51,V48,I.routes
T85|x|sweep V55(∆) `Layout.astro` `segmentLabel` post `/proof` → `/mailpilot` rename per B22 — scope: pattern `proof: 'Proof'` ∈ `src/layouts/Layout.astro` `segmentLabel` map (current line 153); remediation ≡ replace key `proof: 'Proof'` w/ `mailpilot: 'MailPilot'` (∵ `/mailpilot` ≡ live route per §I(∆); `/proof` ≡ 301-redirect surface ∈ `public/_redirects` only, ⊥ live route ∴ ⊥ §I-enumerated); verify `make check` ∧ `make build` pass ∧ `/sdd:check --incremental --all` ⊥ flag V55|V55,B22
T86|x|sweep V57 link-affordance carriers per V57 ∧ V46(b)(∆) ∧ V46(e)(∆) ∧ V2(∆) — scope: (1) `src/pages/index.astro:46,59,72` pattern `<a href="/projects" class="animate-on-scroll group block"` ∀ 3 //what-I-do tiles → replace anchor wrapper w/ `<div>`, drop closing `</a>` ∧ drop `group-hover:text-gh-green transition-colors` from H3 class (V46(b)(∆) tile-link drop); (2) `src/pages/index.astro:124` pattern `<span class="text-sm font-bold text-gh-fg sm:text-base">Let AI` → replace `text-gh-fg` token w/ `text-gh-blue` ∈ class string per V46(e)(∆) + V57(c) accent-title affordance; (3) `src/pages/projects.astro:69` pattern `hover:text-gh-green transition-colors` ∈ mailpilot H3 `<a>` → replace w/ `text-gh-blue hover:text-gh-blue-hover transition-colors` (resting-state accent per V57(b)); verify `make check` ∧ `make build` pass|V57,V46,V2
T87|x|sweep V57(∆) accent-color narrowing gh-blue → gh-green across link carriers (gh-blue reserved for V14 kicker scope) + fix /blog post-title H2 hover-only affordance drift — scope: (1) `src/pages/index.astro:124` proof-of-skills title `text-gh-blue` → `text-gh-green` per V46(e)(∆); (2) `src/pages/projects.astro:69` mailpilot H3 link `text-gh-blue hover:text-gh-blue-hover` → `text-gh-green hover:text-gh-green-hover` per V57(b); (3) `src/styles/global.css:191,198` `.prose a` color `gh-blue` → `gh-green`, `.prose a:hover` color `gh-blue-hover` → `gh-green-hover` per V57(a); (4) `src/pages/blog/index.astro:51` post-title H2 link add `text-gh-green hover:text-gh-green-hover` (currently hover-only via `link-animated` + `hover:text-gh-green` ∴ violates V57 resting-affordance rule); (5) `src/pages/blog/index.astro:66` "read →" link `text-gh-blue hover:text-gh-blue-hover` → `text-gh-green hover:text-gh-green-hover` per V57(b); (6) `src/pages/blog/[...slug].astro:76` `← all posts` link `text-gh-blue hover:text-gh-blue-hover` → `text-gh-green hover:text-gh-green-hover` per V57(b); (7) `src/pages/resume.astro:23,27,32` contact links `hover:text-gh-blue` (hover-only + gh-blue ∴ double-violation) → `text-gh-green hover:text-gh-green-hover` resting per V57(a); verify `make check` ∧ `make build` pass|V57,V46,V14
T88|x|sweep V57(b)(∆) ∧ V57(c)(∆) resting-underline carriers per B23 — scope: pattern `text-gh-green hover:text-gh-green-hover` ∈ {`src/pages/projects.astro`, `src/pages/index.astro`, `src/pages/blog/index.astro`, `src/pages/blog/[...slug].astro`, `src/pages/resume.astro`} (5 files, 6 link sites: projects mailpilot H3, index proof-of-skills card title span, blog/index post-title H2, blog/index `read →`, blog/[...slug] `← all posts`, resume contact 3-link sweep); remediation ≡ append `underline underline-offset-[3px]` Tailwind utility ∀ matched carrier (matches `.prose a` 3px offset ∈ `src/styles/global.css:192` ∀ visual parity), drop `link-animated` class @ `src/pages/blog/index.astro:51` (hover-only underline animation superseded by resting underline); verify `make check` ∧ `make build` pass → 2026-05-22 (8 pages built, 0 check errors)|V57,B23
T89|x|sweep V50(6)(∆) canonical-name carriers per B24 — scope: pattern `<h3[^>]*>(MailPilot\|Pilot Skills)</h3>` ∈ `src/pages/resume.astro` (2 sites: L97 ≡ `MailPilot`, L131 ≡ `Pilot Skills`) ∧ pattern `^(MailPilot\|Pilot Skills)$` ∈ `public/kb-resume.txt` `## projects` section (2 sites: L42 ≡ `MailPilot`, L50 ≡ `Pilot Skills`); remediation ≡ replace `MailPilot` → `mailpilot`, `Pilot Skills` → `pilot-skills` ∀ matched site; verify `make check` ∧ `make build` pass + /resume `// projects` H3 set ≡ /projects card H3 set ≡ {`mailpilot`, `gcp-devops`, `pilot-skills`, `lab5.ca`}|V50,V51,B24
T90|x|sweep V3(∆) written-out-number carriers per B25 — scope: grep pattern `\b(zero|one|two|three|four|five|six|seven|eight|nine|ten|eleven|twelve|thirteen|fourteen|fifteen|twenty|thirty|forty|fifty|sixty|seventy|eighty|ninety|hundred|thousand)(\s|-)(second|minute|hour|day|year|week|month|percent|page|line)s?\b` (case-insensitive) ∈ {`src/pages/{index,projects,mailpilot,resume}.astro`, `src/pages/blog/index.astro`, `src/pages/llms{,-full}.txt.ts`, `src/layouts/Layout.astro`} (8 files); current violations: `src/pages/mailpilot.astro:18` `Within sixty seconds`, `src/pages/mailpilot.astro:69` `under sixty seconds`; remediation ≡ `sixty seconds` → `60 seconds` ∀ matched site (matches `/index.astro:128` site-wide form `under 60 seconds`); verify `make check` ∧ `make build` pass + grep ∀ primary-page set ≡ ∅|V3,B25
T91|x|sweep V58(b) footer-nav link set — scope: `src/components/Footer.astro:9-11` `<nav>` block (currently ∋ `<a href="/blog" class="text-xs text-gh-fg-muted transition-colors hover:text-gh-fg">Blog</a>` only); remediation ≡ prepend `<a href="/">Home</a>` ∧ `<a href="/projects">Projects</a>` ∈ `<nav>` block matching `<Nav />` `links[]` order (Home → Projects → Blog), retain class string `text-xs text-gh-fg-muted transition-colors hover:text-gh-fg` ∀ each ∈ V57 nav-surface exemption; verify `make check` ∧ `make build` pass + Playwright snapshot `/` footer nav text set ≡ {Home, Projects, Blog}|V58
T92|x|sweep V50(5)(∆) sub-base-prose carriers per B26 — scope: grep pattern `text-(xs|sm)\b` ∈ `src/pages/resume.astro` (case-sensitive, word-boundary; 30 carrier sites @ amend-time per B26 inventory — contact secondary prose (3), tech-stack container `text-sm sm:text-base` (1), project descriptions (4), GitHub button labels (4), engagement-row date ranges (6), engagement ledes ∧ bullet lists (9), education degree line (1), education date range (1), footer-nav `text-xs` exempt per V57 nav-surface but ⊥ ∈ resume.astro scope ∴ ⊥ relevant); remediation ≡ replace `text-sm` ∧ `text-xs` ∧ `text-sm sm:text-base` ∧ `text-sm leading-relaxed text-... sm:text-base` patterns w/ `text-base` (∨ `text-base leading-relaxed text-...`) ∀ visible prose carrier — drop `sm:text-base` mobile-breakpoint pair entirely ∵ base-at-all-viewports per V50(5)(∆); verify `make check` ∧ `make build` pass + grep `text-(xs|sm)\b` ∈ `src/pages/resume.astro` ≡ ∅|V50,B26
T93|x|sweep V50(6)(∆) project-link form per /resume print-canonical-path — scope: grep pattern `rounded-lg border border-gh-border bg-white px-3 py-1\.5` ∈ `src/pages/resume.astro` (4 carrier sites: project entries @ mailpilot/gcp-devops/pilot-skills/lab5.ca, L102-109/L119-126/L136-143/L153-160; pattern matches the discriminative button-class substring); remediation ≡ replace each bordered button anchor wrapper (incl. inline GitHub SVG icon + `github.com/kborovik/<repo>` label text) w/ inline V57(a) text link form `<a href=... target="_blank" rel="noopener noreferrer" class="text-gh-green hover:text-gh-green-hover underline underline-offset-[3px]">github.com/kborovik/<repo></a>` matching V50(6) contact-block link form @ resume.astro:23/27/32 (LinkedIn ∧ GitHub ∧ lab5.ca); drop wrapping `<p class="mt-2">` if it carries the button anchor only (re-inline the link); kb-resume.txt mirror per V51 no-op ∵ plain-text already carries `https://github.com/kborovik/<repo>` URL on its own line; verify `make check` ∧ `make build` pass + grep `rounded-lg border border-gh-border bg-white px-3 py-1\.5` ∈ `src/pages/resume.astro` ≡ ∅ + Playwright `/resume` `// projects` section ∀ 4 entries ∋ green-URL link form|V50,V57
T94|x|implement V50(5)(∆) 2026-05-23 print-page-budget per /resume 3-page form — scope: `src/pages/resume.astro` `<style is:global>` `@media print` block + tech-stack container @ resume.astro:44 (`<div class="mt-8 space-y-6 text-base leading-relaxed text-gh-fg-muted">`); remediation ≡ (1) revert unstaged `html { font-size: 6px }` → `10px` per V50(5) print-base, (2) add `@media print { .resume .tech-stack-grid { display: grid; grid-template-columns: repeat(3, 1fr); column-gap: 1rem; row-gap: 0.4rem; } }` rule + add `tech-stack-grid` class to tech-stack container, (3) tighten section ∧ article ∧ subgroup gaps ∈ `@media print` per V50(5)(∆) (b), (4) add `.resume section { break-inside: avoid }` ∈ `@media print`, (5) verify via `make dev` → Chrome `--print-to-pdf` → `pdftoppm -r 100 /tmp/resume.pdf /tmp/p -png` → 3-page output ∧ no orphan section-header @ page bottom; verify `make check` ∧ `make build` pass|V50
T95|x|implement V50(5)(∆∆) print-pacing follow-up — scope: `src/pages/resume.astro` `<style is:global>` `@media print` block; remediation ≡ (1) `.resume .tech-stack-grid grid-template-columns` `repeat(3, 1fr)` → `repeat(2, 1fr)`, (2) section gap `.resume .mt-12 margin-top` ~0.8rem → ~1.5rem, section pt `.resume .pt-10 padding-top` ~0.5rem → ~1rem, h2 ↔ content gap `.resume section > h2 + div / + article margin-top` ~0.35rem → ~0.75rem, (3) tech-stack subgroup gap `.resume .space-y-6 > :not([hidden]) ~ :not([hidden]) margin-top` 0 → ~0.5rem; verify via `make dev` → Chrome `--print-to-pdf` → `pdftoppm -r 100 /tmp/resume.pdf /tmp/p -png` → ≤ 3 pages ∧ visible wider section breaks|V50
T96|x|sweep V3(∆) numeric-form clause carrier set per B27 audit-gap recurrence — scope: case-insensitive grep pattern `\b(zero|one|two|three|four|five|six|seven|eight|nine|ten|eleven|twelve|thirteen|fourteen|fifteen|twenty|thirty|forty|fifty|sixty|seventy|eighty|ninety|hundred|thousand)[ -](second|minute|hour|day|year|week|month|percent|page|line)s?\b` ∈ V3 primary-page carrier set ≡ {`src/pages/{index,projects,mailpilot,resume}.astro`, `src/pages/blog/index.astro`, `src/pages/llms{,-full}.txt.ts`, `src/layouts/Layout.astro`} (8 files); current violations: `src/pages/blog/index.astro:30` `five years` ∈ /blog meta lede (`...whether a system runs for a quarter or five years.`); remediation ≡ `five years` → `5 years` ∀ matched site; verify `make check` ∧ `make build` pass + re-run pattern ∀ V3 carrier set ≡ ∅ post-fix (close T90 execution-gap per B27)|V3,B27
T97|x|swap `public/kb-resume.txt` → `public/kb-resume.md` ∀ surface per V51(∆) — scope: grep pattern `kb-resume\.txt` ∈ {`src/`, `public/`} (current violations: `public/kb-resume.txt` itself, `src/pages/llms-full.txt.ts:3` Vite `?raw` import, `src/pages/llms-full.txt.ts:59` Resume narrative ref, `src/pages/llms-full.txt.ts:72` Contact line, `src/pages/llms.txt.ts:33` Contact list line); remediation ≡ `git rm public/kb-resume.txt` + rewrite carriers to `kb-resume.md` per T100 ∧ T101 (this task ≡ removal step only, carrier-rewrites delegated to T100/T101); verify post-fix `rg -F 'kb-resume.txt' src/ public/` ≡ ∅|V47,V48,V51
T98|x|rewrite `src/pages/resume.astro` `// tech stack` section per V50(6)(∆) — scope: tech-stack-grid block @ resume.astro:37-89 (current 9 H3 + 1-line body each); remediation ≡ replace w/ 3 H3 groups {Software Development, DevOps, AI & LLM} ∀ matching kb-resume.md Skills section L14-47; ∀ group ∋ multiple labeled body lines (`<p>` per `Label: tokens` line per V50(6)(∆) form, e.g. `Languages: Python, TypeScript, Node.js, Bash, SQL`); `tech-stack-grid` CSS class retained; print-form reflow per V50(5)(∆) ≤ 3-page budget — adjust `@media print { .tech-stack-grid }` grid-template-columns ∨ row-gap as needed to fit budget (impl-determined); verify `make check` ∧ `make build` pass + Playwright `/resume` ∋ exactly 3 H3 ∈ `// tech stack` section + Chrome `--print-to-pdf` `/resume` ≤ 3 pages|V50,V51,V52
T99|x|rewrite `src/pages/index.astro` `// what I do` 3-tile per V46(b)(∆) — scope: tile H3 + body `<p>` set @ index.astro:46-83 (current 3 tiles: Back-End Development, Front-End Development, Cloud Infrastructure); remediation ≡ replace tile H3 set → {Software Development, DevOps, AI & LLM} + rewrite each body `<p>` to middot-separated keyword stream per V46(b)(∆) draft (Software Development: `Python · TypeScript · Bash · SQL · FastAPI · Pydantic · Astro · Cloudflare Workers · REST APIs · OpenAPI · Event-Driven Architecture · Microservices · PostgreSQL · ETL · Structured-Extraction Pipelines · Pytest · TDD`; DevOps: `Google Cloud · Azure · Terraform · Ansible · Helm · GitHub Actions · Azure DevOps · Docker · Kubernetes · GKE · AKS · Cloud Run · Apache Airflow · IaC · CI/CD · OpenTelemetry · IAM · NIST 800-53 · PCI DSS`; AI & LLM: `Anthropic Claude · Google Gemini · Pydantic AI · LLM Agents · Agentic AI Development · RAG · Structured Extraction · MCP · Prompt Engineering · Claude Code · Claude Skills · AI-Augmented Development`); verify `make check` ∧ `make build` pass + Playwright `/` ∋ 3 tile H3 matching new set|V46
T100|x|patch `src/pages/llms-full.txt.ts` per V48(e)(∆) — scope: import line @ :3 (`import resumeRaw from '../../public/kb-resume.txt?raw'`), demote logic @ :6 (`resumeRaw.replace(/^## /gm, '### ')`), narrative ref @ :59 (`plain-text mirror at https://lab5.ca/kb-resume.txt`), Contact line @ :72 (`Plain-text resume: https://lab5.ca/kb-resume.txt`); remediation ≡ (1) swap import path → `'../../public/kb-resume.md?raw'`, (2) extend demote → strip kb-resume.md H1 (`^# .+\n+`) + dual demote (`^### ` → `^#### ` ∧ `^## ` → `### ` — order ! reverse-deepest-first to ⊥ collide), (3) narrative ref `.txt` → `.md` (label may read `markdown mirror` ∨ similar), (4) Contact line `.txt` → `.md` (label may read `Markdown resume`); verify `make check` ∧ `make build` pass + `dist/llms-full.txt` Resume section ∋ kb-resume.md content w/ `###` H3 ∀ section headings ∧ ⊥ orphan `# Konstantin Borovik` H1|V48
T101|x|patch `src/pages/llms.txt.ts` per V47(∆) ∧ V48(c)(∆) — scope: Contact list entry @ :33 (`- [Plain-text resume](https://lab5.ca/kb-resume.txt)`); remediation ≡ swap `.txt` → `.md` ∧ optional re-label `Plain-text resume` → `Markdown resume`; verify `make check` ∧ `make build` pass + `dist/llms.txt` Contact set ∋ updated URL|V47,V48
T102|x|rewrite `src/layouts/Layout.astro` `knowsAbout` JSON-LD array per V40 ∧ V52 against §I(∆) primary-audience keyword set — scope: array @ Layout.astro:58-103 (current 41 tokens shorthand form); remediation ≡ replace w/ canonical LinkedIn-skill token set per §I(∆) vocab line (3-group ordered: Software Development → DevOps → AI & LLM), token form ≡ LinkedIn-skill verbatim (`Google Cloud Platform (GCP)` ⊥ `Google Cloud`, `Identity and Access Management (IAM)` ⊥ `IAM`, etc.); verify `make check` ∧ `make build` pass + view-source `/` ∋ updated `knowsAbout` array w/ canonical-name tokens|V40,V52
T103|x|swap role-title canonical `Software Developer | DevOps Engineer` → `Software Engineer | DevOps Engineer` ∀ live carrier per V46(a)(∆) ∧ V50(1)(∆) — scope: grep pattern `Software Developer` ∈ {`src/`, `public/`, `assets-src/`} (live invariant carriers, ⊥ historical §T row text @ T65/T69/T72 ∈ SPEC.md which preserves prior canonical as history); current carriers (pre-T98/T99/T100 fold): `src/pages/index.astro:19` hero subtitle, `src/pages/resume.astro:8` description prop ∧ :13 H1, `src/layouts/Layout.astro:8` default `description` ∧ :47 `Person.jobTitle`, `src/pages/llms-full.txt.ts:20` H1 string, `src/pages/llms.txt.ts:15` blockquote intro, `assets-src/og.html` + `assets-src/linkedin-banner.html` visible string; remediation ≡ `Software Developer` → `Software Engineer` ∀ matched site + regen `public/og.png` + `public/banner-linkedin.png` per CLAUDE.md headless-Chrome screenshot recipe (1200×630 ∧ 1584×396); verify post-fix `rg -F 'Software Developer' src/ public/ assets-src/` ≡ ∅ (excluding historical SPEC.md §T row text) + `make check` ∧ `make build` pass|V1,V46,V50
T104|x|verify post-amend — scope: `make check` (0 errors) ∧ `make build` (page count unchanged); Playwright `/resume` ∋ 3 H3 ∈ `// tech stack` + matching kb-resume.md Skills group set; Playwright `/` ∋ 3 tile H3 set {Software Development, DevOps, AI & LLM}; Chrome `--print-to-pdf` `/resume` page count ≤ 3 per V50(5)(∆); `https://lab5.ca/kb-resume.md` serves 200 OK (manual post-deploy); `/sdd:check --incremental --all` ⊥ flag V46/V47/V48/V50/V51/V52|V30,V31,V49,V50
T105|x|sweep — port `public/kb-resume.md` engagement content (lede + bullets) to `src/pages/resume.astro` `// engagements` per V51 ∧ V54(∆) ∧ V50(6); scope vocab table: {kb-resume.md L80-92 → resume.astro lab5.ca article L158-185, L94-102 → Colliers L187-216, L104-112 → PayPal L218-247, L114-120 → CDW L239-262, L122-126 → TD Bank L264-290}; remediation ≡ replace lede + bullet prose w/ kb-resume.md verbatim equivalent (HTML markup preserved, content sourced from kb-resume.md — lab5.ca rich lede + `What I deliver:` 3-bullet sub-block per V54(∆) richer-content admit, Colliers/PayPal compact lede + dash-bullets per V54(∆) compact-form preference, CDW/TD Bank bullets-only per V50(6) older-engagement pattern); engagement-row composition baseline per V50(6) preserved (H3 + job-title + date range + lede + bullets); CDW date range 2014-2018 → 2010-2018 + Tech Data row removal (mirror of prior-turn unstaged code change) folded into this §T ∴ resume.astro engagement set ≡ kb-resume.md 5-row set {lab5.ca, Colliers, PayPal, CDW, TD Bank} per V51; verify `make check` ∧ `make build` pass + Playwright snapshot `/resume` engagements section ≡ kb-resume.md content + Chrome `--print-to-pdf` `/resume` ≤ 3 pages per V50(5)(∆)|V50,V51,V54,B28
T106|x|sweep — `src/pages/llms-full.txt.ts` `## What I do` capability block ordering → V2(∆) 2026-05-23 (B28 recurrence of B20); scope grep pattern `^0[1-3]\s+(Back-End Development\|Front-End Development\|Cloud Infrastructure)\.` ∈ `src/pages/llms-full.txt.ts` (3 paragraph leaders @ current lines 40, 42, 44); remediation ≡ rewrite 3 capability paragraphs to V2 ordering {01 Software Development, 02 DevOps, 03 AI & LLM} w/ hand-tuned narrative prose per V48(d) (wording hand-tuned, ⊥ machine-derived; carry kb-resume.md keyword stream signal — `Python · TypeScript · FastAPI · PostgreSQL · REST APIs · Pydantic AI · LLM agents` ∀ Software Development paragraph, `Google Cloud · Azure · Terraform · GitHub Actions · Docker · Kubernetes · GKE · IaC · CI/CD · OpenTelemetry` ∀ DevOps paragraph, `Anthropic Claude · Google Gemini · Pydantic AI · LLM agents · RAG · MCP · Claude Code · Claude Skills` ∀ AI & LLM paragraph); verify `make check` ∧ `make build` pass + grep pattern ≡ ∅ post-fix + `/sdd:check --incremental --all` ⊥ flag V48|V48,V2,B28
T107|x|sweep — `public/kb-resume.md` `## Projects` section reorder to V50(6) canonical {mailpilot, gcp-devops, pilot-skills, lab5.ca}; scope vocab table: {kb-resume.md L52-74 `## Projects` block — entries: mailpilot (L52-56), pilot-skills (L58-62), gcp-devops (L64-68), lab5.ca (L70-74)}; current order {mailpilot, pilot-skills, gcp-devops, lab5.ca}; remediation ≡ swap pilot-skills ∧ gcp-devops H3+body blocks (3-way mirror w/ resume.astro + projects.astro per V50(6) ∧ V51); verify `make check` ∧ `make build` pass + diff `## Projects` section ∈ kb-resume.md ≡ project order ∈ projects.astro + `/sdd:check --incremental --all` ⊥ flag V50|V50,V51,B28
T108|x|sweep `// tech stack` → `// tech skills` label per V50(6)(∆∆∆) — scope: grep pattern `[Tt]ech [Ss]tack` ∈ {`src/pages/resume.astro`, `src/pages/llms.txt.ts`, `src/pages/llms-full.txt.ts`} (3 files, 5 carrier sites: resume.astro:8 description prop, :37 HTML comment, :39 visible H2 kicker, llms.txt.ts:21 narrative ref, llms-full.txt.ts:63 narrative ref); remediation ≡ swap `tech stack` → `tech skills` preserving casing (`Tech stack` → `Tech skills` ∀ description-prop site, `tech stack` → `tech skills` ∀ kicker ∧ narrative sites); CSS class identifier `tech-stack-grid` retained (technical id ⊥ visible string); `public/kb-resume.md ## Skills` heading ⊥ ∈ scope per V50(6)(∆∆∆) (Markdown heading convention independent of kicker form); verify post-fix `rg -i 'tech stack' src/` ≡ ∅ + `make check` ∧ `make build` pass + Playwright `/resume` H2 set ∋ `// tech skills`|V50,V51
T109|x|implement /resume print-section page-break per V50(5)(∆∆∆) — scope: `src/pages/resume.astro` `<style is:global>` `@media print` block; remediation ≡ add rule `.resume section + section { break-before: page }` (adjacent-sibling selector matches projects + engagements + education sections — tech-skills excluded ∵ preceded by `<header>` not `<section>`); existing `.resume section { break-inside: avoid }` retained; verify via `make dev` → Chrome `--print-to-pdf` per CLAUDE.md /resume PDF render recipe → `pdftoppm -r 100 /tmp/resume.pdf /tmp/p -png` → 4-page output {p1: contact + tech-skills, p2: projects, p3: engagements, p4: education} per V50(5)(∆∆∆) budget + `make check` ∧ `make build` pass|V50
T110|.|init `src/pages/blog/[...slug].md.ts` Astro endpoint per V59 — `getStaticPaths()` ∀ non-draft blog post; GET emits frontmatter front-matter block (YAML-fenced: title, description, pubDate ISO, optional updatedDate, optional tags) + `entry.body` raw text; Content-Type `text/markdown; charset=utf-8`; verify `make build` emits `dist/blog/<slug>.md` ∀ 3 current posts ∧ `curl -sI` returns `Content-Type: text/markdown` post-deploy|V59,I.routes
T111|.|patch `src/pages/blog/[...slug].astro` per V45(∆) — emit `<link rel="alternate" type="text/markdown" href="/blog/<slug>.md">` ∈ `<head>` via new Layout `markdownAlternate` prop (∨ inline `<link>` in slot if Layout extension defers to T116); discovery hint per V59|V45,V59
T112|.|init `src/pages/rss.xml.ts` Astro endpoint via `@astrojs/rss` per V60 — `pnpm add @astrojs/rss`; channel{title:'lab5.ca blog', description:'Notes on cloud infrastructure, AI agents for business workflows, and web development.', site:'https://lab5.ca'}; items ≡ `getCollection('blog', !draft)` sorted pubDate desc, ∀ item {title, link:`${site}/blog/${id}/`, pubDate, description, categories: tags}; verify `make build` emits `dist/rss.xml` ∧ XML validates as RSS 2.0 (e.g. `xmllint --noout dist/rss.xml`)|V60,I.routes
T113|.|patch `src/layouts/Layout.astro` `<head>` per V45(∆) — emit `<link rel="alternate" type="application/rss+xml" title="lab5.ca blog" href="/rss.xml">` site-wide (∀ page); verify view-source `/` ∧ `/resume` ∧ `/blog` ∧ `/blog/<slug>` ∋ RSS alternate link tag|V45,V60
T114|.|patch `src/pages/llms.txt.ts:10` Posts entry shape per V47(∆) + V48(a)(∆) — render `- [${p.data.title}](https://lab5.ca/blog/${p.id}/): ${p.data.description}` (drop pubDate, add description per per-file shape); verify `dist/llms.txt` Posts entries ∋ description per V47(∆) shape; closes §B.29|V47,V48,B29
T115|.|patch `src/pages/llms.txt.ts:43` ∧ `src/pages/llms-full.txt.ts:86` Content-Type header per V47(∆) ∧ V48(e)(∆) — `text/plain; charset=utf-8` → `text/markdown; charset=utf-8`; verify `curl -sI https://lab5.ca/llms.txt` ∧ `curl -sI https://lab5.ca/llms-full.txt` return `Content-Type: text/markdown; charset=utf-8` post-deploy|V47,V48
T116|.|patch `src/layouts/Layout.astro` per V45(∆) — accept optional `markdownAlternate?: string` prop; emit `<link rel="alternate" type="text/markdown" href={markdownAlternate}>` ∈ `<head>` when set; patch `src/pages/resume.astro` to pass `markdownAlternate="/kb-resume.md"`; verify view-source `/resume` ∋ markdown alternate link tag pointing @ `/kb-resume.md` (mirror per V51)|V45,V51
T117|.|patch `src/layouts/Layout.astro` Person JSON-LD `personNode` block per V40(∆) — add `potentialAction: {@type:'EmailAction', name:'Email MailPilot live agent', description:'Send a question to demo@lab5.ca; live AI agent reply returns under 60 seconds.', target:{@type:'EntryPoint', urlTemplate:'mailto:demo@lab5.ca', actionPlatform:['http://schema.org/DesktopWebPlatform','http://schema.org/MobileWebPlatform']}}`; verify view-source `/` JSON-LD `@graph[0].potentialAction.target.urlTemplate` ≡ `mailto:demo@lab5.ca` + schema.org validator passes|V40
T118|.|sweep section kicker per V14(∆) — scope: grep pattern `(>// (what I do|claims|proof of skills|résumé|projects|proof of capability|tech skills|engagements|education|blog|how it works|sample questions|under the hood)<|>\\s*// (what I do|claims|proof of skills|résumé|projects|proof of capability|tech skills|engagements|education|blog|how it works|sample questions|under the hood)\\s*<)` ∈ `src/pages/{index,projects,mailpilot,resume}.astro`, `src/pages/blog/{index,[...slug]}.astro` (covers H2/p kickers w/ literal `// ` prefix); remediation ≡ (1) `src/styles/global.css` add `@layer components { .section-kicker::before { content: '// '; } }` (∨ raw CSS class outside @layer for Tailwind v4 @theme stack); (2) ∀ kicker element: append ` section-kicker` to existing Tailwind utility `class` string ∧ strip literal `// ` prefix from inner text content; verify `make check` ∧ `make build` pass + Playwright snapshot ∀ primary page renders `// xxx` visually identical to pre-amend + DOM text content of `.section-kicker` element ⊥ ∋ `// ` literal (i.e. `textContent` ≡ section name only)|V14
T119|.|restore `@astrojs/sitemap` integration per V61 ∧ §B.15(∆) — (1) `pnpm add @astrojs/sitemap`, (2) `astro.config.mjs` add `import sitemap from '@astrojs/sitemap'` + `integrations: [sitemap()]`, (3) `public/robots.txt` add `Sitemap: https://lab5.ca/sitemap-index.xml` line, (4) `public/_headers` restore cache-rule block ∀ `/sitemap-index.xml` ∧ `/sitemap-0.xml` (pre-T76 form: `Cache-Control: public, max-age=3600`); verify `make check` ∧ `make build` emits `dist/sitemap-{index,0}.xml` + `curl -sI https://lab5.ca/sitemap-index.xml` returns 200 post-deploy + `/sdd:check --all` V55 ⊥ flag sitemap surface (now §I-declared)|V55,V61,B15
T120|.|verify post-amend per V30/V31 — `make check` (0 errors) ∧ `make build` (page count = pre-amend + N new endpoints: `/rss.xml`, `/sitemap-{index,0}.xml`, `/blog/<slug>.md` ∀ slug); endpoint sniff via `curl -sI https://lab5.ca/{rss.xml,sitemap-index.xml,llms.txt,llms-full.txt,blog/<sample-slug>.md}` confirms Content-Type per V47(∆)/V48(e)(∆)/V59/V60/V61; view-source `/` confirms `<link rel="alternate" type="application/rss+xml">` + Person JSON-LD `potentialAction.target.urlTemplate` ≡ `mailto:demo@lab5.ca`; view-source `/resume` confirms `<link rel="alternate" type="text/markdown" href="/kb-resume.md">`; view-source `/blog/<slug>` confirms `<link rel="alternate" type="text/markdown" href="/blog/<slug>.md">`; view-source ∀ kicker carrier site (per T118 grep scope) confirms `.section-kicker` element `textContent` ⊥ ∋ literal `// ` prefix + visual rendering matches pre-amend; out-of-repo schema.org validator (https://validator.schema.org/) on `/` passes ∀ Person + WebSite + WebPage nodes|V30,V31

## §B — bugs

id|date|cause|fix
B1|2026-05-18|`global.css` decl 5 `--color-gh-*` tokens (`gh-green-muted`, `gh-blue-muted`, `gh-red`, `gh-red-hover`, `gh-yellow`) ⊥ ∈ §I palette list; `gh-red*`/`gh-yellow` ref only ∈ unused `Logo.astro` (CLAUDE.md notes `Logo ⊥ used`); `*-muted` pair zero refs; V17 pinwheel uses raw hex ∈ `Nav.astro:17–20` + `favicon.svg`, ⊥ tokens; surfaced by `/sdd:check` set-diff|V12
B2|2026-05-18|blog `.md` external-link anchor text wraps `**bold**` ∈ `spec-driven-development.md:18` ∧ `` `code` `` ∈ `measuring-glyph-compression.md:214` — no rule pre-amend, surfaced by `/sdd:check §V` post V35 anchor-text clause|V35
B3|2026-05-19|linkedin post body referenced `SPEC.md` ∈ `linkedin/{measuring-glyph-compression,spec-driven-development}.txt`; LinkedIn auto-linkified `SPEC.md` → `<a href="http://SPEC.md">` broken-scheme link (Playwright DOM dump @ `/in/kborovik/recent-activity/all/`); single-quote `'SPEC.md'` ⊥ defeat matcher (re-linkified on edit); backtick `` `SPEC.md` `` ⊥ defeat either; resolved by drop `.md` → `SPEC` ∈ source; 2 older live posts (pre-fix) retain broken `http://SPEC.md` ∈ LinkedIn DOM — out-of-repo remediation only (manual edit ∈ LinkedIn UI)|V36
B4|2026-05-19|V36 markdown-syntax prohibition list ∋ `- bullet` but §V.16 establishes dash-prefix ≡ canonical site bullet ∧ Playwright DOM dump shows LinkedIn renders `- ` literal (benign); rule-as-written ≠ rule-as-intended (V36 over-reach), surfaced by /sdd:check post-V36-amend audit; resolved by narrow V36 list ∧ admit dash-prefix per §V.16|V36
B5|2026-05-19|`/sdd:check --incremental --all` post-V36-amend flagged T.37 (line 99), B.6/B.7/B.8 (lines 140,142,165,167) ∈ `src/content/blog/measuring-glyph-compression.md` as UNRESOLVED — target rows absent ∈ lab5.ca SPEC.md (§T max ≡ T10, §B max ≡ B4); surrounding prose explicitly qualifies cites as cross-repo into pilot-skills SPEC.md (line 85: ``Take `§V.6` from the pilot-skills `SPEC.md`:``); mechanical grep ⊥ distinguish documentary cross-repo cite from live cite ∴ false-positive recurrence class — any blog post discussing external SPEC re-triggers; resolved by exclude `src/content/blog/**/*.md` from §-cite drift recipe scope per V37; documentary cite tokens dropped § prefix 2026-05-19 amend to defeat free-text `§[VTB]\.[0-9]+` grep — narrow fix only, broader §B/§T narrative-column filter ∈ B8|V37
B6|2026-05-19|V36 amend eb4343f (2026-05-19) added "catchy ∨ controversial" + "∋ bold/specific claim ∨ stance ∨ provocation" to headline rule; companion sweep through `linkedin/*.txt` ⊥ run post-amend ∴ `linkedin/spec-driven-development.txt:1` "I kept losing track of what Claude was doing." remains autobiographical setup matching V36 anti-example class ("I claimed X"); prior amend 79e8e63 (length-only rule) ∧ companion fix c8212e1 swept both files, but eb4343f (catchy/controversial dimension) ⊥ paired w/ re-sweep; /sdd:check surfaced 2026-05-19|V36
B7|2026-05-19|where-pytest-stops-and-claude-code-starts.md drafted (untracked) ∋ 2 latent violations of existing rules surfaced by `/sdd:check` 2026-05-19: (1) V35 — line 149 `[lab5.ca/demo](https://lab5.ca/proof/)` writes internal lab5.ca link as full `https://` URL + trailing `/`, ⊥ absolute path `/proof` per V35 internal-link clause; (2) V36 — companion `linkedin/where-pytest-stops-and-claude-code-starts.txt` absent ∴ pair-rule unmet. root cause ≡ author discipline gap — blog .md drafted w/o running `/sdd:check` pre-publish ∴ V35/V36 audits skipped; both rules pre-exist ∴ ⊥ rule-gap, ⊥ new invariant. remediation: fix line 149 → `/proof` absolute path + author linkedin/<slug>.txt per V36 headline+structure rules|V35,V36
B8|2026-05-19|`/sdd:check` 2026-05-19 (first-run, full-scan) surfaced 3 UNRESOLVED free-text §-cites @ SPEC.md:96 (B5 body): T.37, B.7 (HOLD coincidentally — B7 added same day step 1), B.8 — all 3 ≡ documentary cites in B5 narrative quoting prior /sdd:check output that originally found them ∈ blog file (cross-repo refs into pilot-skills SPEC.md); B5 captured blog-content variant via V37 scope exclusion, but ⊥ extended to SPEC.md's own §B/§T narrative-column cite quotes ∴ recurrence class re-fires whenever §B records document prior tool runs. Narrow patch f343a77 dropped § prefix on B5's specific tokens — closes B5 instance only. Broader fix ≡ V37 scope extension (this commit) covering §B/§T narrative columns + check-extras.md filter update ∈ T15 — codifies convention that live cites stay ∈ typed `§T.cites`/`§B.fix` columns, narrative free-text cite forms ≡ documentary by definition|V37
B9|2026-05-19|`linkedin/where-pytest-stops-and-claude-code-starts.txt` published w/ 2 URL rows (`Write-up: https://lab5.ca/blog/...` + `Skill: https://github.com/.../SKILL.md`); LinkedIn DOM (Playwright screenshot 2026-05-19, live post @ `/in/kborovik/recent-activity/all/`) shows trailing `Skill:` URL grabbed card preview (rendered `mailpilot/.claude/skills/smoke-test/SKILL.md at main · kborovik/mailpilot` GitHub card), suppressing canonical `Write-up:` link to `lab5.ca/blog/<slug>/` — LinkedIn auto-card logic promotes the LAST URL ∴ ≥ 2 URLs ⇒ non-Write-up URL wins card slot. Prior V36 explicitly admitted `optional additional <Label>: <url> rows (e.g. Benchmark:, Plugin:)` ∴ rule-gap surfaced 2026-05-19; V36 amend forbids non-Write-up trailing URL rows ∧ mandates `Blog: <title>` line 1; companion sweep §T16 collapses existing `linkedin/{measuring-glyph-compression,spec-driven-development,where-pytest-stops-and-claude-code-starts}.txt` to single Write-up URL ∧ adds `Blog: <title>` headers|V36
B10|2026-05-20|blog title `Measuring math-glyph compression` (frontmatter @ `src/content/blog/measuring-glyph-compression.md:2`; mirrored @ `linkedin/measuring-glyph-compression.txt:1` per V36) — bare `compression` ⊥ disambiguate dimension (token count? bytes? semantic? ratio?); body measures token-count reduction via Claude tokenizer ∴ title ! `token` qualifier; reader landing cold via search ∨ social-card ⊥ infer subject; surfaced by user 2026-05-20; resolved by amend title + Blog: + description per T18; slug ⊥ rename (V23)|V38
B11|2026-05-20|blog title `Where pytest stops and Claude Code starts` (frontmatter @ `src/content/blog/where-pytest-stops-and-claude-code-starts.md:2`; mirrored @ `linkedin/where-pytest-stops-and-claude-code-starts.txt:1` per V36) — rhetorical/positional framing (`Where X stops and Y starts`) ⊥ name technical subject (smoke-testing LLM agent via Claude Code skill executing real APIs ∧ structured-JSON LLM-judge gate); reader landing cold via search ∨ social-card share ⊥ infer technical subject pre-body; V38 pre-amend admitted bare-noun-needs-qualifier class only, ⊥ rhetorical-framing class ∴ rule-gap surfaced 2026-05-20; resolved by amend V38 to admit rhetorical/positional class + retitle per T19; slug ⊥ rename (V23)|V38
B12|2026-05-20|`src/content/blog/where-pytest-stops-and-claude-code-starts.md` TLDR (lines 10–18) opens impersonal — "[MailPilot] is...", "Standard test layers verify...", "The runner is..." — setting impersonal register pre-body; body scatters first-person (L29 "the layers I actually run", L86 "when I type 'smoke test'", L194 "whatever I write into the mock") but lede dominates ∴ violates V28 first-person-singular + simple/clear register clauses; pre-amend V28 ⊃ first-person clause ∴ drift latent pre-amend, surfaced by /sdd:check 2026-05-20 post commit 8bce5a3 (V28 audience/register extension); other 2 posts (measuring-glyph-compression, spec-driven-development) HOLD ∴ ⊥ recurrence-class — single-post drift, ⊥ authoring-pattern gap; remediation bundled into T19 (single edit pass over file w/ V38 title retitle)|V28
B13|2026-05-20|Lighthouse v13.0.2 audit (`~/Downloads/lab5.ca-20260520T074642.json`, desktop, simulate throttling) @ `/` returned accessibility=0.73 (perf/SEO/best-practices ≥0.99); 4 defect classes — color-contrast (5 nodes), heading-order (1 reported, ≥4 latent ∀ page), link-name (1), landmark-one-main (1); SPEC.md pre-amend ⊥ a11y invariant ∴ ⊥ recurrence-prevention rule, defects accumulated as `Layout.astro`/`Nav.astro` grew (3/4 ∈ those files ∴ propagated ∀ page); resolved by V49 baseline + T29–T33 sweep|V49
B14|2026-05-20|/resume print PDF (Chrome headless `--print-to-pdf` 2026-05-20) rendered H2 (`.resume h2 { font-size: 11pt }`) ∧ H3 (`.resume h3 { font-size: 10.5pt }`) smaller than prose `sm:text-base` (1rem = 13.5pt @ print width 816px > sm:640px breakpoint; `html { font-size: 18px }` media-agnostic); V50(5) wording "all ≥ body" interpreted as body-element 10pt baseline @ authoring (T46/T47), but visible prose context governs reader scan ∴ section markers visually subordinated below body in PDF; root cause ≡ absolute pt units ⊥ tracking Tailwind utility rem sizes; resolved by V50(5) amend → rem-based heading sizing + body-floor redefined as visible prose|V50
B15|2026-05-21|`@astrojs/sitemap` integration (`astro.config.mjs:3,8`; `package.json` dep `^3.7.2`) emits `dist/sitemap-{index,0}.xml` @ build; `public/robots.txt:4` references `/sitemap-index.xml` ∴ surface URL-accessible ∧ live; integration ∈ initial commit `76ebddb` (2026-03-22) ∴ never crossed §I gate; forward amend `9e1190f` admitting sitemap reverted `0fcd07c` ("orthogonal to V48 — search-engine vs LLM audience") but integration ∧ dep stayed ∴ undeclared surface persists; `/sdd:check` §V-default ⊥ catch (§I drift fires only on `--all` ∨ §I scope); surfaced 2026-05-21 by `/sdd:check --all` post-T57; B15(∆) 2026-05-24 drop-policy reversed — LLM-agent usability audit (2026-05-24) flagged sitemap absence as general-purpose crawler (search engines, ChatGPT search index, Perplexity) discovery gap; sitemap restored as DECLARED §I surface per V61 ∧ §I `route:` + `asset:` extensions ∴ V55 surface-completeness gate now satisfied (sitemap presence governed by V61 invariant, no longer undeclared-surface class); T58 drop step retired forward, restoration step ≡ T119|V55,V61
B16|2026-05-21|`public/_redirects` ∋ CF redirect rule (`/demo /proof 301`) per T9; previously held `/services /expertise 301` per T3 — dropped 2026-05-21 same-commit (/services ⊥ ∈ §I routes ∴ redirect retired); §I `asset:` brace-list ∋ `_headers` ∧ ⊥ `_redirects` ∴ CF-Pages routing-config-pair enumerated asymmetrically; T9 closure ⊥ paired w/ §I sweep ∴ closed-task evidence ⊥ §I enumeration; same recurrence class as B15 (§I surface completeness gap) localized to `public/*` file-add axis; surfaced 2026-05-21 by `/sdd:check --all`|V55
B17|2026-05-22|2026-05-21 V1(∆) website re-design fold-in swept visible page bodies (hero, tiles, claims, capability copy) but ⊥ enumerate non-page-body carrier set ∴ 3 surfaces retain pre-fold pitch vocab — `src/layouts/Layout.astro:25` default `description` fallback (`AI automation for business operations — ERP integration, document workflows, email, and reporting`), `src/pages/blog/index.astro:18` /blog meta `description` (`Notes on AI automation, ERP integration, document workflows, and production engineering`), `src/pages/resume.astro:177-180` + `public/kb-resume.txt:64-66` lab5.ca engagement lede (same retired vocab; concurrent V52 vocab-set ∧ V54 lede-content drift); recurrence class ≡ pitch-amend carrier-set under-enumeration — re-fires on every future identity pivot unless V1 carries explicit carrier list; surfaced 2026-05-22 by `/sdd:check --all`; resolved by V1 amend enumerating carrier set + T74 sweep|V1
B18|2026-05-22|V48(c) mandates 6-element contact link set ≡ §I `cta.linkedin` ∪ `https://github.com/kborovik` ∪ §I `cta.mailpilot-source` ∪ §I `cta.book-call` ∪ `https://lab5.ca/resume` ∪ `https://lab5.ca/kb-resume.txt` ∀ both `public/llms.txt` ∧ `public/llms-full.txt`; T70 (commit `29b0030`) swept `src/pages/llms.txt.ts` to all 6 entries but `src/pages/llms-full.txt.ts:67-73` Contact block remained 5-of-6 — `https://lab5.ca/resume` absent; URL ∃ ∈ Resume narrative block @ line 59 but V48(c) governs Contact-list set specifically ∴ inclusion-via-narrative ⊥ satisfy; ⊥ rule-gap (V48(c) already governs); single-surface code fix via T75|V48
B19|2026-05-22|two internal config refs point @ surfaces removed earlier — `public/_headers:28-31` retains cache rules for `/sitemap-index.xml` ∧ `/sitemap-0.xml` post-T58 (`@astrojs/sitemap` dropped per B15); `src/layouts/Layout.astro:151-156` `segmentLabel` map holds dead keys {`expertise`, `about`} (routes deleted per T64) ∧ ⊥ live route labels {`projects`, `resume`} ∴ `/projects` breadcrumb item falls through to lowercase `projects`; T58 closure ⊥ paired w/ `_headers` sweep, T63/T64 route churn ⊥ paired w/ `segmentLabel` sweep; same surface-completeness recurrence class as B15/B16 extended from §I asset-list to internal config refs; surfaced 2026-05-22 by `/sdd:check --all`; resolved by V55 amend extending scope + T76 sweep|V55
B20|2026-05-22|commit `d3f743f` (2026-05-22) amended V2 3-tile capability ordering (`01 Cloud Infrastructure → 02 AI Agents → 03 Web Development` → `01 Back-End Development → 02 Front-End Development → 03 Cloud Infrastructure`) ∧ V46 page-body but ⊥ swept V48 carrier `src/pages/llms-full.txt.ts:38-44` `## What I do` narrative block which still emits prior ordering; V48(b) parenthetical text @ SPEC.md:85 also pinned prior ordering example inline ∴ rule text itself stale alongside code carrier; same recurrence class as B17 (amend-carrier under-enumeration — V48 enumerates carrier llms-full.txt.ts but V2-amend gate ⊥ surface dependency) amplified by pinned-example-in-rule-body class — rule parenthetical text goes stale on V2(∆) even when carrier enumeration correct ∴ V48(b)(∆) drops pinned example, V2 ref by name only; surfaced 2026-05-22 by `/sdd:check --incremental`; resolved by V48(b)(∆) + T81 sweep|V48
B21|2026-05-22|`src/pages/resume.astro:32` contact-block "lab5.ca" link written as `<a href="https://lab5.ca">` — full external URL form for internal home-page link; V35 mandates internal links use absolute path `/` ∴ href ! ≡ `/`; T48 task text (`replace mailto w/ https://lab5.ca link`) ∧ V50(6) parenthetical (`canonical site URL https://lab5.ca`) both pinned the full-URL form as authorial directive on href value, overriding V35's general internal-link form rule by virtue of being inline-specific; V35 was in scope @ T48 close but missed ∵ V50(6) inline-pin appeared to supersede; same recurrence class as B20 (pinned-example-in-rule-body) — V50(6) parenthetical pinned a specific href form that collided w/ V35's general rule, cross-rule pinned-example collision; surfaced 2026-05-22 by `/sdd:check --incremental`; resolved by V50(6)(∆) drops href-form pin ∧ delegates form to V35 + T82 sweep|V35
B22|2026-05-22|`src/layouts/Layout.astro:151-156` `segmentLabel` map retains dead key `proof: 'Proof'` post-T83 (∵ `/proof` route renamed → `/mailpilot` per §I(∆) ∴ `/proof` ⊥ §I-enumerated live route — `/proof` ∈ `public/_redirects` ≡ 301-redirect surface only) ∧ ⊥ live key `mailpilot: 'MailPilot'` ∴ `/mailpilot` breadcrumb item falls through to lowercase `mailpilot`; V55(∆) enumerates `route-label maps ∈ src/layouts/Layout.astro` as carrier per B19 fix but T83 task text enumerated sweep paths {`Nav.astro`, `Footer.astro`, `llms.txt.ts`, `llms-full.txt.ts`, `_redirects`, blog `.md`} ⊥ inheriting V55's enumerated carrier list ∴ Layout.astro `segmentLabel` carrier missed; same recurrence class as B19 (config-ref carrier missed by route-churn task) amplified by task-text under-enumeration — V55 rule already governs ∴ ⊥ rule-gap, gap ≡ task-author discipline (route-churn §T ! cross-ref V55 carrier list pre-show-user; chain B17/B19/B20/B21/B22 ≡ carrier-set under-enumeration recurrence class, V55-localized form); surfaced 2026-05-22 by `/sdd:check --incremental`; resolved by T85 sweep|V55
B23|2026-05-22|user feedback 2026-05-22 (image-compare /projects card vs /blog post inline link): /projects `mailpilot` H3 link rendered `text-gh-green` only (⊥ resting underline) ∧ /blog body inline links rendered `text-gh-green` + resting underline per `.prose a` ∈ `src/styles/global.css:192`; both ≡ V57-class green-accent affordance but visible-signal variance — V57(b) pre-amend admitted "w/ ∨ w/o underline" optionality ∀ heading/title link form ∴ /projects card H3 sat on no-underline branch, blog body inline lived on underline branch ∴ inconsistent affordance signal ∈ green-link family; green accent alone ⊥ trigger "this is a link" reader-expectation when visually adjacent to body emphasis (bold, `<strong>` tags); resolved by V57(b)(∆) drop optionality + V57(c)(∆) require resting underline on accent-title form + T88 sweep|V57
B24|2026-05-22|user feedback 2026-05-22 (image-compare /resume `// projects` H3 set vs /projects card grid H3 set): /resume H3 set ≡ {`MailPilot` (brand cap @ resume.astro:97), `gcp-devops`, `Pilot Skills` (Title Case w/ space @ resume.astro:131), `lab5.ca`} vs /projects card H3 set ≡ {`mailpilot`, `gcp-devops`, `pilot-skills`, `lab5.ca`} per projects.astro `p.repo` field (GitHub repo slug verbatim); `public/kb-resume.txt:42,50` mirrors /resume variant per V51 ∴ same casing-drift on third surface, 3-way mirror visibly inconsistent in 2 of 4 entries; V50(6) pre-amend required `content ≡` 3-way equivalence but ⊥ pin canonical name form ∴ casing variance went uncaught — single canonical identifier (GitHub repo slug per `kborovik/<repo>`) eliminates drift surface ∵ identifier = display = link target; resolved by V50(6)(∆) extend 3-way mirror clause to require GitHub-repo-slug-verbatim H3 form + T89 sweep|V50
B25|2026-05-23|user feedback 2026-05-23 (on-page form-collision): `/mailpilot` carries the same 60-second reply-latency fact in 3 places, 2 forms — L88 numeric chip `replies in &lt;60s · no signup · real production agent`, L18 step body `Within sixty seconds the agent replies…`, L69 hero subtitle `replies in under sixty seconds`; `/index.astro:128` carries same fact as `under 60 seconds` (numeric) ∴ site-wide pattern already numeric ∧ /mailpilot the lone written-form straggler; V3 pre-amend ⊥ governed number form ∴ written-out drift uncaught; resolved by V3(∆) numeric-form clause + T90 sweep|V3
B26|2026-05-23|user feedback 2026-05-23 (screen snapshot /resume contact block): `Konstantin Borovik · 20+ years engineering` (resume.astro:14, `text-sm` 0.875rem ≡ 14px) ∧ adjacent secondary prose render below 16px base; pattern repeats across 30 carrier sites ∈ resume.astro — contact secondary prose (L14/L15/L17), tech-stack container `text-sm sm:text-base` (L41), project descriptions (L98/L115/L132/L149), GitHub button labels (L106/L123/L140/L157), engagement-row date ranges (L174/L198/L229/L260/L285/L305), engagement ledes ∧ bullet lists (L177/L181/L201/L205/L232/L236/L263/L288/L308), education degree line (L326), education date range (L324); V50(5) pre-amend "all ≥ body" floor pinned heading floor (H1>H3>H2≈job-title>body) ∧ defined body as `1rem = 16px @ screen` but ⊥ explicit enforcement clause ∀ visible prose itself ∴ Tailwind `text-sm` (0.875rem) ∧ `text-xs` (0.75rem) carriers fell below the body floor undetected; recurrence class ≡ floor-defined-but-not-enforced ∀ /resume prose family — re-fires on any future /resume content addition that defaults to `text-sm` Tailwind body styling; resolved by V50(5)(∆) explicit visible-prose-≥-base clause + T92 sweep|V50
B27|2026-05-23|V3(∆) numeric-form clause introduced by the B25 fix (2026-05-23) enumerated primary-page carrier list ∋ `src/pages/blog/index.astro`; T90's case-insensitive grep pattern `\b(zero|one|...|five|...|hundred|thousand)[ -](second|...|year|...)s?\b` matches `five years` ∈ blog/index.astro:30 (/blog meta lede `...whether a system runs for a quarter or five years.`) ∴ site ∈ T90 pattern ∩ T90 declared scope; T90 "current violations" enumeration listed only the 2 mailpilot `sixty seconds` sites ∧ post-fix verification step `+ grep ∀ primary-page set ≡ ∅` apparently scanned the cited-violations subset rather than re-running the full-scope pattern post-fix ∴ `five years` ∈ blog/index.astro slipped through; surfaced 2026-05-23 by `/sdd:check §V` post-T90 close; same recurrence class as B22 (sweep-§T scope correctly enumerated but audit-step under-executed), extended along V3 prose-vocab axis; chain B17/B19/B20/B22/B27 ≡ sweep-§T scope-declared-but-under-executed recurrence class — V3 already governs the carrier list ∴ ⊥ rule-gap, gap ≡ sweep-§T author discipline (verification step ! re-run full-scope pattern post-fix, ⊥ trust pre-fix "current violations" enumeration as terminal state); resolved by T96 sweep|V3
B28|2026-05-23|`/sdd:check --incremental --all` post-T104 close (2026-05-23) surfaced 3 carrier-drift violations from kb-resume.md canonical fold-in batch (T97-T104, commits 069b9cb..66b7116). (1) `src/pages/llms-full.txt.ts:40-44` `## What I do` narrative block still on prior {01 Back-End Development → 02 Front-End Development → 03 Cloud Infrastructure} ordering — V2(∆) 2026-05-23 amend (`Software Development / DevOps / AI & LLM`) was swept through `src/pages/index.astro` tiles (T99) ∧ `src/pages/resume.astro` tech stack (T98) but llms-full.txt.ts narrative carrier (already declared in V48(b)) ⊥ swept; same recurrence class as B20 (V48(b) carrier ⊥ re-swept on V2 amend), re-firing because the T97-T104 fold-in §T batch ⊥ explicitly enumerate V48(b) carrier despite V48 already listing it ∴ §T author discipline gap, ⊥ rule-gap; (2) `public/kb-resume.md:52-74` `## Projects` order ≡ {mailpilot, pilot-skills, gcp-devops, lab5.ca} but V50(6) mandates canonical {mailpilot, gcp-devops, pilot-skills, lab5.ca}; T97 fold-in step bestowed kb-resume.md as canonical mirror without pre-bestowal audit of kb-resume.md content vs V50(6) project-order pin — same recurrence class as B22 (canonical-tag-without-pre-tag-audit on file becoming canonical mid-flight); (3) `public/kb-resume.md` lab5.ca lede content exceeds V54(b) ≤50-word cap (~70 words) ∧ opens subject-noun (`lab5.ca is my...`) vs V54(a) first-person verb-first; resolved forward by V54(∆) relaxing length cap to ~120 words ∧ admitting subject-noun opening when 2nd clause shifts first-person, rather than compressing kb-resume.md ∵ kb-resume.md ≡ canonical source per §G ∴ kb-resume.md authoring drives shape, V54 mirrors. Chain B17/B19/B20/B22/B27/B28 ≡ carrier-set under-enumeration / canonical-tag without-pre-tag-audit / sweep-§T scope-declared-but-under-executed recurrence class; B28 ≡ this class re-firing through kb-resume.md fold-in batch ∴ rule-set already governs all 3 sub-classes, gap ≡ §T author discipline (kb-resume.md fold-in §T batch ! explicitly enumerate V48 carriers + pre-tag-audit kb-resume.md content vs V50(6) ∧ V54 specs); resolved forward by V54(∆) + T105 + T106 + T107|V48,V50,V54
B29|2026-05-24|LLM-agent usability audit 2026-05-24 surfaced: `src/pages/llms.txt.ts:10` emits Posts entries `- [${title}](${url})` (title + URL only); V48(a) entry shape rule pre-amend required `title ∧ pubDate ∧ description ∧ canonical URL` ∀ both files but llms.txt impl emitted neither pubDate nor description — interpretation gap: rule scope ambiguous between two surfaces, impl diverged on llms.txt ∀ pointer-form brevity heuristic without rule-text codifying the divergence. Latent since T57 close (commit @ llms.txt.ts init); ⊥ surfaced by `/sdd:check` ∵ V48 free-text scope ⊥ pin entry-shape grep recipe — code-vs-spec entry-shape drift ≡ class V48 ⊥ catch mechanically (rule-text governs content shape, no grep pattern enforces). Surfaced 2026-05-24 by external LLM-agent usability audit (out-of-repo). Resolved forward by V47(∆) explicit per-file entry shape (llms.txt entry ∋ {title, description, URL} drops pubDate; llms-full.txt entry ∋ {title, pubDate, description, URL} unchanged) + V48(a)(∆) admit per-file divergence + T114 sweep emitting description ∈ llms.txt Posts entries. Recurrence class ≡ rule-scope-ambiguous-between-N-surfaces — re-fires when shared invariant governs N surfaces ∧ impl diverges silently per surface; mitigated forward by explicit per-surface entry-shape declarations in V47/V48 (∴ next ambiguous rule applying to ≥ 2 surfaces ! declare per-surface shape upfront)|V47,V48

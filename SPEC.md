# SPEC.md — lab5.ca

## §G — goal

personal portfolio + career site @ `lab5.ca` ∀ Konstantin Borovik (Software Engineer | AI & LLM Engineering). pitch ≡ AI & LLM engineering + software development + DevOps & Cloud Platforms; 3 surfaced offerings, each portfolio-anchored on `/projects`. proof-of-capability surfaces ≡ MailPilot demo (`/mailpilot`) + `/projects` GitHub-repo showcase + `/blog` writing. resume content split out 2026-06-04 → `github.com/kborovik/resume` (separate repo w/ own SPEC.md); `/resume` route ∧ `public/kb-resume.md` retired ∴ lab5.ca SPEC ⊥ governs resume content forward — keyword set ∀ tile bodies ∧ Person.knowsAbout JSON-LD ∧ llms.txt narrative sourced from §I `vocab: SEO keyword set` (self-canonical, ⊥ external mirror); SEO posture re-tunes toward AI & LLM Engineering primary keyword density per T135.

## §C — constraints

- stack: Astro 6 static (⊥ SSR adapter), Tailwind v4 via `@theme`, TS 6, pnpm 10, Node ≥22.
- host: Cloudflare Workers static assets; deploy via GH Actions on push → `main`; domain via CF DNS + auto SSL.
- typeface: IBM Plex Mono only across site + social assets.

## §I — interfaces

- route: `/` → home (hero w/ 2-line title block: H1 `Konstantin Borovik` + subtitle `Software Engineer | AI & LLM Engineering` (pipe verbatim); sections: hero, `// what I do` 3-tile, `// claims`, `// proof`, `// résumé`)
- route: `/projects` → skills→GitHub-repo proof grid (4 hand-authored cards: gcp-devops, mailpilot, pilot-skills, lab5.ca; Salesforce card reserved ∀ future)
- route: `/mailpilot` → MailPilot evidence (code-chip CTA, how-it-works, sample Q's, architecture); hidden surface ⊥ ∈ `<Nav />` (reachable via `/projects` card #1 H3 link only — analogous to `/resume` hidden via index `// résumé` button); `/demo` ∧ `/proof` ≡ legacy → CF redirect → `/mailpilot`
- route: `/blog` → post list (sort pubDate desc)
- route: `/blog/<slug>` → post detail
- route: `/blog/<slug>.md` → markdown mirror of `/blog/<slug>` HTML; build-output via `src/pages/blog/[...slug].md.ts` Astro endpoint; content ≡ frontmatter front-matter block (title, description, pubDate, optional updatedDate, optional tags) + raw `entry.body` text; `Content-Type: text/markdown; charset=utf-8`; agent-discovery surface per V59
- route: `/rss.xml` → RSS 2.0 feed via `@astrojs/rss`; ∀ non-draft blog post enumerated, order ≡ pubDate desc; item ∋ {title, link, pubDate, description, categories=tags}; build-output via `src/pages/rss.xml.ts` per V60
- route: `/sitemap-{index,0}.xml` → `@astrojs/sitemap` integration build-output; auto-tracks ∀ Astro route; restored 2026-05-24 per §B.15(∆) LLM-agent usability audit override; declared §I surface ∴ V55 surface-completeness gate satisfied (pre-T58 drop ≡ undeclared-surface class); per V61
- domain: `lab5.ca` ≡ primary; `mailpilot.ca` ≡ alias → serves same site (`wrangler.jsonc` routes)
- analytics: Cloudflare Web Analytics — server-side, automatic via CF dashboard; ⊥ in-repo script tag, ⊥ cookies
- cta: book-call `https://calendar.app.google/cYM3H3TsHsequR587`
- cta: linkedin `https://www.linkedin.com/in/kborovik`
- cta: mailpilot-source `https://github.com/kborovik/mailpilot` — link to MailPilot source; surfaced ∈ `/mailpilot` (under-the-hood) ∧ home (proof-of-capability)
- cta: demo-email ≡ code-chip clipboard-copy widget @ `/mailpilot` — copies `hello@lab5.ca`; ⊥ `<a href="mailto:...">`
- schema: blog frontmatter ≡ {title:str, description:str, pubDate:date, updatedDate?:date, draft?:bool=false, tags?:str[]=[]} per `src/content.config.ts`
- cmd: `make {help,install,dev,preview,build,check,clean,clean-all,deploy,status,wrangler,playwright}`
- env: GH Actions ! set `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`
- asset: `public/{og.png,banner-linkedin.png,favicon.{ico,svg},_headers,_redirects,robots.txt,logo-linkedin.{svg,png},kb-headshot.jpg}`; `public/blog/<slug>/*.webp` ≡ per-post blog assets per V57; `llms.txt` ∧ `llms-full.txt` ≡ build-output via `src/pages/llms.txt.ts` ∧ `src/pages/llms-full.txt.ts` per V48(e), ⊥ ∈ `public/`; `rss.xml` ≡ build-output via `src/pages/rss.xml.ts` (`@astrojs/rss` dep) per V60; `sitemap-{index,0}.xml` ≡ build-output via `@astrojs/sitemap` integration per V61 (restored per §B.15(∆)); `blog/<slug>.md` ≡ build-output via `src/pages/blog/[...slug].md.ts` per V59; `kb-resume.md` retired 2026-06-04 per resume migration (content → `github.com/kborovik/resume`)
- token: palette ≡ GH Primer Light — `gh-canvas`, `gh-canvas-subtle`, `gh-border`, `gh-fg`, `gh-fg-muted`, `gh-fg-subtle`, `gh-green`, `gh-green-hover`, `gh-blue`, `gh-blue-hover`
- token: typeface ≡ IBM Plex Mono (400/500/600/700) via Google Fonts `display=swap`
- vocab: SEO keyword set ≡ self-canonical 3-group H3 set (resume-migration retire of prior `vocab: /resume primary audience` per T134 2026-06-04 — kb-resume.md ## Skills canonical-source mirror dropped, vocab block now self-canonical ∵ kb-resume.md retired) — H3 group set {(1) AI & LLM Engineering, (2) Software Development, (3) DevOps & Cloud Platforms} (group order ≡ tile-set order per V2 — primary AI & LLM Engineering positioning per V4(∆∆∆) 2026-06-04 pivot; prior `DevOps` ∧ `DevOps & Operations` shortened-forms retired forward 2026-06-04 per T132 + T133, historical refs preserve prior literals per documentary convention) + per-group labeled body lines (`Label: token, token, ...` form): (1) AI & LLM Engineering — `AI agents: Pydantic AI, OpenAI Agents SDK, Claude Code, Model Context Protocol (MCP); Retrieval & extraction: Retrieval-Augmented Generation (RAG), structured extraction, Large Language Models (LLM); Foundations: OpenAI, Anthropic, Google Vertex AI`; (2) Software Development — `Languages: Python, TypeScript, SQL; Web & API: Astro, React, FastAPI, REST; Data: PostgreSQL, BigQuery, Redis`; (3) DevOps & Cloud Platforms — `Cloud: Google Cloud Platform (GCP), Microsoft Azure, Cloudflare; Platform Engineering: Google Kubernetes Engine (GKE), Cloud Run, Docker; IaC & CI/CD: Terraform, GitHub Actions, Infrastructure as Code (IaC); Observability: Pydantic Logfire, OpenTelemetry, structured logging`; token form ≡ LinkedIn-skill verbatim w/ qualifier-and-acronym shape — e.g. `Google Cloud Platform (GCP)` ⊥ `Google Cloud`, `Retrieval-Augmented Generation (RAG)` ⊥ `RAG`, `Large Language Models (LLM)` ⊥ `LLM`, `Model Context Protocol (MCP)` ⊥ `MCP`, `Google Kubernetes Engine (GKE)` ⊥ `GKE`, `Infrastructure as Code (IaC)` ⊥ `IaC`; mirror surfaces ≡ (a) `src/pages/index.astro` `// what I do` tile bodies in digest form per V4(b), (b) `src/layouts/Layout.astro` Person.knowsAbout JSON-LD per V61; set framed ∈ KB's actual experience per V83 (⊥ fabricate platform-fluency tokens); AWS tokens dropped 2026-05-21 (⊥ shipped AWS work), OAuth/RBAC dropped (IAM-config-only experience); history ≡ git log + §B

## §V — invariants

V1: pitch ≡ AI & LLM Engineering primary + software development + DevOps & Cloud Platforms; 3 surfaced offerings, each portfolio-anchored on `/projects` GitHub-repo card; cloud platforms ≡ first-class offering (⊥ impl detail per prior V1); AI-narrow framing retired 2026-05-21 (website re-design fold-in); generalist `cloud infrastructure ∧ AI agents for business workflows ∧ web development` framing retired 2026-06-04 via resume migration + SEO re-tune (T134 + T135); pitch-language carrier set ≡ {(1) `src/layouts/Layout.astro` default `description` fallback, (2) per-page meta `description` prop ∀ primary page per V44, (3) hero ∧ claims ∧ capability blurbs ∈ `src/pages/index.astro`, (4) narrative blocks (hero / claims / capability / proof) ∈ `src/pages/llms.txt.ts` ∧ `src/pages/llms-full.txt.ts`} — V1(∆) 2026-06-04 carrier-set amend: prior carrier (4) `engagement ledes ∈ src/pages/resume.astro ∧ public/kb-resume.md per V54` retired ∵ /resume + kb-resume.md deleted per T134; prior carrier (5) `... + resume` narrative segment retired ∵ llms-full.txt.ts Resume block deleted per T134; V1 amend ! sweep ∀ surviving carrier
V2: `// what I do` tile order @ `/` ≡ 01 AI & LLM Engineering → 02 Software Development → 03 DevOps & Cloud Platforms (3 tiles; Title Case per V7 — group names mirror `public/kb-resume.md` Skills section H3 set verbatim per V46(b)(∆) ∧ V50(6)(∆) kb-resume.md canonical fold-in 2026-05-23 ∧ V2(∆∆∆) 2026-06-04 positioning pivot reordering kb-resume.md ## Skills section + tile-set to AI & LLM-first); tile→/projects link retired 2026-05-22 per V46(b)(∆) ∧ V57 affordance audit — tiles content-only blocks post-amend; prior "tiles link to `/projects` ∵ products belong on /projects directly" retired same date ∵ link form ⊥ resting-state affordance ∴ violated V57, top-nav + V46(e) bordered link strip cover /projects nav; prior product-anchor rule (∀ tile ! portfolio-anchored on `/projects` GitHub-repo card: cloud → gcp-devops, AI → mailpilot, web → lab5.ca) retired 2026-05-22 along w/ product-led tile set; prior 3-tile layer-based set {Back-End Development, Front-End Development, Cloud Infrastructure} retired 2026-05-23 via kb-resume.md canonical fold-in ∵ tile-H3 set ! ≡ kb-resume.md Skills group taxonomy (3-way mirror w/ /resume `// tech stack` per V50(6)(∆) ∧ kb-resume.md Skills section); prior 3-tile set {Cloud Infrastructure, AI Agents for Business Workflows, Web Development} retired 2026-05-22 in favor of layer-based identity framing matching "Software Developer | DevOps Engineer" generalist pitch — AI work folds into back-end as capability rather than headlining as separate offering; prior 5-tile ERP-led order superseded 2026-05-21 — `data & reporting` dropped (⊥ portfolio anchor; BigQuery/SQL coverage retained ∈ /resume tech-stack only), `document workflow automation` ∧ `AI automation` merged (MailPilot + future LeadPilot ≡ single product pattern); V2(∆∆) 2026-05-26 H3 label rename amend (user feedback 2026-05-26): tile-3 label `AI & LLM` → `AI & LLM Engineering` per §I vocab Group (3) rename + V46(b)(∆∆) + V50(6)(∆∆∆∆∆) — discipline-noun parallel w/ tile-1 `Software Development` ∧ tile-2 `DevOps`; sweep ∀ carriers per T122; prior `AI & LLM` label retired forward, historical/past-state refs preserve prior literal per §V/§T/§B documentary convention; V2(∆∆∆) 2026-06-04 positioning pivot amend (user direction 2026-06-04): tile order `{01 Software Development → 02 DevOps → 03 AI & LLM Engineering}` → `{01 AI & LLM Engineering → 02 Software Development → 03 DevOps & Operations}` ∧ tile-2 H3 (post-reorder slot) `DevOps` → `DevOps & Operations` per §I vocab Group rename + V4(∆∆∆) + V87(∆); pivot reflects identity shift to AI & LLM Engineering as primary discipline (Software Development + DevOps supporting); sweep ∀ carriers per T132; prior order ∧ prior `DevOps` shortened-form retired forward, historical/past-state refs preserve prior literals per §V/§T/§B documentary convention; V2(∆∆∆∆) 2026-06-04 same-day H3 rename amend (user direction 2026-06-04): tile-3 H3 (post-reorder slot) `DevOps & Operations` → `DevOps & Cloud Platforms` per §I vocab Group (3) rename + V4(∆∆∆∆) + V87(∆∆); sweep ∀ carriers per T133; prior `DevOps & Operations` shortened-form retired forward, historical refs preserve prior literal per §V/§T/§B documentary convention
V3: retired 2026-05-21 — `/expertise` route deleted via website re-design fold-in (capability content folded into `/` `// what I do` 3-tile section per V2 + V46); rule vacuous post-fold (⊥ /expertise H2 to enforce)
V4: `/` ! ∋ (a) hero block w/ H1 `Konstantin Borovik` + subtitle `Software Engineer | AI & LLM Engineering` (pipe verbatim; prior `Software Engineer | DevOps Engineer` retired 2026-06-04 via positioning pivot per V4(∆∆∆) below — subtitle ≡ kb-resume.md L3 verbatim post-sweep; pre-amend `Software Developer | DevOps Engineer` retired 2026-05-23 via kb-resume.md canonical fold-in) + visible text `Toronto, Ontario, Canada` ∈ `<address>` semantic tag + headshot; stack-token sub-line `Python · Google Cloud · LLM agents · CI/CD` dropped 2026-05-22 ∵ prior V46(c) `// tech stack` 4-group covered same tokens w/ grouped structure ∴ hero sub-line ≡ redundant scan signal (V46(c) itself subsequently DROPPED 2026-05-22 per below); social-card surfaces (`assets-src/og.html`, `assets-src/linkedin-banner.html`) ⊥ V46 scope — separate surface, ⊥ scroll-down stack ∴ retain stack-token line, (b) `// what I do` 3-tile services grid per V2; tile H3 set ≡ `§I vocab: SEO keyword set` H3 group set verbatim (current: {01 AI & LLM Engineering, 02 Software Development, 03 DevOps & Cloud Platforms} per V2(∆∆∆∆) + V4(∆∆∆∆) 2026-06-04 same-day H3 rename amend; prior set retired forward, historical refs preserve prior literal); ∀ tile body ≡ `&middot;`-separated keyword stream digest of corresponding §I vocab group body lines, hand-tuned scan form ∀ hiring-manager grep — shorthand admitted ∀ tile-body density (e.g. `Google Cloud`, `Azure`, `IaC`, `RAG`, `MCP`, `GKE`) ∵ canonical LinkedIn-skill expansion lives in §I vocab body lines; carrier ≡ `src/pages/index.astro`; tile-link wrapper retired 2026-05-22 per V57 affordance audit — tiles content-only blocks; V4(b)(∆∆∆) 2026-06-04 source amend per T134 resume migration: tile-body source `kb-resume.md group body lines` → `§I vocab: SEO keyword set body lines` ∵ kb-resume.md retired, (c) DROPPED 2026-05-22 — prior `// tech stack` 4-group section {Python & TypeScript, Google Cloud & Azure, AI Automation, CI/CD & Terraform} retired ∵ V46(b) keyword-stream tile bodies carry same token surface ∧ /resume per V50(6) carries 9-subgroup depth ∴ home-page mid-tier redundant, (d) `// claims` dash-bullet section listing 4 falsifiable facts ∈ uniform `<phrase> &mdash; <comma-list>` shape ∀ one-line viewport fit: {20+ years shipping production software (data centers, payments, real estate); AI agents in production (LLMs, RAG, structured extraction); solo delivery, end-to-end (back-end, IaC, CI/CD, deploy, observability); Toronto-based, long-term contract (embedded, full product cycle ownership)}; content refined 2026-05-22 — uniform shape ∀ scanability, drops business-automation horizontal (3 industry verticals only), drops Python/TS/SQL daily-stack bullet ∵ V46(b) tech-grid covers token surface ∴ slot reassigned to AI-agents-in-production claim, drops on-call from ownership scope per V56 dev-not-SRE positioning, "owned end-to-end" → "full product cycle ownership"; prior content shifted to generalist identity 2026-05-22 — drops AI-narrow framing, drops MailPilot product-name claim (V46(e) carries live demo as evidence w/ stronger framing), (e) `// proof of skills` ≡ featured AI-agent call-out (bordered subtle-bg box → `/mailpilot`, title `Let AI handle your repeat email questions →`, body describes function in hiring-manager vocab: production LLM agent, RAG, knowledge base, sub-60s reply, send a question to `hello@lab5.ca` — prior `demo@lab5.ca` retired 2026-05-25 per V40(∆∆∆) Gmail account migration) + compact bordered icon-link strip → `/projects` ∧ `/blog` ∧ GitHub; LinkedIn moved to V46(f) per identity/CV semantic grouping; heading renamed `// proof` → `// proof of skills` 2026-05-22 ∀ non-technical-reader legibility; tile title renamed `Try the live AI email agent →` → `Let AI handle your repeat email questions →` 2026-05-22 — names buyer pain (repeat-email work) ∧ relief (AI delegation), retains "AI" keyword token ∀ scan; title rendered ∈ `text-gh-green` accent color per V57(c) visible-affordance form ∵ bordered card-shape + corner arrow alone ⊥ trigger card-as-link reader expectation w/o accent-colored title (user feedback 2026-05-22; `text-gh-blue` reserved for V14 kicker per V57); prior 5-destination flat list (`/projects` ∧ `/blog` ∧ `/proof` ∧ LinkedIn ∧ GitHub) retired 2026-05-22 — featured call-out elevates strongest single evidence (live demo) above flat link equality; (f) retired 2026-06-04 per T134 resume migration — prior `// résumé` bordered button cluster → `/resume` ∧ LinkedIn profile dropped ∵ /resume route deleted (resume content → `github.com/kborovik/resume` separate repo); `/about` route deleted 2026-05-21 — (a)+(d)+(e) content owner re-targeted from prior /about to `/` via website re-design fold-in; V46(b)(∆∆) 2026-05-26 H3 label rename amend (user feedback 2026-05-26): tile-3 H3 `AI & LLM` → `AI & LLM Engineering` (∧ keyword-stream tile-body line label `AI & LLM tile:` → `AI & LLM Engineering tile:`) per §I vocab Group (3) rename + V2(∆∆) + V50(6)(∆∆∆∆∆) — discipline-noun parallel w/ tiles 1∧2; sweep ∀ carriers per T122; prior `AI & LLM` retired forward, historical refs preserve prior literal; V4(∆∆∆) 2026-06-04 positioning pivot amend (user direction 2026-06-04): (a) hero subtitle `Software Engineer | DevOps Engineer` → `Software Engineer | AI & LLM Engineering` (pipe verbatim) per §G + §I + V81(∆) + kb-resume.md L3 canonical-source rule (kb-resume.md L3 swept same amend) — subtitle ≡ kb-resume.md L3 verbatim post-sweep; prior `Software Engineer | DevOps Engineer` retired forward, historical refs preserve prior literal per documentary convention; (b) tile H3 set reordered + DevOps tile renamed per V2(∆∆∆) — current: {01 AI & LLM Engineering, 02 Software Development, 03 DevOps & Operations}; sweep ∀ carriers per T132; V4(∆∆∆∆) 2026-06-04 same-day H3 rename amend (user direction 2026-06-04): tile-3 H3 (post-reorder slot) `DevOps & Operations` → `DevOps & Cloud Platforms` per §I vocab Group (3) rename + V2(∆∆∆∆) + V87(∆∆); sweep ∀ carriers per T133; prior `DevOps & Operations` retired forward, historical refs preserve prior literal per §V/§T/§B documentary convention
V5: footer (`src/components/Footer.astro`) composition ≡ (a) brand wordmark `Konstantin Borovik` → `/` left (`text-sm font-bold text-gh-fg`); (b) nav link set ≡ {Home → `/`, Projects → `/projects`, Blog → `/blog`} right — mirrors `<Nav />` `links[]` minus Book-a-Call per V4 ("⊥ page-footer CTA repeat"); (c) copyright line `© <year> Konstantin Borovik. All rights reserved.` below (year ≡ `new Date().getFullYear()` runtime, ∴ year auto-rolls ∀ build); (d) bg ≡ `bg-gh-canvas-subtle` per V13 contained-surface clause. Footer-nav link affordance per V57 nav-surface exemption (hover-only color shift admitted ∀ link row). Hidden surfaces (`/resume`, `/mailpilot`) ⊥ admitted ∈ footer nav ∵ outside `<Nav />` `links[]` set per §I — hidden-by-Nav ≡ hidden-by-Footer. New route ∈ `<Nav />` `links[]` set ! pair w/ footer-nav update ∈ same commit per (b) mirror clause; recurrence-class ≡ symmetric route-churn carrier per V55 surface-completeness pattern (Nav ∪ Footer both governed).
V11: voice ≡ factual + tech vocab + numeric form ∀ measurable quantity (duration, count, percentage, year span); ⊥ aspirational verbs, ⊥ persona breakouts, ⊥ marketing flourish, ⊥ written-out numbers (e.g. `sixty seconds`, `twenty percent`, `hundred-page report`) ∈ primary-page copy (`src/pages/{index,projects,mailpilot,resume}.astro`, `src/pages/blog/index.astro`, narrative blocks ∈ `src/pages/llms{,-full}.txt.ts`, Layout default `description` fallback); blog body (`src/content/blog/**/*.md`) exempt per V28 first-person narrative voice — written-out form admitted ∀ narrative time references (`three weeks later`, `six months`, `five years`)
V12: heading ⊥ trailing `.`
V13: page H1 ∧ blog title casing ≡ sentence case
V14: tile title ∧ step title casing ≡ Title Case (rebrand 2026-05-22 — `cloud infrastructure` → `Cloud Infrastructure`); section kicker `// xxx` retained lowercase per V14 (different element class — visual marker, ⊥ content header); H1 sentence case per V6 unchanged
V15: section kicker ≡ `// section name`; class ≡ `text-base font-bold uppercase tracking-[0.2em] text-gh-blue`; V14(∆) 2026-05-24 LLM-agent usability amend: DOM text content of kicker element ! ≡ section name only (e.g. `what I do`); literal `// ` prefix ⊥ ∈ DOM text — injected via CSS `::before { content: '// '; }` on element bearing `.section-kicker` class (decl ∈ `src/styles/global.css`); rationale ≡ agent parsers (JSON-LD ingesters, llms.txt consumers, RSS readers, generic HTML→markdown crawlers) read heading text without `// ` comment-marker noise — `//` is C-family inline-comment syntax ∴ may be misparsed as comment opener by naive tooling. Visual rendering ≡ pre-amend (CSS pseudo-element renders `// xxx` identically @ same class string). Carrier class augmentation ≡ append `section-kicker` to existing Tailwind utility string ∀ kicker carrier site (current set: index.astro:41/91/116/166, projects.astro:56, mailpilot.astro:58 + internal kickers, resume.astro:39/83/166/330, blog/index.astro:23, blog/[...slug].astro:43); orthogonal to V49(b) heading-element rule (V49(b) ≡ `<h2>` element semantic, V14(∆) ≡ DOM text semantic — different axes).
V16: numbered hairline ≡ `<NN> ─────` above tiles, mono `text-xs text-gh-fg-muted`; numbering continues across grids when narrative ≡ one
V17: bullet list ≡ dash-prefix; ⊥ check/x SVG icons
V18: claims ∧ capability tiles ∧ llms-full.txt narrative blocks ! ⊥ ∋ on-call ∨ pager ∨ incident-response ∨ SRE ∨ oncall-engineer vocabulary — KB self-positions as `Software Engineer | AI & LLM Engineering` per V4(a) hero subtitle (positioning pivot 2026-06-04 per V4(∆∆∆); prior `Software Engineer | DevOps Engineer per V50(1) H1 ∧ V46(a) hero subtitle` retired forward — `V50(1)→V81(1)` ∧ `V46(a)→V4(a)` per `.claude/spec-renumber-map.json` post-T131 sweep gap, ∧ subtitle literal swept per T132 positioning pivot; V81 retired 2026-06-04 per T134 resume migration; historical refs preserve prior literals per documentary convention), ⊥ SRE/oncall-engineer adjacent role despite IAM-config ∧ on-call-rotation experience present; tile-3 H3 label `DevOps & Cloud Platforms` per V2(∆∆∆∆) — "Cloud Platforms" admitted as platform-engineering noun ∈ tile label (⊥ SRE-adjacent ∴ ⊥ trigger role-shift vocabulary class); prior `DevOps & Operations` label retired forward 2026-06-04 same-day per T133 rename, historical refs preserve prior literal; V18(∆) 2026-06-04 carrier-set amend per T134 resume migration: prior carrier set `{... /resume ∧ kb-resume.md engagement-row + skills mirror, llms-full.txt resume block}` retired ∵ /resume + kb-resume.md deleted ∧ llms-full.txt Resume block dropped per V72(e)(∆); forward carrier set ≡ {`src/pages/index.astro` //claims dash bullets ∧ //what-I-do tile bodies, `src/pages/llms-full.txt.ts` claims ∧ capability blocks per V72}; dev-adjacent ops tokens ∈-bounds: `deploy`, `observability`, `IaC`, `CI/CD`, `infrastructure-as-code`, `Terraform`, `Docker`, `Kubernetes`; role-shifting tokens ⊥ admitted: `on-call`, `pager`, `paging`, `incident-response`, `incident response`, `SRE`, `site reliability`, `oncall-engineer`, `on-call engineer`; orthogonal to V83 (V83 ≡ ⊥ fabricate token coverage on absent capability; V18 ≡ ⊥ self-position into adjacent role despite present capability)
V21: ≤ 1 prominent CTA per page; Book-a-Call ∈ nav only (`cta-pulse`), ⊥ page-footer repeat
V22: link href ≡ — internal: absolute path `/...`, ⊥ trailing `/`, ⊥ relative; cross-page anchor: `/path#frag`; intra-page anchor: `#frag`; external: full `https://...`; mailto: `mailto:<addr>`. ∈ `.astro`: external `<a>` ! `target="_blank"` ∧ `rel="noopener noreferrer"`; mailto ⊥ `target="_blank"`. ∈ blog `.md`: plain markdown `[text](url)` only — `rehype-external-links` (`astro.config.mjs`) auto-inserts attrs @ build ∴ ⊥ author raw `<a>`; external link anchor text ≡ plain inline prose, ⊥ `**bold**` wrap ∨ `` `code` `` wrap ∈ `[...]`. V35(∆) 2026-06-01 partial-wrap subclass: code-fragment ∨ bold ∨ italic wrap on sub-token ∈ otherwise-plain anchor-text (e.g. `[`token` description](url)`) ≡ violation per same plain-prose clause — sub-token markup wrap inside `[...]` falls under same prohibition as full-wrap; remediation ≡ promote sub-token to plain prose ∨ relocate markup outside anchor (`the [semantic conventions for `gen_ai.*`](url)` ⊥ `[`gen_ai.*` semantic conventions](url)`) per §B.32.
V23: ∀ primary page (`/`, `/projects`, `/blog`, `/blog/<slug>`) ! satisfy WCAG 2.1 Level AA baseline — (a) text contrast ≥ 4.5:1 normal-size, ≥ 3:1 large (≥18pt ∨ ≥14pt bold); `gh-fg-subtle` (#818b98) ≡ large-text-only — body-size sites ! use `gh-fg-muted` (#424a53); (b) heading order sequentially-descending — `<h3>` ⊥ appear w/o prior `<h2>` in scope; V14 section kicker `// xxx` ≡ `<h2>` element when section ⊃ tile `<h3>`s, V14 visual class string (`text-sm font-bold uppercase tracking-[0.2em] text-gh-blue`) retained; (c) ∀ interactive element ! ∋ accessible name — links ∋ visible text ∨ `aria-label`; brand-logo link `<a href="/">` ∈ `Nav.astro` ! `aria-label="Home"`; (d) exactly one `<main>` landmark per page — `Layout.astro` ! wrap `<slot />` ∈ `<main>` between `<Nav />` ∧ `<Footer />`; verification ≡ Lighthouse a11y category ≥ 0.95 ∀ primary page — manual periodic audit (⊥ CI-gated; matches T28 verification pattern, promote to CI only if drift recurs); V49(∆) 2026-05-25 /sdd:check verdict mapping: V49 verification clause "manual periodic audit (⊥ CI-gated)" ≡ HOLD-equivalent ∀ /sdd:check classification ∴ /sdd:check ! emit HOLD (⊥ UNVERIFIABLE) absent recorded a11y regression ∈ §B. UNVERIFIABLE verdict reserved ∀ invariants where audit infra should mechanically exist but doesn't, ⊥ ∀ invariants explicitly opting out of CI gating per their own body clause. Closes /sdd:check stale-memo class — V49 UNVERIFIABLE froze memo advance despite invariant's explicit manual-periodic design (V49 ≢ "audit-infra-gap"; V49 ≡ "manual-periodic-by-design"). Promote-to-CI trigger (per pre-amend "if drift recurs" clause) unchanged ∴ a11y regression ∈ §B → V49 reclassifies VIOLATE until §T sweep closes ∧ §B-recorded recurrence-class triggers CI promotion review.
V24: ∀ clickable surface (`<a href>` element, including card-as-link wrappers ∧ button-shaped links) @ lab5.ca user-facing pages ! signal clickability pre-hover via ≥ 1 admitted affordance form — (a) inline text link: underline + `text-gh-green` accent; (b) heading/title link: `text-gh-green` accent + resting underline (prior `w/ ∨ w/o underline` optionality dropped 2026-05-22 ∵ no-underline branch ⊥ trigger "this is a link" reader-expectation when visually adjacent to body emphasis bold/`<strong>`, per user feedback 2026-05-22 image-compare /projects mailpilot H3 vs /blog body inline link → §B.23); (c) card-as-link: bordered card-shape + (i) `text-gh-green` accent-colored title + resting underline ∨ (ii) visible corner chevron `→` (accent-without-underline branch dropped 2026-05-22 per (b)(∆) consistency → §B.23; border alone ⊥ sufficient per prior 2026-05-22 user feedback); (d) button-shaped link: bordered + icon + label text (current /projects GitHub-link button pattern). Underline impl ∀ Tailwind carriers ≡ `underline underline-offset-[3px]` — 3px offset matches `.prose a` ∈ `src/styles/global.css:192` ∀ visual parity body-prose ↔ heading/title link sites. `text-gh-blue` reserved for V14 section-kicker scope ∴ ⊥ admitted ∀ link affordance ∵ visual collision w/ uppercase-tracked kicker class (user feedback 2026-05-22). Nav-surface exemption: top-nav links (`src/components/Nav.astro`) ∧ footer-nav links (`src/components/Footer.astro`) operate under nav-position convention (horizontal nav bar + grouped labels ≡ universally-recognized nav affordance) ∴ per-link resting accent ⊥ required, hover-only color shift admitted ∀ nav-surface link rows only. Hover-only signals (cursor change, color shift on hover, bg shift on hover) ⊥ sufficient ∵ touch devices ⊥ hover ∧ scan-first reader ⊥ test-by-hover. Uniformity within repeating group ! enforced — ∀ card H3 link ∈ single grid (e.g. /projects card grid) carry same affordance form ∨ none do; mixed treatment within one grid ≡ violation. Carrier set ≡ {`src/pages/index.astro` //what-I-do tiles (post-V46(b)(∆) ≡ content-only, ⊥ links), //proof-of-skills tile, //résumé bordered buttons; `src/pages/projects.astro` card H3 links (mailpilot only currently) ∧ GitHub-repo buttons; `src/pages/blog/index.astro` post entry links; `src/components/Nav.astro` ∧ `src/components/Footer.astro` nav links; `.prose` inline anchors @ blog body via `src/styles/global.css:191`}. Orthogonal to V49(c) (V49(c) ≡ accessible-name presence ∀ screen reader; V57 ≡ visible-affordance presence ∀ sighted scan — different reader, different signal).
V31: typeface ≡ IBM Plex Mono only; `@theme` declares `--font-mono` only, ⊥ `font-sans`, ⊥ `font-serif`
V32: hierarchy ∈ {weight, size, color}; ⊥ family swap
V33: html base size ≡ 16px; body weight ≡ 400
V34: palette ≡ GH Primer Light `gh-*` tokens only (per §I); ∀ `--color-gh-*` decl ∈ `src/styles/global.css` @theme ≡ §I palette set ∧ ! ≥ 1 ref ∈ `src/`
V35: ⊥ alternating section bg; `bg-gh-canvas-subtle` ≡ contained surfaces only (cards, footer, code chips)
V36: logo ≡ 4-color pinwheel — `#0969da` ∧ `#1f883d` ∧ `#f9c513` ∧ `#cf222e`
V37: Tailwind v4 config ≡ `@theme` ∈ `src/styles/global.css`; ⊥ `tailwind.config.js`
V41: build ≡ static output; ⊥ SSR adapter, ⊥ API routes
V42: Node version `>=22` per `package.json` engines ∧ `.nvmrc`
V43: package manager ≡ pnpm
V44: host ≡ Cloudflare Workers; push → `main` ⇒ GH Actions deploy
V45: pre-commit (local discipline) ! `make check` ∧ `make build` pass; enforcement ∈ V31
V46: CI on PR → `main` ≡ `astro check` + `astro build`
V47: deploy on push → `main` ≡ check + build + `wrangler deploy`
V48: `.github/workflows/deploy.yml` ! reference `${{ secrets.CLOUDFLARE_API_TOKEN }}` ∧ `${{ secrets.CLOUDFLARE_ACCOUNT_ID }}` ∀ wrangler step; actual secret population ∈ GH repo settings ≡ out-of-scope ∀ static audit (deploy-fail-on-missing ≡ natural backstop)
V49: Playwright screenshot ⊥ SVG @ `file://` URL (timeout) — LATENT pre-trigger (`playwright.config.*` ∨ `*.spec.ts` ⊥ ∈ repo; Playwright ⊥ project dep, on-demand via MCP only); discipline-only rule ∈ CLAUDE.md
V51: blog post path ≡ `src/content/blog/<slug>.md` flat; filename ⊥ `.md` ≡ URL slug → rename ≡ URL break
V52: blog frontmatter ! match `src/content.config.ts` schema; undeclared fields (`author`, `image`, `canonical`) → `astro check` fails
V53: `src/content/blog/` ⊥ non-post `.md`; glob `**/*.md` loads ∀ → schema-fail breaks build
V54: blog body ⊥ `# Title`, ⊥ byline, ⊥ footer, ⊥ back-link → `[...slug].astro` renders these
V55: blog headings ≡ `##`/`###` sentence case, ⊥ trailing `.`
V56: blog voice ≡ first-person singular, factual + descriptive; audience ≡ technical reader (developer/engineer familiar w/ stacks, APIs, LLM tooling) ∴ ⊥ over-explain primitives, ⊥ define common acronyms (LLM, API, JSON, etc.); register ≡ simple ∧ clear — jargon ≡ precision shorthand, ⊥ decoration; sentence shape per `core:steno` — short, verb-first, one idea per sentence; ⊥ idiom, ⊥ marketing flourish (inherits §V.11); V28(∆) 2026-06-01 admits 2nd-person tutorial register (`you have an ID`, `you run`, `you open the file`) ∀ step-by-step workflow posts (e.g. SQL-query sequence, debugging procedure, build pipeline) where instructional 2nd-person ≡ genre convention; first-person singular preferred ∀ narrative posts (post-mortem, lessons-learned, observation logs); register choice per-post ≡ author judgment based on genre dominance — mixed register admitted ∀ tutorial-w/-narrative-asides (2nd-person procedure body + 1st-person observation interjections), pure-narrative posts ⊥ admit 2nd-person register-shift mid-body per §B.33
V57: blog markdown ⊥ raw HTML (beyond entities ∧ `<br>`), ⊥ footnotes — `.prose` ⊥ support; images admitted via `![alt](src)` markdown form, src ∈ `public/blog/<slug>/` per-post asset dir (URL form `/blog/<slug>/<file>.webp` per V29(∆) WebP-only clause), alt ! mandatory per V23(c) accessible-name rule; tables admitted via standard pipe-syntax; carriers ∈ `src/styles/global.css` — `.prose img` (max-width 100%, height auto, bordered + rounded matching `.prose pre` ∧ `.prose blockquote` aesthetic, `cursor: zoom-in` per V29(∆) click-affordance clause), `.prose table` (full-width, border-separate + spacing 0 ∵ border-radius compat, gh-border 1px outer + per-row bottom inner, gh-canvas-subtle thead bg, 0.5em × 0.85em cell padding); V29(∆) 2026-06-01 blog-image WebP-only + click-to-enlarge fold-in: (a) blog image format ! ≡ WebP — src ∈ `public/blog/<slug>/<file>.webp`; `.png` ∧ `.jpg` ∧ `.jpeg` ⊥ admitted (smaller bytes ∀ screen-density screenshots, modern-browser support universal post-2020); pre-amend pinned `.png` URL-form example retired forward (pinned-example-in-rule-body class per §B.20/§B.21/§B.26/§B.35) — main V57 body URL-form example updated to `.webp` same amend; (b) blog image click-to-enlarge viewer ≡ native HTML5 `<dialog>` overlay invoked on `.prose img` click — carriers ≡ (1) `src/pages/blog/[...slug].astro` (`<dialog>` element + client-side `<script>` attaching click handler on `.prose img`; on click → set dialog inner `<img>` src to clicked img src, `dialog.showModal()`; close on Esc key ∨ backdrop click ∨ explicit close-control), (2) `src/styles/global.css` `.prose img { cursor: zoom-in }` rule signalling click affordance ∀ sighted readers; ⊥ JS dep, ⊥ external lightbox library — native HTML5 dialog element only ∵ minimal-dep voice per §V.11 + framework-docs aesthetic per CLAUDE.md voice. Orthogonal to V57 main raw-HTML prohibition — markdown body stays `![alt](src)` form ∴ ⊥ raw HTML ∈ blog `.md`; viewer chrome (`<dialog>` element + `<script>`) lives ∈ Astro template (compiled HTML), ⊥ ∈ markdown content ∴ V57 main clause unaffected.
V58: LinkedIn feed post ≡ `linkedin/<slug>.txt` ∀ matching `src/content/blog/<slug>.md`; purpose ≡ promotional surface ∀ canonical blog write-up @ `lab5.ca/blog/<slug>/` — post ⊥ standalone substitute, ⊥ replace blog body, ⊥ self-contained read ∴ earns reader → write-up; objective ≡ short ∧ simple ∧ clear — reader ! grasp what blog post is about; format ≡ plain text + line breaks (`\n` → `<br>`, `\n\n` → paragraph; LinkedIn ⊥ render markdown); body ⊥ `word.tld` pattern — LinkedIn auto-linkifies (`SPEC.md` → `<a href="http://SPEC.md">` broken-scheme; `lab5.ca`, `github.com` → `lnkd.in/<hash>`), quotes/backticks ⊥ escape; rephrase ∨ drop TLD; body ⊥ markdown syntax (`**bold**`, `` `code` ``, `[text](url)`, `# heading`) → LinkedIn renders literal ∨ strips; dash-prefix bullets `- ` ≡ permitted per §V.17 (LinkedIn renders literal ≡ benign); em-dash ≡ real `—` (U+2014), ⊥ `--` literal (LinkedIn ⊥ auto-convert); voice ≡ §V.11; structure ≡ line 1 `Blog: <post-headline>` (mirrors `src/content/blog/<slug>.md` frontmatter `title` verbatim ∴ reader sees title-then-summary structure ∧ grasps subject pre-body) → body (short, simple, clear summary) → trailing `Read the full post: https://lab5.ca/blog/<slug>/` line (authored canonical; LinkedIn rewrites displayed URL → `lnkd.in/<hash>` post-publish ≡ expected, ⊥ authoring concern); URL count ≡ exactly 1 (the `Read the full post:` line) — LinkedIn auto-cards the LAST URL ∴ ≥ 2 URLs → non-canonical URL grabs card preview, suppresses canonical post link; ⊥ trailing `<Label>: <url>` rows beyond `Read the full post:` (⊥ `Skill:`, ⊥ `Plugin:`, ⊥ `Benchmark:`, etc.); `#hashtag` ∧ `@mention` ≡ LinkedIn syntax (sparingly). `linkedin/resume.txt` ≡ profile content, ⊥ scope.
V59: blog `title` (frontmatter @ `src/content/blog/<slug>.md`) ∧ mirrored linkedin `Blog:` line per V36 ∧ `description` frontmatter ! self-disambiguate ∧ literally name technical subject — (a) bare domain noun w/ ≥ 2 plausible dimension readings (e.g. `compression`, `drift`, `decoder`) ! qualifier specifying dimension; (b) rhetorical ∨ positional framing (e.g. `Where X stops and Y starts`, `X is dead`, `The future of Y`) ⊥ admitted ≡ standalone title — ! literal technical noun-phrase naming subject (what + how) ∴ keyword surface ⊥ rely on metaphor decode; reader landing cold via search ∨ social-card share ! grasp technical subject pre-body; slug ⊥ rename to fix (V23 URL-break rule).
V60: blog body ≡ skim-first + progressive disclosure — `##` heading sequence ! advance topical spine ∴ reader scanning headings alone grasps post arc; ∀ section ! lede sentence stating claim before code ∨ deep prose; ¶ ≤ ~4 lines (visual breath, ⊥ wall-of-text); detail ordered shallow → deep (1-line claim → bullet expansion → code block ∨ edge-case prose) ∴ reader stopping @ any depth retains coherent take-away
V61: ∀ page ! emit `<script type="application/ld+json">` ∈ `Layout.astro` `<head>` ∋ `@graph` w/ (1) `Person` @ `@id "https://lab5.ca/#person"` ∋ `name`, `alternateName "K. Borovik"`, `url`, `image`, `jobTitle`, `address` (`addressLocality "Toronto"` ∧ `addressRegion "ON"` ∧ `addressCountry "CA"`), `sameAs`, `knowsAbout`, `worksFor`, `potentialAction` (V40(∆) 2026-05-24 LLM-agent usability amend: Person ! additionally ∋ `potentialAction: {@type:'AskAction', name:'Ask MailPilot live agent', description:'Send a question to hello@lab5.ca; live AI agent reply returns under 60 seconds.', target:{@type:'EntryPoint', urlTemplate:'mailto:hello@lab5.ca', actionPlatform:['http://schema.org/DesktopWebPlatform','http://schema.org/MobileWebPlatform']}}` (V40(∆∆) 2026-05-24 same-day correction: prior `@type:'EmailAction'` retired per §B.30 — `EmailAction` ⊥ ∈ schema.org recognized vocabulary, flagged by validator.schema.org; `AskAction` ≡ canonical CommunicateAction subclass for "act of posing a question to an entity" ∴ semantic match ∀ MailPilot framing "ask the agent, get a reply") (V40(∆∆∆) 2026-05-25 Gmail account migration: prior `demo@lab5.ca` → `hello@lab5.ca` ∀ `description` ∧ `target.urlTemplate` — `hello@` ≡ canonical customer-comms alias on new lab5.ca Gmail account, `demo@` retired w/ prior account decom; mandate literal updated ∈ V40(∆) text per current-mandate authoring discipline ∴ future readers see live endpoint, ⊥ stale literal) — declares machine-callable email endpoint per §G `MailPilot ≡ proof-of-capability` framing ∴ Person advertises endpoint to LLM agents via Schema.org Action vocabulary; orthogonal to §I `cta.demo-email` visible code-chip widget (JSON-LD ≡ machine surface, code-chip ≡ visual surface) ∧ orthogonal to V35 `mailto:` href-form rule (V35 governs visible `<a href="mailto:">`, V40 governs JSON-LD machine-action declaration)); (2) `WebSite` @ `@id "https://lab5.ca/#website"` ∋ `url`, `name`, `publisher → Person.@id`, `inLanguage "en-CA"`; (3) page-specific node ∈ {`WebPage`,`CollectionPage`,`BlogPosting`} (selected via Layout `schemaType` prop) ref Person via `@id` — `ProfilePage` retired 2026-06-04 per T134 resume migration ∵ /resume route deleted (sole ProfilePage carrier); (4) `BreadcrumbList` ∀ non-root page; (5) `Person.knowsAbout` keyword set source ≡ `§I vocab: SEO keyword set` (per T134 2026-06-04 — prior `kb-resume.md ## Skills` source retired via resume migration; §I vocab self-canonical post-migration)
V62: `Person.image` ! ref stable URL `https://lab5.ca/kb-headshot.jpg` (file ∈ `public/`); ⊥ astro:assets-hashed URL; ⊥ `og.png`
V63: `Person.sameAs` ≡ exactly `["https://www.linkedin.com/in/kborovik", "https://github.com/kborovik"]` — ⊥ X, ⊥ Bluesky, ⊥ Mastodon, ⊥ calendar URL, ⊥ MailPilot repo (LinkedIn ∧ GitHub ≡ primary maintained channels; X ∧ Bluesky dropped 2026-05-21 ∵ unmaintained ∴ ⊥ entity-binding signal — avoid sameAs entity-dilution); LinkedIn retained ∀ entity binding despite gated content (canonical surface @ `lab5.ca` per V47 pointer line)
V64: ∀ blog post → `BlogPosting` JSON-LD ! ∋ `author → Person.@id`, `datePublished ≡ frontmatter pubDate`, `dateModified ≡ frontmatter updatedDate` (if present), `headline ≡ frontmatter title`, `description ≡ frontmatter description`, `keywords ≡ frontmatter tags`
V65: ∀ primary page (`/`, `/projects`, `/blog`, `/blog/<slug>`) ! pass explicit `description` prop → `Layout.astro`; Layout default-fallback (`Layout.astro:13`) ≡ last-resort safety net only; /expertise + /about removed 2026-05-21 (route deletion via website re-design fold-in), /projects added; /resume ∧ /mailpilot ⊥ ∈ primary list ∵ hidden surface per §I (`/proof` renamed `/mailpilot` 2026-05-22 ∧ dropped from primary list + Nav menu — Nav `links[]` retired `/proof` entry ∴ menu set ≡ {Home, Projects, Blog}; `/mailpilot` reachable via `/projects` card #1 H3 link, ⊥ via primary nav)
V66: `Layout.astro` `<head>` ! emit `<link rel="alternate" hreflang="en-ca" href={canonical}>` ∧ `<link rel="alternate" hreflang="x-default" href={canonical}>`; existing `og:locale "en_CA"` stays; V45(∆) 2026-05-24 LLM-agent usability amend: `<head>` additionally ! emit (a) `<link rel="alternate" type="text/markdown" href="<page>.md">` ∀ HTML page w/ markdown mirror — current mirror set ≡ {`/blog/<slug>` → `/blog/<slug>.md` per V59}; per-page emission via Layout `markdownAlternate` prop (string href); ⊥ emitted ∀ pages w/o mirror; V66(a)(∆) 2026-06-04 per T134 resume migration: prior mirror entry `/resume → /kb-resume.md` retired ∵ /resume route ∧ kb-resume.md deleted (resume content → `github.com/kborovik/resume` separate repo); (b) `<link rel="alternate" type="application/rss+xml" title="lab5.ca blog" href="/rss.xml">` site-wide (∀ Layout invocation) per V60 RSS feed surface — discovery affordance ∀ feed readers + agents monitoring for new writing; rationale ≡ markdown mirror + RSS feed otherwise undiscoverable from HTML alone (agent ! guess URL pattern absent `<link rel="alternate">` hint).
V71: `public/llms.txt` ! ∋ pointer-form summary per llms.txt convention (H1 `Konstantin Borovik`, blockquote intro, `## Pages` link list ∋ `/projects` entry, `## Contact` link list ∋ `https://github.com/kborovik/resume` external resume repo, `## Notes` section ∋ gated-LinkedIn pointer line `LinkedIn (linkedin.com/in/kborovik) requires login; canonical identity ∧ writing @ lab5.ca` — routes agents off doomed gated-fetch); `public/llms-full.txt` ! ∋ prose dump ≡ `/` hero + claims + `// what I do` capabilities + `/projects` card grid + `/mailpilot` description + blog post titles + descriptions (single-file machine-readable corpus ∀ agents that ⊥ multi-page crawl); both files ! carry gated-LinkedIn pointer line; V71(∆) 2026-06-04 per T134 resume migration: prior `## Pages` `/resume` entry ∧ prior `## Contact` `https://lab5.ca/kb-resume.md` entry retired ∵ /resume route ∧ kb-resume.md deleted; new `## Contact` entry `https://github.com/kborovik/resume` added pointing to external resume repo per §G migration; llms-full.txt `/resume skills ∧ engagements ∧ projects` block retired same amend ∵ source kb-resume.md retired; `/about` + `/expertise` content refs dropped 2026-05-21 via website re-design fold-in (routes deleted); plain-text resume ref retired 2026-05-23 via kb-resume.md canonical fold-in — `kb-resume.txt` retired then `kb-resume.md` retired 2026-06-04 per T134; V47(∆) 2026-05-24 LLM-agent usability amend: (a) `## Posts` link-list entry shape ! ∋ {title, frontmatter description, canonical URL `https://lab5.ca/blog/<slug>/`} rendered as `- [title](url): description` per llms.txt link-list+description form — pre-amend entry shape `- [title](url)` (title + URL only) retired ∵ agents ranking page relevance ⊥ infer subject from title alone; pubDate dropped from llms.txt entries ∵ pointer-form prioritizes scannability ∧ pubDate ≡ derivable via llms-full.txt for full-fidelity; (b) endpoint Content-Type ! ≡ `text/markdown; charset=utf-8` per llmstxt.org convention — pre-amend `text/plain; charset=utf-8` retired; (c) llms-full.txt endpoint Content-Type ≡ same per V48(e)(∆).
V72: `public/llms-full.txt` ∧ `public/llms.txt` ! sync w/ structured site sources — (a) blog-post enumeration ≡ ∀ non-draft `src/content/blog/<slug>.md` set, order ≡ pubDate desc (mirror `/blog` route per §I); per-file entry shape: llms-full.txt entry ∋ {title, pubDate, description, canonical `https://lab5.ca/blog/<slug>/`}; llms.txt entry ∋ {title, description, canonical URL} per V47(∆) — pubDate dropped from llms.txt entries 2026-05-24 LLM-agent usability amend ∵ pointer-form scannability ∧ pubDate ≡ derivable via llms-full.txt for full-fidelity; (a)(∆) 2026-05-24 amend retires prior unified entry shape pin (`entry ∋ frontmatter title ∧ pubDate ∧ description ∧ canonical`) — recurrence-class ≡ rule-scope-ambiguous-between-two-surfaces per §B.29; (b) capability block ordering ! ≡ V2 (3-tile set; Title Case per V14 amend 2026-05-22); prior parenthetical pinning ordering example text retired 2026-05-22 per B20 ∵ inline-pinned ordering went stale on V2(∆) re-amend even w/ carriers correctly enumerated ∴ rule references V2 by name only ⊥ pin example; (c) contact link set ! ≡ §I `cta.linkedin` ∪ `https://github.com/kborovik` ∪ `cta.mailpilot-source` ∪ `cta.book-call` ∪ `https://github.com/kborovik/resume`; V72(c)(∆) 2026-06-04 per T134 resume migration: prior `https://lab5.ca/resume` ∧ `https://lab5.ca/kb-resume.md` entries retired ∵ /resume route ∧ kb-resume.md deleted; `https://github.com/kborovik/resume` added pointing to external resume repo per §G migration; (d) both files ! carry gated-LinkedIn pointer line per V71; narrative prose (hero, claims, capability stack descriptions) ≡ hand-tuned voice per §V.11 ∴ ⊥ machine-derived, ⊥ §V drift target — sync covers enumeration ∧ ordering ∧ link set only, ⊥ wording; (e) mechanical gen: `llms.txt` ∧ `llms-full.txt` ≡ build-output via Astro static endpoints `src/pages/llms.txt.ts` ∧ `src/pages/llms-full.txt.ts`, ⊥ check-in @ `public/`; both endpoints ! set `Content-Type: text/markdown; charset=utf-8` per V47(∆) (pre-amend `text/plain; charset=utf-8` retired 2026-05-24 LLM-agent usability amend); `llms-full.txt` Blog section auto-derived from `getCollection('blog')` filtered non-draft ∧ sorted pubDate desc per (a), hand-tuned narrative blocks (hero, claims, capability blurbs, proof blurb, contact links, notes) ≡ inline TS strings ∈ endpoint code (wording hand-tuned per (d), emission ≡ build-time); `llms.txt` ∋ pointer-form per V71 — `## Pages` link list w/ hand-tuned page descriptions inline, `## Posts` auto-derived from `getCollection('blog')` per (a), `## Contact` link list inline mirroring §I cta refs ∪ `https://github.com/kborovik` ∪ `https://github.com/kborovik/resume` per (c), `## Notes` gated-LinkedIn pointer line per V71 inline; ∴ blog-post enumeration drift impossible by construction (`getCollection('blog')` single source ∀ Posts ∧ Blog sections); V72(e)(∆) 2026-06-04 per T134 resume migration: prior `llms-full.txt ∋ Resume section auto-derived from public/kb-resume.md (Vite ?raw import + embed-boundary transform)` clause retired ∵ kb-resume.md deleted ∧ llms-full.txt Resume block dropped; kb-resume.md embed-boundary heading-demote mechanic retired w/ source; resume content pointer ≡ `## Contact` link to `https://github.com/kborovik/resume` only.
V73: ∀ non-draft blog post @ `/blog/<slug>` ! ∃ matching markdown mirror @ `/blog/<slug>.md` — build-output via `src/pages/blog/[...slug].md.ts` Astro static endpoint; content ≡ frontmatter front-matter block (YAML-fenced: title, description, pubDate ISO-8601, optional updatedDate, optional tags) + raw `entry.body` text (CommonMark, unrendered); endpoint sets `Content-Type: text/markdown; charset=utf-8`; HTML page emits `<link rel="alternate" type="text/markdown" href="/blog/<slug>.md">` per V45(∆); mirror equivalence by construction (same `src/content/blog/<slug>.md` source file). Orthogonal to V51 — V51 ≡ hand-maintained content equivalence b/w two human-authored surfaces (`kb-resume.md` ↔ `resume.astro`) requiring sweep discipline; V59 ≡ build-generated alternate-format export surface (no equivalence sweep ∵ derivation deterministic from single source). Rationale ≡ agent-discovery surface — LLM agents prefer raw markdown over HTML-stripping ∀ blog ingestion; mirrors `/kb-resume.md` pattern @ blog-corpus scale.
V74: `/rss.xml` ! emit per `@astrojs/rss` package — Astro endpoint `src/pages/rss.xml.ts`; RSS 2.0 channel{title:'lab5.ca blog', description:'Notes on cloud infrastructure, AI agents for business workflows, and web development.', site:'https://lab5.ca', items: ∀ non-draft blog post enumerated via `getCollection('blog')`, order ≡ pubDate desc}; ∀ item ∋ {title:frontmatter title, link:`https://lab5.ca/blog/<slug>/`, pubDate, description:frontmatter description, categories:frontmatter tags}; Layout.astro `<head>` emits `<link rel="alternate" type="application/rss+xml" title="lab5.ca blog" href="/rss.xml">` site-wide per V45(∆); new dep `@astrojs/rss` ∈ `package.json` (Node ≥22 per V20 retained). Rationale ≡ agent-discovery surface — feed readers ∧ portfolio-monitoring agents ⊥ subscription path absent RSS; `/llms.txt` covers static enumeration, ⊥ delta-discovery.
V75: `@astrojs/sitemap` integration restored 2026-05-24 per §B.15(∆) LLM-agent usability audit override — `astro.config.mjs integrations[]` ! ∋ `sitemap()`; auto-emits `/sitemap-index.xml` + `/sitemap-0.xml` @ build covering ∀ Astro route; `public/robots.txt` ! ∋ `Sitemap: https://lab5.ca/sitemap-index.xml` reference; `public/_headers` ! restore cache-rule block ∀ `/sitemap-index.xml` ∧ `/sitemap-0.xml` (parity w/ pre-T76 form, swept out per §B.19 fix); new dep `@astrojs/sitemap` re-added to `package.json` (pre-T58 ∈ `^3.7.2`, dropped per §B.15, restored via this fold-in). Originally dropped (T58 per §B.15) ∵ undeclared §I surface ∴ V55 violation — now restored as DECLARED §I surface (per §I `route:` + `asset:` extensions 2026-05-24) ∴ V55 surface-completeness gate satisfied. Rationale ≡ general-purpose crawlers (search engines, ChatGPT search index, Perplexity) key off `/sitemap-*.xml` discovery; `/llms.txt` covers LLM-agent-first audience, ⊥ displaces sitemap ∀ general indexing reach. Cousin to V59 ∧ V60 — 3 ≡ machine-discovery enumeration surfaces, distinct protocols/consumers (sitemap ≡ search index, RSS ≡ feed readers, .md mirror ≡ LLM agents).
V81: retired 2026-06-04 — `/resume` route deleted via T134 resume migration (resume content → `github.com/kborovik/resume` separate repo); rule vacuous post-migration (⊥ /resume H1 ∨ page-chrome to enforce); pre-retire literal refs preserve `V81(1) H1` ∨ `V81(∆)` per §V/§T/§B documentary convention.
V82: retired 2026-06-04 — `src/pages/resume.astro` ∧ `public/kb-resume.md` deleted via T134 resume migration; mirror equivalence rule vacuous post-migration (⊥ paired surfaces to enforce); pre-retire literal refs preserve `V82 mirror` per documentary convention.
V83: tile bodies ∧ /projects card descriptions ∧ Person.knowsAbout JSON-LD ∧ llms*.txt narrative blocks ! frame KB's actual experience in vocab ∈ §I `vocab: SEO keyword set` (3-group canonical-name set per §I); ⊥ fabricate platform-fluency tokens absent from KB's actual project history — positioning ≡ frame what KB has shipped, ⊥ claim what he hasn't; SEO keyword set ∈ §I ≡ market-driven parameter — re-target ≡ §I amend; AWS tokens dropped 2026-05-21 (⊥ shipped AWS work), OAuth/RBAC dropped (IAM-config-only experience); V83(∆) 2026-06-04 per T134 resume migration: prior scope `(/resume ∧ kb-resume.md skill ∧ engagement content)` retired ∵ both surfaces deleted; forward scope ≡ tile bodies + /projects card descriptions + Person.knowsAbout JSON-LD + llms*.txt narrative blocks; prior `§I /resume primary audience` source-name retired forward (renamed `§I vocab: SEO keyword set`), historical refs preserve prior literal.
V84: retired 2026-06-04 — `/resume` engagement bullets deleted via T134 resume migration (resume content → `github.com/kborovik/resume` separate repo); era-floor rule vacuous post-migration (⊥ engagement bullets on lab5.ca to enforce); `§I tech-token era-floor` table dropped same amend; pre-retire literal refs preserve `V84` ∨ `era-floor` per documentary convention.
V85: retired 2026-06-04 — `src/pages/resume.astro` ∧ `public/kb-resume.md` engagement ledes deleted via T134 resume migration; lede-shape rule vacuous post-migration (⊥ engagement ledes on lab5.ca to enforce); pre-retire literal refs preserve `V85` per documentary convention.
V86: retired 2026-06-04 — `/resume` print stylesheet deleted via T134 resume migration; print-PDF rule vacuous post-migration (⊥ /resume to print on lab5.ca); pre-retire literal refs preserve `V86` per documentary convention.
V87: retired 2026-06-04 — `/resume` section order + project entry shape + Skills-annotation mirror rules deleted via T134 resume migration (`src/pages/resume.astro` ∧ `public/kb-resume.md` both deleted); rule vacuous post-migration (⊥ /resume sections to enforce); pre-retire literal refs preserve `V87` ∨ `V87(∆)` per documentary convention; project-card 3-way mirror narrows to single-surface `src/pages/projects.astro` only (kb-resume.md project entries dropped, resume.astro project entries dropped) ∴ casing-drift recurrence class vacates by surface-reduction.
V87: `/resume` section order + per-section content rules — split from V50(6) 2026-05-29 per §B.21 + §B.24 + §B.28 clause-6 recurrence-class (premature-split mitigation per fold-first authoring); section order ≡ contact block (role title @ H1 per V81(1) primary; name + location + link set secondary ≡ supporting prose, links: LinkedIn ∧ GitHub ∧ home (canonical site `lab5.ca` ≡ home-page link; visible text `lab5.ca`; href form per V22 internal-link rule ∴ `/`, ⊥ full URL — prior parenthetical pinned `https://lab5.ca` as href form ∧ collided w/ V22, retired 2026-05-22 per §B.21) — visible email mailto dropped ∵ public-served HTML + PDF harvest-scrape exposure denied; private contact path retained via LinkedIn DM ∧ book-call CTA ∈ site nav; book-call CTA retained in site nav ∧ `llms-full.txt` link set per V72 but ⊥ rendered as visible /resume link) → `// engagements` (prior employers named w/ neutral one-line framing per §V.11 voice; independent-contractor positioning under `lab5.ca` umbrella; engagement-row composition ≡ H3 company name (anchor) + visible job-title line (`text-gh-fg` full color ∧ `font-medium` weight ∧ font-size ≥ body — primary scan signal per V83, ⊥ muted ∧ ⊥ subordinated below surrounding body prose) + date range (top-right, muted small) → lede + dash-bullet evidence; recent 3 engagements (lab5.ca, Colliers, PayPal) ! ∋ first-person 1-2 sentence factual lede preceding dash-bullet evidence list per V85; older engagements bullets-only) → `// projects` (plain-text mirror of `/projects` page card grid; 4 entries matching `/projects` order ∀ 3 surfaces: mailpilot, gcp-devops, pilot-skills, lab5.ca per T84 mailpilot-first elevation 2026-05-22; ∀ entry ! H3 project name + tech context + green-URL repo link — link form ≡ V24(a) inline text link form (`text-gh-green hover:text-gh-green-hover underline underline-offset-[3px]`) matching contact-block link form @ resume.astro:23/27/32; prior bordered V24(d) button-shaped form w/ GitHub SVG icon retired ∵ /resume `@media print` → PDF renders bordered+SVG-icon button as visual noise, plain URL prints cleanly + visually consistent w/ contact-block links above; H3 form per surface ≡ resume.astro + kb-resume.md: H3 ≡ full `github.com/kborovik/<repo>` URL form (URL ≡ heading text rendered as V24(b) green-underlined link on screen, black plain text on print per `@media print { .resume a { color: #000; text-decoration: none } }`; kb-resume.md uses markdown autolink `<https://github.com/kborovik/<repo>>` ∨ inline link `[github.com/kborovik/<repo>](https://github.com/kborovik/<repo>)`); print-canonical PDF rationale — H3 carries discoverable link signal directly, redundant URL-row-below dropped; projects.astro card grid: H3 ≡ short repo slug (`<repo>` form, e.g. `mailpilot`); web browse-surface context — card-as-link wrapper + GitHub button card carry URL affordance ∴ short H3 reads better in grid layout; 3-way mirror clause: project ORDER + DESCRIPTION CONTENT mirror ∀ 3 surfaces (V82-style equivalence preserved on text content), H3 FORM diverges per surface (resume.astro + kb-resume.md ≡ URL form, projects.astro ≡ slug form); §B.24-class casing-drift recurrence mitigated: ⊥ brand-cased variant (`MailPilot`, `Pilot Skills`) admitted ∈ any of the 3 surfaces — each surface uses a single canonical form for its own scope; description SHAPE per surface — resume.astro + kb-resume.md ≡ paragraph form (single `<p>` ∨ Markdown paragraph, ~2 sentence / ~50-80 word per kb-resume.md canonical fold-in), projects.astro ≡ card-grid 2-field split (`context` lead sentence + `stack` token list) ∵ projects.astro card grid layout incompatible w/ inline paragraph; description CONTENT semantically mirrors across 3 surfaces (V82-style equivalence preserved on canonical-claim content), description SHAPE diverges per surface; narrative product mentions outside 3-way mirror scope (engagement bullets ∈ resume.astro ∧ kb-resume.md, hero/claims/capability blurbs ∈ `llms*.txt.ts`, `/mailpilot` page H1 ∈ mailpilot.astro, segmentLabel ∈ Layout.astro) keep brand form (`MailPilot`); V63(∆) 2026-06-01 Skills-annotation mirror clause per §B.34: kb-resume.md project entry shape ∋ `*Skills: <group-list>*` italic annotation line below H3 URL ∧ above description paragraph — project↔skill-group mapping ∀ /resume primary audience scan affordance; resume.astro mirror ! emit equivalent `<p class="text-base italic text-gh-fg-muted">Skills: <group-list></p>` row per project article block (immediate-following sibling of H3 anchor, immediate-preceding sibling of description `<p>`); group-list strings ! ≡ kb-resume.md project↔skill-group mapping verbatim per V82 content equivalence; projects.astro card-grid ⊥ mirror (card-grid layout incompatible w/ inline label-line ∴ 3-way mirror scope restricts annotation mirror to 2-of-3: resume.astro + kb-resume.md)) → `// education` (degrees + institutions) → `// tech skills` (H3 group set + per-group labeled body lines ≡ `public/kb-resume.md ## Skills` section verbatim per V82 mirror; canonical source per §G; current set: {(1) AI & LLM Engineering, (2) Software Development, (3) DevOps & Cloud Platforms} per V87(∆∆) 2026-06-04 same-day H3 rename amend — Group (3) H3 shortened-form `DevOps & Operations` → `DevOps & Cloud Platforms` per §I vocab Group rename + V2(∆∆∆∆) + kb-resume.md ## Skills H3 set verbatim mirror per V82 (canonical-source kb-resume.md Group (3) heading `### DevOps & Operations` renamed to `### DevOps & Cloud Platforms` same amend ∵ "Platform Engineering" stays in subgroup body lines per V83 LinkedIn-skill verbatim — H3 carries Cloud-Platforms discipline-noun parallel w/ Group (1) ∧ Group (2)); V87(∆) 2026-06-04 positioning pivot amend — group set reordered + Group (3) H3 shortened-form `DevOps` → `DevOps & Operations` per §I vocab Group rename + V2(∆∆∆) + kb-resume.md ## Skills H3 set verbatim mirror per V82 (canonical-source kb-resume.md Group (3) heading `### DevOps & Cloud Platforms & Platform Engineering` shortened to `### DevOps & Operations` same amend); prior set `{(1) Software Development, (2) DevOps, (3) AI & LLM Engineering}` ∧ prior `DevOps` ∧ prior `DevOps & Operations` shortened-forms retired forward, historical refs preserve prior literals per documentary convention; sweep ∀ carriers per T132 + T133; canonical-name expansion preserved in subgroup body lines (Google Cloud Platform (GCP), Microsoft Azure, Platform Engineering all named explicitly); body-line shape ≡ `Label: token, token, ...` mirroring kb-resume.md; stack tokens ! cover §I primary-audience keyword set per V83 (canonical-name LinkedIn-skill verbatim per §I vocab), era-floor per V84).
V91: `.claude/check-extras.md` ! ∋ `§-cite drift recipe extension` section excluding (1) `src/content/blog/**/*.md` from §-cite drift recipe scope — blog body ≡ documentary prose discussing external SPECs (pilot-skills SPEC.md, MailPilot, etc.) ∴ cites ∈ blog content ≡ cross-repo references, ⊥ live cites into lab5.ca SPEC.md; ∧ (2) §B `cause` ∧ §T `task` narrative columns ∈ SPEC.md from free-text `§[VTB]\.[0-9]+` grep — narrative prose quoting historical cite forms (e.g. `/sdd:check` tool output records, prior-state snapshots) ≡ documentary, ⊥ live; live cites stay ∈ typed `§T.cites` ∧ `§B.fix` columns per cite-form convention; extension recipe loads via path probe per sdd:check skill body (REPO-LOCAL extension recipes).
V92: ∀ URL-accessible surface ∈ {(1) tracked file ∈ `public/`, (2) build-output from `astro.config.mjs` `integrations[]` emitting URL-addressable asset (e.g. `@astrojs/sitemap` → `/sitemap-{index,0}.xml`), (3) static endpoint ∈ `src/pages/*.{ts,js}` (e.g. `llms.txt.ts` → `/llms.txt`)} ! enumerated ∈ §I `asset:` list ∨ `route:` list; new surface introduced via closed §T touching `public/` ∨ `astro.config.mjs integrations[]` ∨ `src/pages/*.{ts,js}` ! pair w/ §I admission ∈ same commit ∨ §B record if intentional deferral; ∧ internal config refs to URL-accessible surfaces (cache-rule blocks ∈ `public/_headers`, route-label maps ∈ `src/layouts/Layout.astro`) ! reference only §I-enumerated live surfaces — dead-route ∨ dead-build-output internal refs ≡ same surface-completeness class as B15/B16/B19; audit + diff recipe ∈ `.claude/check-extras.md` (surface-completeness audit)

## §T — tasks

closed: T2–T123 archived → `SPEC.archive.md` (slim 2026-05-29 phases 1-3 dedup); open below.

id|status|task|cites
T124|x|sweep — `§V.50(5)` → `§V.62` ∧ `§V.50(6)` → `§V.63` forward-state refs ∈ non-SPEC.md carriers per V50 split — scope: pattern `§V\.50\((5\|6)\)` ∈ `CLAUDE.md` (1 known site @ L79 /resume PDF render recipe — `per §V.50(5)` → `per §V.62`); remediation ≡ literal cite-form swap on forward-state refs only (historical/past-state refs preserve `§V.50(5)`/`§V.50(6)` literal per §V/§T/§B documentary convention); verify `make check` ∧ `make build` pass post-sweep ∧ `/sdd:check --incremental --all` ⊥ flag stale V50 sub-clause refs|V81,V86,V87
T125|x|sweep — write `linkedin/troubleshooting-application-failures-with-logfire.txt` per V36 structure (line 1 `Blog: Troubleshooting application failures with Logfire`, body summary, trailing `Read the full post: https://lab5.ca/blog/troubleshooting-application-failures-with-logfire/`); pattern scope: file path equality `linkedin/troubleshooting-application-failures-with-logfire.txt` ! ∃ post-write; verify `make check` ∧ `make build` pass|V58
T126|x|sweep — drop backtick wrap around `gen_ai.*` in anchor text @ `src/content/blog/troubleshooting-application-failures-with-logfire.md:70` (`[`gen_ai.*` semantic conventions](url)` → `[gen_ai.* semantic conventions](url)` ∨ `the [semantic conventions for `gen_ai.*`](url)` per V35(∆) remediation forms); pattern scope: grep `\[\`[^]]*\`[^]]*\]` (anchor-text contains backtick-wrapped sub-token) ∈ `src/content/blog/**/*.md` ≡ ∅ post-sweep; verify `make check` ∧ `make build` pass|V22
T127|x|sweep — no content rewrite per V28(∆) admitting 2nd-person tutorial register; pattern scope: `src/content/blog/troubleshooting-application-failures-with-logfire.md` HOLDs by construction post-V28(∆); verify `/sdd:check --incremental` ⊥ flag V28 post-amend|V56
T128|x|sweep — emit `<p class="text-base italic text-gh-fg-muted">Skills: <group-list></p>` row per project article block ∈ `src/pages/resume.astro` (mailpilot, gcp-devops, pilot-skills, lab5.ca) mirroring `public/kb-resume.md` `*Skills: ...*` lines per V63(∆); pattern scope: ∀ project entry ∈ `public/kb-resume.md`, matching `Skills: <same-group-list>` row ∃ ∈ `src/pages/resume.astro` projects section (4 each post-sweep); verify `make check` ∧ `make build` pass ∧ `/sdd:check --incremental` ⊥ flag V51|V82,V87
T129|x|sweep — convert ∀ blog image src ∈ `src/content/blog/**/*.md` to `.webp` ∧ matching asset files ∈ `public/blog/<slug>/` to `.webp` per V29(∆)(a); pattern scope: grep `!\[[^]]*\]\([^)]*\.(png\|jpe?g)\)` ∈ `src/content/blog/**/*.md` ≡ ∅ post-sweep ∧ `find public/blog -type f \( -name '*.png' -o -name '*.jpg' -o -name '*.jpeg' \)` ≡ ∅ post-sweep; verify `make check` ∧ `make build` pass|V57
T130|x|impl — blog image click-to-enlarge `<dialog>` viewer per V29(∆)(b); add `<dialog>` element + client-side `<script>` to `src/pages/blog/[...slug].astro` (click handler on `.prose img`, `dialog.showModal()` w/ enlarged img, close on Esc ∨ backdrop click ∨ explicit close-control) ∧ add `.prose img { cursor: zoom-in }` rule to `src/styles/global.css`; pattern scope: grep `<dialog` ∧ `addEventListener\('click'` targeting `.prose img` ∈ `src/pages/blog/[...slug].astro` post-impl ∧ grep `\.prose img.*cursor: zoom-in` ∈ `src/styles/global.css` post-impl; verify `make check` ∧ `make build` pass ∧ Playwright MCP click-test confirms dialog opens w/ enlarged image @ blog post w/ images|V57
T131|x|sweep — addresses /sdd:check 2026-06-02 drift across 3 classes: (1) §V reorganization sweep gap (commit 814dd6a renumbered 61 rows but ⊥ swept forward-state cross-refs): ∀ stale operative `§V\.[0-9]+` ∨ bare `V[0-9]+` ref ∈ {`CLAUDE.md`, SPEC.md §V bodies (V56,V57,V58,V72,V81,V85,V86,V87), `.claude/check-extras.md` recipe-extension headers} ! swept per `.claude/spec-renumber-map.json` (old→new); pattern scope: grep `§V\.[0-9]+` ∈ `CLAUDE.md` ≡ ∀ token resolves to existing §V row ∈ SPEC.md ∧ semantic operative match post-sweep; SPEC.md §V body forward-state refs (excluding documentary change-log `V<n>(∆)` tags per §V/§T/§B documentary convention) resolve to live operative target post-sweep; `.claude/check-extras.md` recipe-extension headers `V37`/`V53`/`V55` → `V91`/`V84`/`V92`. (2) V86 print 2-col mandate vs 1-col impl: pattern scope: grep `grid-template-columns:\s*1fr\b` ∈ `@media print` block of `src/pages/resume.astro` ≡ ∅ post-impl `1fr 1fr` ∨ V86 body grep `2-col @ print` ≡ ∅ post-V86(∆) drop pinned example per pinned-example-in-rule-body recurrence class §B.20/§B.21/§B.26/§B.35 — author choice. (3) V92 blog per-post asset surface-completeness: ∀ tracked `public/blog/<slug>/<file>.webp` ! enumerated ∈ SPEC.md §I `asset:` line via pattern admitting blog per-post asset dirs; pattern scope: grep `public/blog/<slug>` ∨ `public/blog/.*\.webp` ∈ SPEC.md §I `asset:` line ≡ ∃ post-amend; verify `make check` ∧ `make build` pass post-sweep ∧ `/sdd:check --incremental --all` ⊥ flag stale §V cite ∨ V86/V92 violation|V56,V57,V58,V72,V81,V85,V86,V87,V92
T132|x|sweep — positioning pivot 2026-06-04 (user direction): (a) subtitle `Software Engineer | DevOps Engineer` → `Software Engineer | AI & LLM Engineering` (pipe verbatim) ∀ carrier ≡ {public/kb-resume.md L3 + src/pages/resume.astro H1 + src/pages/resume.astro lab5.ca engagement-row job-title line (drops `Independent Contractor &middot; ` prefix per user direction) + public/kb-resume.md lab5.ca engagement H3 (drops `Independent Contractor · ` prefix same amend) + src/pages/index.astro hero subtitle + src/pages/index.astro meta description + src/pages/resume.astro meta description + src/layouts/Layout.astro default description + src/layouts/Layout.astro default title fallback + src/layouts/Layout.astro jobTitle JSON-LD + src/layouts/Layout.astro OG image alt + src/layouts/Layout.astro Twitter image alt + src/pages/llms.txt.ts blockquote + src/pages/llms-full.txt.ts H1 narrative}; (b) tile order ∈ src/pages/index.astro: {01 Software Development → 02 DevOps → 03 AI & LLM Engineering} → {01 AI & LLM Engineering → 02 Software Development → 03 DevOps & Operations} ∧ post-reorder tile-3 H3 `DevOps` → `DevOps & Operations`; (c) src/pages/llms-full.txt.ts `## What I do` narrative block ordering ∧ labels mirror tile set per V72(b); (d) public/kb-resume.md ## Skills H3 group order ∧ Group (3) heading `### DevOps & Cloud Platforms & Platform Engineering` → `### DevOps & Operations` (canonical-source rename — `Platform Engineering` retained ∈ subgroup body lines per V83); src/pages/resume.astro `// tech skills` H3 set + ordering mirrors per V82 + V87(∆); (e) src/pages/resume.astro project-row `<p>Skills: ...</p>` annotation lines + public/kb-resume.md project entries `*Skills: ...*` lines updated where group-name strings reference renamed group (`DevOps & Cloud Platforms` → `DevOps & Operations`); pattern scope: `rg 'Software Engineer \\| DevOps Engineer' src/ public/kb-resume.md` ≡ ∅ post-sweep; `rg 'DevOps & Cloud Platforms & Platform Engineering' public/kb-resume.md` ≡ ∅ post-sweep; `rg '^### DevOps$' src/pages/resume.astro` ≡ ∅ post-sweep; `rg '^### DevOps & Cloud' public/kb-resume.md` ≡ ∅ post-sweep; tile order in src/pages/index.astro places `AI & LLM Engineering` first; verify `make check` ∧ `make build` pass post-sweep ∧ `/sdd:check --incremental --all` ⊥ flag stale subtitle ∨ stale tile-set|V1,V2,V4,V18,V72,V81,V82,V87
T133|x|sweep — same-day H3 rename 2026-06-04 (user direction): Group (3) shortened-form `DevOps & Operations` → `DevOps & Cloud Platforms` ∀ carrier ≡ {public/kb-resume.md L123 ## Skills section H3 (`### DevOps & Operations` → `### DevOps & Cloud Platforms`) + public/kb-resume.md L74 mailpilot project Skills annotation (`*Skills: DevOps & Operations*` → `*Skills: DevOps & Cloud Platforms*`) + public/kb-resume.md L86 pilot-skills project Skills annotation (`*Skills: Software Development, DevOps & Operations*` → `*Skills: Software Development, DevOps & Cloud Platforms*`) + src/pages/index.astro L78 tile-3 H3 (`DevOps &amp; Operations` → `DevOps &amp; Cloud Platforms`) + src/pages/resume.astro L238 mailpilot project Skills annotation (`Skills: DevOps &amp; Operations` → `Skills: DevOps &amp; Cloud Platforms`) + src/pages/resume.astro L278 pilot-skills project Skills annotation (`Skills: Software Development, DevOps &amp; Operations` → `Skills: Software Development, DevOps &amp; Cloud Platforms`) + src/pages/resume.astro L330 tech-skills Group (3) H3 (`DevOps &amp; Operations` → `DevOps &amp; Cloud Platforms`) + src/pages/llms-full.txt.ts L48 narrative block label (`03 DevOps & Operations` → `03 DevOps & Cloud Platforms`)}; pattern scope: `rg 'DevOps & Operations' src/ public/kb-resume.md` ≡ ∅ post-sweep ∧ `rg 'DevOps &amp; Operations' src/` ≡ ∅ post-sweep ∧ `rg '^### DevOps & Operations$' public/kb-resume.md` ≡ ∅ post-sweep; verify `make check` ∧ `make build` pass post-sweep ∧ `/sdd:check --incremental --all` ⊥ flag stale Group (3) label|V2,V4,V18,V87
T134|x|sweep — resume migration 2026-06-04 (user direction): resume content split out of lab5.ca → `github.com/kborovik/resume` separate repo (own SPEC.md); (a) delete `src/pages/resume.astro`; (b) delete `public/kb-resume.md`; (c) drop `// résumé` button cluster section from `src/pages/index.astro` (entire bordered button cluster → /resume + LinkedIn — per V4(f) retire); (d) drop Resume section + Vite `?raw` kb-resume.md import + embed-boundary heading-demote transform from `src/pages/llms-full.txt.ts`; (e) drop `https://lab5.ca/resume` ∧ `https://lab5.ca/kb-resume.md` from contact link lists ∈ `src/pages/llms.txt.ts` ∧ `src/pages/llms-full.txt.ts`; add `https://github.com/kborovik/resume` to `## Contact` block ∀ both endpoints per V71(∆) ∧ V72(c)(∆); (f) drop `## Pages /resume` entry from `src/pages/llms.txt.ts`; (g) drop `ProfilePage` from `schemaType` literal-union ∈ `src/layouts/Layout.astro` (sole carrier ≡ resume.astro deletion); drop `/resume` route-label entry from `segmentLabel` map; drop `/kb-resume.md` markdown-alternate emission per V66(a)(∆); (h) drop `/resume` PDF render recipe + `kb-resume.md` ## paths ref from `CLAUDE.md`; (i) update `README.md` if resume mention present; pattern scope: `find src/pages/resume.astro public/kb-resume.md` ≡ ∅ post-sweep ∧ `rg -l 'kb-resume\|lab5\\.ca/resume\\b\|résumé\|resume\\.astro\|ProfilePage' src/ public/ CLAUDE.md README.md` ≡ ∅ post-sweep (excluding `SPEC.md` ∧ `SPEC.archive.md` ∧ `src/content/blog/**/*.md` documentary refs per V91) ∧ `rg 'github\\.com/kborovik/resume' src/pages/llms.txt.ts src/pages/llms-full.txt.ts` ≡ ≥ 1 post-sweep; verify `make check` ∧ `make build` pass post-sweep ∧ `/sdd:check --incremental --all` ⊥ flag stale resume cite|V1,V4,V18,V61,V66,V71,V72,V83
T135|x|sweep — SEO keyword re-tune 2026-06-04 toward AI & LLM Engineering primary keyword density (3-tile set unchanged per user direction): re-weight meta descriptions + JSON-LD + llms narrative blocks so AI ∧ LLM tokens lead; (a) `src/pages/index.astro` meta description ! lead w/ `AI & LLM` ∨ `AI agents` ∨ `LLM` tokens; (b) `src/pages/projects.astro` meta description ! ∋ AI ∨ LLM ∨ agents tokens; (c) `src/pages/blog/index.astro` meta description ! ∋ AI ∨ LLM tokens; (d) `src/pages/mailpilot.astro` meta description ! lead w/ AI ∨ LLM ∨ agent tokens; (e) `src/layouts/Layout.astro` default `description` fallback ! lead w/ AI ∧ LLM ∧ Software Engineer tokens; (f) `src/layouts/Layout.astro` Person.knowsAbout JSON-LD ! first 3 tokens ∋ {`Large Language Models (LLM)`, `Retrieval-Augmented Generation (RAG)`, `Model Context Protocol (MCP)`} ∨ equivalent AI/LLM tokens from §I vocab Group (1); (g) `src/pages/llms.txt.ts` blockquote intro ! lead w/ AI ∨ LLM ∨ agent tokens; (h) `src/pages/llms-full.txt.ts` hero + claims + capability narrative blocks ! re-weight to lead w/ AI ∨ LLM tokens per V72(d) hand-tuned voice; pattern scope: ∀ carrier ∈ {a..h} `rg -c '(AI|LLM|agent)' <carrier>` ≥ 1 post-sweep ∧ Layout.astro `knowsAbout` first array element grep-matches `(LLM\|RAG\|MCP\|Large Language Models)`; verify `make check` ∧ `make build` pass post-sweep|V1,V11,V18,V61,V71,V72,V83

## §B — bugs

id|date|cause|fix
B1|2026-05-18|`global.css` decl 5 `--color-gh-*` tokens (`gh-green-muted`, `gh-blue-muted`, `gh-red`, `gh-red-hover`, `gh-yellow`) ⊥ ∈ §I palette list; `gh-red*`/`gh-yellow` ref only ∈ unused `Logo.astro` (CLAUDE.md notes `Logo ⊥ used`); `*-muted` pair zero refs; V17 pinwheel uses raw hex ∈ `Nav.astro:17–20` + `favicon.svg`, ⊥ tokens; surfaced by `/sdd:check` set-diff|V34
B2|2026-05-18|blog `.md` external-link anchor text wraps `**bold**` ∈ `spec-driven-development.md:18` ∧ `` `code` `` ∈ `measuring-glyph-compression.md:214` — no rule pre-amend, surfaced by `/sdd:check §V` post V35 anchor-text clause|V22
B3|2026-05-19|linkedin post body referenced `SPEC.md` ∈ `linkedin/{measuring-glyph-compression,spec-driven-development}.txt`; LinkedIn auto-linkified `SPEC.md` → `<a href="http://SPEC.md">` broken-scheme link (Playwright DOM dump @ `/in/kborovik/recent-activity/all/`); single-quote `'SPEC.md'` ⊥ defeat matcher (re-linkified on edit); backtick `` `SPEC.md` `` ⊥ defeat either; resolved by drop `.md` → `SPEC` ∈ source; 2 older live posts (pre-fix) retain broken `http://SPEC.md` ∈ LinkedIn DOM — out-of-repo remediation only (manual edit ∈ LinkedIn UI)|V58
B4|2026-05-19|V36 markdown-syntax prohibition list ∋ `- bullet` but §V.16 establishes dash-prefix ≡ canonical site bullet ∧ Playwright DOM dump shows LinkedIn renders `- ` literal (benign); rule-as-written ≠ rule-as-intended (V36 over-reach), surfaced by /sdd:check post-V36-amend audit; resolved by narrow V36 list ∧ admit dash-prefix per §V.16|V58
B5|2026-05-19|`/sdd:check --incremental --all` post-V36-amend flagged T.37 (line 99), B.6/B.7/B.8 (lines 140,142,165,167) ∈ `src/content/blog/measuring-glyph-compression.md` as UNRESOLVED — target rows absent ∈ lab5.ca SPEC.md (§T max ≡ T10, §B max ≡ B4); surrounding prose explicitly qualifies cites as cross-repo into pilot-skills SPEC.md (line 85: ``Take `§V.6` from the pilot-skills `SPEC.md`:``); mechanical grep ⊥ distinguish documentary cross-repo cite from live cite ∴ false-positive recurrence class — any blog post discussing external SPEC re-triggers; resolved by exclude `src/content/blog/**/*.md` from §-cite drift recipe scope per V37; documentary cite tokens dropped § prefix 2026-05-19 amend to defeat free-text `§[VTB]\.[0-9]+` grep — narrow fix only, broader §B/§T narrative-column filter ∈ B8|V91
B6|2026-05-19|V36 amend eb4343f (2026-05-19) added "catchy ∨ controversial" + "∋ bold/specific claim ∨ stance ∨ provocation" to headline rule; companion sweep through `linkedin/*.txt` ⊥ run post-amend ∴ `linkedin/spec-driven-development.txt:1` "I kept losing track of what Claude was doing." remains autobiographical setup matching V36 anti-example class ("I claimed X"); prior amend 79e8e63 (length-only rule) ∧ companion fix c8212e1 swept both files, but eb4343f (catchy/controversial dimension) ⊥ paired w/ re-sweep; /sdd:check surfaced 2026-05-19|V58
B7|2026-05-19|where-pytest-stops-and-claude-code-starts.md drafted (untracked) ∋ 2 latent violations of existing rules surfaced by `/sdd:check` 2026-05-19: (1) V35 — line 149 `[lab5.ca/demo](https://lab5.ca/proof/)` writes internal lab5.ca link as full `https://` URL + trailing `/`, ⊥ absolute path `/proof` per V35 internal-link clause; (2) V36 — companion `linkedin/where-pytest-stops-and-claude-code-starts.txt` absent ∴ pair-rule unmet. root cause ≡ author discipline gap — blog .md drafted w/o running `/sdd:check` pre-publish ∴ V35/V36 audits skipped; both rules pre-exist ∴ ⊥ rule-gap, ⊥ new invariant. remediation: fix line 149 → `/proof` absolute path + author linkedin/<slug>.txt per V36 headline+structure rules|V22,V58
B8|2026-05-19|`/sdd:check` 2026-05-19 (first-run, full-scan) surfaced 3 UNRESOLVED free-text §-cites @ SPEC.md:96 (B5 body): T.37, B.7 (HOLD coincidentally — B7 added same day step 1), B.8 — all 3 ≡ documentary cites in B5 narrative quoting prior /sdd:check output that originally found them ∈ blog file (cross-repo refs into pilot-skills SPEC.md); B5 captured blog-content variant via V37 scope exclusion, but ⊥ extended to SPEC.md's own §B/§T narrative-column cite quotes ∴ recurrence class re-fires whenever §B records document prior tool runs. Narrow patch f343a77 dropped § prefix on B5's specific tokens — closes B5 instance only. Broader fix ≡ V37 scope extension (this commit) covering §B/§T narrative columns + check-extras.md filter update ∈ T15 — codifies convention that live cites stay ∈ typed `§T.cites`/`§B.fix` columns, narrative free-text cite forms ≡ documentary by definition|V91
B9|2026-05-19|`linkedin/where-pytest-stops-and-claude-code-starts.txt` published w/ 2 URL rows (`Write-up: https://lab5.ca/blog/...` + `Skill: https://github.com/.../SKILL.md`); LinkedIn DOM (Playwright screenshot 2026-05-19, live post @ `/in/kborovik/recent-activity/all/`) shows trailing `Skill:` URL grabbed card preview (rendered `mailpilot/.claude/skills/smoke-test/SKILL.md at main · kborovik/mailpilot` GitHub card), suppressing canonical `Write-up:` link to `lab5.ca/blog/<slug>/` — LinkedIn auto-card logic promotes the LAST URL ∴ ≥ 2 URLs ⇒ non-Write-up URL wins card slot. Prior V36 explicitly admitted `optional additional <Label>: <url> rows (e.g. Benchmark:, Plugin:)` ∴ rule-gap surfaced 2026-05-19; V36 amend forbids non-Write-up trailing URL rows ∧ mandates `Blog: <title>` line 1; companion sweep §T16 collapses existing `linkedin/{measuring-glyph-compression,spec-driven-development,where-pytest-stops-and-claude-code-starts}.txt` to single Write-up URL ∧ adds `Blog: <title>` headers|V58
B10|2026-05-20|blog title `Measuring math-glyph compression` (frontmatter @ `src/content/blog/measuring-glyph-compression.md:2`; mirrored @ `linkedin/measuring-glyph-compression.txt:1` per V36) — bare `compression` ⊥ disambiguate dimension (token count? bytes? semantic? ratio?); body measures token-count reduction via Claude tokenizer ∴ title ! `token` qualifier; reader landing cold via search ∨ social-card ⊥ infer subject; surfaced by user 2026-05-20; resolved by amend title + Blog: + description per T18; slug ⊥ rename (V23)|V59
B11|2026-05-20|blog title `Where pytest stops and Claude Code starts` (frontmatter @ `src/content/blog/where-pytest-stops-and-claude-code-starts.md:2`; mirrored @ `linkedin/where-pytest-stops-and-claude-code-starts.txt:1` per V36) — rhetorical/positional framing (`Where X stops and Y starts`) ⊥ name technical subject (smoke-testing LLM agent via Claude Code skill executing real APIs ∧ structured-JSON LLM-judge gate); reader landing cold via search ∨ social-card share ⊥ infer technical subject pre-body; V38 pre-amend admitted bare-noun-needs-qualifier class only, ⊥ rhetorical-framing class ∴ rule-gap surfaced 2026-05-20; resolved by amend V38 to admit rhetorical/positional class + retitle per T19; slug ⊥ rename (V23)|V59
B12|2026-05-20|`src/content/blog/where-pytest-stops-and-claude-code-starts.md` TLDR (lines 10–18) opens impersonal — "[MailPilot] is...", "Standard test layers verify...", "The runner is..." — setting impersonal register pre-body; body scatters first-person (L29 "the layers I actually run", L86 "when I type 'smoke test'", L194 "whatever I write into the mock") but lede dominates ∴ violates V28 first-person-singular + simple/clear register clauses; pre-amend V28 ⊃ first-person clause ∴ drift latent pre-amend, surfaced by /sdd:check 2026-05-20 post commit 8bce5a3 (V28 audience/register extension); other 2 posts (measuring-glyph-compression, spec-driven-development) HOLD ∴ ⊥ recurrence-class — single-post drift, ⊥ authoring-pattern gap; remediation bundled into T19 (single edit pass over file w/ V38 title retitle)|V56
B13|2026-05-20|Lighthouse v13.0.2 audit (`~/Downloads/lab5.ca-20260520T074642.json`, desktop, simulate throttling) @ `/` returned accessibility=0.73 (perf/SEO/best-practices ≥0.99); 4 defect classes — color-contrast (5 nodes), heading-order (1 reported, ≥4 latent ∀ page), link-name (1), landmark-one-main (1); SPEC.md pre-amend ⊥ a11y invariant ∴ ⊥ recurrence-prevention rule, defects accumulated as `Layout.astro`/`Nav.astro` grew (3/4 ∈ those files ∴ propagated ∀ page); resolved by V49 baseline + T29–T33 sweep|V23
B14|2026-05-20|/resume print PDF (Chrome headless `--print-to-pdf` 2026-05-20) rendered H2 (`.resume h2 { font-size: 11pt }`) ∧ H3 (`.resume h3 { font-size: 10.5pt }`) smaller than prose `sm:text-base` (1rem = 13.5pt @ print width 816px > sm:640px breakpoint; `html { font-size: 18px }` media-agnostic); V50(5) wording "all ≥ body" interpreted as body-element 10pt baseline @ authoring (T46/T47), but visible prose context governs reader scan ∴ section markers visually subordinated below body in PDF; root cause ≡ absolute pt units ⊥ tracking Tailwind utility rem sizes; resolved by V50(5) amend → rem-based heading sizing + body-floor redefined as visible prose|V86
B15|2026-05-21|`@astrojs/sitemap` integration (`astro.config.mjs:3,8`; `package.json` dep `^3.7.2`) emits `dist/sitemap-{index,0}.xml` @ build; `public/robots.txt:4` references `/sitemap-index.xml` ∴ surface URL-accessible ∧ live; integration ∈ initial commit `76ebddb` (2026-03-22) ∴ never crossed §I gate; forward amend `9e1190f` admitting sitemap reverted `0fcd07c` ("orthogonal to V48 — search-engine vs LLM audience") but integration ∧ dep stayed ∴ undeclared surface persists; `/sdd:check` §V-default ⊥ catch (§I drift fires only on `--all` ∨ §I scope); surfaced 2026-05-21 by `/sdd:check --all` post-T57; B15(∆) 2026-05-24 drop-policy reversed — LLM-agent usability audit (2026-05-24) flagged sitemap absence as general-purpose crawler (search engines, ChatGPT search index, Perplexity) discovery gap; sitemap restored as DECLARED §I surface per V61 ∧ §I `route:` + `asset:` extensions ∴ V55 surface-completeness gate now satisfied (sitemap presence governed by V61 invariant, no longer undeclared-surface class); T58 drop step retired forward, restoration step ≡ T119|V92,V75
B16|2026-05-21|`public/_redirects` ∋ CF redirect rule (`/demo /proof 301`) per T9; previously held `/services /expertise 301` per T3 — dropped 2026-05-21 same-commit (/services ⊥ ∈ §I routes ∴ redirect retired); §I `asset:` brace-list ∋ `_headers` ∧ ⊥ `_redirects` ∴ CF-Pages routing-config-pair enumerated asymmetrically; T9 closure ⊥ paired w/ §I sweep ∴ closed-task evidence ⊥ §I enumeration; same recurrence class as B15 (§I surface completeness gap) localized to `public/*` file-add axis; surfaced 2026-05-21 by `/sdd:check --all`|V92
B17|2026-05-22|2026-05-21 V1(∆) website re-design fold-in swept visible page bodies (hero, tiles, claims, capability copy) but ⊥ enumerate non-page-body carrier set ∴ 3 surfaces retain pre-fold pitch vocab — `src/layouts/Layout.astro:25` default `description` fallback (`AI automation for business operations — ERP integration, document workflows, email, and reporting`), `src/pages/blog/index.astro:18` /blog meta `description` (`Notes on AI automation, ERP integration, document workflows, and production engineering`), `src/pages/resume.astro:177-180` + `public/kb-resume.txt:64-66` lab5.ca engagement lede (same retired vocab; concurrent V52 vocab-set ∧ V54 lede-content drift); recurrence class ≡ pitch-amend carrier-set under-enumeration — re-fires on every future identity pivot unless V1 carries explicit carrier list; surfaced 2026-05-22 by `/sdd:check --all`; resolved by V1 amend enumerating carrier set + T74 sweep|V1
B18|2026-05-22|V48(c) mandates 6-element contact link set ≡ §I `cta.linkedin` ∪ `https://github.com/kborovik` ∪ §I `cta.mailpilot-source` ∪ §I `cta.book-call` ∪ `https://lab5.ca/resume` ∪ `https://lab5.ca/kb-resume.txt` ∀ both `public/llms.txt` ∧ `public/llms-full.txt`; T70 (commit `29b0030`) swept `src/pages/llms.txt.ts` to all 6 entries but `src/pages/llms-full.txt.ts:67-73` Contact block remained 5-of-6 — `https://lab5.ca/resume` absent; URL ∃ ∈ Resume narrative block @ line 59 but V48(c) governs Contact-list set specifically ∴ inclusion-via-narrative ⊥ satisfy; ⊥ rule-gap (V48(c) already governs); single-surface code fix via T75|V72
B19|2026-05-22|two internal config refs point @ surfaces removed earlier — `public/_headers:28-31` retains cache rules for `/sitemap-index.xml` ∧ `/sitemap-0.xml` post-T58 (`@astrojs/sitemap` dropped per B15); `src/layouts/Layout.astro:151-156` `segmentLabel` map holds dead keys {`expertise`, `about`} (routes deleted per T64) ∧ ⊥ live route labels {`projects`, `resume`} ∴ `/projects` breadcrumb item falls through to lowercase `projects`; T58 closure ⊥ paired w/ `_headers` sweep, T63/T64 route churn ⊥ paired w/ `segmentLabel` sweep; same surface-completeness recurrence class as B15/B16 extended from §I asset-list to internal config refs; surfaced 2026-05-22 by `/sdd:check --all`; resolved by V55 amend extending scope + T76 sweep|V92
B20|2026-05-22|commit `d3f743f` (2026-05-22) amended V2 3-tile capability ordering (`01 Cloud Infrastructure → 02 AI Agents → 03 Web Development` → `01 Back-End Development → 02 Front-End Development → 03 Cloud Infrastructure`) ∧ V46 page-body but ⊥ swept V48 carrier `src/pages/llms-full.txt.ts:38-44` `## What I do` narrative block which still emits prior ordering; V48(b) parenthetical text @ SPEC.md:85 also pinned prior ordering example inline ∴ rule text itself stale alongside code carrier; same recurrence class as B17 (amend-carrier under-enumeration — V48 enumerates carrier llms-full.txt.ts but V2-amend gate ⊥ surface dependency) amplified by pinned-example-in-rule-body class — rule parenthetical text goes stale on V2(∆) even when carrier enumeration correct ∴ V48(b)(∆) drops pinned example, V2 ref by name only; surfaced 2026-05-22 by `/sdd:check --incremental`; resolved by V48(b)(∆) + T81 sweep|V72
B21|2026-05-22|`src/pages/resume.astro:32` contact-block "lab5.ca" link written as `<a href="https://lab5.ca">` — full external URL form for internal home-page link; V35 mandates internal links use absolute path `/` ∴ href ! ≡ `/`; T48 task text (`replace mailto w/ https://lab5.ca link`) ∧ V50(6) parenthetical (`canonical site URL https://lab5.ca`) both pinned the full-URL form as authorial directive on href value, overriding V35's general internal-link form rule by virtue of being inline-specific; V35 was in scope @ T48 close but missed ∵ V50(6) inline-pin appeared to supersede; same recurrence class as B20 (pinned-example-in-rule-body) — V50(6) parenthetical pinned a specific href form that collided w/ V35's general rule, cross-rule pinned-example collision; surfaced 2026-05-22 by `/sdd:check --incremental`; resolved by V50(6)(∆) drops href-form pin ∧ delegates form to V35 + T82 sweep|V22
B22|2026-05-22|`src/layouts/Layout.astro:151-156` `segmentLabel` map retains dead key `proof: 'Proof'` post-T83 (∵ `/proof` route renamed → `/mailpilot` per §I(∆) ∴ `/proof` ⊥ §I-enumerated live route — `/proof` ∈ `public/_redirects` ≡ 301-redirect surface only) ∧ ⊥ live key `mailpilot: 'MailPilot'` ∴ `/mailpilot` breadcrumb item falls through to lowercase `mailpilot`; V55(∆) enumerates `route-label maps ∈ src/layouts/Layout.astro` as carrier per B19 fix but T83 task text enumerated sweep paths {`Nav.astro`, `Footer.astro`, `llms.txt.ts`, `llms-full.txt.ts`, `_redirects`, blog `.md`} ⊥ inheriting V55's enumerated carrier list ∴ Layout.astro `segmentLabel` carrier missed; same recurrence class as B19 (config-ref carrier missed by route-churn task) amplified by task-text under-enumeration — V55 rule already governs ∴ ⊥ rule-gap, gap ≡ task-author discipline (route-churn §T ! cross-ref V55 carrier list pre-show-user; chain B17/B19/B20/B21/B22 ≡ carrier-set under-enumeration recurrence class, V55-localized form); surfaced 2026-05-22 by `/sdd:check --incremental`; resolved by T85 sweep|V92
B23|2026-05-22|user feedback 2026-05-22 (image-compare /projects card vs /blog post inline link): /projects `mailpilot` H3 link rendered `text-gh-green` only (⊥ resting underline) ∧ /blog body inline links rendered `text-gh-green` + resting underline per `.prose a` ∈ `src/styles/global.css:192`; both ≡ V57-class green-accent affordance but visible-signal variance — V57(b) pre-amend admitted "w/ ∨ w/o underline" optionality ∀ heading/title link form ∴ /projects card H3 sat on no-underline branch, blog body inline lived on underline branch ∴ inconsistent affordance signal ∈ green-link family; green accent alone ⊥ trigger "this is a link" reader-expectation when visually adjacent to body emphasis (bold, `<strong>` tags); resolved by V57(b)(∆) drop optionality + V57(c)(∆) require resting underline on accent-title form + T88 sweep|V24
B24|2026-05-22|user feedback 2026-05-22 (image-compare /resume `// projects` H3 set vs /projects card grid H3 set): /resume H3 set ≡ {`MailPilot` (brand cap @ resume.astro:97), `gcp-devops`, `Pilot Skills` (Title Case w/ space @ resume.astro:131), `lab5.ca`} vs /projects card H3 set ≡ {`mailpilot`, `gcp-devops`, `pilot-skills`, `lab5.ca`} per projects.astro `p.repo` field (GitHub repo slug verbatim); `public/kb-resume.txt:42,50` mirrors /resume variant per V51 ∴ same casing-drift on third surface, 3-way mirror visibly inconsistent in 2 of 4 entries; V50(6) pre-amend required `content ≡` 3-way equivalence but ⊥ pin canonical name form ∴ casing variance went uncaught — single canonical identifier (GitHub repo slug per `kborovik/<repo>`) eliminates drift surface ∵ identifier = display = link target; resolved by V50(6)(∆) extend 3-way mirror clause to require GitHub-repo-slug-verbatim H3 form + T89 sweep|V87
B25|2026-05-23|user feedback 2026-05-23 (on-page form-collision): `/mailpilot` carries the same 60-second reply-latency fact in 3 places, 2 forms — L88 numeric chip `replies in &lt;60s · no signup · real production agent`, L18 step body `Within sixty seconds the agent replies…`, L69 hero subtitle `replies in under sixty seconds`; `/index.astro:128` carries same fact as `under 60 seconds` (numeric) ∴ site-wide pattern already numeric ∧ /mailpilot the lone written-form straggler; V3 pre-amend ⊥ governed number form ∴ written-out drift uncaught; resolved by V3(∆) numeric-form clause + T90 sweep|V11
B26|2026-05-23|user feedback 2026-05-23 (screen snapshot /resume contact block): `Konstantin Borovik · 20+ years engineering` (resume.astro:14, `text-sm` 0.875rem ≡ 14px) ∧ adjacent secondary prose render below 16px base; pattern repeats across 30 carrier sites ∈ resume.astro — contact secondary prose (L14/L15/L17), tech-stack container `text-sm sm:text-base` (L41), project descriptions (L98/L115/L132/L149), GitHub button labels (L106/L123/L140/L157), engagement-row date ranges (L174/L198/L229/L260/L285/L305), engagement ledes ∧ bullet lists (L177/L181/L201/L205/L232/L236/L263/L288/L308), education degree line (L326), education date range (L324); V50(5) pre-amend "all ≥ body" floor pinned heading floor (H1>H3>H2≈job-title>body) ∧ defined body as `1rem = 16px @ screen` but ⊥ explicit enforcement clause ∀ visible prose itself ∴ Tailwind `text-sm` (0.875rem) ∧ `text-xs` (0.75rem) carriers fell below the body floor undetected; recurrence class ≡ floor-defined-but-not-enforced ∀ /resume prose family — re-fires on any future /resume content addition that defaults to `text-sm` Tailwind body styling; resolved by V50(5)(∆) explicit visible-prose-≥-base clause + T92 sweep|V86
B27|2026-05-23|V3(∆) numeric-form clause introduced by the B25 fix (2026-05-23) enumerated primary-page carrier list ∋ `src/pages/blog/index.astro`; T90's case-insensitive grep pattern `\b(zero|one|...|five|...|hundred|thousand)[ -](second|...|year|...)s?\b` matches `five years` ∈ blog/index.astro:30 (/blog meta lede `...whether a system runs for a quarter or five years.`) ∴ site ∈ T90 pattern ∩ T90 declared scope; T90 "current violations" enumeration listed only the 2 mailpilot `sixty seconds` sites ∧ post-fix verification step `+ grep ∀ primary-page set ≡ ∅` apparently scanned the cited-violations subset rather than re-running the full-scope pattern post-fix ∴ `five years` ∈ blog/index.astro slipped through; surfaced 2026-05-23 by `/sdd:check §V` post-T90 close; same recurrence class as B22 (sweep-§T scope correctly enumerated but audit-step under-executed), extended along V3 prose-vocab axis; chain B17/B19/B20/B22/B27 ≡ sweep-§T scope-declared-but-under-executed recurrence class — V3 already governs the carrier list ∴ ⊥ rule-gap, gap ≡ sweep-§T author discipline (verification step ! re-run full-scope pattern post-fix, ⊥ trust pre-fix "current violations" enumeration as terminal state); resolved by T96 sweep|V11
B28|2026-05-23|`/sdd:check --incremental --all` post-T104 close (2026-05-23) surfaced 3 carrier-drift violations from kb-resume.md canonical fold-in batch (T97-T104, commits 069b9cb..66b7116). (1) `src/pages/llms-full.txt.ts:40-44` `## What I do` narrative block still on prior {01 Back-End Development → 02 Front-End Development → 03 Cloud Infrastructure} ordering — V2(∆) 2026-05-23 amend (`Software Development / DevOps / AI & LLM`) was swept through `src/pages/index.astro` tiles (T99) ∧ `src/pages/resume.astro` tech stack (T98) but llms-full.txt.ts narrative carrier (already declared in V48(b)) ⊥ swept; same recurrence class as B20 (V48(b) carrier ⊥ re-swept on V2 amend), re-firing because the T97-T104 fold-in §T batch ⊥ explicitly enumerate V48(b) carrier despite V48 already listing it ∴ §T author discipline gap, ⊥ rule-gap; (2) `public/kb-resume.md:52-74` `## Projects` order ≡ {mailpilot, pilot-skills, gcp-devops, lab5.ca} but V50(6) mandates canonical {mailpilot, gcp-devops, pilot-skills, lab5.ca}; T97 fold-in step bestowed kb-resume.md as canonical mirror without pre-bestowal audit of kb-resume.md content vs V50(6) project-order pin — same recurrence class as B22 (canonical-tag-without-pre-tag-audit on file becoming canonical mid-flight); (3) `public/kb-resume.md` lab5.ca lede content exceeds V54(b) ≤50-word cap (~70 words) ∧ opens subject-noun (`lab5.ca is my...`) vs V54(a) first-person verb-first; resolved forward by V54(∆) relaxing length cap to ~120 words ∧ admitting subject-noun opening when 2nd clause shifts first-person, rather than compressing kb-resume.md ∵ kb-resume.md ≡ canonical source per §G ∴ kb-resume.md authoring drives shape, V54 mirrors. Chain B17/B19/B20/B22/B27/B28 ≡ carrier-set under-enumeration / canonical-tag without-pre-tag-audit / sweep-§T scope-declared-but-under-executed recurrence class; B28 ≡ this class re-firing through kb-resume.md fold-in batch ∴ rule-set already governs all 3 sub-classes, gap ≡ §T author discipline (kb-resume.md fold-in §T batch ! explicitly enumerate V48 carriers + pre-tag-audit kb-resume.md content vs V50(6) ∧ V54 specs); resolved forward by V54(∆) + T105 + T106 + T107|V72,V87,V85
B29|2026-05-24|LLM-agent usability audit 2026-05-24 surfaced: `src/pages/llms.txt.ts:10` emits Posts entries `- [${title}](${url})` (title + URL only); V48(a) entry shape rule pre-amend required `title ∧ pubDate ∧ description ∧ canonical URL` ∀ both files but llms.txt impl emitted neither pubDate nor description — interpretation gap: rule scope ambiguous between two surfaces, impl diverged on llms.txt ∀ pointer-form brevity heuristic without rule-text codifying the divergence. Latent since T57 close (commit @ llms.txt.ts init); ⊥ surfaced by `/sdd:check` ∵ V48 free-text scope ⊥ pin entry-shape grep recipe — code-vs-spec entry-shape drift ≡ class V48 ⊥ catch mechanically (rule-text governs content shape, no grep pattern enforces). Surfaced 2026-05-24 by external LLM-agent usability audit (out-of-repo). Resolved forward by V47(∆) explicit per-file entry shape (llms.txt entry ∋ {title, description, URL} drops pubDate; llms-full.txt entry ∋ {title, pubDate, description, URL} unchanged) + V48(a)(∆) admit per-file divergence + T114 sweep emitting description ∈ llms.txt Posts entries. Recurrence class ≡ rule-scope-ambiguous-between-N-surfaces — re-fires when shared invariant governs N surfaces ∧ impl diverges silently per surface; mitigated forward by explicit per-surface entry-shape declarations in V47/V48 (∴ next ambiguous rule applying to ≥ 2 surfaces ! declare per-surface shape upfront)|V71,V72
B30|2026-05-24|V40(∆) 2026-05-24 amend (commit `9a1877b`) introduced `Person.potentialAction` w/ `@type:'EmailAction'`; schema.org validator (https://validator.schema.org/) post-deploy flagged 2 errors: (1) `EmailAction is not a known valid target type for the potentialAction property`, (2) `EmailAction (The type EmailAction is not a type defined by the recognized schema (e.g. schema.org))` — `EmailAction` ⊥ ∈ schema.org recognized vocabulary; recognized Action subtypes for "communicate w/ entity" ≡ {`CommunicateAction` (parent), `AskAction`, `ReplyAction`, `ShareAction`, `CheckInAction`}. V40(∆) authoring referenced training-data recall ∀ EmailAction without out-of-repo verification against schema.org canonical vocab — same recurrence class as B27/B28 "rule landed without post-amend audit step" but extended along external-vocab axis (rule cites identifier ⊥ ∈ external authority vocab). Resolved forward by V40(∆∆) same-day amend (`@type:'EmailAction'` → `@type:'AskAction'`) + Layout.astro sweep. Recurrence-mitigation: ∀ V40 amend citing schema.org `@type` token ! cross-check against https://schema.org/<TypeName> ∃ pre-show-user; T120-style validator pass moved from out-of-repo follow-up to in-loop verification ∀ Person/WebSite/page-node JSON-LD changes|V61
B31|2026-06-01|new blog post `src/content/blog/troubleshooting-application-failures-with-logfire.md` shipped without paired `linkedin/troubleshooting-application-failures-with-logfire.txt` per V36 pair-rule; surfaced by `/sdd:check --incremental` 2026-06-01; same recurrence class as B6/B9 (companion-sweep-after-blog-add) — V36 already governs ∴ ⊥ rule-gap, gap ≡ author discipline (blog .md drafted w/o `/sdd:check` pre-publish ∴ V36 pair audit skipped); resolved by T125 sweep|V58
B32|2026-06-01|`src/content/blog/troubleshooting-application-failures-with-logfire.md:70` external-link anchor wraps sub-token `gen_ai.*` in backticks inside `[...]` (`[`gen_ai.*` semantic conventions](https://opentelemetry.io/docs/specs/semconv/gen-ai/)`); V35 anti-example (per B2) pinned full-anchor backtick wrap class (`measuring-glyph-compression.md:214`), partial-wrap subclass (code-fragment ∈ otherwise-plain anchor) ⊥ explicitly enumerated ∴ rule-text ambiguity admitted partial form; surfaced 2026-06-01 by `/sdd:check`; resolved by V35(∆) extending anchor-text wrap prohibition to partial-wrap subclass + T126 sweep|V22
B33|2026-06-01|`src/content/blog/troubleshooting-application-failures-with-logfire.md` body uses 2nd-person tutorial register ("You have an ID...", "You go to look", "You are not trying to understand anything yet") through majority of prose; first-person singular sporadic (L104, L233, L260, L267); V28 mandates "first-person singular, factual + descriptive"; same recurrence class as B12 (register-drift) but distinct subclass — B12 ≡ 3rd-person impersonal lede, B33 ≡ 2nd-person tutorial body; pre-amend V28 first-person clause covers narrative posts only, tutorial-genre posts (step-by-step workflows w/ SQL queries) industry-convention admits 2nd-person instructional register; surfaced 2026-06-01 by `/sdd:check`; resolved forward by V28(∆) admitting 2nd-person tutorial register ∀ step-by-step workflow posts (rule extension, ⊥ content rewrite — post pattern matches industry tutorial convention; V28 carries blog body voice rule ∴ rule-text extension preferred over forcing narrative-only register) + T127 no-op carrier|V56
B34|2026-06-01|`public/kb-resume.md` projects each carry `*Skills: <groups>*` italic annotation line (L68, L74, L80, L86); `src/pages/resume.astro` projects (L207–281) lack mirror — no per-project skill-group annotation; V51 mandates kb-resume.md ↔ resume.astro content equivalence ∴ drift; V63 governs description SHAPE paragraph form (matches both surfaces ✓) — `*Skills: ...*` line ≡ separate annotation row, ⊥ enumerated in V63 ∴ V51 catches it; cause ≡ kb-resume.md canonical fold-in (T97-T104, 2026-05-23) authored `*Skills: ...*` annotation in canonical source; resume.astro template ⊥ swept to mirror — same recurrence class as B28 (kb-resume.md fold-in carrier-sweep gap); resolved by V63(∆) explicit Skills-annotation-line mirror clause ∋ resume.astro carrier + T128 sweep|V82,V87
B35|2026-06-01|`src/pages/resume.astro:362–366` print stylesheet `.resume { padding: 0 !important }` + `:344–347` `@page { margin: 0.5in 0.55in }`; V62 pins `.resume { padding: 0.75in !important } ∀ page-margin breathing room`; both numbers (0.75in vs 0.5in/0.55in) ∧ mechanism (container padding vs @page margin) diverge; cause ≡ V62 split from V50(5) (2026-05-29 per §B.14/§B.26) carried forward literal pinned padding value, impl evolved to @page-margin mechanism (cleaner break w/ browser print-margin defaults), rule-text ⊥ re-swept; same pinned-example-in-rule-body recurrence class as B20 (V48(b) pinned ordering example), B21 (V50(6) pinned href form), B26 (V50(5) wording interpretation); surfaced 2026-06-01 by `/sdd:check`; resolved forward by V62(∆) drops pinned padding form + delegates mechanism abstractly (`@page margin ∨ container padding ∀ page-margin breathing room`)|V86
B36|2026-06-04|T134 pattern-scope assertion `rg -l 'kb-resume\|/resume\\b\|résumé\|resume\\.astro\|ProfilePage' src/ public/ CLAUDE.md README.md` ≡ ∅ post-sweep collided w/ new `github.com/kborovik/resume` URL T134(e) mandates adding ∈ `src/pages/llms.txt.ts` ∧ `src/pages/llms-full.txt.ts`; `/resume\b` matches `kborovik/resume` (word-boundary after `e`) ∴ pattern false-positives w/ 2 files post-sweep when intent is ∅. Sweep intent met — `make check` ∧ `make build` pass, all T134(a–i) sub-steps complete; only over-broad grep flags. Recurrence class ≡ §T pattern-scope assertion ⊥ anticipate own newly-added literal — same authoring-time class as B20 (V48(b) pinned ordering example) / B21 (V50(6) pinned href form) / B26 (V50(5) wording interpretation) / B35 (V62 pinned padding form) pinned-example-in-rule-body but on §T pattern-scope axis rather than §V body axis; ⊥ rule-gap (T134 ≡ §T row body, ⊥ §V invariant), gap ≡ §T author discipline (pattern-scope grep ! anticipate own newly-added literal pre-show-user). Surfaced 2026-06-04 by `/sdd:build` verification during T134 execution. Resolved forward by T134 pattern amend (`/resume\b` → `lab5\.ca/resume\b`) — scope to stale lab5.ca/resume refs ∧ ⊥ new github.com/kborovik/resume add|T134
